# v0.54 Sign-off

**Signed off**: 2026-08-14

## Proof Claims

All claims in the v0.54 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| Summation tolerance commitment: under sustained load without stalls, the sum of decomposed lag components ($\sum_{i=1}^7 \text{lag}_i$) matches the independently measured end-to-end freshness lag within the published tolerance ($\le 5\text{ms}$) | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_summation_tolerance_published` | PASS: published tolerance constant is 5ms; assert `breakdown.sum_decomposed() == breakdown.total_lag_ms` and `is_within_tolerance(5)` across 100 epochs |
| Steady state accounting: each stage's lag is measured independently and accurately accounted for | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_steady_state` | PASS: accurately attributes 12ms source, 4ms decode, 20ms compute, 4ms align, 15ms sink, total 55ms |
| Stage non-absorption under source stall: only `source_lag_ms` and total lag grow; downstream stage components do not silently absorb or mask the stall | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_source_stall` | PASS: `source_lag_ms` increases by 50ms, `total_lag_ms` increases by 50ms, all other 6 stage lags remain exactly invariant |
| Stage non-absorption under spill delay: only `spill_lag_ms` and total lag grow; other stage components remain invariant | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_spill` | PASS: `spill_lag_ms` records 35ms, other stages invariant |
| Stage non-absorption under transactional sink block: only `sink_commit_lag_ms` and total lag grow; upstream stages remain invariant | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_sink_block` | PASS: `sink_lag_ms` increases by 40ms, other stages invariant |
| Barrier flight time under uncongested channels: barrier flight time from coordinator injection to shard receipt is small (< 5ms) and tracked separately from checkpoint commit time | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_uncongested` | PASS: `barrier_flight_time_ms = 3ms` (< 5ms), commit time ~ 5-8ms |
| Barrier flight time divergence under saturated channels: control messages queuing behind data packets causes barrier flight time to diverge from commit time | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_divergence_under_channel_saturation` | PASS: `barrier_flight_time_ms = 85ms` (> 50ms), proving control message queuing accounts for majority of total checkpoint completion latency |
| Barrier flight isolation under slow storage: checkpoint commit time grows while barrier flight time remains low (< 5ms) | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_slow_storage_isolated` | PASS: `barrier_flight_time_ms = 3ms`, `checkpoint_completion_time_ms = 75ms` ($\text{CommitTime} \gg \text{FlightTime}$) |
| Prometheus `/metrics` export: decomposed lag gauges and barrier flight metrics exported with documented units (`ms`), non-negative, and monotonic where required | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_metrics_prometheus_stage_lag_and_barrier_flight_units_monotonic` | PASS: exports all 7 decomposed lag gauges, total lag gauge, and barrier flight/checkpoint completion gauges with OpenMetrics `# HELP` and `# TYPE` |
| CLI inspection: `rockstream view status [<view>]` surfaces stage lag breakdown in human-readable table and `--json` format | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_cli_view_status_text_with_lag_breakdown` & `test_cli_view_status_json_with_lag_breakdown` | PASS: formatted text output and structured JSON with `StageLagBreakdown` verified |
| Durability across SlateDB LFS and MinIO object store with zero range deletions | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_durability_lfs_and_minio` | PASS: SlateDB flush/point-get roundtrip with 0 range deletions |
| Deterministic coordination & fault recovery: coordinator rejects stale confirmations and unknown shards | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_sim_faults` | PASS: confirms `CoordinatorError::StaleConfirmation` and `CoordinatorError::UnknownShard` handling |
| Documentation and conformance lock: CLI and OpenMetrics documentation verified against implementation | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | PASS: all subcommands and error codes verified in `docs/cli.md` and `docs/metrics.md` |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] New behavior has unit tests, property/summation tests, LFS, MinIO, and seeded simulation tests.
- [x] SQL/wire-protocol features have reachability tests (e2e pgwire), negative tests (`RS-1001`), dispatch-wiring verification, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-0001` through `RS-0004`, `RS-1001`, `RS-1005`, `RS-1012`).
- [x] Every control-plane read/tail action operates cleanly with audit records.
- [x] Every new public surface (CLI subcommands, SQL statements, JSON output models, Prometheus `/metrics` gauges) is documented in `docs/cli.md` and `docs/metrics.md`.
- [x] Every new buffer/window has a named bound (`DEFAULT_STAGE_LAG_TOLERANCE_MS = 5`, `MAX_CHECKPOINT_TIMEOUT_SECS = 60`).
- [x] Every new distributed-coordination path has a seeded `SimRuntime` test (`test_barrier_flight_sim_faults`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate is introduced by v0.54.
- [x] Confirmation that no item has been deferred.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test -p rockstream-cli --test lag_attribution_tests
rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.54 is complete.
