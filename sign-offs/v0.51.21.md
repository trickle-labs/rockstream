# v0.51.21 Sign-off: Real Kafka & Object-Store Connector Clients

**Signed off**: 2026-08-07

**Status**: ✅ Done

## Proof Claims

- [x] P1 — M3/M5 sink assertions and connector regressions pass unchanged: `m3_runtime_asserts` (1), connector library tests (67), and edge-case recovery tests (10).
- [x] P2 — `kafka_source_exactly_once_tests` proves real Kafka assignment, bounded polling, exact values `[10, 20]`, offsets `(0, 1)`/`(1, 1)`, and committed epoch/token.
- [x] P3 — `kafka_sink_exactly_once_tests` proves a real Kafka transactional recovery has zero duplicates and zero loss; its independent `read_committed` consumer reads exactly `{"epoch":1,"rows":3}` and `{"epoch":2,"rows":5}`.
- [x] P4 — `object_store_sink_real_bucket_tests` proves real MinIO conditional finalization: pending cleanup, byte-identical re-drive, exact final key/bytes, and conflicting-byte rejection.
- [x] P5 — `connector_client_use_tests` constructs `rdkafka::StreamConsumer` and `object_store::aws::AmazonS3Builder`, references `aws_sdk_s3::Client`, and `cargo tree -p rockstream-connectors` resolves all three dependencies.
- [x] P6 — `check-no-mock-connectors.sh` accepts the source tree, while `check-no-mock-connectors.test.sh` rejects a deliberately reintroduced simulated connector docstring with the expected diagnostic.
- [x] Formatting — `rtk cargo fmt --all -- --check` passes.
- [x] Lints — `rtk cargo clippy --workspace -- -D warnings` passes.
- [x] Workspace test — `rtk cargo test --workspace` passes.
- [x] Exit criteria — `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] E2E — `rtk make e2e` passes.
- [x] No required item has been deferred.

## Reproduction

```bash
rtk cargo test -p rockstream-connectors --lib m3_runtime_asserts
rtk cargo test -p rockstream-connectors --lib
rtk cargo test -p rockstream-sim --test edge_case_recovery_tests
rtk cargo test -p rockstream-connectors --test kafka_source_exactly_once_tests
rtk cargo test -p rockstream-connectors --test kafka_sink_exactly_once_tests
rtk cargo test -p rockstream-connectors --test object_store_sink_real_bucket_tests
rtk cargo test -p rockstream-connectors --test connector_client_use_tests
rtk cargo tree -p rockstream-connectors
rtk ./scripts/check-no-mock-connectors.sh
rtk ./scripts/check-no-mock-connectors.test.sh
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace -- -D warnings
rtk cargo test --workspace
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```
