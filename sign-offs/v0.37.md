# v0.37 Sign-off

**Signed off**: 2026-06-27

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.37 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Claims Verification

- [x] **P1: SSL Negotiation & Parameter Status**
  - **Evidence**: Verified SSLRequest does not cause connection failures, and that startup ParameterStatus messages (`server_version` of `"14.9 (RockStream)"`, `server_encoding` of `UTF8`, and `DateStyle` of `ISO, YMD`) are returned.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_ssl_negotiation_downgrade`

- [x] **P2: Stateful Prepared Statement Cache**
  - **Evidence**: Verified stateful Prepared Statement Cache: maps statement name → SQL text + inferred parameter types, and `DEALLOCATE [ALL]` statement handling.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_prepared_statement_caching_and_deallocate`

- [x] **P3: Describe Statement & Portal**
  - **Evidence**: Verified `Describe(Statement)` returns `ParameterDescription` + `RowDescription`, and `Describe(Portal)` returns `RowDescription`.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_prepared_statement_caching_and_deallocate`

- [x] **P4: Extended Query Pipeline & Limits**
  - **Evidence**: Verified Parse -> Bind -> Execute pipeline, statements limit of 1000 with custom PG error code `53200` carrying `RS-2600`, and portals limit of 1000 with custom PG error code `53200` carrying `RS-2601`.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_extended_query_pipeline` / `test_prepared_statements_limit` / `test_portals_limit`

- [x] **P5: Portal Suspension with `max_rows`**
  - **Evidence**: Verified `Execute` honours `max_rows` and emits `PortalSuspended` when the row limit is reached, resuming on subsequent `Execute`.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_portal_suspension_max_rows`

- [x] **P6: Multi-Statement & Empty Queries**
  - **Evidence**: Verified multi-statement simple query (semicolon-separated) emitting multiple result sets/CommandComplete messages in order, and `EmptyQueryResponse` for empty/whitespace-only queries.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_multi_statement_and_empty_queries`

- [x] **P7: E2E Driver Verification**
  - **Evidence**: Verified `tokio-postgres` driver connects, prepares, and queries using the extended query protocol with correct parameters.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests`

- [x] **Workspace Regression Suite**
  - **Evidence**: Verified all 104 existing gateway tests pass (7 suites).
  - **Command**: `rtk cargo test -p rockstream-gateway`

## Notes

All extended query protocol, prepared statement caching, parameter inference, describe stubs, portal suspension, and SSL downgrade mechanisms have been implemented and fully verified with tokio-postgres driver E2E tests. No items have been deferred to a later version.
