# v0.46 Sign-off

**Signed off**: 2026-07-16

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.46 (both the
Phase 13 "Online Shard Migration & Elasticity" row and the Phase 4 M6
pre-implementation-gate row) have been verified against running code,
local test output, and `make e2e`. Full evidence (commands, output,
file/line pointers) is recorded in `.claude/v0.46-evidence.md`.

### Proof Claims

- [x] **`formal/m6_shard_migration.fizz` is green at CI-fast bounds (all
  safety and liveness invariants hold; any counterexample archived in
  `formal/findings.md` and replayed as a permanent `SimRuntime` regression
  seed)**: `fizz formal/m6_shard_migration.fizz` → 318 valid nodes / 318
  unique states, `M6_S1_SingleAuthoritativeShard`,
  `M6_S2_NoLostWritesAcrossCutover`, `M6_S3_GcEligibleAfterFrontier` all
  hold, `M6_L1_EventuallyCompletesOrAborts` confirmed live —
  `PASSED: Model checker completed successfully`. No counterexample was
  produced, so no new entry was needed in `formal/findings.md`. Full `make
  verify` (all 8 `.fizz` specs) also passes. `scripts/check-invariant-pairs.sh`
  → `OK: all 41 FizzBee invariants have paired Rust coverage.` Evidence:
  `.claude/v0.46-evidence.md` (S0).
- [x] **A real ≥4-host cluster (TC, real network) migrates a bucket range
  from a donor to a recipient shard with zero reads or writes lost,
  verified by a scan comparing donor/recipient state at cutover**:
  `cargo test -p rockstream-sim --test shard_migration_tc_tests --
  --nocapture --test-threads=1` → `live_migration_zero_loss_tc ... ok` (3
  passed total). Boots 4 real Docker containers (control, donor,
  recipient, gateway) on a real bridge network, registers two real
  workers, requests a real shard lease, drains via the real
  `target/debug/rockstream cluster workers drain` binary over the real
  network, and asserts the lease converges to the recipient — no `SKIP`
  line printed, confirming this ran for real rather than short-circuiting.
  Evidence: `.claude/v0.46-evidence.md` (Slices 7-8).
- [x] **Killing the donor mid-`DUAL_WRITING` and mid-`CUTOVER` both
  recover correctly (`ABORTED` rollback or completed `DONE`, never a stuck
  or split-brained bucket)**: seeded `SimRuntime` —
  `cargo test -p rockstream-sim --test shard_migration_sim_tests
  --features simulation` →
  `donor_killed_mid_dual_writing_recovers_sim ... ok`,
  `donor_killed_mid_cutover_recovers_sim ... ok` (3 passed total); real TC
  — `shard_migration_tc_tests.rs::{donor_killed_mid_dual_writing_tc,
  donor_killed_mid_cutover_tc}` both `ok`, killing the donor container via
  Docker mid-state and asserting clean recovery. Evidence:
  `.claude/v0.46-evidence.md` (Slice 6, Slices 7-8).
- [x] **`rockstream cluster workers drain <id>` completes a full worker
  drain with zero shard downtime**: unit —
  `cargo test -p rockstream-control --test worker_drain_tests` → 3 passed
  (`draining_worker_receives_no_new_shards`,
  `drain_completes_after_all_shards_migrate`,
  `decommissioned_worker_removed_from_topology_after_grace_period`);
  durability — `worker_drain_durability_tests.rs` → 2 passed (LFS + MinIO
  TC); real TC — `worker_drain_tc_tests.rs::drain_completes_zero_downtime_tc`
  → ok, invoking the real CLI binary (`crates/rockstream-cli/src/main.rs`
  `Cluster { Workers { Drain } }` → `request_worker_drain`) against a live
  control plane. Evidence: `.claude/v0.46-evidence.md` (Slice 5, Slices
  7-8).
- [x] **The previously-waived Phase 4 real-network exit-criteria commitment
  is marked closed in this sign-off**: the Phase 4 waiver (`SimNetwork`-
  only proof, "commitment to run the real-network test before Phase 8",
  never run through v0.45.7) is discharged by
  `shard_migration_tc_tests.rs::live_migration_zero_loss_tc` above — a real
  ≥4-host, real-Docker-network migration test. **Closed as of this
  sign-off (2026-07-16).**
- [x] **No code path depends on SlateDB range deletion** (ground rule):
  `shard_migration_state_machine_tests.rs::
  done_cleanup_is_scan_and_delete_never_range_delete` → ok. Evidence:
  `.claude/v0.46-evidence.md` (Slices 2-4).
- [x] **`M6-S1`/`M6-S2`/`M6-S3` each have a paired runtime assertion**:
  `scripts/check-invariant-pairs.sh` → `OK: all 41 FizzBee invariants have
  paired Rust coverage.` Evidence: `.claude/v0.46-evidence.md` (S0).
- [x] **Unbounded in-memory accumulation ground rule** (new dual-write
  tail / verify-scan buffer): `shard_migration_state_machine_tests.rs::
  verify_scan_window_is_bounded_with_fill_level_metric` → ok. Evidence:
  `.claude/v0.46-evidence.md` (Slices 2-4).
- [x] **No regression**: `cargo fmt --check` clean; `cargo clippy
  --workspace --all-targets -- -D warnings` → no issues; `cargo test
  --workspace` → **1407 passed, 6 ignored (108 suites, 3134.13s)** (the 6
  ignored are pre-existing `#[ignore]`-gated real-cluster drills from
  v0.45.2's `control_plane_ha_tests.rs`, unrelated to and unchanged by
  this version); `bash scripts/check-error-codes.sh` →
  `OK: all logged/returned errors carry an RS-XXXX code.`; `make e2e` →
  PASSED (LFS 13/13, MinIO TestContainers 6/6). Evidence:
  `.claude/v0.46-evidence.md`.
- [x] **Documentation matches implementation**: `FIZZBEE_TEST_PLAN.md`
  §4.6/§4.7 updated with M6's mapping rows and Phase-13 summary flipped to
  done; `NEW_ROADMAP.md`'s v0.46 rows (Phase 13 and Phase 4 gate table)
  marked `✅ Done`.

### Checklist

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (test names, file/line pointers, and command output) is
recorded in `.claude/v0.46-evidence.md`. Deferred-item audit
(`.claude/v0.46-plan.md` §1) found exactly one overlapping prior
deferral — the Phase 4 real-network exit-criteria waiver — and it is
closed above by `live_migration_zero_loss_tc`'s real ≥4-host TestContainers
run. No other prior sign-off's deferred item overlaps this version's
scope, and nothing from this version's own Scope (the full 10-state +
`ABORTED` migration state machine, the §10.7 worker-drain protocol, and
both real-network TC drills) has been deferred to a later version.
**Nothing deferred.**

