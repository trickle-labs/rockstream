# v0.52.2 Sign-off

**Signed off**: 2026-08-12

## Proof claims

| Proof claim | Evidence | Result |
|---|---|---|
| A multi-table upstream transaction is never observed partially, including restart/disconnect recovery. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_atomicity_tests`; `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests`; exact envelope, rows, weights, LSN, and raw-pgwire visibility assertions. | PASS |
| Large transactions spill and remain atomically batch-identical. | `rtk cargo test -p rockstream-gateway cdc_tx_spilled_two_tables_exact_atomic_batch_oracle`; exact two-relation spilled-envelope oracle passes. | PASS |
| One shared replication slot feeds all imported tables. | `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests`; raw pgwire DDL and `pg_replication_slots` count `1` asserted. | PASS |
| Compatible schema changes persist history; incompatible changes return `RS-1002` and block the source. | `rtk cargo test -p rockstream-gateway --test postgres_cdc_schema_evolution_tests`; raw-pgwire incompatible relation test asserts `BLOCKED` and exact `RS-1002`. | PASS |
| Restart resumes from the committed LSN without loss or duplication. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_durability_lfs_tests`; `rtk cargo test -p rockstream-connectors --test cdc_transaction_durability_minio_tests`; `rtk cargo test -p rockstream-sim --test cdc_transaction_sim_tests --features simulation`; exact output, frontier, checkpoint, lifecycle, and LSN assertions. | PASS |

## Exit criteria

- [x] `rtk cargo fmt --check` passes
- [x] `rtk cargo clippy -- -D warnings` passes
- [x] `rtk cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] New behavior has unit, oracle/property, LFS, MinIO, TestContainers, and seeded SimRuntime coverage where applicable
- [x] SQL/wire features have raw-pgwire reachability, negative, dispatch-wiring, and coverage-matrix tests
- [x] User/operator-visible failures use actionable `RS-XXXX` codes
- [x] Control-plane actions write audit events
- [x] No new performance claim lacks a benchmark or measurement note
- [x] New public surfaces are documented or explicitly covered by the existing status contract
- [x] New queues, buffers, and scan windows have named bounds, metrics, and rejection paths
- [x] No code path depends on SlateDB range deletion
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No soak gate is required for this version
- [x] No items are deferred

## Reproduction

```text
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

The complete evidence table is recorded in `.claude/v0.52.2-evidence.md`.
