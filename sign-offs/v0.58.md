# v0.58 Sign-off

**Signed off:** 2026-08-17

## Proof

| Claim | Evidence | Result |
|---|---|---|
| The failure matrix is published in `docs/failure-matrix.md`, every cell naming its scenario, its asserted recovery outcome, and the version that owns its proof | `rtk bash scripts/check-failure-matrix.sh` and `rtk cargo test -p rockstream-sim --test failure_matrix_tests test_failure_matrix_doc_schema` | PASS — All 11 production failure modes validated successfully against markdown table and schema. |
| Every cell has a passing deterministic `SimRuntime` scenario with a permanent seed corpus | `rtk cargo test -p rockstream-sim --test failure_matrix_tests` | PASS — 13 passed in failure matrix deterministic simulation test suite. |
| A deliberately deleted scenario fails a new matrix-coverage CI gate | `rtk bash scripts/check-failure-matrix.test.sh` (`mutation_deleted_scenario`) | PASS — check-failure-matrix self-test verifies rejection of deleted scenarios. |
| Every asserted outcome is a recovery property (no loss, no duplicates, bounded recovery time against the v0.22 and v0.31 SLOs) — and a scenario asserting only "did not crash" fails the same gate | `rtk bash scripts/check-failure-matrix.test.sh` (`mutation_vacuous_assertion_did_not_crash`) | PASS — gate rejects vacuous assertions ("did not crash", "passes without panic"). |
| Every historical FizzBee counterexample seed and chaos seed still replays clean | `rtk cargo test -p rockstream-sim --test chaos_tests` and `rtk cargo test -p rockstream-sim --test recovery_slo_tests` | PASS — 15 passed in `chaos_tests`, 4 passed in `recovery_slo_tests` with zero divergence. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e` passes (LFS: 14 passed; MinIO/TestContainers: 7 passed; audit log & support bundle verified).
- [x] New behavior has unit, deterministic simulation (`SimRuntime`), and integration coverage across all 11 production failure modes.
- [x] SQL/wire-protocol reachability, negative `RS-XXXX` codes, dispatch wiring, and matrix tests pass.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new public surface is documented in `docs/failure-matrix.md`.
- [x] Every new queue/buffer has named bounds and backpressure paths; every distributed coordination failure scenario has a seeded `SimRuntime` test.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-failure-matrix.test.sh
rtk cargo test -p rockstream-sim --test failure_matrix_tests
rtk cargo test -p rockstream-sim --test chaos_tests
rtk cargo test -p rockstream-sim --test recovery_slo_tests
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk make e2e
rtk ./scripts/check-exit-criteria.sh
```

All v0.58 Proof claims and exit criteria pass. v0.58 is done; nothing in its scope is deferred.
