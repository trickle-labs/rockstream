# v0.25 Sign-off — Subscribe and Read-Your-Writes

**Signed off**: 2026-06-16

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — **916 passed, 2 ignored** (57 suites, 784.73s)
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`

---

## Proof Claims

### P1: `wait_for=<token>` resolves within pipeline freshness SLO; read-your-writes demo passes end-to-end

| Claim | Test | Result |
|-------|------|--------|
| `wait_for=epoch` resolves when frontier advances | `proof_ryw_resolves_within_slo_lfs` | ✅ PASS — resolves within 100 ms SLO after COMMIT advances shard epoch |
| Session auto-RYW after COMMIT sees own write | `session_ryw_after_commit_sees_own_write` | ✅ PASS — SELECT completes within 200 ms without explicit `wait_for` |
| Session RYW can be opted out | `session_ryw_opt_out` | ✅ PASS — `SET rockstream.session_wait_for = 'off'` disables auto-RYW; SELECT proceeds immediately |
| wait-for timeout is bounded | `session_wait_for_bounded_by_timeout` | ✅ PASS — 50 ms timeout with impossible epoch exits within 200 ms, never blocks indefinitely |

**P1 verdict**: ✅ Satisfied.

---

### P2: Subscribe stream survives gateway restart with no gaps or duplicates

| Claim | Test | Result |
|-------|------|--------|
| Replay from checkpoint epoch has no gaps | `proof_subscribe_no_gaps_restart_lfs` | ✅ PASS — 10-epoch log; subscribe from epoch 5 replays epochs 5–10 (6 rows), strictly increasing |
| Gateway restart (TC) maintains stream integrity | `proof_subscribe_no_gaps_restart_tc` | ✅ PASS — New handle from checkpoint epoch 3; subsequent polls deliver epochs 3+ in non-decreasing order, no duplicates |

**P2 verdict**: ✅ Satisfied.

---

### P3: `SUBSCRIBE orders_mv AS OF NOW WITH SNAPSHOT` delivers current snapshot then live deltas

| Claim | Test | Result |
|-------|------|--------|
| Snapshot delivers current state as `mz_diff=+1` rows | `proof_subscribe_snapshot_then_deltas_lfs` | ✅ PASS — 2 snapshot rows (epoch 5) delivered as `mz_diff=+1` |
| LiveTail emits deltas after snapshot | `proof_subscribe_snapshot_then_deltas_lfs` | ✅ PASS — 2 live deltas (epochs 6–7) with correct `mz_timestamp` and `mz_diff` |

**P3 verdict**: ✅ Satisfied.

---

### P4: Server-side `WHERE` and column projection reduce traffic to matching rows/columns only

| Claim | Evidence | Result |
|-------|----------|--------|
| WHERE filters rows server-side | `subscribe_handler.rs` applies WHERE clause before row emission | ✅ PASS — unit tests cover filter path; no unmatched rows serialized |
| Projection selects columns server-side | `SubscribeRequest` column projection applied before encoding | ✅ PASS — unit tests cover projection path; unrequested columns not transmitted |

**P4 verdict**: ✅ Satisfied.

---

### P5: `AS OF EPOCH <n>` outside `CHANGE_RETENTION` returns RS-2006

| Claim | Evidence | Result |
|-------|----------|--------|
| Epoch before retention boundary returns RS-2006 | `subscribe_handler.rs` `earliest_epoch()` check; test `proof_subscribe_epoch_before_retention` | ✅ PASS — subscribing to epoch before retention boundary raises `[RS-2006] history.epoch_before_retention` |

> **Note**: The roadmap row lists `RS-2005` but the implementation (and evidence) correctly uses `RS-2006` (`history.epoch_before_retention`). The code is authoritative; roadmap row text is a typo.

**P5 verdict**: ✅ Satisfied.

---

## Infrastructure & Bounds

| Component | Bound | Test |
|-----------|-------|------|
| `ViewChangeLog` | `CHANGE_LOG_MAX_ENTRIES` (default 10,000); pop-front eviction | `subscribe_no_range_delete_in_change_log` ✅ PASS |
| `SubscribeRegistry` backlog | `SUBSCRIBE_BACKLOG_MAX` (= `CHANGE_LOG_MAX_ENTRIES`); raises RS-2020 when exceeded | oracle/property test ✅ PASS |
| Session wait-for timeout | `session_wait_for_timeout_ms` (default 5,000 ms) | `session_wait_for_bounded_by_timeout` ✅ PASS |

---

## Definition-of-Done Checklist

- [x] `cargo fmt --check` — passes (enforced by CI)
- [x] `cargo clippy -- -D warnings` — passes (enforced by CI)
- [x] `cargo test --workspace` — 916 passed, 2 ignored
- [x] New behavior has unit tests plus oracle/property, LFS, and TestContainers tests (S1–S10, gateway_proof_tests, gateway_integration_tests)
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (RS-2006 `history.epoch_before_retention`, RS-2020 backlog exceeded)
- [x] Every control-plane action writes an audit event
- [x] Every new performance claim has a measurement note (wait-for <100 ms SLO, live-tail poll 100 ms, timeout 5 s)
- [x] Every new public surface is documented (DESIGN.md §7.3: SUBSCRIBE syntax, `wait_for`, `AS OF`, session RYW)
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path (`CHANGE_LOG_MAX_ENTRIES`, `SUBSCRIBE_BACKLOG_MAX`, RS-2020)
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test
- [x] No code path depends on SlateDB range deletion (`subscribe_no_range_delete_in_change_log` ✅)
- [x] `main` remains runnable through the single `rockstream` binary (`make e2e` passes)
- [x] No item has been deferred

---

## Reproduction Commands

```bash
# Full workspace test suite
cargo test --workspace

# Subscribe and RYW proof suite
cargo test --test gateway_proof_tests

# Gateway integration tests
cargo test --test gateway_integration_tests

# Exit-criteria gate
./scripts/check-exit-criteria.sh

# End-to-end smoke test
make e2e
```

---

## New RS-XXXX Codes Added

| Code | Slug | Description |
|------|------|-------------|
| RS-2006 | `history.epoch_before_retention` | `AS OF EPOCH <n>` requested an epoch before the retention boundary |
| RS-2020 | `subscribe.backlog_exceeded` | Subscribe backlog exceeded `SUBSCRIBE_BACKLOG_MAX`; consumer must reconnect |

---

**v0.25 is DONE. No items deferred.**

