# v0.56 Sign-off

**Signed off**: 2026-08-15

## Proof claims

| Proof claim | Evidence | Result |
|---|---|---|
| Three-worker N→N+1 rolling upgrade has no epoch loss or in-flight downtime. | `rtk cargo test -p rockstream-sim --test rolling_upgrade_tests --features docker_tests`; `three_worker_n_to_n_plus_1_zero_loss_tc` | PASS |
| Out-of-range or malformed shard format is rejected as `RS-5001`. | `rtk cargo test -p rockstream-storage --test storage_format_tests`; `n_binary_refuses_v2_shard_rs5001`, `malformed_format_marker_refused_rs5001` | PASS |
| Incompatible peer is rejected as `RS-5021`, never `RS-5002`. | `rtk cargo test -p rockstream-runtime --test protocol_version_tests`; `newer_protocol_refused_rs5021_not_rs5002` | PASS |
| Root offline migration preserves populated-shard results across 1→2. | LFS, MinIO, and CLI format-migration test targets; `migrates_populated_v1_shards_to_v2_bit_identically`, `migrates_populated_v1_shards_to_v2_bit_identically_tc`, `root_migrate_dispatches_and_reports_exact_results` | PASS |
| Interrupted migration is rerunnable and keeps every shard openable. | LFS and MinIO format-migration test targets; `interrupted_migration_is_rerunnable_and_never_leaves_unopenable_shard`, `interrupted_migration_reruns_exactly_tc` | PASS |
| Control never assigns until the mixed-version compatibility floor is met. | `rtk cargo test -p rockstream-sim --test rolling_upgrade_sim_tests --features simulation`; seeds `0x56_0001`, `0x56_0002` | PASS |
| Migration never uses SlateDB range delete. | `rtk cargo test -p rockstream-storage --test format_migration_tests`; `migration_uses_scan_copy_verify_and_point_delete_only` | PASS |

## Definition of done

- [x] `rtk cargo fmt --check`
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings`
- [x] `rtk cargo test --workspace`
- [x] `rtk make e2e` — LFS 14 passed; MinIO/TestContainers 7 passed; 0 failed
- [x] Unit, oracle/property, LFS, MinIO, and TestContainers coverage added where applicable
- [x] Reachability, negative, dispatch-wiring, and coverage-matrix tests cover the live gRPC/storage paths; SQL gates are not applicable
- [x] User-visible failures use actionable `RS-XXXX` codes; control-plane audit coverage is retained
- [x] No new performance, queue/buffer, or distributed-coordination claim lacks its required proof
- [x] No code path depends on SlateDB range deletion
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No soak gate applies to v0.56
- [x] New error codes, docs, and regression seeds are recorded: `RS-5001`, `RS-5021`, `docs/rolling-upgrades.md`, `0x56_0001`, `0x56_0002`
- [x] No item has been deferred; disaster recovery is explicitly v0.56.1 scope
- [x] `NEW_ROADMAP.md` status is `✅ Done`

## Reproduction

```text
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk make e2e
rtk ./scripts/check-exit-criteria.sh
```

All v0.56 Proof claims and exit criteria pass. v0.56 is done; nothing in its scope is deferred.
