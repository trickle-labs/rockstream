# v0.51.23 Sign-off: Real Operator State-Size Accounting & Wiring the Admission/Skew Control Loop

**Signed off**: 2026-08-10  
**Status**: ✅ Done

## Exit Criteria Verification

All Proof criteria in `NEW_ROADMAP.md` (v0.51.23, line 1249) were verified against `.claude/v0.51.23-evidence.md` and the release gates below.

### Proof Claims

- [x] **Claim 1: O(1) Operator State Accounting (11 stateful operators)**: Each of the 11 stateful operators (`join`, `outer_join`, `aggregate`, `minmax`, `distinct`, `topk`, `window`, `time_window`, `index_arrange`, `lateral`, `recursion`) maintains O(1) incremental `state_bytes()`, updated on insert/retract, and reports through `set_pipeline_state_bytes`. Proven by `operator_state_accounting_tests` (13 passed).
- [x] **Claim 2: Hostile Tenant Prospective Quota Enforcement**: Real production ingestion path consults `WorkerQuotaManager::try_allocate_batch` before arrangement growth allocation, returning `ViewState::OverBudgetRejected` with `RS-5003`/`RS-9001` audit events under prospective overflow. Proven by `hostile_tenant_real_ingest_quota_tests` (3 passed).
- [x] **Claim 3: Skew Control Loop Integration with Measured State**: Real state metrics feed `ShardFootprintReport` in `rockstream-control/src/skew.rs` driving `AdaptiveSkewSplitter` split/merge decisions without test-only setters. Proven by `skew_control_loop_sim_tests` (1 passed).
- [x] **Claim 4: CI Operator State Accounting Invariant Gate**: The CI gate `scripts/check-operator-state-bytes.sh` and integration test `accounting_gate_tests.rs` verify that any stateful operator with arrangement fields implements `state_bytes()` accounting. Proven by `check-operator-state-bytes.sh` and `accounting_gate_tests` (2 passed).
- [x] **LFS Durability**: SlateDB checkpoint and recovery restores operator `state_bytes()` accurately. Proven by `test_operator_state_accounting_durability_lfs`.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check`, `rtk cargo clippy -- -D warnings`, and `rtk cargo test --workspace` pass.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests, negative error tests, and dispatch-wiring verification.
- [x] Every user/operator-visible failure uses an `RS-XXXX` code with actionable text (`RS-5003`, `RS-9001`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface is documented.
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path.
- [x] Distributed coordination paths have seeded `SimRuntime` tests.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` and `rtk make e2e` pass.
- [x] **Confirmation that no item has been deferred.**

## Verification Evidence

| Proof claim | Test / command | Result | Evidence |
| --- | --- | --- | --- |
| Claim 1 | `rtk cargo test -p rockstream-ops --test operator_state_accounting_tests` | PASS | `13 passed (1 suite, 0.11s)` |
| Claim 2 | `rtk cargo test -p rockstream-sim --test hostile_tenant_real_ingest_quota_tests` | PASS | `3 passed (1 suite, 0.00s)` |
| Claim 3 | `rtk cargo test -p rockstream-sim --test skew_control_loop_sim_tests --features simulation` | PASS | `1 passed (1 suite, 5.23s)` |
| Claim 4 | `rtk bash scripts/check-operator-state-bytes.sh` && `rtk cargo test -p rockstream-sim --test accounting_gate_tests` | PASS | `OK: all stateful operators implement state_bytes accounting` & `2 passed (1 suite, 0.51s)` |
| LFS Durability | `rtk cargo test -p rockstream-ops --test operator_state_accounting_tests test_operator_state_accounting_durability_lfs` | PASS | `1 passed (1 suite, 0.11s)` |

## Dispatch-Wiring Verification

- Trait definition: `crates/rockstream-ops/src/op.rs` (`Operator::state_bytes(&self) -> u64`)
- 11 Stateful Operator Implementations: `join.rs`, `outer_join.rs`, `aggregate.rs`, `minmax.rs`, `distinct.rs`, `topk.rs`, `window.rs`, `time_window.rs`, `index_arrange.rs`, `lateral.rs`, `recursion.rs`
- Pipeline Aggregation & Metrics: `StatefulPipeline::state_bytes` and `set_pipeline_state_bytes`
- Ingest Path Quota Enforcement: `WorkerQuotaManager::try_allocate_batch` in `crates/rockstream-runtime/src/quota.rs` returning `ViewState::OverBudgetRejected`
- Skew Control Loop: `ShardFootprintReport` feeding `AdaptiveSkewSplitter` in `crates/rockstream-control/src/skew.rs`
- CI Invariant Gate: `scripts/check-operator-state-bytes.sh` and `.github/workflows/ci.yml`

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test --workspace
rtk cargo test -p rockstream-ops --test operator_state_accounting_tests
rtk cargo test -p rockstream-sim --test hostile_tenant_real_ingest_quota_tests
rtk cargo test -p rockstream-sim --test skew_control_loop_sim_tests --features simulation
rtk bash scripts/check-operator-state-bytes.sh
rtk cargo test -p rockstream-sim --test accounting_gate_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

No item has been deferred. v0.51.23 is done.
