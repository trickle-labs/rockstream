# v0.51.8 Sign-off

**Signed off**: 2026-08-03

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` (line 1040) for v0.51.8 have been verified against running code, local test output, and `.claude/v0.51.8-evidence.md`.

### Proof Claims

- [x] **P1 — Rejected Transcript Query**: `CREATE TABLE o (cust int, amt int); INSERT INTO o VALUES (1,10),(1,5),(2,3); CREATE MATERIALIZED VIEW mv AS SELECT cust, SUM(amt) FROM o GROUP BY cust; SELECT * FROM mv;` compiles and returns `(1,15),(2,3)` over ordinary `int` columns without `RS-1019`/`RS-1013`. Verified by `serving_path_aggregate_matrix_tests.rs::test_basic_int_sum_group_by`. Evidence: `.claude/v0.51.8-evidence.md` §2 P1.
- [x] **P2 — Text Key & Fractional Aggregate**: `text`-keyed `SELECT product, SUM(qty)...` and `AVG` view compile and return oracle-matching fractional results. Verified by `serving_path_aggregate_matrix_tests.rs::test_text_key_sum_and_avg_fractional`. Evidence: `.claude/v0.51.8-evidence.md` §2 P2.
- [x] **P3 — Nexmark Regression**: All pre-existing Nexmark `BIGINT` views pass unchanged (`q0` through `q11`). Verified by `crates/rockstream-sql/tests/nexmark/main.rs`. Evidence: `.claude/v0.51.8-evidence.md` §2 P3.
- [x] **P4 — CI Guard**: CI guard fails if serving compiler regains `Int64`-only rejection path for standard aggregate shapes. Verified by `serving_path_aggregate_matrix_tests.rs::test_ci_guard_no_int64_only_rejection`. Evidence: `.claude/v0.51.8-evidence.md` §2 P4.
- [x] **LFS Backend**: Multi-type aggregate view state durability & crash recovery on local SlateDB. Verified by `serving_aggregate_durability_lfs_tests.rs::test_serving_aggregate_durability_lfs`. Evidence: `.claude/v0.51.8-evidence.md` §2 LFS Backend.
- [x] **MinIO Backend**: Real S3-compatible store (via TestContainers) proves state persistence and recovery across gateway restart. Verified by `serving_aggregate_durability_minio_tests.rs::test_serving_aggregate_durability_minio`. Evidence: `.claude/v0.51.8-evidence.md` §2 MinIO Backend.
- [x] **Deterministic Simulation**: Seeded `SimRuntime` network frame jitter and pipeline state consistency. Verified by `sim_aggregate_coordination_tests.rs::test_sim_aggregate_coordination_jitter`. Evidence: `.claude/v0.51.8-evidence.md` §2 Simulation.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check` passes cleanly.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes cleanly.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-1019` unsupported aggregate error handling with physical-plan context).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface (SQL aggregate support across types) is documented.
- [x] Every new queue/buffer/scan window has a named bound, a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test (`sim_aggregate_coordination_tests.rs`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] All Proof criteria verified against running code or CI output.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `scripts/check-exit-criteria.sh` passes cleanly.
- [x] `make e2e` passes cleanly.
- [x] Nothing deferred to a later version.

## Notes

v0.51.8 resolves Blocker 2 (functional regression from July transcript) by wiring general physical-plan lowering and type-widening key-packing (`GroupKeyPacker`) into `crates/rockstream-ops/src/compile.rs` and `live_exec.rs`. 
Non-`Int64` group-by keys (`int`, `text`, `date`, `float`, `numeric`, `bool`) and aggregates (`SUM`, `AVG`, `COUNT`, `MIN`, `MAX`) now compile and incrementally evaluate on the serving path without `RS-1019`/`RS-1013` errors.

Per the ground rules, no item has been deferred to a later version.


