# v0.59.6 Sign-off — Durable Shared Arrangement Fabric

**Signed off:** 2026-08-20

## Proof

| Claim | Evidence | Result |
|---|---|---|
| Canonical `ArrangementSpec` containing 12 normalization fields (`tenant_id`, `security_policy_digest`, normalized source identity, source-schema generation, key expressions/types, value projection, predicate, null semantics, decimal scale, collation identifier/version, time-zone/time-domain semantics, merge-law identifier/version, and partitioning), domain-separated BLAKE3/SHA-256 stable `ArrangementId` hashing, and normalizer (harmless casts, commutative predicates, aliases). | `rtk cargo test -p rockstream-types --test arrangement_spec_unit_tests`<br>`rtk cargo test -p rockstream-sql --test expression_canonicalization_tests`<br>`rtk cargo test -p rockstream-types --test arrangement_spec_isolation_tests` | PASS (15/15 tests) — Proves canonical spec construction, stable arrangement hashing, normalizer equivalence, and distinct IDs across all 10 isolation variation axes. |
| Durable arrangement catalog, deduplication and reference-counted lifecycle; registering views sharing spec increments refcount; dropping views decrements refcount; dropping 0th refcount marks arrangement for GC; safe deferred reclamation executes after horizon. | `rtk cargo test -p rockstream-storage --test arrangement_catalog_tests`<br>`rtk cargo test -p rockstream-storage --test arrangement_lifecycle_reclamation_tests` | PASS (7/7 tests) — Proves catalog registration, reference counting, dynamic query attachment, and safe deferred reclamation of unreferenced arrangements. |
| Immutable consolidated trace batches, per-consumer read frontiers, and compaction frontier derived strictly from the slowest live consumer ($F_{\text{compaction}} = \min F_c$); complete coverage matrices across aggregates, joins, and windows. | `rtk cargo test -p rockstream-storage --test shared_trace_batch_tests`<br>`rtk cargo test -p rockstream-storage --test trace_compaction_slowest_consumer_tests`<br>`rtk cargo test -p rockstream-storage --test shared_trace_matrix_tests` | PASS (61/61 tests) — Proves multi-consumer snapshot reading, slowest-consumer compaction protection, and full aggregate/join/window matrix evaluation. |
| Frontier-safe zero-rescan new-view installation: pin frontier $F$, snapshot shared trace through $F$, buffer deltas $> F$, and attach without source rescan, with zero duplicate rows and zero dropped rows against multiset oracle. | `rtk cargo test -p rockstream-ops --test view_attachment_zero_rescan_tests`<br>`rtk cargo test -p rockstream-ops --test view_attachment_gap_duplicate_tests` | PASS (4/4 tests) — Proves dynamic view attachment at frontier $F$ with 0 source scans (`source_scan_requests == 0`), bounded delta buffering, and zero gap/duplicate records. |
| Worker-wide shared storage context (`WorkerStorageContext`) unifying decoded-block, index, and object caches under explicit memory budgets (`storage.cache_budget_bytes`), and strict tenant-partitioned cache isolation (`CacheKey { tenant_id, security_policy_digest, block_id }`). | `rtk cargo test -p rockstream-storage --test worker_storage_context_cache_tests`<br>`rtk cargo test -p rockstream-storage --test cache_tenant_isolation_tests` | PASS (4/4 tests) — Proves worker cache reuse across shards and views within configured budgets, LRU eviction, and zero cross-tenant block leakage. |
| Storage format versioning (V3 / `V3SharedTrace`), `TraceManifestHeader` serialization and validation, deterministic V2→V3 format migration, mixed N/N+1 cluster read/write operation, and fail-closed rejection on invalid or corrupted trace headers (`RS-5001` / `RS-5003`). | `rtk cargo test -p rockstream-storage --test format_migration_v2_to_v3_tests`<br>`rtk cargo test -p rockstream-storage --test mixed_version_trace_compatibility_tests`<br>`rtk cargo test -p rockstream-storage --test fail_closed_trace_manifest_tests` | PASS (6/6 tests) — Proves V3 shared trace initialization, migration from V2 delta state, mixed-version cluster compatibility, and fail-closed format rejection. |
| Introspection: `EXPLAIN INCREMENTAL` and `SHOW VIEW STATUS` expose `arrangement_id`, `consumer_count`, `shared_state_bytes`, `bytes_saved_by_sharing`, and `compaction_frontier` over pgwire protocol. | `rtk cargo test -p rockstream-sql --test explain_incremental_shared_arrangement_tests`<br>`rtk cargo test -p rockstream-gateway --test show_view_status_arrangement_tests`<br>`rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests` | PASS (28/28 tests) — Proves arrangement sharing facts and compaction frontiers exposed via pgwire and tabular explain diagnostics. |
| Formal M2 model extension (`formal/m2_frontier_agg.fizz`) proving shared-consumer frontiers, dynamic attachment, deferred reclamation, and no premature GC; SimRuntime crash chaos at migration, compaction, and reclamation boundaries. | `rtk bash scripts/check-invariant-pairs.sh`<br>`rtk cargo test -p rockstream-sim --test sim_shared_trace_crash_boundary_tests` | PASS (46/46 invariants paired) — Proves formal invariants paired with Rust runtime assertions and atomic crash recovery at all operational boundaries. |
| Scale Proof: 20 semantically equivalent consumers with syntax variations canonicalize to 1 physical arrangement in catalog; shared storage byte volume remains $\le 1.3\times$ baseline; 21st view attaches dynamically without source rescan or visibility gap; sequential dropping reclaims trace. | `rtk cargo test -p rockstream-sim --test scale_proof_20_views_sharing_tests` | PASS (1/1 test) — Proves 20-view deduplication to 1 physical arrangement, sublinear storage scaling, zero-rescan 21st view attachment, and clean lifecycle reclamation. |
| Durability across SlateDB LocalFileSystem (LFS) and MinIO S3 object storage backends without range deletions. | `rtk cargo test -p rockstream-storage --test lfs_shared_trace_durability_tests`<br>`rtk cargo test -p rockstream-storage --test minio_shared_trace_durability_tests` | PASS (3/3 tests) — Proves atomic shared trace commits, multi-consumer snapshot reads, slowest-consumer compaction, and clean recovery on LFS and MinIO S3. |
| Dispatch-Wiring Audit | `rtk bash scripts/check-dispatch-wiring.sh` | PASS — 21/21 statement families and CLI command dispatch paths verified connected with 0 MISSING paths. |
| Capability Contract & Security/Failure Matrices | `rtk python3 scripts/check-capability-contract.py`<br>`rtk bash scripts/check-failure-matrix.sh`<br>`rtk bash scripts/check-threat-model-links.sh`<br>`rtk bash scripts/check-error-codes.sh` | PASS — All scripts and contract conformance checks passed. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes (2,857 passed, 0 failed across 360 suites).
- [x] `rtk make e2e` passes (all steps pass: no-op binary, LFS backend, MinIO TestContainers).
- [x] `rtk make e2e-lfs` passes (14/14 tests pass).
- [x] New behavior has unit tests (`arrangement_spec_unit_tests.rs`, `arrangement_spec_isolation_tests.rs`, `expression_canonicalization_tests.rs`), storage & trace tests (`arrangement_catalog_tests.rs`, `arrangement_lifecycle_reclamation_tests.rs`, `shared_trace_batch_tests.rs`, `trace_compaction_slowest_consumer_tests.rs`, `shared_trace_matrix_tests.rs`, `worker_storage_context_cache_tests.rs`, `cache_tenant_isolation_tests.rs`, `format_migration_v2_to_v3_tests.rs`, `mixed_version_trace_compatibility_tests.rs`, `fail_closed_trace_manifest_tests.rs`, `lfs_shared_trace_durability_tests.rs`, `minio_shared_trace_durability_tests.rs`), ops & attachment tests (`view_attachment_zero_rescan_tests.rs`, `view_attachment_gap_duplicate_tests.rs`), gateway/SQL introspection tests (`explain_incremental_shared_arrangement_tests.rs`, `show_view_status_arrangement_tests.rs`, `show_view_status_pgwire_tests.rs`), and simulation/scale tests (`sim_shared_trace_crash_boundary_tests.rs`, `scale_proof_20_views_sharing_tests.rs`).
- [x] SQL/wire-protocol features maintain reachability, negative tests, and dispatch wiring (`scripts/check-dispatch-wiring.sh` reports 0 MISSING paths).
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text (`RS-4020`, `RS-5001`, `RS-5003`, `RS-0002`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion benchmark / measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml` and documentation.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime` and paired with formal invariants (46/46 invariants paired).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-types --test arrangement_spec_unit_tests
rtk cargo test -p rockstream-sql --test expression_canonicalization_tests
rtk cargo test -p rockstream-types --test arrangement_spec_isolation_tests
rtk cargo test -p rockstream-storage --test arrangement_catalog_tests
rtk cargo test -p rockstream-storage --test arrangement_lifecycle_reclamation_tests
rtk cargo test -p rockstream-storage --test shared_trace_batch_tests
rtk cargo test -p rockstream-storage --test trace_compaction_slowest_consumer_tests
rtk cargo test -p rockstream-storage --test shared_trace_matrix_tests
rtk cargo test -p rockstream-ops --test view_attachment_zero_rescan_tests
rtk cargo test -p rockstream-ops --test view_attachment_gap_duplicate_tests
rtk cargo test -p rockstream-storage --test worker_storage_context_cache_tests
rtk cargo test -p rockstream-storage --test cache_tenant_isolation_tests
rtk cargo test -p rockstream-storage --test format_migration_v2_to_v3_tests
rtk cargo test -p rockstream-storage --test mixed_version_trace_compatibility_tests
rtk cargo test -p rockstream-storage --test fail_closed_trace_manifest_tests
rtk cargo test -p rockstream-sql --test explain_incremental_shared_arrangement_tests
rtk cargo test -p rockstream-gateway --test show_view_status_arrangement_tests
rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests
rtk cargo test -p rockstream-sim --test sim_shared_trace_crash_boundary_tests
rtk cargo test -p rockstream-sim --test scale_proof_20_views_sharing_tests
rtk cargo test -p rockstream-storage --test lfs_shared_trace_durability_tests
rtk cargo test -p rockstream-storage --test minio_shared_trace_durability_tests
rtk bash scripts/check-invariant-pairs.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk python3 scripts/check-capability-contract.py
rtk bash scripts/check-threat-model-links.sh
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-path-coupling.sh
rtk make e2e
rtk make e2e-lfs
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk ./scripts/check-exit-criteria.sh
```

All v0.59.6 Proof claims and exit criteria pass. v0.59.6 is done; nothing in its scope is deferred.
