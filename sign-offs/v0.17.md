# v0.17 Sign-off

**Signed off**: 2026-06-15

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.17 have been verified.

- [x] `cargo test --workspace` passes
- [x] ROADMAP.md status updated to `✅ Done`
- [x] **Proof Claim 1**: Distributed results are **bit-identical** to single-shard runs across the entire oracle/TPC-H suite
  - *Evidence*: `rtk cargo test -p rockstream-runtime --test exchange_integration test_distributed_fuzz_parity` passes.
- [x] **Proof Claim 2**: An injected receiver failure forces durable fallback and the receiver catches up with no duplicates
  - *Evidence*: `rtk cargo test -p rockstream-runtime --test exchange_integration test_durable_shuffle_fallback` passes.
- [x] **Proof Claim 3**: A 1,000-shard exchange stress test stays within connection and shuffle-object budgets against MinIO
  - *Evidence*: `rtk cargo test -p rockstream-runtime --test exchange_integration test_thousand_shard_exchange_stress` passes.
- [x] **Proof Claim 4**: Rendezvous Hashing Minimality and uniform distribution
  - *Evidence*: `rtk cargo test -p rockstream-types --lib rendezvous` passes.
- [x] **Proof Claim 5**: Deterministic control plane simulation test under fault injection
  - *Evidence*: `rtk cargo test -p rockstream-sim --features simulation --test control_sim` passes.

## Notes

All tests pass cleanly. Distributed results are confirmed bit-identical to single-shard runs, durable fallback operates correctly under injected receiver failure, virtual node rendezvous hashing distribution guarantees are verified, and the control plane lease manager behaves correctly under simulated network partitions. No issues remain.
