# v0.52.6 Sign-off

**Signed off**: 2026-06-04

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.52.6 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

- Implemented DataFusion SQL logical plan lowering for `LogicalPlan::Window` in `crates/rockstream-sql/src/lowering/mod.rs`.
- Added exact column schema, OID, type, and row value matching/assertions to gateway query E2E tests in `crates/rockstream-e2e/tests/e2e.rs`.
- Added E2E precision test `test_v0_52_6_sql_lowering_precision` to verify exact plans and merge law annotations (`WeightAdd/v1`, `MaxRegister/v1`, `extremum_requires_rmw`) for JOIN, Aggregate (SUM/MAX), and WINDOW queries.
- Bumped workspace version to `v0.52.6` (now at `v0.52.7`).
