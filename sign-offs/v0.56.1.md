# v0.56.1 Sign-off

**Signed off**: 2026-08-16

## Proof Claims

| Proof claim | Evidence | Result |
|---|---|---|
| A full checkpoint in a separate bucket/region restores into a new cluster with every view bit-identical. | `disaster_recovery_tests::lfs_export_restore_fresh_cluster_is_bit_identical`; `disaster_recovery_tests::minio_tc_export_restore_fresh_cluster_is_bit_identical` | PASS — LFS and real TestContainers MinIO restore every view and control record bit-identically. |
| The drill follows only `docs/disaster-recovery.md`, and reports measured RPO and RTO. | `disaster_recovery_runbook_tests::runbook_only_drill_records_measured_rpo_and_rto` | PASS — the documented command sequence produces and publishes the measured RPO/RTO transcript. |
| Export during sustained writes restores one committed epoch with no partial epoch or torn view. | `disaster_recovery_tests::lfs_export_under_writes_restores_one_committed_epoch`; `disaster_recovery_tests::minio_tc_export_under_writes_restores_one_committed_epoch`; `checkpoint_export_sim_tests::m3_export_selection_is_single_epoch_under_buggify` (seed `0x56_01_0001`) | PASS — both durable backends and the seeded coordination proof select one M3-committed epoch. |
| A deliberately truncated export fails closed with an enumerated code and never produces partial cluster state. | `disaster_recovery_tests::lfs_truncated_export_fails_closed_rs5035`; `disaster_recovery_tests::minio_tc_truncated_export_fails_closed_rs5035` | PASS — both backends reject the truncated export with `RS-5035` and publish no partial target state. |
| The export/restore route is a real documented CLI path with authorization and auditable result. | `cli_disaster_recovery_tests::checkpoint_export_restore_cli_outputs_and_audits_exactly`; `cli_disaster_recovery_tests::checkpoint_restore_rejects_missing_export_with_rs5035`; `cli_doc_coverage_tests` | PASS — parser, dispatcher, executor, text/JSON responses, Admin enforcement, audit events, and documentation coverage are exact. |
| No DR cleanup/copy path depends on SlateDB range deletion. | `disaster_recovery_tests::export_restore_uses_no_slatedb_range_deletion`; `checkpoint_export_source_has_no_range_delete_path` | PASS — operation traces and source scan contain no range-delete path. |
| DR resources remain bounded and a coordinated/in-flight export cannot mix epochs. | `checkpoint_export_` bound/integrity tests; `checkpoint_export_sim_tests::m3_export_selection_is_single_epoch_under_buggify` | PASS — named scan/copy bounds, progress/in-flight metrics, and single-epoch coordination are verified. |

## Definition of Done

- [x] `rtk cargo fmt --all --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace -- --test-threads=1` passes; the serial workspace run is green after making the existing simulation lease test wait on bounded readiness and passing Admin identity to the migration TestContainers CLI.
- [x] New behavior has unit, LFS, MinIO/TestContainers, and seeded SimRuntime tests.
- [x] SQL/wire-protocol criteria are not applicable; v0.56.1 adds no SQL or pgwire feature.
- [x] Every user/operator-visible failure has an actionable `RS-XXXX` code (`RS-5035`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a measurement note; no criterion introduces a benchmark claim.
- [x] Every new public surface is documented in `docs/cli.md` and `docs/disaster-recovery.md`.
- [x] Every new scan/copy window has named bounds, progress/in-flight metrics, and an error path.
- [x] Every new distributed-coordination path has a seeded SimRuntime test (`0x56_01_0001`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate is introduced by v0.56.1.
- [x] No item has been deferred.
- [x] `NEW_ROADMAP.md` marks v0.56.1 Done.

## Reproduction

```text
rtk cargo fmt --all --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace -- --test-threads=1
rtk proxy cargo test -p rockstream-control --test disaster_recovery_tests
rtk proxy cargo test -p rockstream-control --test checkpoint_export_sim_tests --features simulation
rtk proxy cargo test -p rockstream-control checkpoint_export_
rtk proxy cargo test -p rockstream-cli --test cli_disaster_recovery_tests --test disaster_recovery_runbook_tests --test cli_doc_coverage_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.56.1 is signed off. The workspace suite passes serially; the two test-only changes remove readiness/authentication races exposed by the new mutating-command authorization.
