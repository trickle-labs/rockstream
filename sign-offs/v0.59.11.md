# v0.59.11 Sign-off — SQL Ergonomics & Common Expression Completeness

**Signed off**: 2026-08-24

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Hardened `UPDATE ... RETURNING` across simple/extended protocols, prepared statements, and arbitrary projections (`SQL-01`) | `update_returning_conformance_tests`, `sql_ergonomics_e2e_tests` | PASS (9) |
| Hardened `DELETE ... RETURNING` pre-image capture across simple/extended protocols, prepared statements, and arbitrary projections (`SQL-02`) | `delete_returning_conformance_tests`, `sql_ergonomics_e2e_tests` | PASS (9) |
| Harmonized idempotent `IF EXISTS` and `IF NOT EXISTS` semantics across all 9 admitted DDL object families (`SQL-03`) | `ddl_if_exists_conformance_tests`, `sql_ergonomics_e2e_tests` | PASS (10) |
| Typed common scalar functions across string, null-handling, and date/time categories with null preservation (`SQL-04`) | `scalar_functions_oracle_tests` | PASS (13) |
| Incremental scalar view maintenance produces bit-identical results to batch PostgreSQL oracle | `scalar_functions_oracle_tests` | PASS (1) |
| DML mutations and DDL state survive restart durably on LFS and MinIO backends | `delete_returning_durability_tests` | PASS (2) |
| Concurrent DML RETURNING and DDL operations maintain consistency under SimRuntime fault chaos | `v05911_sql_ergonomics_sim_tests` | PASS (1) |

## Definition of Done

- [x] Targeted proof suites pass.
- [x] `cargo fmt --all --check` passes.
- [x] `NEW_ROADMAP.md` marks v0.59.11 `✅ Done`.
- [x] Workspace package version is `0.59.11`.
- [x] `./scripts/check-exit-criteria.sh` passes.
- [x] `./scripts/check-dispatch-wiring.sh` passes.
- [x] `./scripts/check-capability-contract.sh` passes.
- [x] `./scripts/check-error-codes.sh` passes.
- [x] `./scripts/check-invariant-pairs.sh` passes.

## Reproduction

```text
rtk cargo test -p rockstream-gateway --test update_returning_conformance_tests --test delete_returning_conformance_tests --test delete_returning_durability_tests --test ddl_if_exists_conformance_tests --test sql_ergonomics_e2e_tests
rtk cargo test -p rockstream-ops --test scalar_functions_oracle_tests
rtk cargo test -p rockstream-sim --test v05911_sql_ergonomics_sim_tests
rtk cargo fmt --all --check
rtk cargo clippy --all-targets -- -D warnings
rtk bash scripts/check-exit-criteria.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk bash scripts/check-capability-contract.sh
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-invariant-pairs.sh
```
