# v0.12 Sign-off

**Version**: v0.12 — Tumbling Time Windows and Top-K (IVM-8, IVM-9)  
**Signed off**: 2026-06-10

## Proof Claims Verification

### Claim 1 — Late-data and TTL tests prove no visible state is removed early (LFS)

- [x] `tumble_window_compaction_filter_no_early_eviction` (TW-5) — PASS  
  `crates/rockstream-ops/src/time_window.rs`  
  CompactionFilter unit test: `may_delete(key)` returns false unless both TTL and
  frontier gate pass. Window [0,1000) with watermark=5000, frontier=500 → deletion
  refused (frontier < window_end=1000). ✓

- [x] `lfs_tumble_window_no_early_eviction` (TW-7) — PASS  
  `crates/rockstream-ops/tests/lfs_time_window.rs:220-274`  
  Close/reopen ShardDb with frontier held below window_end; window state survives
  compaction. 4 tests in lfs_time_window.rs all green. ✓

### Claim 2 — Late-data and TTL tests prove no visible state is removed early (MinIO)

- [x] `minio_tumble_window_late_data_and_ttl` (TW-8) — PASS  
  `crates/rockstream-ops/tests/minio_time_window.rs:178-235`  
  Late row (t=50 < watermark=5000) dropped; partial state persisted to MinIO and
  reloaded (fill_level matches); CompactionFilter refuses deletion when
  frontier(500) < window_end(1000). ✓

### Claim 3 — Top-K random insert/update/delete tests match batch

- [x] `oracle_topk_incremental_matches_batch` (TK-6) — PASS  
  `crates/rockstream-ops/src/topk.rs`  
  1,000-iteration randomised test; K ∈ {1,3,5,10}; multiple partition keys;
  incremental TopKOp output == batch oracle (sort_desc + take(K)) at every epoch. ✓

- [x] `minio_topk_random_changes` (TK-8) — PASS  
  `crates/rockstream-ops/tests/minio_topk.rs:197-260`  
  6-epoch insert/update/delete sequence; K=3; incremental matches batch at every
  epoch; persisted to MinIO, reloaded, one more epoch — still correct. ✓

### Claim 4 — Delete from the current top-K refills correctly

- [x] `topk_delete_refill_all_positions` (TK-3) — PASS  
  `crates/rockstream-ops/src/topk.rs`  
  K=3, 6 rows inserted (buffer = K+ε); delete rank-1 → rank-4 refills; delete
  rank-2 → rank-5 refills; delete rank-3 → rank-6 refills; each deletion emits
  exactly one retraction + one insertion delta. ✓

---

## Test Execution Summary

| Suite | Tests | Result | Time |
|---|---|---|---|
| Unit (`--lib`) — time_window | 5 | PASS | — |
| Unit (`--lib`) — topk | 9 | PASS | — |
| Unit (`--lib`) — all workspace | 633 | PASS | 39.59 s |
| Integration (`--test '*'`) — lfs_time_window | 4 | PASS | — |
| Integration (`--test '*'`) — lfs_topk | 3 | PASS | — |
| Integration (`--test '*'`) — minio_time_window | 1 | PASS | — |
| Integration (`--test '*'`) — minio_topk | 1 | PASS | — |
| Integration (`--test '*'`) — all | 82 | PASS | 187.96 s |

Commands to reproduce:

```bash
rtk cargo test --lib -p rockstream-ops
rtk cargo test --workspace
rtk cargo test --test lfs_time_window -p rockstream-ops
rtk cargo test --test lfs_topk -p rockstream-ops
rtk cargo test --test minio_time_window -p rockstream-ops
rtk cargo test --test minio_topk -p rockstream-ops
```

---

## Exit Criteria Verification

- [x] `cargo test --workspace` passes (633 unit + 82 integration = 715 tests)
- [x] All Proof criteria verified against running code and CI output
- [x] ROADMAP.md status updated to `✅ Done`

### Definition-of-Done Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy -- -D warnings` passes
- [x] `cargo test --workspace` passes
- [x] New behaviour has unit tests **plus** oracle/property (TK-6), LFS (TW-7, TK LFS), and MinIO (TW-8, TK-8) tests
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text
- [x] Every control-plane action writes an audit event
- [x] Every new performance claim has a `criterion` benchmark or measurement note (compaction-filter gate documented)
- [x] Every new public surface (SQL syntax, CLI command, config key, system table) is documented
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path (TopK K+ε buffer bound documented)
- [x] No code path depends on SlateDB range deletion (frontier-gated CompactionFilter used instead)
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No item has been deferred — HOP/SESSION deferred by design in the v0.12 scope definition itself (not a deferral from the implemented scope)

---

## New Artefacts Added

| Type | Name | Location |
|---|---|---|
| Operator | `TumbleWindowOp` | `crates/rockstream-ops/src/time_window.rs` |
| Operator | `TopKOp` | `crates/rockstream-ops/src/topk.rs` |
| CompactionFilter | frontier-gated TTL filter | `crates/rockstream-ops/src/time_window.rs` |
| LFS tests | lfs_time_window.rs (4), lfs_topk.rs (3) | `crates/rockstream-ops/tests/` |
| MinIO tests | minio_time_window.rs (1), minio_topk.rs (1) | `crates/rockstream-ops/tests/` |

---

v0.12 is **done**.
