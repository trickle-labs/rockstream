# v0.15 Sign-off

**Signed off**: 2026-06-12

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.15 have been verified.

- [x] `cargo test --workspace` passes
- [x] ROADMAP.md status updated to `✅ Done`
- [x] **Proof Claim 1**: A two-writer fence test proves only one writer can commit to a shard
  - *Evidence*: `rtk cargo test -p rockstream-storage --test lfs_backend fencing_lfs` and `rtk cargo test -p rockstream-storage --test minio_backend fencing_minio` pass.
- [x] **Proof Claim 2**: Killing a worker releases its leases and they are reassigned cleanly
  - *Evidence*: `rtk cargo test -p rockstream-sim --test control_sim sim_control_plane_leases --all-features` and `rtk cargo test -p rockstream-control --lib service::tests::shard_leases_released_on_worker_disconnect` pass.
- [x] **Proof Claim 3**: Topology changes are audited
  - *Evidence*: `rtk cargo test -p rockstream-cli --lib tests::run_start_writes_audit_log_and_support_bundle` passes.
- [x] **Proof Claim 4**: Unified entrypoint supports roles and control address
  - *Evidence*: `rtk cargo test -p rockstream-cli --lib tests::worker_or_gateway_role_without_control_fails_with_rs_0002` and `rtk cargo test -p rockstream-cli --lib tests::known_roles_are_accepted` pass.

## Notes

All tests pass cleanly. Fencing, lease reassignment upon worker failure, topology change auditing, and CLI validation rules are active and correct.
