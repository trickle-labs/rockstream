# v0.58.3 Sign-off

**Signed off:** 2026-08-18

## Proof

| Claim | Evidence | Result |
|---|---|---|
| Every SQL/DDL/`SHOW` keyword documented as implemented in `docs/language-features.md` has a passing e2e pgwire reachability test (raw SQL through the real dispatcher, not a unit test importing private modules) | `rtk cargo test -p rockstream-gateway --test unscoped_pgwire_reachability_tests` | PASS — 14/14 tests pass across all documented statement families: materialized views, inline views, scalar expressions, aggregates, joins/set ops, window functions, subscribe streams, DML operations, workload DDL, secret DDL, index DDL & EXPLAIN, system catalog queries, and removed connector rejection. |
| The unscoped dispatch-wiring audit reports zero `MISSING` parser→dispatcher→executor→response paths anywhere in the documented surface | `rtk bash scripts/check-dispatch-wiring.sh`<br>`rtk bash scripts/check-dispatch-wiring.test.sh` | PASS — 21/21 statement families and all declared dispatch endpoints in `capabilities.toml` verified with 0 `MISSING` paths. Mutation self-test confirms broken parser or dispatcher routes are caught and rejected. |
| The unscoped silent-wrong-answer sweep reports zero reachable branches producing a wrong answer, a panic, or a silent no-op | `rtk cargo test -p rockstream-gateway --test unscoped_silent_wrong_answer_tests`<br>`rtk bash scripts/check-no-unreachable.sh`<br>`rtk bash scripts/check-no-unreachable.test.sh` | PASS — 5/5 negative case tests pass. Zero `unreachable!()` calls found across all production source files (`crates/*/src/`), with mutation test confirming injection detection. |
| The audit itself is checked in as a repeatable script with a paired self-test, so v0.59 re-runs it as an entry criterion instead of re-deriving it by hand | `scripts/check-dispatch-wiring.sh`<br>`scripts/check-dispatch-wiring.test.sh`<br>`scripts/check-no-unreachable.sh`<br>`scripts/check-no-unreachable.test.sh` | PASS — Checked into repository with executable permissions and passing self-test suites, ready for v0.59 RC entry gate execution. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes (2562 passed).
- [x] `rtk make e2e-lfs` and `rtk make e2e` pass (LFS: 14 passed; MinIO/TestContainers: 7 passed; audit log & support bundle verified).
- [x] New behavior has unit, reachability, and negative case tests (`unscoped_pgwire_reachability_tests.rs`, `unscoped_silent_wrong_answer_tests.rs`).
- [x] SQL/wire-protocol reachability: Every SQL keyword/statement has raw SQL through the real gateway dispatcher.
- [x] Negative tests: Invalid input returns structured `RS-XXXX` errors with actionable text.
- [x] Dispatch-wiring verification: Every path in 4-stage pipeline (parser → dispatcher → executor → response) is tested and proven connected.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text (`RS-4017`, `RS-4021`, etc.).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml` and `docs/language-features.md`.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime`.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-gateway --test unscoped_pgwire_reachability_tests
rtk cargo test -p rockstream-gateway --test unscoped_silent_wrong_answer_tests
rtk bash scripts/check-dispatch-wiring.sh
rtk bash scripts/check-dispatch-wiring.test.sh
rtk bash scripts/check-no-unreachable.sh
rtk bash scripts/check-no-unreachable.test.sh
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk make e2e-lfs
rtk ./scripts/check-exit-criteria.sh
```

All v0.58.3 Proof claims and exit criteria pass. v0.58.3 is done; nothing in its scope is deferred.

