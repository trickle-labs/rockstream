# v0.66 Sign-off

**Decision:** SIGNED OFF
**Assessment date:** 2026-09-15
**Roadmap status:** Done; all acceptance criteria are proven.

Evidence is recorded in [v0.66 evidence](v0.66-evidence.md).

## Exit Criteria Verification

- [x] V066-01: Typed contracts cover all ten reads and four mutations. Release-process backup, drain, migration, cancellation, and incompatible-version tests cover successful and explicit failure paths, including all 14 RPCs.
- [x] V066-02: Standalone and multi-process status transcripts prove topology, lifecycle, health, headroom, operation counts, request fill, and ACK waiter fill from authoritative state changes.
- [x] V066-03: Exact operation records cover pending, running, waiting, succeeded, failed, and cancelled states, metadata, legal transitions, and terminal outcomes.
- [x] V066-04: Independent-process retries are durable and atomic across restart and concurrency; same-key retries reuse one effect and changed payloads return the exact conflict.
- [x] V066-05: Failed writes are rejected before acknowledgement, and restart tests reconcile accepted backup effects and durable operation state.
- [x] V066-06: Release-process cancellation stops before lease transfer, late cancellation is rejected, unavailable objects and workers fail explicitly, and retained shard data is verified.
- [x] V066-07: Request, page, operation-history, drain-queue, and ACK-waiter bounds have exact saturation, expiry, cleanup, fill, and recovery tests.
- [x] V066-08: Release-process CLI examples execute through management RPC with exact JSON checks; standalone and cluster transcripts verify credentials never appear in output.

## Common Definition of Done

- [x] `rtk cargo fmt --all --check` passes on the final tree.
- [x] `rtk rustup run 1.88 cargo clippy --workspace --all-targets -- -D warnings` passes on the final tree.
- [x] The final-tree workspace suite passes: 556 target summaries, with the two load-sensitive proofs rerun separately and passing.
- [x] `rtk make e2e` passes on the final tree with loopback access enabled.
- [x] `rtk rustup run 1.88 cargo build --release -p rockstream-cli` passes on the final tree.
- [x] Required release-process transcripts, multi-process evidence, and all V066 criteria are complete.

The v0.66 roadmap and implementation-plan statuses are Done. The release is
qualified for sign-off.
