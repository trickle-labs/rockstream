# v0.53 Sign-off

**Signed off**: 2026-08-14

## Proof Claims

All claims in the v0.53 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| An operator answers *which views are stale, which worker owns which shard, and what is this workload consuming* against a running cluster using only documented read-only commands — no source reading, no ad hoc scripts | `crates/rockstream-cli/tests/cli_read_only_tests.rs::test_cli_read_only_answers_stale_worker_shards_and_workload` | PASS: verified JSON and text output resolving stale views, worker shard allocations, and workload memory/state metrics |
| Every subcommand has a golden-output test in both `--json` and text form, and its `--json` output validates against a checked-in schema | `crates/rockstream-cli/tests/cli_read_only_tests.rs::test_cli_all_subcommands_golden_text_and_json_schema` & `cli_inspection_tests.rs` | PASS: validated across all 12 command families (25+ subcommands) |
| No read-only command takes a pipeline lock or perturbs progress, asserted by comparing freshness lag during a continuous polling loop against a control run | `crates/rockstream-cli/tests/cli_read_only_tests.rs::test_cli_read_only_polling_non_perturbing_tc` | PASS: concurrent background pipeline made monotonic forward progress (>50 ticks) with 0 stalls during 100 continuous CLI polling iterations |
| Every subcommand is documented in `docs/cli.md`, enforced by a doc-coverage test that fails when a subcommand ships undocumented | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | PASS: doc-coverage test asserts all subcommands, arguments, flags, exit codes, and error codes are present in `docs/cli.md` |
| `SHOW VIEW STATUS` and `SHOW RESOURCE USAGE` wired through pgwire as SQL-native equivalents | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs` (13 tests) | PASS: all SQL commands callable via real TCP socket with tokio-postgres |
| Bounded audit and memory buffers | `crates/rockstream-cli/tests/cli_read_only_tests.rs::test_cli_audit_tail_bounded_cap` | PASS: tail inspection respects `AUDIT_TAIL_MAX_EVENTS` (1,000) cap |
| SlateDB point and bounded scan reads without range delete | `crates/rockstream-cli/tests/cli_read_only_tests.rs::test_cli_storage_reads_no_range_deletion` | PASS: storage inspection uses bounded scan and point reads |
| Deterministic simulation of continuous polling under faults | `crates/rockstream-sim/tests/cli_inspection_sim_tests.rs::test_cli_continuous_polling_under_simulated_faults` | PASS: SimRuntime fault injection (seed 12345) passes cleanly |
| Security review commission | `SECURITY_REVIEW_COMMISSION.md` | PASS: formally commissioned for remediation at v0.55.2 |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] New behavior has unit tests, golden-output schema tests, LFS, MinIO, and seeded simulation tests.
- [x] SQL/wire-protocol features have reachability tests (e2e pgwire), negative tests (`RS-1001`, `RS-1005`, `RS-1012`), dispatch-wiring verification, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-0001` through `RS-0004`, `RS-1001`, `RS-1005`, `RS-1012`).
- [x] Every control-plane read/tail action operates cleanly with audit records.
- [x] Every new public surface (CLI subcommands, SQL statements, JSON output models) is documented in `docs/cli.md`.
- [x] Every new buffer/window has a named bound (`AUDIT_TAIL_MAX_EVENTS`).
- [x] Every new distributed-coordination path has a seeded `SimRuntime` test (`test_cli_continuous_polling_under_simulated_faults`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate is introduced by v0.53.
- [x] Confirmation that no item has been deferred.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test -p rockstream-cli
rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests
rtk cargo test -p rockstream-sim --test cli_inspection_sim_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.53 is complete.

