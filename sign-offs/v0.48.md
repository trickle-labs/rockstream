# v0.48 Sign-off

**Signed off**: 2026-06-02

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.48 have been verified.

- [x] `cargo test --workspace` passes cleanly (0 failures, 0 ignored)
- [x] `cargo fmt --check` passes
- [x] `cargo clippy -D warnings` passes
- [x] Third-party example connector (`ExampleSdkSource` / `ExampleSdkSink`) passes Tier 1 contract tests
- [x] Iceberg sink implementing Tier 2 `should_flush` with a 10ms epoch produces ≤ 2 files/minute (≥ 256 MB each)
- [x] A Tier 1 connector (Kafka) passes contract tests with the default flush-every-epoch `should_flush`
- [x] Connector-declared CRDT columns round-trip through schema discovery and `EXPLAIN`
- [x] SDK example shows a connector declaring a `COUNTER` column end-to-end
- [x] `partition_filter_support() -> bool` returns false on connectors that do not implement pushdown
- [x] Operator-layer filtering verified to produce identical output when `partition_filter_support=false`
- [x] Connector write-classification metadata surfaces in `EXPLAIN TRANSACTION`
- [x] `LawSchemaMetadata` with `blind_delta`, `read_dependent_delta`, `exact_key_guarded_delta`, `source_exactly_once_protected` write-classification fields implemented and serialises correctly
- [x] Connector lifecycle (pause/resume/delete) implemented on all new connectors
- [x] Connector developer guide published at `docs/connector-developer-guide.md`
- [x] ROADMAP.md status updated to `✅ Done`

## Scope Delivered

### New Types (`rockstream-types/src/connector.rs`)
- `PartitionFilter` / `PartitionPredicate` — opt-in partition push-down
- `WriteClassification` — four variants (`blind_delta`, `read_dependent_delta`, `exact_key_guarded_delta`, `source_exactly_once_protected`)
- `LawColumnMetadata` + `LawSchemaMetadata` — CRDT column schema declaration
- `ExplainTransaction` / `ExplainTransactionColumn` — write-classification in EXPLAIN output
- `ConnectorLifecycleState` — running/paused/deleted state machine

### Updated Contracts
- `Source` trait — `partition_filter_support()`, `start_snapshot()`, `poll_delta()`, `discover_schema()`, lifecycle methods
- `Sink` trait — explicit `should_flush(bytes_buffered, epochs_buffered)`, `discover_schema()`, lifecycle methods

### New Connectors
- `IcebergSink` — Tier 2 `should_flush` with 256 MB threshold
- `ExampleSdkSource` + `ExampleSdkSink` — third-party SDK example with COUNTER column

### Documentation
- `docs/connector-developer-guide.md` — Tier 1 and Tier 2 contracts, SDK usage, DLQ configuration, end-to-end example

## Notes

All unit and integration tests run and pass. The v0.48.0 implementation is
ready for tagging and release.
