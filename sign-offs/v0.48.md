# v0.48 Sign-off

**Signed off**: 2026-07-18

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.48 have been
verified against running code, local test output, and `.claude/v0.48-evidence.md`.

### Proof Claims

- [x] **`UPDATE ... RETURNING` and `DELETE ... RETURNING` now have true read-modify-write semantics**: parser + error-code gates — `rtk cargo test -p rockstream-gateway update_returning_star_parses` and `rtk cargo test -p rockstream-gateway delete_returning_column_list_parses` → 1 passed each; `rtk cargo test -p rockstream-types advanced_dml_and_scatter_pruning_error_codes_registered` → 1 passed, wiring `RS-2013`, `RS-2017`, and `RS-2022`; behavior + oracle + durability — `rtk cargo test -p rockstream-gateway --test gateway_proof_tests update_read_modify_write_preserves_untouched_columns`, `update_returning_returns_new_row_after_commit`, `delete_returning_returns_row_that_existed_before_delete`, `delete_returning_discarded_on_rollback`, `oracle_update_returning_incremental_equals_batch`, and `oracle_delete_returning_incremental_equals_batch` all pass; `rtk cargo test -p rockstream-gateway --test update_read_modify_write_durability_tests` and `rtk cargo test -p rockstream-gateway --test delete_returning_durability_tests` → 2 passed each. Evidence: `.claude/v0.48-evidence.md` (Claims 1–3, Notes A–E).
- [x] **Frontier summaries now piggyback `ShardColumnStats` (min/max + budget-capped Bloom) and publish durable checkpoint state**: `rtk cargo test -p rockstream-types shard_column_stats_roundtrip_tests`, `bloom_filter_never_exceeds_budget_bytes_at_high_cardinality`, and `config_scatter_pruning_defaults_roundtrip` all pass; `rtk cargo test -p rockstream-control checkpoint_persists_shard_column_stats_to_control_catalog` → 1 passed; `rtk cargo test -p rockstream-control --test shard_column_stats_durability_tests` → 2 passed. Evidence: `.claude/v0.48-evidence.md` (Claim 4, Notes F/G).
- [x] **Multi-shard point reads safely bypass >90% of shards based on frontier-summary Bloom filters**: planner/unit gates — `rtk cargo test -p rockstream-gateway scatter_planner_prunes_shards_outside_min_max_bounds`, `scatter_planner_prunes_shards_via_bloom_negative`, and `stale_shard_stats_falls_back_to_full_scatter_with_warning` → 1 passed each; safety oracle — `rtk cargo test -p rockstream-oracle scatter_pruning_result_set_equals_full_scatter_result_set` → 1 passed, 123 filtered out; seeded coordination regressions — `rtk cargo test -p rockstream-sim --test shard_stats_checkpoint_sim_tests --features simulation` → 2 passed (seeds 4801/4802); headline proof — `rtk cargo test -p rockstream-gateway --test gateway_proof_tests proof_multi_shard_point_read_prunes_over_90_percent_of_shards` → 1 passed, 100 filtered out; metrics export — `... scatter_pruning_metrics_exported_correctly` → 1 passed, 100 filtered out. Evidence: `.claude/v0.48-evidence.md` (Claim 5, Notes H/I/J).
- [x] **Secondary indexes publish exact stats into the same pruning path immediately on readiness**: `rtk cargo test -p rockstream-gateway --test gateway_proof_tests create_index_publishes_precise_stats_immediately` and `indexed_column_stats_are_exact_not_probabilistic` both pass, proving `MARK INDEX ... READY` publishes exact membership filters and prunes an in-range non-member with zero false positives. Evidence: `.claude/v0.48-evidence.md` (Claim 6, Note K).
- [x] **Ground rules and no-regression gates hold**: `rtk cargo fmt --check` clean; `rtk cargo clippy --workspace --all-targets -- -D warnings` → no issues; `rtk bash scripts/check-error-codes.sh` → `OK`; `rtk bash scripts/check-invariant-pairs.sh` → `OK`; `rtk cargo test --workspace` → **1498 passed, 6 ignored (128 suites, 2246.89s)**. Evidence: `.claude/v0.48-evidence.md` (Claim 7, Note L).
- [x] **Documentation/status match implementation**: `NEW_ROADMAP.md` marks `v0.48` as `✅ Done`; this sign-off and `.claude/v0.48-evidence.md` record the proof set.

### Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (commands, outputs, regression seeds, and the exact proof mapping)
is recorded in `.claude/v0.48-evidence.md`. New v0.48 additions proven here
include `RS-2013` reuse on the `UPDATE ... RETURNING` read-back miss path,
new `RS-2017`/`RS-2022` registration, `ShardColumnStats` and
`ScatterPruningConfig`, checkpoint durability for shard stats, exact
secondary-index-fed pruning stats, scatter-pruning metrics, and permanent
simulation seeds 4801–4802. Transaction-local update/delete resolution now
consults buffered writes, which also fixed the previously failing Nexmark
retraction workspace regressions.

**Nothing deferred.**
