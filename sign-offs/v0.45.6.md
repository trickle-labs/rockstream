# v0.45.6 Sign-off

**Signed off**: 2026-07-15

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.45.6 have been
verified against running code, local test output, and `make e2e`.

### Proof Claims

- [x] **`scripts/check-invariant-pairs.sh` runs in CI**: New `check` job step
  in `.github/workflows/ci.yml` (lines 37–40), running immediately after
  `check-error-codes.sh`. Local run: `OK: all 36 FizzBee invariants have
  paired Rust coverage.` Evidence: `.claude/v0.45.6-evidence.md` (S1).
- [x] **It fails on a deliberately-reverted `M2-S3`/`M4-S1` assertion-removal
  test case**: `scripts/check-invariant-pairs.test.sh` copies the tree,
  deletes the `M2-S3`/`M4-S1` ID references, asserts exit code 1 on the
  mutated copy, then asserts exit code 0 on the real tree. Local run: `OK:
  check-invariant-pairs.sh self-test passed.` Evidence: `.claude/v0.45.6-evidence.md` (S2).
- [x] **It passes on the real tree only after every one of
  `M1-S1`/`S3`/`S4`/`S5`/`M2-S3`/`M4-S4`/`M5-S1` carries a runtime check or
  `INVARIANT-BY-CONSTRUCTION` comment**: M1-S1 (`shard_db.rs:236`),
  M1-S3/M1-S4 (`recovery.rs:318`), M4-S4 (`fence.rs:127`,
  `brownout.rs:30`), M5-S1 (`sink_connector.rs`/`object_store_sink.rs`
  label fix alongside M5-S3), M2-S3 (new `assert_valid_publisher` +
  `assert_flush_before_lease_handoff_read` in `frontier.rs`). Script exits 0
  on the full tree. Evidence: `.claude/v0.45.6-evidence.md` (S9, S3–S6).
- [x] **The Formal Verification Track table's CI claim is true, not
  aspirational**: `FIZZBEE_TEST_PLAN.md` §3.7 now explicitly names
  `scripts/check-invariant-pairs.sh` (line 667), and the script is actually
  wired into CI. Evidence: `.claude/v0.45.6-evidence.md` (S10).
- [x] **A real 3-aggregator `SimRuntime` scenario proves a stale-fenced
  publisher can never re-publish a frontier after a new leader's token
  supersedes it**: `three_frontier_aggregators_stale_publisher_never_republishes`
  in `crates/rockstream-sim/tests/frontier_publisher_election.rs`, seeds
  100–104, gated by `#[cfg(feature = "simulation")]`. `cargo test -p
  rockstream-sim --features simulation
  three_frontier_aggregators_stale_publisher_never_republishes`: **1
  passed**. Evidence: `.claude/v0.45.6-evidence.md` (S8).
- [x] **Fencing-token CAS check on the frontier-aggregator's publisher role
  (Scope)**: `FrontierLeaseStore::acquire_publisher_lease`/
  `publish_frontier` in `crates/rockstream-control/src/frontier.rs`;
  `assert_valid_publisher` panics with `RS-8002` on a stale token. Evidence:
  `.claude/v0.45.6-evidence.md` (S3, S4).
- [x] **`sync:true`-flush-before-lease-handoff-read assertion (Scope)**:
  `assert_flush_before_lease_handoff_read` in `frontier.rs`, panics with
  `RS-8003` on violation. `cargo test -p rockstream-control
  sync_flush_before_lease_handoff`: **2 passed** (LFS + MinIO). Evidence:
  `.claude/v0.45.6-evidence.md` (S5, S7).
- [x] **No new FizzBee model needed; `formal/m2_frontier_agg.fizz` already
  models and passes `M2_S3_SinglePublisherSafety`**: File unmodified in
  v0.45.6; `make verify` confirms the model still passes (251889 nodes
  explored, `IsLive: true`). Evidence: `.claude/v0.45.6-evidence.md`.
- [x] **`M1-S1`/`S3`/`S4`/`S5`, `M4-S4`, `M5-S1` each get a real assertion or
  justified `INVARIANT-BY-CONSTRUCTION` comment**: Per-ID breakdown in
  `.claude/v0.45.6-plan.md` §2 (S9); all seven confirmed covered.
- [x] **All existing workspace tests pass**: `cargo test --workspace` green
  workspace-wide, plus `check-error-codes.sh`, `check-path-coupling.sh`, and
  `check-exit-criteria.sh` all `OK` with no regression from the new CI step.

### Checklist

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (test names, file/line pointers, and command output) is
recorded in `.claude/v0.45.6-evidence.md`. Deferred-item audit
(`.claude/v0.45.6-plan.md` §1) confirms no prior sign-off's deferred items
overlap this version's scope, and no item from this version's own Scope
(S1–S11) has been deferred: `scripts/check-invariant-pairs.sh` and its
self-test are wired into CI, all seven previously-uncovered invariants
(`M1-S1`/`S3`/`S4`/`S5`, `M4-S4`, `M5-S1`) plus `M2-S3` carry real coverage,
the new frontier-lease CAS path has both durability tests (LFS + MinIO) and
a seeded `SimRuntime` coordination test, and documentation
(`FIZZBEE_TEST_PLAN.md`, `DESIGN.md`) is updated to match.

