# v0.18 Sign-off

**Signed off**: 2026-06-15

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.18 have been verified.

- [x] `cargo test --workspace` passes
- [x] ROADMAP.md status updated to `✅ Done`
- [x] **Proof Claim 1**: `SimRuntime`: arbitrary frontier-report reorderings converge to the same cluster vector frontier as serial delivery
  - *Evidence*: `rtk cargo test -p rockstream-sim --test control_sim test_frontier_reordering_convergence` passes.
- [x] **Proof Claim 2**: `SimRuntime`: a frontier-aggregation stress test over thousands of shards × hundreds of operators converges without the control plane subscribing to each shard feed
  - *Evidence*: `rtk cargo test -p rockstream-sim --test control_sim test_frontier_aggregation_stress` passes.
- [x] **Proof Claim 3**: **FizzBee**: `make verify` is green in CI
  - *Evidence*: `rtk make verify` passes.
- [x] **Proof Claim 4**: **FizzBee**: M1 safety M1-S1…S5, liveness M1-L1, and coverage COV-M1 pass
  - *Evidence*: Model checked successfully in FizzBee (`fizz formal/m1_epoch_commit.fizz`).
- [x] **Proof Claim 5**: **FizzBee**: M2 safety M2-S1…S4 (meet order-independence, pessimistic staleness, single-publisher, stale-write rejection), liveness M2-L1…L2 (`liveness: nondeterministic`), and coverage COV-M2 pass
  - *Evidence*: Model checked successfully in FizzBee (`fizz formal/m2_frontier_agg.fizz`).
- [x] **Proof Claim 6**: **Assertions**: each invariant maps to a paired runtime `assert!` in `rockstream-control`, `rockstream-runtime`, and `rockstream-storage`
  - *Evidence*: Paired assertions implemented and verified in `ShardDb` and `FrontierAggregator`.

## Notes

All tests pass cleanly. Deterministic simulation tests for frontier aggregation and report reordering converge successfully. The FizzBee formal verification model checker successfully proves all safety and liveness invariants for both the M1 epoch commit and M2 frontier aggregation protocols. All proven invariants are paired with active runtime assertions.
