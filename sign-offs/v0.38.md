# v0.38 Sign-off

**Signed off**: 2026-06-28

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.38 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Claims Verification

- [x] **S1: Type System Mapping Extensions**
  - **Evidence**: Asserted OID round-trips for every new type, and `arrow_type_to_pg_oid("Date32")` returns `1082`.
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_type_mappings_s1`

- [x] **S2: Binary Encoding of Numeric Types**
  - **Evidence**: Verified binary serialization matches network byte order (big-endian) compared to expected format.
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_binary_encoding_unit`

- [x] **S2: Postgres 14 Comparison**
  - **Evidence**: Verified binary `INT4`/`INT8`/`FLOAT8`/`BOOL` format is bit-identical to Postgres 14 wire encoding (compared against a reference Postgres 14 TC instance).
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_binary_encoding_postgres_comparison --features testcontainers`

- [x] **S3: pg_catalog & information_schema**
  - **Evidence**: Verified catalog tables return expected columns and rows case-insensitively.
  - **Command**: `rtk cargo test -p rockstream-gateway --test gateway_proof_tests`

- [x] **S4: ORM Conformance - SQLAlchemy**
  - **Evidence**: Verified SQLAlchemy `inspect(engine).get_columns('my_view')` returns correct column names and Python types without warning.
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_orm_conformance --features testcontainers`

- [x] **S4: ORM Conformance - Prisma**
  - **Evidence**: Verified `prisma db pull` exits 0 and the resulting `schema.prisma` is parseable.
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_orm_conformance --features testcontainers`

- [x] **S4: ORM Conformance - Hibernate**
  - **Evidence**: Verified Hibernate `getColumns(null, "public", "my_view", "%")` returns correct columns.
  - **Command**: `rtk cargo test -p rockstream-gateway --test type_system_tests test_orm_conformance --features testcontainers`

- [x] **Workspace Regression Suite**
  - **Evidence**: Verified all 106 existing gateway tests pass (8 suites).
  - **Command**: `rtk cargo test -p rockstream-gateway`

## Notes

All OID mappings, binary numeric encoding, catalog/information_schema stubs, and ORM conformance testing for SQLAlchemy, Prisma, and Hibernate have been successfully implemented and verified. No items have been deferred to a later version.
