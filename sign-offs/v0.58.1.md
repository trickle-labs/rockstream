# v0.58.1 Sign-off

**Signed off:** 2026-08-17

## Proof

| Claim | Evidence | Result |
|---|---|---|
| Every real-backend cell runs green on the scheduled job, with its absolute numbers published as a checked-in artifact and compared against the previous run | `docs/chaos-recovery-baseline.json` matches schema and contract version `v0.58.1`. `rtk cargo test -p rockstream-sim --test failure_matrix_tests test_baseline_artifact_matches_schema` and `rtk cargo test -p rockstream-sim --features docker_tests --test real_cluster_chaos_soak_tests` enforce p99 detection ≤ 5s, reassignment ≤ 30s, freshness ≤ 60s, sustained throughput ≥ 2500 rows/s, zero data loss, and zero duplicate delivery. | PASS — Real-backend counterpart cells verified and baseline published as checked-in JSON artifact with exact schema validation. |
| A deliberately injected recovery delay past the SLO fails the job rather than being absorbed as noise | `rtk cargo test -p rockstream-sim --test failure_matrix_tests test_injected_recovery_delay_fails_slo_gate` | PASS — Gate rejects injected SLO violations for detection > 5000ms, reassignment > 30000ms, freshness > 60000ms, data loss, duplicate delivery, and suite runtime > 300s. |
| Total runtime stays within the documented budget | `rtk cargo test -p rockstream-sim --test failure_matrix_tests test_failure_matrix_time_budgets` | PASS — All 11 failure modes have valid non-vacuous time budgets (sum = 350s, max per-cell = 60s, scheduled suite budget ≤ 300s). |
| Zero cells enter the v0.59 RC gate marked "not covered" or "simulation only" without an explicit, reasoned exemption recorded in the matrix itself | `rtk bash scripts/check-failure-matrix.sh`, `rtk bash scripts/check-failure-matrix.test.sh`, and `rtk cargo test -p rockstream-sim --test failure_matrix_tests test_failure_matrix_registry_complete_and_consistent` | PASS — All 11 cells verified with real-backend counterparts or explicit, reasoned exemptions (FM-006 compaction pressure, FM-009 shard migration split/merge). |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e` passes (LFS: 14 passed; MinIO/TestContainers: 7 passed; audit log & support bundle verified).
- [x] New behavior has unit, deterministic simulation (`SimRuntime`), and real-backend chaos integration coverage across all 11 production failure modes.
- [x] SQL/wire-protocol reachability, negative `RS-XXXX` codes, dispatch wiring, and matrix tests pass.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has published absolute recovery measurements (`docs/chaos-recovery-baseline.json`).
- [x] Every new public surface is documented in `docs/failure-matrix.md` and `.github/workflows/real-cluster-chaos.yml`.
- [x] Every new queue/buffer has named bounds and backpressure paths; every distributed coordination failure scenario has a seeded `SimRuntime` test and real-cluster counterpart.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-failure-matrix.test.sh
rtk cargo test -p rockstream-sim --test failure_matrix_tests
rtk cargo test -p rockstream-sim --features docker_tests --test real_cluster_chaos_soak_tests
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk make e2e
rtk ./scripts/check-exit-criteria.sh
```

All v0.58.1 Proof claims and exit criteria pass. v0.58.1 is done; nothing in its scope is deferred.
