# v0.49 Sign-off

**Signed off**: 2026-07-19

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.49 have been
verified against running code, local test output, and
`.claude/v0.49-evidence.md`.

### Proof Claims

- [x] **Zero byte-copying observed in CPU profiles for same-host worker exchanges**: `rtk cargo test -p rockstream-runtime same_host_shared_memory_fast_path_delivers_exact_rows`, `same_host_shared_memory_never_opens_a_grpc_payload_stream`, and `proof_same_host_shared_memory_cpu_profile_reports_zero_copies` all pass, proving the shared-memory fast path replaces the old gRPC payload path with zero copies. Peer-crash fallback is covered by a new seeded sim test, `same_host_shared_memory_peer_crash_falls_back_to_grpc_without_duplication_sim` (`rtk cargo test -p rockstream-sim --test az_aware_exchange_sim_tests --features simulation` → 3 passed), using `buggify!("exchange.shm_segment_unavailable", p)` to force a fallback with no duplication. Evidence: `.claude/v0.49-evidence.md` (Claims 1–2).
- [x] **Cross-AZ traffic drops to near zero during shuffle phases**: `rtk cargo test -p rockstream-runtime cross_az_peers_never_open_direct_grpc_streams`, `hierarchical_domains_group_workers_by_az`, and `proof_cross_az_direct_bytes_drop_to_near_zero` all pass. Two new seeded sim tests close fault-injection gaps: `missing_az_metadata_falls_back_without_row_loss_sim` (`buggify!("exchange.az_metadata_missing", p)`) and `az_domain_rebuild_during_drain_preserves_delivery_sim` (`buggify!("exchange.domain_rebuild_during_drain", p)`), both in `crates/rockstream-sim/tests/az_aware_exchange_sim_tests.rs`, passing with zero row loss and no stray cross-AZ direct streams. Evidence: `.claude/v0.49-evidence.md` (Claim 3).
- [x] **Shuffle-payload encoding is implemented, path-specific, and rolling-upgrade-safe**: `rtk cargo test -p rockstream-runtime legacy_raw_arrow_ipc_decodes_under_codec_v1`, `lz4_shuffle_payload_roundtrip_is_exact`, `zstd_shuffle_payload_roundtrip_is_exact`, and `legacy_peer_receives_raw_ipc_until_capability_floor_advances` all pass. New durability coverage in `crates/rockstream-runtime/tests/shuffle_codec_durability_tests.rs` (`rtk cargo test -p rockstream-runtime --test shuffle_codec_durability_tests` → 4 passed) proves legacy raw-IPC, LZ4/ZSTD-framed shuffle payloads, and the same-host SHM fast path all replay correctly after both an LFS restart and a MinIO/TestContainers restart. Evidence: `.claude/v0.49-evidence.md` (Claim 4).
- [x] **Shuffle-payload compression reduces measured cross-worker network bytes by ≥40% on a representative wide-shuffle TPC-H/Nexmark workload with zero divergence from the oracle**: `rtk cargo test -p rockstream-runtime proof_wide_shuffle_network_bytes_drop_over_40_percent` passes; `rtk cargo test -p rockstream-oracle --test shuffle_transport_oracle` → 2 passed (`oracle_shuffle_transport_modes_match_tpch_batch`, `oracle_shuffle_transport_modes_match_nexmark_batch`), proving zero divergence between compressed and uncompressed shuffle transport across both workloads. Evidence: `.claude/v0.49-evidence.md` (Claim 5).
- [x] **Per-epoch CPU overhead on the direct gRPC path stays within the SLO budget, and the auto-tuner can disable compression per exchange if it does not**: `rtk cargo test -p rockstream-runtime compression_tuner_disables_lz4_when_cpu_budget_exceeded`, `compression_tuner_holds_disable_until_hysteresis_clears`, and `compression_state_map_never_exceeds_bound` all pass. New test `exchange_bench_direct_lz4_epoch_cpu_budget` (`rtk cargo test -p rockstream-runtime --test exchange_bench_direct_lz4_epoch_cpu_budget` → 1 passed) enforces the measured per-epoch LZ4 cost against the `AutotunerConfig` budget knobs as a pass/fail assertion, backed by the `bench_direct_lz4_epoch_cpu_budget` criterion group in `crates/rockstream-runtime/benches/exchange_bench.rs`. Evidence: `.claude/v0.49-evidence.md` (Claim 6).
- [x] **Cluster checkpoints are ZSTD-compressed once legal, and both legacy/new manifests recover correctly**: `rtk cargo test -p rockstream-control checkpoint_store_roundtrip_zstd_manifest` and `legacy_json_checkpoint_manifest_still_loads` both pass. New durability coverage in `crates/rockstream-control/tests/checkpoint_manifest_durability_tests.rs` (`rtk cargo test -p rockstream-control --test checkpoint_manifest_durability_tests` → 2 passed) proves ZSTD-framed manifests survive an LFS restart and both legacy plain-JSON and new ZSTD manifests survive a MinIO/TestContainers restart. Evidence: `.claude/v0.49-evidence.md` (Claim 7).
- [x] **No SlateDB range deletion is introduced on any new persistence path**: `rtk cargo test -p rockstream-control checkpoint_store_uses_put_and_scan_delete_not_range_delete` and `rtk cargo test -p rockstream-runtime exchange_codec_gc_uses_scan_and_delete_not_range_delete` both pass. Evidence: `.claude/v0.49-evidence.md` (Claim 8).
- [x] **Every new buffer / cache / pool has a named bound + fill-level metric + fallback/backpressure path**: `rtk cargo test -p rockstream-runtime shared_memory_segment_pool_enforces_bound_and_falls_back_to_grpc` and `compression_state_map_never_exceeds_bound` both pass. New durability coverage in `crates/rockstream-control/tests/worker_location_capabilities_durability_tests.rs` (`rtk cargo test -p rockstream-control --test worker_location_capabilities_durability_tests` → 2 passed) proves the bounded `WorkerLocation`/`WorkerCapabilities` routing metadata persists correctly across an LFS restart and a MinIO/TestContainers restart. Evidence: `.claude/v0.49-evidence.md` (Claim 9).
- [x] **Ground rules and no-regression gates hold**: `rtk cargo fmt --check` clean; `rtk cargo clippy --workspace --all-targets -- -D warnings` → no issues; `rtk bash scripts/check-error-codes.sh` → `OK: all logged/returned errors carry an RS-XXXX code.`; `rtk bash scripts/check-invariant-pairs.sh` → `OK: all 41 FizzBee invariants have paired Rust coverage.`; `rtk cargo test --workspace` → **1546 passed, 6 ignored (134 suites, 2163.66s)**, 0 failed; `rtk make e2e` → `=== e2e PASSED ===` (LFS backend 13/13, MinIO backend 6/6). Evidence: `.claude/v0.49-evidence.md` (ground-rule table, Claim 10).
- [x] **Documentation/status match implementation**: `NEW_ROADMAP.md` marks `v0.49` as `✅ Done`; this sign-off and `.claude/v0.49-evidence.md` record the full proof set.

### Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (commands, outputs, regression seeds, and the exact proof
mapping) is recorded in `.claude/v0.49-evidence.md`. New v0.49 additions
proven here include the exchange path classifier consuming `ExchangeAnn`
directly, the same-host shared-memory (Arrow Flight SHM) zero-copy fast
path with `buggify!`-driven fallback coverage, AZ-aware hierarchical
exchange domains with two new fault-injected sim tests
(`exchange.az_metadata_missing`, `exchange.domain_rebuild_during_drain`),
the LZ4/ZSTD shuffle-payload codec (`codec_v1`) with rolling-upgrade
legacy-raw-IPC compatibility, the CPU-budget-aware direct-path compression
auto-tuner, ZSTD-compressed cluster checkpoint manifests with legacy
fallback, and durability coverage across LFS and MinIO/TestContainers for
all of the above. Nine net-new passing tests
(`crates/rockstream-control/tests/worker_location_capabilities_durability_tests.rs`,
`crates/rockstream-control/tests/checkpoint_manifest_durability_tests.rs`,
`crates/rockstream-runtime/tests/shuffle_codec_durability_tests.rs`,
`crates/rockstream-runtime/tests/exchange_bench_direct_lz4_epoch_cpu_budget.rs`,
`crates/rockstream-sim/tests/az_aware_exchange_sim_tests.rs`) closed a gap
the deferred-item audit found between the previously-implemented
Tracks A/B/C/D9/D10 core work and the plan's full §4/§5/§6 proof
obligations. Nothing deferred.
