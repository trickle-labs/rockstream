# v0.52.5 Sign-off

**Signed off**: 2026-08-14

## Proof Claims

All claims in the v0.52.5 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| Supported boundary is PostgreSQL CDC source, Kafka source, and Kafka sink; removed surfaces are absent | `connector_surface_contract_tests`; `check-connector-admission.sh`; `check-no-mock-connectors.sh` | PASS: published surface and admission gates pass; removed-connector prose gate passes |
| Removed surfaces fail closed with `RS-4017` and replacement guidance | `source_ddl_tests` | PASS: pgwire reachability and negative cases pass with actionable errors |
| Published machine-checked guarantee table and connector registry | `cargo test -p rockstream-connectors --test connector_surface_contract_tests` | PASS: 6 tests |
| Exact actionable `RS-4001` through `RS-4022` registry | `cargo test -p rockstream-types connector_error_codes_registered_and_actionable` | PASS: 1 test |
| PostgreSQL CDC nine-cell real-Postgres matrix | `postgres_cdc_guarantee_matrix_tests` | PASS: 9 tests |
| Kafka source seven-cell real-broker matrix | `kafka_source_guarantee_matrix_tests` | PASS: 7 tests |
| Kafka sink seven-cell real-broker matrix | `kafka_sink_guarantee_matrix_tests` | PASS: 7 tests |
| LFS committed-checkpoint recovery | `retained_source_checkpoint_recovery_has_exact_cdc_and_kafka_transcript_lfs` | PASS: 1 test |
| MinIO committed-checkpoint recovery | `retained_source_checkpoint_recovery_has_exact_cdc_and_kafka_transcript_minio` | PASS: 1 test |
| No SlateDB range-delete cleanup dependency | `source_cleanup_tests backfill_cleanup_uses_bounded_scan_and_point_delete` | PASS: 1 test |
| Seeded coordination regressions | `source_coordination_sim_tests` | PASS: 9 tests |
| Admission rejection and acceptance | `scripts/check-connector-admission.test.sh` and `check-connector-admission.sh` | PASS |
| Pgwire `CREATE SOURCE` reachability and negative errors | `rockstream-gateway --test source_ddl_tests` | PASS: 9 tests |
| Real Kafka source pgwire reachability | `source_ddl_postgres_kafka_tests::kafka_source_backfill_and_live_updates_reach_pgwire` | PASS |
| Real PostgreSQL CDC pgwire reachability and invalid-option handling | `source_ddl_postgres_kafka_tests::postgres_cdc_pgoutput_backfill_live_update_and_restart_reach_pgwire` and invalid-option test | PASS |
| Kafka parser/dispatcher/executor wiring | `server.rs` wiring grep and source pgwire tests | PASS |
| PostgreSQL CDC parser/dispatcher/executor wiring | `server.rs` wiring grep and source pgwire tests | PASS |
| SQL route and response encoder wiring | `server.rs` route/response grep and source pgwire tests | PASS |
| Connector coverage floor | CI configuration and coverage evidence | PASS: lines 84%, regions 86% floors |
| CLI remains runnable | `cargo run -p rockstream-cli --bin rockstream -- --help` | PASS |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy -- -D warnings` passes.
- [x] `rtk cargo test --workspace` completes outside the TCP-restricted sandbox.
- [x] New behavior has unit tests and real external-system, LFS, MinIO, and seeded simulation tests where applicable.
- [x] SQL/wire reachability, negative errors, dispatch wiring, and coverage matrix tests pass.
- [x] User-visible failures use actionable `RS-XXXX` codes.
- [x] No control-plane action, benchmark claim, queue/buffer/scan window, or distributed-coordination path is introduced without its required evidence.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate is introduced by v0.52.5.
- [x] No new deferred item remains.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```text
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.52.5 is complete.
