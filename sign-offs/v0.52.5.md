# v0.52.5 Sign-off

**Signed off**: 2026-06-04

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.52.5 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

- Implemented `catalog_rest.rs` in `crates/rockstream-gateway/src/` — a minimal tokio TCP HTTP server exposing:
  - `GET /catalog/v1/health` — liveness probe returning `{"status":"ok"}`
  - `GET /catalog/v1/namespaces` — lists all namespaces with names
  - `GET /catalog/v1/namespaces/{ns}/tables` — lists tables in a namespace
  - `GET /catalog/v1/merge-laws` — exposes all registered merge laws from `LawRegistry`
- Added `CatalogRegistry` — a thread-safe, in-process namespace/table registry seeded with the demo schema (`public`, `marketing`) and synced from `InlineViewCatalog::view_names()`.
- Wired `--catalog-bind` CLI flag (default `0.0.0.0:8181`) to `rockstream start` in all relevant roles (`gateway`, `all`).
- Added `test_v0_52_5_catalog_rest_server` E2E test covering:
  1. Catalog health probe
  2. Namespace listing (including JSON validation)
  3. Tables listing with known demo table assertions
  4. Metadata coherence: SQL-visible schemas appear in HTTP catalog
  5. Merge laws via HTTP
  6. Post-restart catalog consistency
  7. Unknown path → 404
  8. Regression matrix smoke-testing v0.52.1..v0.52.4 surfaces
- Added `view_names()` method to `InlineViewCatalog` for catalog sync.
- Bumped workspace version to `0.52.5` in root `Cargo.toml`.
