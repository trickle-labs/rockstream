# v0.26 Sign-off — Auth, RBAC, and Read Pushdown

**Signed off**: 2026-06-17

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — **794 passed** (13 suites, 46.31s)
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`

---

## Proof Claims

### P1: Standard Postgres driver creates a view, writes rows, reads them back (RYW), and subscribes to changes end-to-end

| Claim | Test | Result |
|-------|------|--------|
| psql connects with valid bearer token | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| `CREATE MATERIALIZED VIEW` succeeds (pipeline_owner role) | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| `INSERT … COMMIT` succeeds; audit log has `actor = "jwt:<sub>"` | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| `SELECT * FROM orders_mv` RYW sees just-written row within freshness SLO | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| `SUBSCRIBE orders_mv AS OF NOW WITH SNAPSHOT` delivers snapshot + live delta | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| Unauthenticated connection attempt → RS-2400 | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| Viewer in ns-a tries SELECT from ns-b → RS-2402 | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| `EXPLAIN SELECT … GROUP BY` output contains `partial_pushdown: true` | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |

**P1 verdict**: ✅ Satisfied. (`--features testcontainers`)

---

### P2: Unauthenticated requests are rejected

| Claim | Test | Result |
|-------|------|--------|
| Missing or expired token returns RS-2400 | `jwt_missing_or_invalid_returns_rs2400` | ✅ PASS |
| Gateway with `--auth=oidc`, no credentials → RS-2400 before any shard read | `proof_auth_rejects_unauthenticated_lfs` | ✅ PASS |

**P2 verdict**: ✅ Satisfied.

---

### P3: Cross-namespace access is denied

| Claim | Test | Result |
|-------|------|--------|
| Session in namespace `a`, SELECT from namespace `b` view → RS-2402 | `namespace_isolation_blocks_cross_access` | ✅ PASS |
| Two namespaces; grant viewer on ns-a; SELECT from ns-b → RS-2402 | `proof_rbac_denies_cross_namespace_access` | ✅ PASS |
| Admin principal may access cross-namespace | `admin_can_access_cross_namespace` | ✅ PASS |

**P3 verdict**: ✅ Satisfied.

---

### P4: Audit log records `actor` on every control action

| Claim | Test | Result |
|-------|------|--------|
| Gateway COMMIT with Jwt principal `sub=alice` → audit event `actor = "jwt:alice"` | `audit_carries_principal_actor` | ✅ PASS |
| `--auth=off` / system path → audit event `actor = "system"` | `audit_carries_system_actor_when_auth_off` | ✅ PASS |

**P4 verdict**: ✅ Satisfied.

---

### P5: `EXPLAIN` names the pushdown effect

| Claim | Test | Result |
|-------|------|--------|
| `EXPLAIN SELECT k, COUNT(*) FROM mv GROUP BY k` → output contains `partial_pushdown: true` | `explain_names_pushdown_effect` | ✅ PASS |
| `EXPLAIN SELECT * FROM mv` does not contain `partial_pushdown: true` | `explain_no_pushdown_for_full_scan` | ✅ PASS |

**P5 verdict**: ✅ Satisfied.

---

## Infrastructure & Bounds

| Component | Bound | Test |
|-----------|-------|------|
| JWKS cache | `MAX_JWKS_ENTRIES` | invariant ✅ PASS |
| ACL cache | `MAX_ACL_CACHE_ENTRIES` (60 s TTL) | invariant ✅ PASS |
| Partial-agg result buffer | `MAX_PARTIAL_AGG_RESULT_ROWS` per shard; RS-2002 on overflow | `partial_agg_shard_query_too_large_returns_rs2002` ✅ PASS |
| ACL entries | Point-delete only, no range deletion | `acl_no_range_delete_in_catalog_acl` ✅ PASS |

---

## Definition-of-Done Checklist

- [x] `cargo fmt --check` — passes (enforced by CI)
- [x] `cargo clippy -- -D warnings` — passes (enforced by CI)
- [x] `cargo test --workspace` — 794 passed (13 suites, 46.31s)
- [x] New behavior has unit tests plus oracle/property, LFS, and TestContainers tests (S1–S9, auth_proof_tests, gateway_proof_tests)
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (RS-2400, RS-2401, RS-2402, RS-2002)
- [x] Every control-plane action writes an audit event (`audit_carries_principal_actor`, `audit_carries_system_actor_when_auth_off`)
- [x] Every new performance claim has a measurement note (partial-agg returns O(distinct groups × shards) rows)
- [x] Every new public surface is documented (DESIGN.md §12.3.1: partial aggregation pushdown; auth flag `--auth=off/oidc/mtls`; RBAC roles viewer/pipeline_owner/admin; `CREATE NAMESPACE`)
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path (`MAX_JWKS_ENTRIES`, `MAX_ACL_CACHE_ENTRIES`, `MAX_PARTIAL_AGG_RESULT_ROWS`, RS-2002)
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test (no new distributed coordination — auth and RBAC are gateway-local/control-plane-local; M1–M4 cover all epoch/frontier/fault-tolerance paths)
- [x] No code path depends on SlateDB range deletion (`acl_no_range_delete_in_catalog_acl` ✅)
- [x] `main` remains runnable through the single `rockstream` binary (`make e2e` passes)
- [x] No item has been deferred

---

## Reproduction Commands

```bash
# Full workspace test suite
cargo test --workspace

# Auth and Postgres Pillar proof suite (no testcontainers)
cargo test --test auth_proof_tests --release

# Auth and Postgres Pillar proof suite (with testcontainers)
cargo test --test auth_proof_tests --release --features testcontainers

# Gateway proof tests
cargo test --test gateway_proof_tests --release

# Exit-criteria gate
./scripts/check-exit-criteria.sh

# End-to-end smoke test
make e2e
```

---

## New RS-XXXX Codes Added

| Code | Slug | Description |
|------|------|-------------|
| RS-2400 | `auth.unauthenticated` | Request carries no valid credential |
| RS-2401 | `auth.permission_denied` | Principal lacks the required role for this operation |
| RS-2402 | `auth.namespace_access_denied` | Principal may not access resources in the target namespace |
| RS-2002 | `query.result_too_large` | Partial-aggregation result buffer exceeded `MAX_PARTIAL_AGG_RESULT_ROWS` |

---

**v0.26 is DONE. No items deferred.**

