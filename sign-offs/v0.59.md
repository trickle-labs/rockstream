# v0.59 Engineering Sign-off — v1 Qualification Reopened

**Signed off:** 2026-08-18

## Superseding Status Correction — 2026-08-18

This document proves completion of the v0.59 implementation scope and its short
CI gate. It does **not** authorize `v1.0.0` or establish that the named release
claims were observed against one immutable, published artifact. The PASS rows
below are retained as reproducible engineering evidence, but their former RC1
interpretation is superseded by [NEW_ROADMAP.md](../NEW_ROADMAP.md)
v0.59.1–v0.59.3.

| Qualification area | Status |
|---|---|
| v0.59 implementation completion | PASS |
| Short CI and formal gate | PASS |
| Candidate identity and SHA-/digest-bound evidence manifest | OPEN — v0.59.1 |
| Independent security-review provenance | OPEN — v0.59.3; current evidence is an internal security readiness review |
| Reproducible signed release artifacts and publication | OPEN — v0.59.3 |
| Real no-skip multi-process correctness, recovery, upgrade, restore, resource, and performance qualification | OPEN — v0.59.2 |
| Extended multi-hour or multi-day soak | OPTIONAL — not run for this candidate and not a release prerequisite |
| Final `v1.0.0` authorization | OPEN — only after v0.59.1–v0.59.3 and the exact signed candidate pass |

## Proof

| Claim | Evidence | Result |
|---|---|---|
| Entry criteria are met before the soak starts: v0.58.3's audit script re-runs clean, v0.58's matrix has zero uncovered cells, and every version through v0.58.3 is signed off | `rtk bash scripts/check-dispatch-wiring.sh`<br>`rtk bash scripts/check-no-unreachable.sh`<br>`rtk bash scripts/check-failure-matrix.sh`<br>`rtk bash scripts/check-exit-criteria.sh` | PASS — 21/21 statement families verified with 0 `MISSING` paths; zero `unreachable!()` calls in production crates; 11/11 failure matrix production modes validated; all prior versions signed off. |
| Automated Release Candidate Gate verification and mutation self-test pass | `rtk bash scripts/check-release-candidate-gate.sh`<br>`rtk bash scripts/check-release-candidate-gate.test.sh` | PASS — Automated RC validator verifies all 4 entry criteria and 7 RC gates; self-test confirms mutation detection on broken reachability, broken recovery SLOs, SlateDB range deletes, missing upgrade docs, open P0 advisories, and low throughput. |
| Gate 1 (Correctness): No known silent-wrong-answer path; every documented SQL keyword/statement reaches pgwire; invalid inputs return `RS-XXXX` codes | `rtk cargo test -p rockstream-gateway --test unscoped_pgwire_reachability_tests`<br>`rtk cargo test -p rockstream-gateway --test unscoped_silent_wrong_answer_tests` | PASS — 14/14 reachability tests pass via pgwire dispatcher; 5/5 negative tests pass with structured `RS-XXXX` error responses. |
| Gate 2 (Recovery): No lost or duplicated committed state under the v0.58/v0.58.1 failure matrix across all 11 production failure modes under v0.22/v0.31 SLOs | `rtk cargo test -p rockstream-sim --test failure_matrix_tests`<br>`rtk cargo test -p rockstream-sim --test chaos_tests` | PASS — 16 passed in `failure_matrix_tests`, 15 passed in `chaos_tests`. Real-backend mappings and simulation assertions confirm zero lost rows and zero duplicates across all 11 production failure modes. |
| Gate 3 (Bounded Resources): Bounded memory and state under v0.51.23/v0.51.24 accounting and spill plus v0.58.2 storage-pressure shedding in 3-step order; zero SlateDB range deletes | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests`<br>`rtk bash scripts/check-operator-state-bytes.sh` | PASS — 13/13 tests pass in `storage_pressure_admission_tests`; all stateful operators implement `state_bytes` accounting; zero SlateDB range deletes across codebase. |
| Gate 4 (Operability): Every degradation explainable through v0.54.1's enumerated reasons; full introspection via `SHOW` queries and metrics | `rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests` | PASS — 25/25 tests pass. `SHOW VIEW STATUS`, `SHOW WORKLOAD STATUS`, and `SHOW RESOURCE USAGE` return full introspection metadata. |
| Gate 5 (Upgradeability & Disaster Recovery): Zero-epoch-loss N→N+1 rolling upgrade; fresh cluster restore from independent S3/bucket export with bit-identical state | `rtk cargo test -p rockstream-control --test disaster_recovery_tests`<br>`rtk cargo test -p rockstream-cli --test disaster_recovery_runbook_tests` | PASS — 7 passed in `disaster_recovery_tests`, 1 passed in `disaster_recovery_runbook_tests`. Bit-identical state restoration and RPO/RTO compliance verified. |
| Gate 6 (Security): Closed security review with zero open P0/P1 findings; threat model linked to passing tests; RBAC and mTLS verified | `rtk bash scripts/check-threat-model-links.sh`<br>`rtk bash scripts/check-dependency-audit.test.sh`<br>`rtk cargo test -p rockstream-cli --test cli_mutating_commands_tests`<br>`rtk cargo test -p rockstream-gateway --test gateway_mutation_authorization_tests` | PASS — Closed commissioned engagement recorded in `SECURITY_REVIEW_COMMISSION.md` with 0 open P0/P1 advisories; threat model linked to passing tests; mutating CLI/DDL and gateway auth verified. |
| Gate 7 (Performance Stability & Single-Region Boundary): Sustained throughput ≥ 2500 rows/s under chaos; p99 freshness recovery ≤ 60s; p99 detection ≤ 5s; flat memory churn; single-region operation | `rtk cargo test -p rockstream-sim --test recovery_slo_tests` | PASS — 4/4 passed. Recovery metrics and performance envelope match `docs/chaos-recovery-baseline.json`. Single-region operation verified. |
| DC.4 Pre-Release Formal Verification: Relaxed-bounds sweep across coordination models (M1–M7) in `formal/*.fizz` and `FIZZBEE_TEST_PLAN.md` | `rtk make verify-relaxed`<br>`rtk bash scripts/check-path-coupling.sh` | PASS — Model checker completed successfully across all models (smoke, M1, M2, M3, M4, M6, M7) at `NUM_WORKERS=3 NUM_SHARDS=3 MAX_EPOCH=4`. Path-coupling check clean. |
| Durability: LFS Backend (real SlateDB on local filesystem) | `rtk make e2e-lfs` | PASS — 14/14 tests pass. |
| Durability: MinIO Backend / TestContainers (real S3-compatible store) | `rtk make e2e-nextest` | PASS — 21/21 tests pass (14 LFS + 7 MinIO). |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes (2559+ tests pass across workspace).
- [x] `rtk make e2e-lfs` and `rtk make e2e-nextest` pass (LFS: 14 passed; MinIO/TestContainers: 7 passed).
- [x] New behavior has unit, simulation, reachability, and negative tests.
- [x] SQL/wire-protocol reachability: Every SQL keyword/statement has raw SQL through the real gateway dispatcher.
- [x] Negative tests: Invalid input returns structured `RS-XXXX` errors with actionable text.
- [x] Dispatch-wiring verification: Every path in 4-stage pipeline is tested and proven connected with 0 missing paths.
- [x] Coverage matrix: All 7 Release Candidate Gates and failure modes verified.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml` and `docs/language-features.md`.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime` and FizzBee relaxed-bounds sweep.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk bash scripts/check-release-candidate-gate.sh
rtk bash scripts/check-release-candidate-gate.test.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk bash scripts/check-no-unreachable.sh
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-threat-model-links.sh
rtk bash scripts/check-dependency-audit.test.sh
rtk bash scripts/check-path-coupling.sh
rtk bash scripts/check-operator-state-bytes.sh
rtk cargo test -p rockstream-gateway --test unscoped_pgwire_reachability_tests
rtk cargo test -p rockstream-gateway --test unscoped_silent_wrong_answer_tests
rtk cargo test -p rockstream-sim --test failure_matrix_tests
rtk cargo test -p rockstream-sim --test chaos_tests
rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests
rtk cargo test -p rockstream-sim --test recovery_slo_tests
rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests
rtk cargo test -p rockstream-control --test disaster_recovery_tests
rtk cargo test -p rockstream-cli --test disaster_recovery_runbook_tests
rtk make verify-relaxed
rtk make e2e-lfs
rtk make e2e-nextest
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk ./scripts/check-exit-criteria.sh
```

All v0.59 implementation proof claims and short-gate exit criteria pass. The
engineering milestone is done; v1 release qualification and authorization remain
open under v0.59.1–v0.59.3.

