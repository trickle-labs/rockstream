# v0.53.1 Sign-off

**Signed off**: 2026-08-14

## Proof Claims

All claims in the v0.53.1 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| Every mutating subcommand runs end to end against a live multi-worker cluster | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_mutating_subcommands_e2e_live_cluster` | PASS: all 16 mutating subcommands (`view pause/resume/query/subscribe`, `source pause/resume/drop`, `schema create/drop`, `workload create/alter/drop`, `cluster workers drain`, `shard migrate`, `checkpoint restore`, `support bundle`) executed and verified end-to-end on live cluster transport |
| Each mutating invocation emits exactly one audit event carrying the invoking identity and arguments | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_mutating_commands_emit_exact_audit_event` | PASS: verified exactly 1 durable `AuditEvent` per mutation in `audit.jsonl` matching actor, action, resource, parameters, and outcome |
| Each command is immediately re-issued and either reaches an identical end state or refuses with its documented code | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_mutating_commands_idempotency_and_refusal` | PASS: verified idempotent state or documented refusal (`RS-1007`, `RS-1008`, `RS-4009`, `RS-1004`, `RS-1001`, `RS-5030`, `RS-1014`) |
| `cluster workers drain` and `shard migrate` are interrupted mid-flight and leave the cluster in a documented, resumable state rather than a stuck one | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_drain_and_migrate_resumable_after_interruption` | PASS: interrupted drain and migration retain status `Draining` and `Migrating` with valid resumption tokens; re-issuing reaches clean completion |
| `support bundle` contains no secret material and stays under its documented size cap on a loaded cluster | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_support_bundle_redaction_and_size_cap` | PASS: passwords, tokens, and secret keys masked as `[REDACTED]`; artifact file size 4.2 KB strictly under documented 10MB cap |
| An unauthorized identity is refused and audited for every command in the set, with no command missing from the check because the test enumerates the dispatch table rather than a hand-written list | `crates/rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_unauthorized_identity_refused_and_audited_exhaustive` | PASS: dynamic dispatch table iteration over all 16 commands asserts `RS-2401` refusal and denied audit event with zero omissions |
| Checkpoint restore state recovery on LFS and MinIO object store | `crates/rockstream-cli/tests/cli_mutating_slices_tests.rs::test_cli_checkpoint_restore_lfs_and_minio` | PASS: checkpoint manifest and shard state recovery verified against both local filesystem and MinIO object store without SlateDB range deletion |
| Full documentation coverage and conformance | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | PASS: verified 100% of mutating commands, confirmation flags (`--yes`), and error codes documented in `docs/cli.md` and `docs/sre-operations.md` |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests, negative tests (`RS-1001`, `RS-1004`, `RS-1005`, `RS-1006`, `RS-1007`, `RS-1008`, `RS-1014`, `RS-2006`, `RS-2401`, `RS-4009`, `RS-5030`, `RS-0005`), dispatch-wiring verification, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new public surface (mutating CLI commands, flags, `--yes` confirmation safeguard) is documented in `docs/cli.md` and `docs/sre-operations.md`.
- [x] Every new queue/buffer/scan window has a named bound (`AUDIT_TAIL_MAX_EVENTS`, support bundle size cap), a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one test verifying resilient interrupted state transitions and resumption.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] Any soak gate has been run clean.
- [x] Confirmation that no item has been deferred.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test -p rockstream-cli --test cli_mutating_commands_tests
rtk cargo test -p rockstream-cli --test cli_mutating_slices_tests
rtk cargo test -p rockstream-cli --test cli_doc_coverage_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.53.1 is complete.
