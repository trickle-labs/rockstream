# v0.45.7 Sign-off

**Signed off**: 2026-07-15

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.45.7 have been
verified against running code, local test output, and `make e2e`.

### Proof Claims

- [x] **The extended `check-error-codes.sh` fails CI on a
  deliberately-reintroduced bare `Err(format!("boom"))` anywhere in
  `crates/`**: `scripts/check-error-codes.sh` Pass 2 now flags any bare
  `Err(format!(...))`/`Err(String::from(...))`/`Err("...".to_string()/.into())`/
  `.ok_or_else(...)` construction site lacking an `RS-XXXX`/`error_code`/
  `ErrorCode` reference, exempting `?`-propagated and test-only sites.
  `scripts/check-error-codes.test.sh` proves this by mutating a copy of the
  tree with a reintroduced bare error and asserting non-zero exit, then
  exit 0 on the real tree. Run: `bash scripts/check-error-codes.test.sh` →
  `OK: check-error-codes.sh self-test passed.` Wired into CI as a new step
  in `.github/workflows/ci.yml` immediately after the base
  `check-error-codes.sh` step. Evidence: `.claude/v0.45.7-evidence.md`
  (S6, S7).
- [x] **`copy_state.rs` and the `CREATE SINK` option parser's error paths
  each carry a distinct, registered code with actionable `next_steps`
  text (new `copy_error_code_tests.rs`)**: `parse_copy_from_stmt`'s 8 bare
  sites now return `RS-2021` (`copy.malformed_stdin_statement`); the
  `CREATE SINK` option parser's ~20 sites carry `RS-4007` inline at
  construction. New
  `crates/rockstream-gateway/tests/copy_error_code_tests.rs` — 2 tests
  (`copy_malformed_variants_return_rs2021`,
  `create_sink_malformed_ddl_variants_return_rs4007`) — both assert code
  and non-empty actionable next-steps text via
  `assert_code_and_next_steps`. Run: `cargo test -p rockstream-gateway
  --test copy_error_code_tests` → **2 passed**. Evidence:
  `.claude/v0.45.7-evidence.md` (S1, S2).
- [x] **`durable.rs`'s six previously-`RS_3010`-only failure modes are
  independently distinguishable in logs/responses (new
  `durable_shuffle_error_code_tests.rs`)**: split into `RS-3011`
  (rate-limit exhaustion), `RS-3012` (object-store I/O failure, write and
  read paths), `RS-3013` (buffer-capacity exceeded), `RS-3014` (footer
  serialization failure), `RS-3015` (footer deserialization failure),
  `RS-3016` (footer corrupt/undersized, covering both truncated and
  oversized-length-field cases). `multiplexer.rs`'s two former `RS_3010`
  reuse sites now use `RS-3017` (new,
  `exchange.ipc_shuffle_decode_error`), kept in lock-step with
  `durable.rs`'s split per the plan's coupling note. New
  `crates/rockstream-runtime/tests/durable_shuffle_error_code_tests.rs` —
  9 tests, including `split_codes_are_pairwise_distinct`, which asserts
  all six new `durable.rs` codes are pairwise unique (no residual
  overload). Run: `cargo test -p rockstream-runtime --test
  durable_shuffle_error_code_tests` → **9 passed**. Evidence:
  `.claude/v0.45.7-evidence.md` (S3).
- [x] **No `RS-XXXX` code left orphaned/duplicated by the split**:
  `crates/rockstream-types/src/error_code.rs` registers `RS-2021`,
  `RS-3011`–`RS-3018`, and `RS-3501` (all new), each with description and
  actionable `next_steps`; the registry's own exhaustiveness test passes.
  `serialization.rs::deserialize_zset`'s 2 bare sites and `loopback.rs`'s
  bare `ok_or_else` sites now carry `RS-3017`/`RS-3018` respectively
  (tightly coupled to the exchange-shuffle code family, not named in the
  roadmap row but caught by the extended check and fixed to keep CI
  green); `sum_count.rs`/`weight_add.rs`'s one bare site each now carries
  `RS-3501` (`merge.law_accumulator_decode_error`). Run: `cargo test -p
  rockstream-types error_code` → **6 passed** (registry completeness/
  uniqueness tests). Evidence: `.claude/v0.45.7-evidence.md` (S4, S5, S8).
- [x] **Documentation matches the registry**: `DESIGN.md` §14.14's
  error-code table updated with all 9 new codes in their correct owner
  ranges, none colliding with the reserved `RS-2501`–`RS-2504`/
  `RS-3001`–`RS-3002` ranges. Evidence: `.claude/v0.45.7-evidence.md`
  (S9).
- [x] **No regression**: `cargo test --workspace` → **1381 passed, 6
  ignored (98 suites, 1902.51s)**; `cargo fmt --check` clean; `cargo
  clippy --workspace --all-targets -- -D warnings` → no issues; `make e2e`
  → PASSED (LFS 13/13, MinIO via TestContainers 6/6). Evidence:
  `.claude/v0.45.7-evidence.md` (S10, S11, S12).

### Checklist

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (test names, file/line pointers, and command output) is
recorded in `.claude/v0.45.7-evidence.md`. Deferred-item audit
(`.claude/v0.45.7-plan.md` §1) confirms no prior sign-off's deferred items
overlap this version's scope, and no item from this version's own Scope has
been deferred: both named files (`copy_state.rs`, `durable.rs`) and the
`CREATE SINK` option parser now carry registered codes, the extended
`check-error-codes.sh` is wired into CI with a self-test proving it
actually catches regressions, and every tightly-coupled bare site the
extended check would otherwise flag today (`multiplexer.rs`,
`serialization.rs`, `sum_count.rs`, `weight_add.rs`, `loopback.rs`) was
fixed alongside the named files rather than deferred. The CLI-argument
binaries under `crates/*/src/bin/` are explicitly out of scope per the
plan's audit (developer/CI console output, not operator-facing) and remain
excluded by the script's file selection.
