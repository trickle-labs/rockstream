# v0.45.3 Sign-off

**Signed off**: 2026-07-13

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.45.3 have been verified against running code, local test output, and CI configuration.

### Proof Claims

- [x] **P1 — Coverage gates for all 13 crates**: `cargo llvm-cov` fails CI if any of the
  thirteen workspace crates' coverage drops below its newly-set floor (not just the
  four hot-path crates). Evidence: `rockstream-diff`, `rockstream-ops`,
  `rockstream-storage`, `rockstream-runtime`, `rockstream-sql`, `rockstream-control`,
  `rockstream-connectors`, `rockstream-types`, `rockstream-plan`, `rockstream-sim`,
  `rockstream-cli`, `rockstream-oracle`, `rockstream-gateway` all have
  `--fail-under-lines`/`--fail-under-regions` steps in `.github/workflows/ci.yml`,
  each measured above its floor locally (`.claude/v0.45.3-evidence.md` §P1 table).
  `test_all_gated_crates_present_in_ci_coverage_job` passes.
- [x] **P2 — Makefile matches CI exactly**: `make coverage-gate`'s output matches
  `ci.yml`'s actual enforced thresholds exactly, asserted by
  `test_makefile_coverage_gate_matches_ci_yml`, which parses both files and asserts
  identical crate sets and numeric floors (13/13 crates match on both lines and
  regions floors).
- [x] **P3 — Gateway clippy clean**: `cargo clippy --workspace --all-targets --
  -D warnings` passes with the gateway's blanket `#![allow(clippy::all,
  unused_variables, dead_code)]` removed and every remaining suppression
  individually justified with an adjacent comment.
  `test_gateway_lib_has_no_blanket_clippy_allow` passes.
- [x] **P4 — Scheduled dependency-audit workflow**: A scheduled workflow run (not
  just a PR) fails within 24h of a newly-published advisory against an unchanged
  `Cargo.lock`, simulated by `test_unignoring_resolved_advisory_is_detected`
  (temporarily un-ignoring a resolved advisory and confirming `cargo deny check
  advisories` fails). `.github/workflows/dependency-audit.yml` has a
  `cron: '0 3 * * *'` schedule trigger (≤24h period), verified by
  `test_scheduled_dependency_audit_workflow_exists`.
- [x] **P5 — Dependabot configuration**: `dependabot.yml` is valid and configured
  to open cargo-ecosystem version-bump PRs weekly, verified by
  `test_dependabot_config_is_valid`. The fork smoke-test procedure is recorded as
  a manual, one-time process validation in `.claude/v0.45.3-evidence.md` §P5
  (not independently unit-testable).

### Checklist

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

Full evidence detail (commands, test names, measured coverage numbers, and CI
config excerpts) is recorded in `.claude/v0.45.3-evidence.md`.

