# v0.43 Sign-off

**Signed off**: 2026-07-11

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` for v0.43 ("FizzBee
Cold-Tier Protocol Model & Simulator Fidelity") have been verified against
running code, formal model output, and full test-suite output. Full evidence
detail is in `.claude/v0.43-evidence.md`.

| Proof Claim | Evidence |
|---|---|
| `formal/m5_cold_tier_sink.fizz` green with safety invariants (M5-S1 no duplicates, M5-S2 no data loss, M5-S3 manifest-pointer atomicity) at CI-fast bounds | `fizz formal/m5_cold_tier_sink.fizz` → `PASSED: Model checker completed successfully`, 1864 valid nodes / 1864 unique states explored, no counterexamples |
| M5-L1: `committed_epoch` always advances (liveness) | Same `fizz` run: `Checking always eventually M5_L1_CommittedEpochAdvances` → `IsLive: true` |
| `COV-M5` (coverage) | Coverage ghost-variable assertions embedded in `formal/m5_cold_tier_sink.fizz` (mirrors M3/M4 pattern), no coverage gaps reported by the model checker |
| Injecting `partial_write_probability=0.5` triggers truncated bytes and cold-tier recovery passes without duplicate output | `cargo test -p rockstream-connectors --test partial_write_recovery_tests` → 2 passed (`test_partial_write_recovery_lfs`, `test_partial_write_recovery_minio_tc`) |
| Paired runtime `assert!`s in `rockstream-connectors` for gap 1 | `assert_commit_pointer_atomic` (new, `object_store_sink.rs`) plus existing `assert_no_duplicate_delivery` / `assert_no_lost_delivery_after_checkpoint`; exercised by the partial-write tests above; `cargo test --lib -p rockstream-connectors` → 44 passed |
| `kafka_tx_timeout_probability` exercises the `CheckBeforeCommit` recovery path | `cargo test -p rockstream-connectors --features simulation --test kafka_tx_timeout_tests` → 1 passed, exactly-once delivery confirmed across 5 seeds |
| `list_staleness_epochs` pulled forward, gap 3 closed | `list_staleness_epochs: u64` added to `SimObjectStore` (`crates/rockstream-sim/src/object_store.rs`); confirmed no correctness `assert!` in `sink_connector.rs` depends on LIST consistency; `cargo test -p rockstream-sim` → 142 passed, 1 ignored |
| All three §17 simulation gaps updated to `[MITIGATED]` | `DESIGN.md` §17.8 gaps 1, 2, 3 all read `Status: [MITIGATED — v0.43]` with evidence pointers, confirmed by direct read of the section |
| All existing workspace tests pass | `cargo test --workspace` → 1181 passed, 5 ignored (81 suites) |

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

- `formal/m5_cold_tier_sink.fizz` was run directly (`fizz formal/m5_cold_tier_sink.fizz`)
  and passed with all safety invariants (M5-S1/S2/S3) and the liveness
  invariant (M5-L1) green; 1864 states explored, no deadlocks, no
  counterexamples.
- All three DESIGN.md §17.8 simulation fidelity gaps (partial object writes,
  Kafka transactional broker timeout, S3 LIST consistency delays) are closed
  and documented as `[MITIGATED — v0.43]` with pointers to the concrete code
  and tests that close them.
- Full workspace test suite (`cargo test --workspace`) is green: 1181 passed,
  5 ignored, 81 suites — no regressions introduced by the v0.43 work.
- Nothing deferred: all deliverables listed in the v0.43 roadmap row (formal
  model, paired runtime assertions, `partial_write_probability`,
  `kafka_tx_timeout_probability`, `list_staleness_epochs`, DESIGN.md gap
  status updates) are implemented and verified in this sign-off.

