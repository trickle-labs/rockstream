# v0.52.3 Sign-off

**Signed off**: 2026-08-13

## Proof claims

| Proof claim | Evidence | Result |
|---|---|---|
| Every removed DDL form returns `RS-4017` with its documented replacement through the real pgwire dispatcher. | `rtk cargo test -p rockstream-gateway --test connector_removal_tests`; 7 exact simple/extended pgwire cases covering Iceberg, Delta, object-store/cold-tier catalog variants, S3, HTTP webhook, endpoint rejection, and documentation drift. | PASS — `cargo test: 7 passed` |
| A v0.52.2 catalog containing Iceberg, object-store, S3, and webhook rows starts cleanly and reports all four as `REMOVED` with `RS-4017` on LFS. | `rtk cargo test -p rockstream-gateway --test connector_removal_lfs_tests`; exact rows and no SlateDB range deletion asserted. | PASS — `cargo test: 1 passed` |
| The same legacy catalog upgrade behavior works on MinIO TestContainers. | `rtk cargo test -p rockstream-gateway --test connector_removal_minio_tests`; exact rows and startup behavior asserted. | PASS — `cargo test: 1 passed` |
| The webhook endpoint rejects before delivery instead of accepting or discarding input. | `connector_removal_tests::webhook_endpoint_returns_rs4017_and_accepts_no_delivery`. | PASS — included in 7/7 pgwire target |
| `RS-4017` is complete and accepted by the error-code enforcement checks. | `rtk cargo test -p rockstream-types all_codes_have_descriptions_and_actionable_next_steps`; `rtk bash scripts/check-error-codes.sh`; `rtk bash scripts/check-error-codes.test.sh`. | PASS — test passed; both scripts `OK` |
| Removed-connector documentation names exact replacement paths and has no live references outside the migration/Removed documentation. | `connector_removal_tests::connector_removal_docs_have_exact_replacements_and_no_live_references`. | PASS — included in 7/7 pgwire target |
| The dispatch audit is fully connected from parser/ingress through dispatcher, executor, and response encoder. | `rtk rg` audit over gateway parser, dispatcher, handler, encoder, catalog, webhook, and CLI locations; all paths are exercised by the proof tests. | PASS |
| The entire existing suite remains green with implementations still compiled. | `rtk cargo test --workspace`; corrected focused rerun for the stale epoch expectation: `rtk env CARGO_TARGET_DIR=/private/tmp/rockstream-v0523-proof rtk cargo test -p rockstream-connectors --lib globally_allocated_epoch_may_gap_but_not_repeat`. | PASS — workspace green; focused test passed |

## Exit criteria

- [x] `rtk cargo fmt --check` passes
- [x] `rtk cargo clippy -- -D warnings` passes
- [x] `rtk cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] New behavior has unit, LFS, MinIO, and TestContainers coverage where applicable; oracle/property and SimRuntime coverage are not applicable to this frontend-only change
- [x] SQL/wire features have raw-pgwire reachability, negative, dispatch-wiring, and complete removed-form coverage-matrix tests
- [x] User/operator-visible failures use actionable `RS-4017`
- [x] Control-plane actions write audit events; no new control-plane action was added
- [x] No new performance claim lacks a benchmark or measurement note
- [x] New public surfaces are documented in `README.md`, `docs/concepts.md`, `docs/language-features.md`, `docs/configuration.md`, and `docs/connector-migration.md`
- [x] No new queues, buffers, or scan windows were added
- [x] No new distributed-coordination path was added
- [x] No code path depends on SlateDB range deletion
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No soak gate is required for this version
- [x] No items are deferred

## Reproduction

```text
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test --workspace
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
rtk cargo test -p rockstream-gateway --test connector_removal_tests
rtk cargo test -p rockstream-gateway --test connector_removal_lfs_tests
rtk cargo test -p rockstream-gateway --test connector_removal_minio_tests
rtk cargo test -p rockstream-cli cold_tier_config_fields_fail_closed_with_rs4017
rtk cargo test -p rockstream-types all_codes_have_descriptions_and_actionable_next_steps
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-error-codes.test.sh
```

No item has been deferred. v0.52.3 is fully signed off.
