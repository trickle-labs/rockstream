# v0.51.1 Sign-off

**Signed off**: 2026-07-20

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.51.1 have been
verified against running code, local test output, and
`.claude/v0.51.1-evidence.md`.

### Proof Claims

- [x] **Vanilla autocommitting `psql`, no `SET`, no explicit `COMMIT`: `CREATE TABLE t (id int, name text); INSERT INTO t VALUES (1, 'alice'); SELECT * FROM t;` returns the one row correctly**: `rtk cargo test -p rockstream-gateway --test gateway_proof_tests postgres_standard_transcript_replay_returns_the_row -- --exact` passes; the underlying mechanics — server-generated idempotency envelope (`missing_idempotency_key_autogenerates_envelope_and_commits`) and bare autocommit of INSERT/UPDATE/DELETE without `RETURNING` (`bare_insert_autocommits_and_is_visible_in_separate_round_trip`, `bare_update_and_delete_autocommit_and_are_visible_in_separate_round_trip`) — also pass. Evidence: `.claude/v0.51.1-evidence.md` (#1, #1a, #1b).
- [x] **A freshly `CREATE MATERIALIZED VIEW`'d view reflects its source table's current data immediately, before any further write**: `create_materialized_view_populates_immediately_without_further_write` passes; the regression `create_materialized_view_immediate_population_then_further_insert_updates_view` confirms further commits still update the view post-commit unchanged. Evidence: `.claude/v0.51.1-evidence.md` (#2, #2a).
- [x] **`INSERT INTO t VALUES (3,'carol'),(4,'dave')` (no column list) inserts two correctly-populated rows, not a NULL phantom row**: `rtk cargo test -p rockstream-gateway --test insert_no_column_list_multi_row_tests` passes (multi-row, single-row, and unknown-table/`RS-2056` cases). Evidence: `.claude/v0.51.1-evidence.md` (#3).
- [x] **The existing 100/100 `gateway_proof_tests` remain green**: `rtk cargo test -p rockstream-gateway --test gateway_proof_tests` — `107 passed; 0 failed; 1 ignored` (102 pre-existing + renamed idempotency test + new Slice 3/4/5 tests). Evidence: `.claude/v0.51.1-evidence.md` (#4).
- [x] **The explicit-transaction buffering path remains green**: `explicit_begin_block_still_buffers_until_commit` plus the full `rtk cargo test -p rockstream-gateway --test transaction_savepoint_tests` suite pass. Evidence: `.claude/v0.51.1-evidence.md` (#5).
- [x] **Explicit `SET` remains available for a client needing a dedup guarantee across an explicit multi-statement transaction**: `explicit_idempotency_key_dedupes_multi_statement_transaction_replay` passes. Evidence: `.claude/v0.51.1-evidence.md` (#6).
- [x] **Server-generated idempotency envelope used instead of `RS-2007` when client sets neither mechanism**: `missing_idempotency_key_autogenerates_envelope_and_commits` passes. Evidence: `.claude/v0.51.1-evidence.md` (#7).
- [x] **Durability of each new/changed commit path across SlateDB LFS and MinIO**: all 8 durability pairs (autocommit, idempotency envelope, immediate MV population, no-column-list INSERT) pass against both real LFS (`insert_no_column_list_durability_lfs_tests`, `idempotency_envelope_durability_lfs_tests`, `mv_immediate_population_durability_lfs_tests`, `autocommit_durability_lfs_tests` — 13 passed across 6 suites incl. `transaction_savepoint_tests` regression) and real MinIO via TestContainers (`insert_no_column_list_durability_minio_tests`, `idempotency_envelope_durability_minio_tests`, `mv_immediate_population_durability_minio_tests`, `autocommit_durability_minio_tests` — 4 passed, `--test-threads=1`). Evidence: `.claude/v0.51.1-evidence.md` (#8, Durability Evidence table).

### Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes (one pre-existing, unrelated `protocol_fuzzer_no_panics_no_hangs` sandbox process-fork-timeout failure reproduced identically on unmodified `main`, orthogonal to this version's changed files — see `.claude/v0.51.1-evidence.md` "Full Regression Run")
- [x] All Proof criteria verified against running code or CI output
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] `scripts/check-exit-criteria.sh` passes
- [x] `make e2e` passes

## Notes

Full evidence (commands, outputs, and the exact proof mapping) is recorded in
`.claude/v0.51.1-evidence.md`. v0.51.1 makes the pgwire front door behave like
standard PostgreSQL for the exact `IMPLEMENTATION_STATUS_20260719.md`
transcript: implicit autocommit outside an explicit `BEGIN`, a
server-generated idempotency envelope when the client sets neither
`rockstream.idempotency_key` nor `rockstream.source_epoch`, immediate
materialized-view population on `CREATE MATERIALIZED VIEW`, and correct
positional-value resolution for no-column-list multi-row `INSERT`. All 8
Proof-Mapping claims from `.claude/v0.51.1-plan.md` §7 are satisfied with
passing tests, all 8 durability pairs pass on both real SlateDB (LFS) and
real MinIO (TestContainers), the full `gateway_proof_tests.rs` suite (107
tests) is green with zero deletions, and the explicit-transaction buffering
regression suite is unaffected. No oracle/property, benchmark, or
`SimRuntime` proof is owed per the plan (no new IVM/DBSP operator, no new
performance claim, no new cross-shard coordination path in this version's
scope). Nothing deferred.
