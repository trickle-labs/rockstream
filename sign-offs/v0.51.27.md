# v0.51.27 Sign-off: Honest Failure Semantics: No Silent-Wrong-Answer or Acknowledged-and-Discarded Branches

**Signed off**: 2026-08-11  
**Status**: ✅ Done

## Exit Criteria Verification

All Proof criteria in `NEW_ROADMAP.md` (v0.51.27) were verified against `.claude/v0.51.27-evidence.md` and the release gates below.

### Proof Claims

- [x] **Claim 1: Tiered Store Fallback Race Handling**: Tiered store fallback race returns object instead of panicking on identical `SimRuntime` seed. Proven by `crates/rockstream-storage/tests/tiered_store_fallback_race_tests.rs`.
- [x] **Claim 2: NTILE Operator Honest Rejection**: `NTILE` request against operator layer returns `RS-1016`, never `0`. Proven by `crates/rockstream-ops/tests/operator_ntile_honest_rejection_tests.rs`.
- [x] **Claim 3: Composite Index Acceleration & Doc Conformance**: Two-column `CREATE INDEX` point lookup accelerated; `EXPLAIN` and `docs/language-features.md` doc-conformance verified. Proven by `crates/rockstream-ops/tests/composite_index_point_lookup_tests.rs`.
- [x] **Claim 4: Control Plane DrainAck & Lifecycle Eviction**: Multi-worker TC drain completes via `DrainAck` action and worker death evicts multiplexer/pool entries. Proven by `crates/rockstream-control/tests/drain_lifecycle_handling_tests.rs`.
- [x] **Claim 5: Zero Unreachable Sweep**: Zero `unreachable!()` remain on input-reachable branches anywhere in `crates/*/src/`. Proven by `scripts/check-no-unreachable.sh`.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check`, `rtk cargo clippy -- -D warnings`, and `rtk cargo test --workspace` pass.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests, negative error tests, and dispatch-wiring verification.
- [x] Every user/operator-visible failure uses an `RS-XXXX` code with actionable text (`RS-1016`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface is documented.
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path.
- [x] Distributed coordination paths have seeded `SimRuntime` tests.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` and `rtk make e2e` pass.
- [x] **Confirmation that no item has been deferred.**

## Verification Evidence

| Proof Claim | Target Verification Test / Command | Pass/Fail | Output Excerpt / Evidence Note |
| --- | --- | --- | --- |
| **P1: Tiered Store Fallback Race** | `rtk cargo test -p rockstream-storage --test tiered_store_fallback_race_tests` | ✅ **PASS** | `cargo test: 3 passed (1 suite, 0.00s)` |
| **P2: NTILE Operator Rejection** | `rtk cargo test -p rockstream-ops --test operator_ntile_honest_rejection_tests` | ✅ **PASS** | `cargo test: 1 passed (1 suite, 0.00s)` |
| **P3: Composite Index Acceleration** | `rtk cargo test -p rockstream-ops --test composite_index_point_lookup_tests` | ✅ **PASS** | `cargo test: 2 passed (1 suite, 0.13s)` |
| **P4: DrainAck & Lifecycle Eviction** | `rtk cargo test -p rockstream-control --test drain_lifecycle_handling_tests` | ✅ **PASS** | `cargo test: 2 passed (1 suite, 0.43s)` |
| **P5: Unreachable Sweep CI Gate** | `rtk bash scripts/check-no-unreachable.sh` | ✅ **PASS** | `OK: zero unreachable!() calls found in production source code.` |

## Dispatch-Wiring Audit Verification

| Feature | Audit Path (Parser → Dispatcher → Executor → Response) | Wiring Verification Method | Status |
| :--- | :--- | :--- | :--- |
| **Tiered Store Fallback Race** | Tiered fallback loop → Primary store re-check | Verified in `tiered_store.rs:93`; returns object cleanly instead of `unreachable!()` | **VERIFIED CONNECTED** |
| **NTILE Operator Rejection** | SQL Lowering → Window Operator Evaluation | Verified in `window.rs`; returns `OpError` (`RS-1016`) when encountered | **VERIFIED CONNECTED** |
| **Composite Index Point Lookup** | Index arrangement → Composite key lookup | Verified in `index_arrange.rs` via `point_lookup_values` | **VERIFIED CONNECTED** |
| **Control Plane DrainAck** | TCP Receiver → DrainAck Handler → DrainState Completion | Verified in `service.rs:903` | **VERIFIED CONNECTED** |
| **Control Plane LifecycleState** | TCP Receiver → LifecycleState Handler → ShardManager Lease Release & Worker Eviction | Verified in `service.rs:913` | **VERIFIED CONNECTED** |
| **Unreachable Sweep** | Production crates (`crates/*/src/`) | Verified via `scripts/check-no-unreachable.sh` CI script | **VERIFIED CLEAN** |

## Reproduction Commands

```bash
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test -p rockstream-storage --test tiered_store_fallback_race_tests
rtk cargo test -p rockstream-ops --test operator_ntile_honest_rejection_tests
rtk cargo test -p rockstream-ops --test composite_index_point_lookup_tests
rtk cargo test -p rockstream-control --test drain_lifecycle_handling_tests
rtk bash scripts/check-no-unreachable.sh
rtk bash scripts/check-exit-criteria.sh
rtk make e2e
```

No item has been deferred. v0.51.27 is done.
