# v0.51.11 Sign-off: Operator-Grade Runtime Diagnostics: Live Op-Stats & Pipeline-Stall Inspector

**Signed off**: 2026-08-04
**Status**: ✅ Done

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` (line 1056) for v0.51.11 have been verified against running code, local test output, and `.claude/v0.51.11-evidence.md`.

### Proof Claims

- [x] **P1 — EXPLAIN INCREMENTAL ANALYZE Op-Stats**: `EXPLAIN INCREMENTAL ANALYZE <view>` returns non-empty, correct per-operator stats matching independently instrumented runs, never the hardcoded-`0` empty result. Verified by `crates/rockstream-gateway/tests/explain_analyze_opstats_tests.rs` (`test_explain_analyze_opstats_aggregate`, `test_explain_analyze_opstats_join`, `test_explain_analyze_opstats_distinct_minmax`). Evidence: `.claude/v0.51.11-evidence.md` §1 P1.
- [x] **P2 — Pipeline Stall Diagnostics & Wire Localization**: A deliberately stalled pipeline is localized to the exact wedged operator/shard via `SHOW PIPELINE STALLS`, `SHOW FRONTIERS`, and `/metrics` over pgwire without reading Rust source. Verified by `crates/rockstream-gateway/tests/pipeline_stall_diagnostics_tests.rs` (`test_pipeline_stall_localization_pgwire`, `test_pipeline_slow_source_localization_pgwire`, `test_pipeline_stall_metrics_endpoint`). Evidence: `.claude/v0.51.11-evidence.md` §1 P2.
- [x] **P3 — Minimal Arrangement Peek**: `SHOW ARRANGEMENT <view> <op_id> <key>` returns exact intermediate Z-set state weights matching hand-computed values via `DbReader` non-blocking reads. Verified by `crates/rockstream-gateway/tests/arrangement_peek_tests.rs` (`test_show_arrangement_aggregate_key`, `test_show_arrangement_join_side_key`, `test_show_arrangement_nonexistent_key`). Evidence: `.claude/v0.51.11-evidence.md` §1 P3.
- [x] **PGWire Reachability & Negative Tests**: Reachability verified for `EXPLAIN INCREMENTAL ANALYZE`, `SHOW PIPELINE STALLS`, `SHOW FRONTIERS`, and `SHOW ARRANGEMENT`; negative tests verify nonexistent views return actionable `RS-1001` / `RS-2001` error responses. Verified in `explain_analyze_opstats_tests.rs`, `pipeline_stall_diagnostics_tests.rs`, and `arrangement_peek_tests.rs`. Evidence: `.claude/v0.51.11-evidence.md` §2.
- [x] **Dispatch-Wiring Audit**: Paths 1–4 (Parser -> Dispatcher -> Topology/Metrics/DbReader -> Response) verified fully connected. Evidence: `.claude/v0.51.11-evidence.md` §2.3.
- [x] **Coverage Matrix Verification**: Matrices for Op-Stats (Aggregate, Join, Distinct/MinMax), Stall Diagnostics (Wedged Operator, Slow Source, Metrics Endpoint), and Arrangement Peek (Aggregate State, Join State, Nonexistent Key) all verified. Evidence: `.claude/v0.51.11-evidence.md` §3.
- [x] **Oracle Property Verification**: Op-Stats rate oracle (`test_explain_analyze_opstats_oracle_property`) and Stall Localization oracle (`test_pipeline_stall_oracle_property`) verified. Evidence: `.claude/v0.51.11-evidence.md` §4.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check` passes cleanly.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes cleanly.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability, negative, dispatch-wiring audit, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-1001`, `RS-2001`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface (`EXPLAIN INCREMENTAL ANALYZE`, `SHOW PIPELINE STALLS`, `SHOW FRONTIERS`, `SHOW ARRANGEMENT`) is documented.
- [x] Every new queue/buffer/scan window has a named bound, a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] All Proof criteria verified against running code or CI output.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `scripts/check-exit-criteria.sh` passes cleanly.
- [x] `make e2e` passes cleanly.
- [x] Nothing deferred to a later version.

## Verification Evidence

| Claim | Target Test | Status | Output Evidence |
|---|---|---|---|
| **P1** `EXPLAIN INCREMENTAL ANALYZE` Op-Stats | `crates/rockstream-gateway/tests/explain_analyze_opstats_tests.rs::test_explain_analyze_opstats_aggregate` | `PASS` | Live per-operator invocation counts, rows processed, and time spent returned over pgwire |
| **P2** Pipeline Stall Localization | `crates/rockstream-gateway/tests/pipeline_stall_diagnostics_tests.rs::test_pipeline_stall_localization_pgwire` | `PASS` | Wedged operator localized with `is_holding_back_commit = true` over pgwire & metrics |
| **P3** Intermediate Arrangement Peek | `crates/rockstream-gateway/tests/arrangement_peek_tests.rs::test_show_arrangement_aggregate_key` | `PASS` | Exact Z-set weights returned via non-blocking `DbReader` snapshot reads |

## Notes

v0.51.11 implements Operator-Grade Runtime Diagnostics: Live Op-Stats & Pipeline-Stall Inspector (Close Area C's runtime half).
It fixes live per-operator statistics for `EXPLAIN INCREMENTAL ANALYZE <view>`, exposes pipeline stall diagnostics over PGWire (`SHOW PIPELINE STALLS`, `SHOW FRONTIERS`) and Prometheus `/metrics`, and introduces intermediate Z-set state inspection (`SHOW ARRANGEMENT <view> <op_id> <key>`).

Per the ground rules, no item has been deferred to a later version.
