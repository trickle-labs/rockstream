# v0.59.7 Sign-off — Factorized & Filtered IVM

**Signed off**: 2026-08-20

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Canonical key capsules are exact, typed, deterministic, and NULL-safe across lookup, partitioning, shuffle, skew, and persistence | `key_capsule_tests` (8 tests) | PASS |
| Factorized PK/FK SUM, COUNT, and AVG maintenance equals the complete incremental oracle | `factorized_join_aggregate_oracle_tests` (14 tests) | PASS |
| Star payloads remain bounded and governor counters refuse atomically across all six dimensions | `factorized_star_join_oracle_tests`; `delta_amplification_governor_tests` | PASS |
| Windowed joins remain on the classic path | `factorized_plan_rejection_tests` | PASS |
| Frontier-versioned predicate transfer preserves inner and outer-join semantics, NULLs, and retractions | `predicate_transfer_tests` | PASS |
| Factorized state commits, checkpoints, replays, retracts, and cleans up without range deletion on LFS | `factorized_join_durability_lfs_tests` | PASS |
| The MinIO S3 durability path replays and retracts correctly | `minio_aggregate::factorized_join_replays_and_retracts_on_minio` | PASS in Phase 4 evidence; requires Docker to rerun locally |
| The factorized plan is reachable through simple and extended pgwire | `live_exec_join_tests::factorized_join_aggregate_is_reachable_over_simple_and_extended_pgwire` | PASS in Phase 4 evidence; local rerun requires listener permission |
| Compile/deploy selection has no live cutover or dual active graph | `factorized_plan_path_coupling_tests` | PASS |
| Classic/factorized shadow comparison uses the independent oracle and immutable selection rule | `external_benchmark_harness_tests` | PASS |

## Definition of Done

- [x] `cargo fmt --all -- --check` passes.
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `cargo test --workspace` passes in Phase 4 evidence.
- [x] New behavior has unit, oracle, LFS, MinIO, and pgwire coverage.
- [x] Dispatch wiring and coverage-matrix tests pass.
- [x] Every touched user-visible failure has an actionable `RS-XXXX` code.
- [x] No new control-plane action or public SQL/configuration surface was added.
- [x] Named payload/governor bounds have fill levels and refusal paths.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-ops --test key_capsule_tests
rtk cargo test -p rockstream-ops --test factorized_join_aggregate_oracle_tests
rtk cargo test -p rockstream-ops --test factorized_star_join_oracle_tests
rtk cargo test -p rockstream-ops --test delta_amplification_governor_tests
rtk cargo test -p rockstream-ops --test predicate_transfer_tests
rtk cargo test -p rockstream-ops --test factorized_plan_rejection_tests
rtk cargo test -p rockstream-ops --test factorized_join_durability_lfs_tests
rtk cargo test -p rockstream-ops --test minio_aggregate factorized_join_replays_and_retracts_on_minio
rtk cargo test -p rockstream-gateway --test live_exec_join_tests factorized_join_aggregate_is_reachable_over_simple_and_extended_pgwire
rtk cargo test -p rockstream-sim --test external_benchmark_harness_tests
rtk cargo test -p rockstream-sim --test factorized_plan_path_coupling_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

Phase 4 evidence records the MinIO and pgwire proof runs as passing; their
reproduction is environment-dependent because MinIO requires Docker and the
pgwire test opens a localhost listener.
