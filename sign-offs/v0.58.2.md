# v0.58.2 Sign-off

**Signed off:** 2026-08-17

## Proof

| Claim | Evidence | Result |
|---|---|---|
| Each of the five storage-pressure signals is induced independently and each triggers the documented shedding step in the documented order, with the acting signal named through v0.54.1's dominant-cause attribution | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests test_sp001`<br>`test_sp002`<br>`test_sp003`<br>`test_sp004`<br>`test_sp005` | PASS — All 5 signals (`l0_backlog`, `pending_compaction_bytes`, `flush_latency_ms`, `write_amplification`, `object_store_latency_ms`/`object_store_failure_rate`) independently trigger Step 1 (backfill throttle via `RS-4021`), Step 2 (50% source throttle reduction), and Step 3 (parallelism scale-up veto), naming exact dominant causes. |
| A large backfill against a compaction-saturated store degrades backfill throughput while already-running views stay inside the freshness SLO | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests test_large_backfill_compaction_saturated_protects_running_views_minio_tc` | PASS — Running view freshness lag strictly maintained at ≤ 450 ms (within 1000 ms SLO), while 500MB backfill reservation rejected with `RS-4021: storage_pressure_pending_compaction_bytes`. Audit logged to MinIO / SimObjectStore. |
| Multi-workload priority arbitration under storage spike | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests test_workload_priority_arbitration_under_storage_spike` | PASS — High priority view capacity preserved with lag ≤ 500 ms; low priority backfill throttled/rejected with `RS-4021: storage_pressure_l0_backlog`. |
| The loop converges rather than oscillates under both a sustained pressure step and an oscillating one, measured over a documented settling window | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests test_control_loop_settles_under_sustained_pressure_step`<br>`test_control_loop_settles_under_oscillating_pressure_step` | PASS — Sustained 5x step settles to steady-state minimum bound in exactly 3 cycles (≤ 30s) with 0 sustained oscillations (`OscillationDetector::detect == false`). Square wave demonstrates damped response without resonant amplification. |
| No new user-facing tuning knob is added, asserted by a configuration-surface lock test | `rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests test_configuration_surface_cli_lock`<br>`test_configuration_surface_struct_lock`<br>`test_storage_signals_separately_exported_no_composite_scalar` | PASS — Zero CLI flags or config fields added (`KNOWN_ROLES` locked). 5 individual Prometheus gauges exported; composite `storage_debt` strictly absent. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e` passes (LFS: 14 passed; MinIO/TestContainers: 7 passed; audit log & support bundle verified).
- [x] New behavior has unit, deterministic simulation (`SimRuntime`), and integration coverage (`crates/rockstream-sim/tests/storage_pressure_admission_tests.rs`).
- [x] SQL/wire-protocol reachability (`show_view_status_pgwire_tests`), negative `RS-4021` codes, dispatch wiring, and matrix tests pass.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text (`RS-4021`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Storage pressure signals and dominant contributors are documented in metrics and system table views.
- [x] Every new queue/buffer has named bounds and backpressure paths; every distributed coordination failure scenario has a seeded `SimRuntime` test.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-sim --test storage_pressure_admission_tests
rtk cargo test -p rockstream-gateway --test show_view_status_pgwire_tests
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk make e2e
rtk ./scripts/check-exit-criteria.sh
```

All v0.58.2 Proof claims and exit criteria pass. v0.58.2 is done; nothing in its scope is deferred.

