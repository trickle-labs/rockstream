# v0.29 Sign-off — Object Store Limit Soak

**Signed off**: 2026-06-18  
**Version focus**: Maximize sustained pressure to validate shard-level group commits, SST/WAL caches, and coalesced durable fallback paths against cloud provider rate limits; native OpenTelemetry/Prometheus metrics (`/metrics`) utilized for monitoring flush latencies.

---

## Proof Claims

**Roadmap proof statement:** The cluster maintains high throughput for 72 hours without HTTP 429 (Too Many Requests) throttling errors from the cloud provider.

### P1 — Expose `/metrics` endpoint with Prometheus metrics

Claim: Expose `/metrics` endpoint with Prometheus metrics.

- [x] `test_metrics_server_exposition` — Verified GET `/metrics` returns Prometheus format metrics including merge law and latency metrics.

**Reproduce:** `rtk cargo test --test soak_proof_tests test_metrics_server_exposition`

### P2 — Simulate object store rate limits and HTTP 429 errors

Claim: Simulate object store rate limits and HTTP 429 errors.

- [x] `test_throttled_object_store` — Verified `SimObjectStore` throws 429 when operations exceed simulated limits.

**Reproduce:** `rtk cargo test -p rockstream-sim -- test_throttled_object_store`

### P3 — Backoff and retry policy recovers from HTTP 429

Claim: Backoff and retry policy recovers from HTTP 429.

- [x] `test_coalesced_durable_fallback_retry` — Verified retry loop successfully recovers from simulated HTTP 429 and finishes transactions.

**Reproduce:** `rtk cargo test --test soak_proof_tests test_coalesced_durable_fallback_retry`

### P4 — Maintain high throughput for 72h without HTTP 429 failures

Claim: Maintain high throughput for 72h without HTTP 429 failures.

- [x] `test_object_store_limit_soak_sim` — Verified deterministic `SimRuntime` time-compressed 72-hour soak under sustained high throughput.

**Reproduce:** `rtk cargo test --test soak_proof_tests test_object_store_limit_soak_sim`

### P5 — Group commits and caching minimize object store request rate

Claim: Group commits and caching minimize object store request rate.

- [x] `test_object_store_limit_soak_sim` — Assert request counts stay within the expected budget under 72h soak.

**Reproduce:** `rtk cargo test --test soak_proof_tests test_object_store_limit_soak_sim`

### P6 — MinIO integration under rate limiting pressure

Claim: MinIO integration under rate limiting pressure.

- [x] `test_minio_rate_limit_soak_tc` — TestContainers MinIO integration test under high request pressure.

**Reproduce:** `rtk cargo test --test soak_proof_tests test_minio_rate_limit_soak_tc`

---

## Definition of Done

- [x] `cargo fmt --check` passes
- [x] `cargo clippy -- -D warnings` passes
- [x] `cargo test --workspace` passes
- [x] New behavior has unit tests plus LFS/MinIO/TestContainers integration tests
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-3010` capacity exceeded, `RS-1015` queue full)
- [x] Every control-plane action writes an audit event
- [x] Every new performance claim has a measurement note
- [x] Every new public surface is documented
- [x] Every new queue/buffer has a named bound, fill-level metric, and backpressure/error path
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test
- [x] No code path depends on SlateDB range deletion
- [x] `main` remains runnable through the single `rockstream` binary
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`
- [x] All Proof criteria verified against running code and CI output

---

## New RS-XXXX Codes

| Code | Meaning | Actionable Text |
|------|---------|----------------|
| RS-3010 | Durable shuffle buffer capacity exceeded | Increase buffer capacity or apply source-level rate limits. |
| RS-1015 | Group-commit queue full | Slow down transaction ingestion rate. |

---

## Bounded Buffers & Queues

| Buffer / Queue | Upper Bound | Fill Metric | Status |
|----------------|-------------|-------------|--------|
| Durable shuffle buffer (bytes) | `MAX_DURABLE_BUFFER_SIZE_BYTES = 16 MiB` | `fill_level` | Checked |
| Group-commit queue (batches) | `GROUP_COMMIT_MAX_BATCHES = 64` | `fill_level` | Checked |

---

## Reproduce All Proofs

```bash
# Run soak proof tests
rtk cargo test --test soak_proof_tests

# Run unit tests
rtk cargo test -p rockstream-sim -- test_throttled_object_store
```

---

## Confirmation

No item has been deferred. All P1–P6 proof claims are satisfied. The feature is complete.
