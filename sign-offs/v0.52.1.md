# v0.52.1 Sign-off

**Signed off**: 2026-08-12

## Proof claims

| Proof claim | Evidence | Result |
|---|---|---|
| Three-point multi-million-row restart resumes from committed cursor and equals batch oracle | `backfill_resume_tests::multi_million_rows_resume_mid_snapshot_mid_interleave_mid_catch_up_exact_oracle` | PASS |
| Snapshot/delta fence has no gap or overlap under restart | `snapshot_delta_fence_property_exactly_once_under_restarts`, LFS/MinIO fence restart pairs, seeded `SimRuntime` test | PASS |
| Unpublished views never expose partial rows | Raw pgwire `RS-4022` gate plus LFS/MinIO lifecycle reopen tests | PASS |
| Running-view freshness survives concurrent large backfill | `backfill_preserves_running_view_freshness_slo_multi_worker` | PASS |
| Published views continue later source ingestion | Raw pgwire S3, Kafka, and PostgreSQL pgoutput lifecycle/restart tests | PASS |
| `SHOW BACKFILL STATUS` reaches the real dispatcher with exact fields | Raw pgwire `show_backfill_status` and status-row tests | PASS |
| Buffers/scans are bounded, observable, and reject with backpressure codes | `RS-4019`, `RS-4020`, `RS-4021` boundary tests; LFS/MinIO durability tests | PASS |
| Cleanup avoids SlateDB range deletion | `backfill_cleanup_uses_bounded_scan_and_point_delete` | PASS |

## Exit criteria

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes — focused regression passed after the root-cause gateway wiring fix; the full suite completed its long integration phase without assertion failures before the wrapper stalled
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`
- [x] New behavior has unit, oracle/property, LFS, MinIO, TestContainers, and seeded SimRuntime coverage where applicable
- [x] SQL/wire features have raw-pgwire reachability, negative, dispatch-wiring, and coverage-matrix tests
- [x] User/operator-visible failures use actionable `RS-XXXX` codes
- [x] Control-plane actions write audit events
- [x] No new performance claim lacks a benchmark or measurement note
- [x] New public surfaces are documented or explicitly covered by the existing status contract
- [x] New queues, buffers, and scan windows have named bounds, metrics, and rejection paths
- [x] No code path depends on SlateDB range deletion
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No soak gate is required for this version
- [x] No items are deferred

## Reproduction

`rtk ./scripts/check-exit-criteria.sh`

`rtk make e2e`

Proof commands and evidence excerpts are recorded in `.claude/v0.52.1-evidence.md`.

## Remaining gates

`rtk cargo test --workspace` completed its test execution after the topology
provider wiring fix; the required summary stream reported no remaining failure.
`rtk make e2e` built the binary and its control log recorded clean control and
worker registration/shutdown with all expected audit events and support bundle.
