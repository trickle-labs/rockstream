# v0.34 Sign-off

**Signed off**: 2026-06-24

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.34 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Claims Verification

- [x] **P1: Each of q0–q9 produces results bit-identical to the DataFusion batch oracle over static Nexmark snapshots**
  - **Evidence**: Verified by tests `test_nexmark_q0_q3_lfs`, `test_nexmark_q4_q9_lfs`, and `test_nexmark_q0_q9_minio` in `crates/rockstream-sql/tests/nexmark/main.rs`.
  - **Command**: `rtk cargo test -p rockstream-sql --test nexmark -- test_nexmark_q0_q3_lfs test_nexmark_q4_q9_lfs test_nexmark_q0_q9_minio`
- [x] **P2: Incremental results match batch results after every event batch under randomised generator seeds**
  - **Evidence**: Verified by tests `test_nexmark_q0_q3_lfs`, `test_nexmark_q4_q9_lfs`, and `test_nexmark_q0_q9_minio` in `crates/rockstream-sql/tests/nexmark/main.rs`.
  - **Command**: `rtk cargo test -p rockstream-sql --test nexmark -- test_nexmark_q0_q3_lfs test_nexmark_q4_q9_lfs test_nexmark_q0_q9_minio`
- [x] **P3: `SUBSCRIBE` delivers correct deltas as events arrive**
  - **Evidence**: Verified by tests `test_nexmark_q0_q3_lfs` and `test_nexmark_q4_q9_lfs` in `crates/rockstream-sql/tests/nexmark/main.rs`.
  - **Command**: `rtk cargo test -p rockstream-sql --test nexmark -- test_nexmark_q0_q3_lfs test_nexmark_q4_q9_lfs`
- [x] **P4: All queries verified end-to-end over pgwire on LFS and MinIO**
  - **Evidence**: Verified by end-to-end integration tests using local filesystem `ShardDb` (LFS) and MinIO docker container via TestContainers (MinIO).
  - **Command**: `rtk cargo test -p rockstream-sql --test nexmark`

## Notes

All tests pass successfully. Query execution compiles SQL views (q0-q9) and updates them incrementally using the IVM engine, verified over both LFS and MinIO backends.
