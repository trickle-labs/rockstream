# v0.50 Sign-Off Checklist

Version: **0.50.0**
Focus: **Observability, admin surface, law diagnostics, SLO enforcement, and IVM engine visibility**

---

## Common Definition of Done

- [x] `cargo fmt --check` passes
- [x] `cargo clippy -D warnings` passes
- [x] `cargo test --workspace` passes (1310 passed, 2 ignored)
- [x] New behavior has unit tests
- [x] Any user-visible failure has an `RS-XXXX` error code
- [x] Any control-plane action writes an audit event
- [x] New public surface documented in `docs/`
- [x] Main remains runnable through single `rockstream` binary
- [x] DX baseline: subcommand paths support `--help`, error diagnostics are self-explanatory
- [x] End-user documentation standard: `docs/configuration.md`, `docs/cli.md`, `docs/sre-operations.md` merged
- [x] MinIO & Object-Storage Integration Testing: `crates/rockstream-runtime/tests/segment_cache_tests.rs` asserts >80% hit rate

---

## v0.50 Proof Criteria

- [x] **Actionable error messages**: All `RS-XXXX` error codes have non-empty `next_steps` — enforced by `all_codes_have_next_steps` unit test
- [x] **Error codes RS-5018, RS-5019, RS-6001** registered with descriptions and `next_steps`
- [x] **WorkloadId / OperatorContext plumbing**: `WorkloadId` threaded through `OperatorContext` to stateful operators; `state_bytes()` hook on all operators
- [x] **WorkloadBudget allocation tracking**: threshold alerts at 80% (`RS-5018`) and 95% (`RS-5019`) implemented in `state_budget.rs`
- [x] **Epoch sizing against freshness_slo_ms**: `EpochCoordinator` reads `freshness_slo_ms` from workload and adjusts epoch windows
- [x] **SHOW RESOURCE USAGE DDL**: parsing validated in `ddl.rs` tests for all three variants
- [x] **SHOW SCHEMA_EVOLUTION DDL**: parsing validated in `ddl.rs` tests for both variants
- [x] **Catalog tables**: `view_resource_usage` and `workload_resource_usage` stubs in `rockstream_catalog.rs` with unit tests
- [x] **ControlService::collect_operator_stats**: RPC implemented in `rockstream-control`
- [x] **LawMetricKey.operator_id**: `operator_id: Option<OperatorId>` added; all instantiations updated
- [x] **Operator introspection hooks**: `snapshot_metrics()`, `reconfigure()`, `state_bytes()` on all stateful operators
- [x] **EXPLAIN INCREMENTAL ANALYZE live round-trip**: `explain_op_node` populates `operator_stats` at `ExplainLevel::Analyze`
- [x] **Worker-side segment cache**: `crates/rockstream-runtime/src/segment_cache.rs` with LRU keyed by `(shard_id, segment_id)` and TTL tied to checkpoint epoch
- [x] **Segment cache >80% hit rate test**: `segment_cache_tests.rs` proof test asserts hit_ratio > 0.80
- [x] **Legacy `rockstream.*` alias removed**: `resolve_catalog_alias` no longer rewrites `rockstream` to `rockstream_catalog`
- [x] **CLI `debug arrangement` subcommand**: parses view/op_id/key args; prints arrangement header and tombstone density
- [x] **CLI `describe` and `support-bundle` subcommands**: implemented in `main.rs`
- [x] **docs/configuration.md**: Published — covers all `rockstream.toml` knobs
- [x] **docs/cli.md**: Published — covers all CLI subcommands and JSON logging
- [x] **docs/sre-operations.md**: Published — covers Prometheus metrics, OTEL spans, alerts, and support bundle parsing
- [x] **docs/grafana-dashboard.json**: Out-of-the-box Grafana dashboard template

---

## Verification

- `cargo test --workspace`: **1310 passed, 2 ignored** (2026-06-03)
- All proof criteria checked above
- ROADMAP.md v0.50 row updated to `✅ Done`

---

*Signed off: 2026-06-03*
