# v0.55 Sign-off

**Signed off**: 2026-08-15

## Proof Claims

All claims in the v0.55 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| A worker or peer connection presenting an invalid, expired, or absent mTLS client certificate is rejected with an audited denial and never processes shard data | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_peer_invalid_expired_absent_cert_rejected_with_audit_denial_no_data`, `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_missing_cert_rejected`, `test_control_worker_mtls_untrusted_ca_rejected`, `test_control_worker_mtls_expired_cert_rejected`, `test_control_worker_mtls_identity_mismatch_rejected`, `crates/rockstream-runtime/tests/shuffle_mtls_tests.rs::test_worker_shuffle_grpc_mtls_unauthenticated_rejected`, `test_worker_shuffle_grpc_mtls_untrusted_rejected` | PASS: asserts untrusted, expired, absent cert, and identity mismatch peers fail TLS handshake/registration, topology catalog remains empty (`catalog.len() == 0`), zero leases granted, zero shard data processed, and audited denial events `security.internal_mtls_denied` emitted with `RS-2410`/`RS-2411`/`RS-2412` |
| A certificate rotation completes on a 3-worker cluster under sustained load with zero epoch loss, zero pipeline restarts, and no window in which an already-authenticated peer is spuriously refused | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_certificate_rotation_3_worker_cluster_under_load_zero_loss_zero_restart`, `crates/rockstream-control/tests/mtls_rotation_load_tests.rs::test_mtls_rotation_control_under_load_zero_loss`, `test_mtls_rotation_stale_cert_rejection_after_grace` | PASS: 3 workers streaming lease requests during Gen2 CA + cert reload maintain continuous active status with `catalog.len() == 3` and 0 lost epochs; dynamic reload in flight succeeds without disconnects; stale Gen1 peer rejected after grace window |
| The v0.53 CLI reaches the control plane with a client certificate and is refused without one, with no change to its command surface — proving the transport seam held | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_cli_control_mtls_transport_seam_and_refusal`, `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_valid_cert_executes`, `test_cli_control_mtls_missing_cert_refused`, `test_cli_control_mtls_invalid_cert_refused` | PASS: valid cert CLI executes status command (`cli.authenticated` emitted), unauthenticated CLI refused with `RS-2410`, untrusted CLI refused with `RS-2411`, with zero syntax changes to CLI commands |
| Distributed coordination under network jitter and live rotation in SimRuntime | `crates/rockstream-sim/tests/internal_mtls_sim_tests.rs::test_mtls_rotation_simruntime_faults`, `test_mtls_concurrent_worker_joins_under_rotation` | PASS: multi-schedule fault injection with `buggify!()` during worker join jitter and live rotation keeps all 3 workers operating without race conditions |
| Durability across SlateDB LFS and MinIO object store with zero range deletions | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_mtls_cluster_durability_lfs_and_minio` | PASS: worker registration, lease assignments, and audit records persist identically across restarts on LFS and MinIO with 0 range deletions |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests (e2e pgwire), negative tests (`RS-2410`, `RS-2411`, `RS-2412`), dispatch-wiring verification, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-2410`, `RS-2411`, `RS-2412`).
- [x] Every control-plane action writes an audit event (`security.internal_mtls_denied`, `worker.registered`, `cli.authenticated`).
- [x] Every new public surface (CLI flags `--tls-ca-cert-path`, `--tls-cert-path`, `--tls-key-path`, config fields) is documented in `docs/cli.md`, `docs/security.md`, and `docs/sre-operations.md`.
- [x] Every new queue/buffer/scan window has a named bound, a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test (`test_mtls_rotation_simruntime_faults`, `test_mtls_concurrent_worker_joins_under_rotation`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] Any soak gate has been run clean.
- [x] Confirmation that no item has been deferred.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test -p rockstream-control --test internal_mtls_tests
rtk cargo test -p rockstream-control --test control_worker_mtls_tests
rtk cargo test -p rockstream-control --test mtls_rotation_load_tests
rtk cargo test -p rockstream-cli --test cli_mtls_transport_tests
rtk cargo test -p rockstream-runtime --test shuffle_mtls_tests
rtk cargo test -p rockstream-sim --test internal_mtls_sim_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.55 is complete.
