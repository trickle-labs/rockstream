# v0.45 Sign-off

**Signed off**: 2026-07-12

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` for v0.45 ("Deep FinOps
Optimizations & Cost/Diagnostics Visibility") have been verified against
running code and full test-suite output. Full evidence detail is in
`.claude/v0.45-evidence.md`.

| Proof Claim | Evidence |
|---|---|
| TCO benchmarks show >50% reduction in steady-state operational costs compared to v0.31 | `rockstream-ops/tests/tco_cost.rs` benchmark harness compares Config A (v0.31-era single-tier Standard, 100% on-demand) vs. Config B (v0.45: Standard-IA tiering, Spot mix) for an identical workload; asserts Config B's `estimated_cost_per_hour` is >50% lower. **PASS** |
| A per-tier cost-visibility metric ships on `/metrics` and the starter Grafana dashboard so an operator can see the FinOps win without re-running the TCO benchmark | `rockstream-gateway/tests/gateway_proof_tests.rs::resource_usage_show_matches_catalog_table()` proves `SHOW RESOURCE USAGE` and `rockstream_catalog.view_resource_usage` expose a live per-workload/per-tier `estimated_cost_per_hour` column sourced from the pricing engine; `deploy/dashboards/rockstream-overview.json` adds a cost-per-hour panel. **PASS** |
| `EXPLAIN INCREMENTAL my_view` typed into `psql` returns the documented operator tree (not the old pushdown-only stub), verified byte-for-byte against `explain_incremental_for_sql`'s output | `gateway_proof_tests.rs::explain_incremental_matches_frontend_byte_for_byte()` executes `EXPLAIN INCREMENTAL <view>` over the wire protocol and diffs it byte-for-byte against a direct `SqlFrontend::explain_incremental_for_sql()` call; the gateway dispatch rewrite (Slice B1) replaced the one-line stub at `server.rs`. **PASS** |
| `SHOW RESOURCE USAGE` returns live per-workload state-byte/memory/SLO figures matching `view_resource_usage` | Same test as above: `resource_usage_show_matches_catalog_table()` asserts `SHOW RESOURCE USAGE`, `SHOW RESOURCE USAGE FOR WORKLOAD <name>`, and `SELECT * FROM rockstream_catalog.view_resource_usage`/`workload_resource_usage` all return identical data sourced from `MetricRegistry` (`workload_memory_bytes`, `state_budget_bytes`, SLO compliance). **PASS** |
| CI fails a nightly Nexmark run that regresses delta-amplification or propagation latency by >10% against the v0.36 baseline | `rockstream-ops/tests/nexmark_regression_gate.rs`: baseline committed at `crates/rockstream-ops/benches/baseline/v0.36.json`; comparator unit test asserts a >10% regression fails the gate and <10% jitter passes it. **PASS** |
| A cluster that has created >256 materialized-view pipelines over its lifetime continues to accept new `CREATE MATERIALIZED VIEW` statements, with per-worker metrics cardinality bounded by the LRU working-set size rather than by total pipeline count | Slice D1 (`rockstream-types::metrics`): bounded `pipeline_id`-labeled metrics backed by a 256-entry `LruCache`. Unit test registers 300 synthetic pipelines, asserts all 300 succeed (no rejection), exactly ≤256 distinct `pipeline_id` labels plus one `"other"` aggregate are ever emitted, and the `"other"` bucket sums evicted contributions correctly. **PASS** |
| `SHOW RESOURCE USAGE` and `rockstream_catalog.workload_resource_usage` return a non-null `estimated_cost_per_hour` once a `[pricing]` profile is configured, and the figure changes visibly when a workload's `MEMORY_LIMIT` or shard count changes | Slice C3, same `resource_usage_show_matches_catalog_table()` test: with `[pricing]` configured, `estimated_cost_per_hour` is non-null and changes when `MEMORY_LIMIT` is modified and re-queried; with no `[pricing]` block the column is `NULL`. **PASS** |
| `SET rockstream.max_staleness` never blocks, emits `RS-2018` NOTICE on staleness exceeded, `frontier_age_ms` in response metadata, and dedicated metrics | `gateway_proof_tests.rs::session_max_staleness_notice_emits_and_query_still_returns()` and `session_mode_tracks_mutual_exclusion_between_wait_for_and_max_staleness()` prove the NOTICE, non-blocking read, `frontier_age_ms` accuracy, and mutual exclusion with `session_wait_for`. **PASS** |
| Cross-session `write_fence()`/`after_fence(:token)` gates visibility of a specific write in another session, reusing the existing `wait_for` code path | `gateway_proof_tests.rs::write_fence_token_can_be_used_by_another_session_via_after_fence()`, a two-connection wire-protocol test: session 1 writes and calls `write_fence()`, session 2 uses `after_fence(:token)` and observes the write. **PASS** |
| `INSERT ... RETURNING` reflects a server-assigned key (UUID default or sequence), not just literal client `VALUES` | Three wire-protocol tests: `insert_returning_generates_uuid_default_and_persists_it()`, `insert_returning_generates_identity_values_sequentially()`, `multi_row_insert_returning_generates_distinct_identity_values_in_order()`. All **PASS** |
| (Scope) Worker pool continues functioning under Spot reclaim (abort-kill fault) with existing recovery SLOs | `rockstream-sim` seeded `SimRuntime` test `worker_spot_preemption_recovery_slo_holds()` injects a sudden worker-kill fault mid-epoch, asserts lease-fencing reassignment ≤30s p99, lossless checkpoint recovery, and no duplicate output (Z-set idempotency). **PASS** |
| (Scope) `TieredObjectStore` prefix router + age-based SST Standard-IA tiering + S3 Express One Zone config construction | `rockstream-storage` tiering tests (4 suites, LFS + MinIO) prove `shard_meta/*` keys route to backend A and aged SSTs to backend B, plus the aged-SST copy/verify/delete cycle with `Attribute::StorageClass`; AWS-only unit test asserts `AmazonS3Builder` Express One Zone configuration (directory-bucket endpoint, session auth). **PASS** |
| All existing workspace tests pass, no regressions | `cargo test --workspace` → 1249 passed, 5 ignored (88 suites), 0 failures |
| Definition-of-Done: `cargo fmt --check` / `cargo clippy -- -D warnings` clean | `cargo fmt --check` initially found unrelated pre-existing formatting drift in `rockstream-types/src/lib.rs` and `metrics.rs`; fixed via `cargo fmt`. `cargo clippy --workspace --all-targets -- -D warnings` → "No issues found" |
| Definition-of-Done: `make e2e` passes | `make e2e` → no-op binary role smoke test, LFS backend (13 tests), and MinIO/TestContainers backend (6 tests) all **PASS**; `=== e2e PASSED ===` |

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

- Fixed pre-existing `cargo fmt` drift in `crates/rockstream-types/src/lib.rs`
  (module ordering) and `crates/rockstream-types/src/metrics.rs` (line-wrap
  formatting) discovered while preparing this sign-off; no functional changes.
- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D
  warnings`, `cargo test --workspace` (1249 passed, 5 ignored, 88 suites), and
  `make e2e` all pass clean.
- `./scripts/check-exit-criteria.sh` passes with this sign-off file complete.
- Every user-visible failure path introduced in this version (max_staleness
  misuse, write_fence/after_fence token errors, EXPLAIN INCREMENTAL parse
  errors, pricing config errors) uses an `RS-XXXX` error code with actionable
  text, verified by unit/wire-protocol tests.
- Nothing deferred: all 10 Proof claims (P1–P10) plus both scope-only
  validations (Spot/preemptible worker pool, storage tiering) from the v0.45
  roadmap row are implemented and verified in this sign-off. See
  `.claude/v0.45-evidence.md` for the full slice-by-slice implementation
  summary (16 vertical slices across groups A–G).

