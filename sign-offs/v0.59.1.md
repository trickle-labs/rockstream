# v0.59.1 Sign-off — Evidence Integrity & Honest Release State

**Signed off:** 2026-08-18

## Proof

| Claim | Evidence | Result |
|---|---|---|
| A release-identity conformance test fails on any package/binary/image/manifest mismatch. | `rtk cargo test -p rockstream-cli --test candidate_identity_tests`<br>`rtk cargo test -p rockstream-types --test candidate_identity_unit_tests`<br>`rtk bash scripts/check-release-candidate-gate.sh` | PASS — 6 passed in `candidate_identity_tests`, 4 passed in `candidate_identity_unit_tests`. Conformance tests verify identity consistency across `Cargo.toml`, CLI `--version` / `rockstream version --json`, Prometheus `rockstream_build_info`, support bundle metadata, `Dockerfile` OCI labels, and evidence manifests. Mismatch in version, commit SHA, build timestamp, rustc version, lockfile digest, or features causes immediate failure. |
| The RC validator's mutation suite rejects a changed SHA or artifact digest, any skipped mandatory prerequisite, missing raw data, and a summary that cannot be regenerated from its raw artifact; checked-in baselines define targets only and cannot satisfy a measured-result field. | `rtk bash scripts/check-release-candidate-gate.test.sh`<br>`rtk cargo test -p rockstream-types --test evidence_manifest_tests`<br>`rtk cargo test -p rockstream-cli --test evidence_manifest_cli_tests` | PASS — `check-release-candidate-gate self-test passed` (13 mutation suites passed); 10 passed in `evidence_manifest_tests`, 5 passed in `evidence_manifest_cli_tests`. Self-test and unit tests confirm rejection of mutated commit SHAs, corrupted artifact SHA-256 digests, skipped mandatory tests (`mandatory_skipped > 0`), missing raw metrics, metric summary discrepancies (>1%), baseline targets substituted for measured results, and tampered manifest signatures. |
| Branch and tag policy is verified before release authorization. | `rtk bash scripts/check-release-governance.sh`<br>`rtk bash scripts/check-release-governance.test.sh` | PASS — `check-release-governance.test.sh` passed all 7 tests. Release governance validator enforces branch protection (no direct pushes, required status checks, no force pushes), signed annotated release tags (`git tag -s`), and `.github/CODEOWNERS` mandatory reviews for CI workflows, formal specifications, security policies, and capability contracts. |
| Reclassify v0.59 publicly as an engineering-complete technical preview & separate release qualification status. | `rtk bash scripts/check-exit-criteria.sh` | PASS — Reclassification confirmed across `README.md`, `DESIGN.md`, `NEW_ROADMAP.md`, `sign-offs/v0.59.md`, and `capabilities.toml`. Status matrix clearly separates implementation, short-CI, candidate identity, security provenance, artifact publication, and final release authorization. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e-lfs` passes (14/14 tests pass).
- [x] New behavior has unit tests (`candidate_identity_unit_tests.rs`, `evidence_manifest_tests.rs`) plus CLI integration tests (`candidate_identity_tests.rs`, `evidence_manifest_cli_tests.rs`) and mutation self-tests (`check-release-candidate-gate.test.sh`, `check-release-governance.test.sh`).
- [x] SQL/wire-protocol features maintain reachability, negative tests, and dispatch wiring.
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text (`RS-4022`, `RS-4023`, `RS-4024`, `RS-4025`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml`, `docs/capability-matrix.md`, and `docs/release-governance.md`.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime`.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary (`rockstream version`, `rockstream manifest`, `rockstream candidate-id`).
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-cli --test candidate_identity_tests
rtk cargo test -p rockstream-types --test candidate_identity_unit_tests
rtk cargo test -p rockstream-types --test evidence_manifest_tests
rtk cargo test -p rockstream-cli --test evidence_manifest_cli_tests
rtk bash scripts/check-release-candidate-gate.sh
rtk bash scripts/check-release-candidate-gate.test.sh
rtk bash scripts/check-release-governance.sh
rtk bash scripts/check-release-governance.test.sh
rtk bash scripts/check-capability-contract.sh
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk make e2e-lfs
rtk ./scripts/check-exit-criteria.sh
```

All v0.59.1 Proof claims and exit criteria pass. v0.59.1 is done; nothing in its scope is deferred.

