# v0.33 Sign-off

**Signed off**: 2026-06-23

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.33 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Claims Verification

- [x] **P1: Same seed produces an identical event stream across two runs**
  - **Evidence**: Verified by test `test_nexmark_generator_determinism` in `crates/rockstream-sim/src/nexmark.rs`.
  - **Command**: `rtk cargo test -p rockstream-sim test_nexmark_generator_determinism`
- [x] **P2: Generator distribution is within 1% of target ratios (~46 bids : 3 auctions : 1 person)**
  - **Evidence**: Verified by test `test_nexmark_generator_distribution` in `crates/rockstream-sim/src/nexmark.rs`.
  - **Command**: `rtk cargo test -p rockstream-sim test_nexmark_generator_distribution`
- [x] **P3: All three tables accept `INSERT` via pgwire and return correct row counts via `SELECT`**
  - **Evidence**: Verified by tests `test_nexmark_ingestion_lfs` and `test_nexmark_ingestion_minio` in `crates/rockstream-sql/tests/nexmark/main.rs`.
  - **Command**: `rtk cargo test -p rockstream-sql --all-features --test nexmark`
- [x] **P4: Schema round-trips through the catalog**
  - **Evidence**: Verified by test `test_nexmark_schema_creation_and_catalog` in `crates/rockstream-sql/tests/nexmark/main.rs`.
  - **Command**: `rtk cargo test -p rockstream-sql --all-features --test nexmark test_nexmark_schema_creation_and_catalog`
- [x] **P5: Verified on LFS and MinIO via TestContainers**
  - **Evidence**: Verified by end-to-end integration tests using local filesystem `ShardDb` and MinIO docker container via TestContainers.
  - **Command**: `rtk cargo test -p rockstream-sql --all-features --test nexmark`

## Notes

All tests pass successfully. Storage layer uses memory/file ShardDb for LFS and MinIO via testcontainers for MinIO storage integration.
