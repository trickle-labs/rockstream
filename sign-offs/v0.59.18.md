# v0.59.18 Sign-off — Lifecycle, Client, Documentation & Backend Test Closure

**Signed off**: 2026-09-01

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Lifecycle race, restart, and recovery across real backends (`TST-007`) | `qualification_recovery_tests::*`, `qualification_lifecycle_tests::*`, `connector_scenario_durability_lfs_tests::*`, `connector_scenario_durability_minio_tests::*` | PASS (14) |
| External-client conformance across 9 ecosystems and all 9 protocol checks (`TST-008`) | `driver_matrix_tests::*` (`tokio-postgres`, `psql 14`, `psql 16`, `libpq`, `psycopg3`, `SQLAlchemy`, `node-postgres`, `Prisma`, `PgJDBC`) | PASS (10) |
| Executable documentation, golden paths, and reference cookbooks (`TST-009`) | `template_verification_tests::*`, `init_tests::*`, `documentation_transcript_tests::*` | PASS (19) |
| Core behavior ledger complete for all 11 Core capabilities across all 5 behaviors (`TST-010`) | `capabilities.toml`, `scripts/check-capability-contract.sh --full-semantics`, `scripts/check-capability-contract.test.sh` | PASS |
| Zero-hidden-skip policy enforced; missing prerequisites fail closed under `ROCKSTREAM_REQUIRE_DOCKER=1` (`TST-010`) | `crates/rockstream-test-support/src/lib.rs::tests`, `scripts/check-no-hidden-skips.sh`, `scripts/check-no-hidden-skips.test.sh` | PASS |
| Durability proofs paired across LFS and MinIO for all durable capabilities | `connector_scenario_durability_lfs_tests::*`, `connector_scenario_durability_minio_tests::*`, `minio_delta_native_durability_tests::*`, `minio_shared_trace_durability_tests::*` | PASS |

## Notable scope decisions

- `capabilities.toml` now defines full 5-behavior ledgers (`incremental`, `backfill`, `checkpoint_recovery`, `state_growth`, `failure`) with paired LFS + MinIO durability proof targets for `connector.postgres-cdc`, `connector.kafka-source`, and `sink.kafka`.
- `scripts/check-no-hidden-skips.sh` and `scripts/check-no-hidden-skips.test.sh` were added to guarantee zero unrecorded `#[ignore]` annotations and zero silent early returns on missing Docker.
- `rockstream-test-support` guarantees fail-closed behavior when `ROCKSTREAM_REQUIRE_DOCKER=1` is set.

## Definition of Done

- [x] All Slice 1–7 test commitments from `.claude/v0.59.18-plan.md` §5 pass.
- [x] `cargo fmt --all --check` passes.
- [x] `NEW_ROADMAP.md` marks v0.59.18 `✅ Done`.
- [x] Workspace package version is `0.59.18`.
- [x] `scripts/check-capability-contract.sh --full-semantics` passes.
- [x] `scripts/check-no-hidden-skips.sh` and self-test pass.
- [x] `scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo build --bin rockstream
rtk cargo test -p rockstream-test-support
rtk cargo test -p rockstream-connectors --test dlq_backpressure_threshold_tests --test kafka_source_non_broker_tests
rtk cargo test -p rockstream-connectors --test kafka_sink_guarantee_matrix_tests --test kafka_source_guarantee_matrix_tests --test postgres_cdc_guarantee_matrix_tests -- --test-threads=1
rtk cargo test -p rockstream-gateway --features testcontainers --test driver_matrix_tests -- --test-threads=1
rtk cargo test -p rockstream-cli --test template_verification_tests --test init_tests --test documentation_transcript_tests
rtk cargo test -p rockstream-sim --test qualification_recovery_tests --test qualification_lifecycle_tests
rtk bash scripts/check-capability-contract.sh --full-semantics
rtk bash scripts/check-capability-contract.test.sh
rtk bash scripts/check-no-hidden-skips.sh
rtk bash scripts/check-no-hidden-skips.test.sh
rtk bash scripts/check-exit-criteria.sh
rtk cargo fmt --all -- --check
```

All v0.59.18 proof claims pass. Lifecycle races and recoveries across real backends,
external-client conformance across all 9 client ecosystems, golden paths and executable docs,
and the Core behavior ledger with fail-closed zero-hidden-skip verification are complete.
