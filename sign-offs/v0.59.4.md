# v0.59.4 Sign-off — CLI & Configuration Usability

**Signed off:** 2026-08-19

## Proof

| Claim | Evidence | Result |
|---|---|---|
| `rockstream demo` runs deterministic orders scenario end-to-end through pgwire and asserts incremental maintenance (`UX-01`). | `rtk cargo test -p rockstream-cli --test demo_tests` | PASS (5/5 tests) — Proves embedded gateway startup on ephemeral port (`127.0.0.1:0`), standard pgwire client connectivity (`tokio-postgres`), orders DDL/DML, incremental aggregate maintenance on `sales_by_store`, storage lifecycle cleanup without leaving orphaned directories, and deterministic text/JSON output. |
| `rockstream doctor` runs bounded non-destructive diagnostic checks within deadline and redacts secrets (`UX-02`). | `rtk cargo test -p rockstream-cli --test doctor_tests` | PASS (15/15 tests) — Proves candidate identity, config diagnostics, system limits (FD, memory, monotonic clock, tempdir), storage permissions, deep probe write/verify/delete, control/gateway reachability failure handling within 5s deadlines, and credential/secret redaction. |
| `config validate` reports syntax, unknown keys, deprecated keys, and semantic diagnostics (`CFG-01`). | `rtk cargo test -p rockstream-types --test config_validation_tests`<br>`rtk cargo test -p rockstream-cli --test config_tests` | PASS (13/13 tests) — Proves TOML line/col syntax errors, unknown key typo suggestions via Levenshtein distance, removed storage tiering rejection (`RS-4017`), semantic section bounds, and TLS certificate file accessibility checks (`--check-files`). |
| `config print-effective` resolves identical config as `start` with origin tracking (`CFG-02`). | `rtk cargo test -p rockstream-types --test config_resolver_tests`<br>`rtk cargo test -p rockstream-cli --test config_tests` | PASS (9/9 tests) — Proves 4-tier precedence (`Defaults < File < Env < CLI`), identical config resolution between `print-effective` and `start`, origin tracking (`Default`, `File`, `Environment`, `Cli`), and secret masking in effective config display. |
| CLI JSON output matches schema across all finite commands (`CLI-01`). | `rtk cargo test -p rockstream-cli --test cli_output_tests` | PASS (4/4 tests) — Proves uniform `--output json`, `--json` alias compatibility, structured JSON error envelopes on stderr, and valid JSON Lines for streaming commands (`audit tail`, `view subscribe`). |
| Completions generate for bash/zsh/fish (`CLI-02`). | `rtk cargo test -p rockstream-cli --test completions_tests` | PASS (3/3 tests) — Proves dynamic `clap_complete` generation for Bash, Zsh, and Fish directly from the live CLI AST. |
| Dispatch-Wiring Audit | `rtk bash scripts/check-dispatch-wiring.sh` | PASS — 21/21 SQL statement families and all 5 new CLI commands verified with 0 MISSING paths. |
| Capability Contract & RC Gates | `rtk python3 scripts/check-capability-contract.py`<br>`rtk python3 scripts/check-release-candidate-gate.py` | PASS — `capabilities.toml` matches contract with version `v0.59.4`; all Entry Criteria, Identity, Governance, Manifest Integrity, and RC Gates passed. |
| Candidate Identity Version Synchronization (0.59.4) | `rtk cargo test -p rockstream-cli --test candidate_identity_tests`<br>`rtk cargo test -p rockstream-types --test candidate_identity_unit_tests` | PASS — 6 passed in `candidate_identity_tests`, 4 passed in `candidate_identity_unit_tests`. Version 0.59.4 verified across `Cargo.toml`, `Dockerfile`, `capabilities.toml`, and CLI `--version`. |
| Durability: LFS Backend (SlateDB local filesystem) | `rtk make e2e-lfs` | PASS — 14/14 tests pass (`tests/lfs_backend.rs`). Proves atomic batch commits, WAL visibility, reader snapshots, prefix scan, checkpointing, and clean scan-and-delete without range deletions. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e-lfs` passes (14/14 tests pass).
- [x] New behavior has unit tests (`config_validation_tests.rs`, `config_resolver_tests.rs`, `candidate_identity_unit_tests.rs`) plus CLI integration tests (`demo_tests.rs`, `doctor_tests.rs`, `cli_output_tests.rs`, `completions_tests.rs`, `config_tests.rs`, `candidate_identity_tests.rs`).
- [x] SQL/wire-protocol features maintain reachability, negative tests, and dispatch wiring (`scripts/check-dispatch-wiring.sh` reports 0 MISSING paths).
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml`, `docs/capability-matrix.md`, and `docs/cli.md`.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime`.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-cli --test demo_tests
rtk cargo test -p rockstream-cli --test doctor_tests
rtk cargo test -p rockstream-cli --test cli_output_tests
rtk cargo test -p rockstream-cli --test completions_tests
rtk cargo test -p rockstream-cli --test config_tests
rtk cargo test -p rockstream-types --test config_validation_tests
rtk cargo test -p rockstream-types --test config_resolver_tests
rtk cargo test -p rockstream-cli --test candidate_identity_tests
rtk cargo test -p rockstream-types --test candidate_identity_unit_tests
rtk bash scripts/check-dispatch-wiring.sh
rtk python3 scripts/check-capability-contract.py
rtk python3 scripts/check-release-candidate-gate.py
rtk bash scripts/check-threat-model-links.sh
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-error-codes.sh
rtk make e2e-lfs
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk ./scripts/check-exit-criteria.sh
```

All v0.59.4 Proof claims and exit criteria pass. v0.59.4 is done; nothing in its scope is deferred.

