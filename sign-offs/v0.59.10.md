# v0.59.10 Sign-off — Runtime Introspection & Operator Clarity

**Signed off**: 2026-08-24

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Embedded capability registry matches `capabilities.toml` with zero diff (`OBS-01`) | `capability_unit_tests`, `capabilities_sql_tests` | PASS (6) |
| `SELECT rockstream_version()` exposes CandidateIdentity fields over pgwire (`OBS-02`) | `candidate_identity_unit_tests`, `candidate_version_sql_tests` | PASS (5) |
| `SHOW VIEW STATUS` and `EXPLAIN INCREMENTAL` report stable raw engine facts (`OBS-03`) | `engine_facts_introspection_tests`, `show_view_status_arrangement_tests`, `show_view_status_pgwire_tests` | PASS (20) |
| Bounded read-only `rockstream_catalog` virtual tables queryable over pgwire (`CAT-01`) | `catalog_system_tables_tests` | PASS (10) |
| Secrets are redacted and scan bounds are enforced in catalog queries | `catalog_system_tables_tests` | PASS (2) |
| Stable catalog identifiers survive restart on LFS and MinIO | `catalog_durability_lfs_tests`, `catalog_durability_minio_tests` | PASS (2) |
| Catalog queries maintain snapshot isolation under SimRuntime fault injection | `v05910_introspection_sim_tests` | PASS (1) |

## Definition of Done

- [x] Targeted proof suites pass.
- [x] `cargo fmt --all --check` passes.
- [x] `NEW_ROADMAP.md` marks v0.59.10 `✅ Done`.
- [x] Workspace package version is `0.59.10`.
- [x] `./scripts/check-exit-criteria.sh` passes.
- [x] `./scripts/check-dispatch-wiring.sh` passes.
- [x] `./scripts/check-capability-contract.sh` passes.
- [x] `./scripts/check-error-codes.sh` passes.
- [x] `./scripts/check-invariant-pairs.sh` passes.

## Reproduction

```text
rtk cargo test -p rockstream-types --test capability_unit_tests --test candidate_identity_unit_tests
rtk cargo test -p rockstream-gateway --test capabilities_sql_tests --test candidate_version_sql_tests --test engine_facts_introspection_tests --test catalog_system_tables_tests --test catalog_durability_lfs_tests --test catalog_durability_minio_tests
rtk cargo test -p rockstream-sim --test v05910_introspection_sim_tests
rtk cargo fmt --all --check
rtk cargo clippy --all-targets -- -D warnings
rtk bash scripts/check-exit-criteria.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk bash scripts/check-capability-contract.sh
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-invariant-pairs.sh
```
