# v0.45.5 Sign-off

**Signed off**: 2026-07-14

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.45.5 have been
verified against running code, local test output, and a full manual re-read
of all seven in-scope documents.

### Proof Claims

- [x] **P1 — Language-features conformance lock**: A new test fails if a
  future PR adds a keyword to `docs/language-features.md`'s "Implemented
  Today" section that `rockstream-sql`'s parser does not recognize. Evidence:
  `test_language_features_doc_keywords_are_parseable` in
  `crates/rockstream-sql/tests/docs_conformance_tests.rs` extracts every
  backtick-quoted keyword from the doc and asserts each parses; passes
  (`.claude/v0.45.5-evidence.md` §P1).
- [x] **P2 — Configuration conformance lock**: A new test fails if
  `docs/configuration.md` drifts from `RockstreamConfig`'s real
  fields/defaults. Evidence:
  `test_configuration_doc_matches_rockstream_config_defaults` in
  `crates/rockstream-types/tests/docs_conformance_tests.rs` diffs the doc's
  reference table against `RockstreamConfig::default()`; passes
  (`.claude/v0.45.5-evidence.md` §P2).
- [x] **P3 — Human diff audit (cli.md/configuration.md exact match, plus
  language-features.md/concepts.md full re-verification)**: `docs/cli.md`
  regenerated flag-for-flag against `crates/rockstream-cli/src/main.rs`'s
  `clap` `Command` (adds all seven v0.45.2 M7 flags and error code
  `RS-1731`); `docs/configuration.md`'s reference table regenerated from
  `RockstreamConfig`/`Default` (7 of 9 values were wrong, one phantom key
  removed, missing fields added); `docs/language-features.md` and
  `docs/concepts.md` re-verified line-by-line with zero further drift found.
  Evidence: `.claude/v0.45.5-evidence.md` §P3 (S1–S4).
- [x] **P4 — Support-bundle documentation accuracy**: `docs/sre-operations.md`'s
  support-bundle section matches the real `write_support_bundle()` JSON
  artifact (`support-bundle-<generated_at_ms>.json`) byte-for-byte in a
  worked example; the fictitious tarball/`audit.jsonl`/`config.toml`/
  `metrics.json` bundle was removed and the nonexistent
  `rockstream support-bundle` command reference corrected to note the
  roadmapped v0.55 `rockstream support bundle` name is not yet available.
  Evidence: `.claude/v0.45.5-evidence.md` §P4.
- [x] **P5 — Distributed-architecture component re-verification**:
  `docs/distributed-architecture.md` names only real types/error codes.
  `WorkerAggregator` → `FrontierAggregator`
  (`crates/rockstream-control/src/frontier.rs`) and `RS-1601` → `RS-3601`
  (`CoordinatorError::AlignmentBufferFull`,
  `crates/rockstream-control/src/checkpoint.rs`) fixed; full
  component-by-component re-read against `crates/rockstream-control` and
  `crates/rockstream-runtime` found no further mismatches. Evidence:
  `.claude/v0.45.5-evidence.md` §P5.
- [x] **P6 — Grafana dashboard metric-name accuracy**:
  `docs/grafana-dashboard.json`'s panels/metric names are confirmed present
  in the real Prometheus exporter (`crates/rockstream-types/src/metrics.rs`);
  the fabricated `exchange_queue_depth` panel replaced with the real
  `exchange_queue_depth_bytes` metric and the state-budget-utilization panel's
  denominator fixed to the real `state_budget_bytes` metric. Evidence:
  `.claude/v0.45.5-evidence.md` §P6.

### Checklist

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] NEW_ROADMAP.md status updated to `✅ Done`

## Notes

Full evidence detail (test names, file/line pointers, before/after diffs for
every corrected value, and the full-suite result: 1008 tests passed in 13
suites, exit code 0) is recorded in `.claude/v0.45.5-evidence.md`.

No item from this version's Scope has been deferred. Deferred-item audit
(`.claude/v0.45.5-plan.md` §Deferred-Item Audit) confirms no prior
sign-off's deferred items overlap this version's scope, and this version's
own plan leaves nothing incomplete: all six slices (S1–S6, spanning the two
new conformance-lock tests and the reconciliation of all seven in-scope
documents — `docs/language-features.md`, `docs/cli.md`,
`docs/configuration.md`, `docs/concepts.md`, `docs/sre-operations.md`,
`docs/distributed-architecture.md`, `docs/grafana-dashboard.json`) are
implemented, tested, and passing.

