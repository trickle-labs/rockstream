# v0.57 Sign-off

**Signed off**: 2026-08-16

## Proof Claims

| Proof claim | Evidence | Result |
|---|---|---|
| The capability gate fails if a Core statement, keyword, connector, or sink lacks a tier or linked passing e2e proof; deleting one tier and one proof link is caught. | `rtk bash scripts/check-capability-contract.test.sh` | PASS — baseline, missing-tier, and missing-proof-link mutation fixtures pass with the expected diagnostics. |
| The capability matrix regenerates byte-identically and roadmap/matrix drift fails CI. | `rtk bash scripts/check-capability-contract.test.sh` | PASS — generated-output and mutated-roadmap fixtures pass; CI wiring runs `scripts/check-capability-contract.sh`. |
| The product promise is byte-identical in `README.md`, `DESIGN.md`, and `NEW_IMPLEMENTATION_PLAN.md`. | `rtk bash scripts/check-capability-contract.test.sh` | PASS — baseline and one-file-divergence fixtures pass with the expected diagnostic. |
| A mock roadmap version missing the completed admission checklist fails `scripts/check-exit-criteria.sh`. | `rtk bash scripts/check-exit-criteria.test.sh` | PASS — failing and completed mock-roadmap fixtures pass their expected assertions. |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] All Proof criteria are verified against running code or CI output.
- [x] New behavior has unit tests plus applicable oracle/property, LFS, MinIO, and/or TestContainers tests; v0.57 adds no runtime behavior, so only contract-gate self-tests apply.
- [x] SQL/wire-protocol reachability, negative, dispatch-wiring, and coverage-matrix tests are not applicable; v0.57 adds no SQL/wire behavior.
- [x] Every user/operator-visible failure has an actionable `RS-XXXX` code; no new user/operator-visible failure path is added.
- [x] Every control-plane action writes an audit event; no control-plane action is added.
- [x] Every new performance claim has a `criterion` benchmark or measurement note; no performance claim is added.
- [x] Every new public surface is documented; the capability contract and admission policy are documented in the checked-in contract sources.
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path; none is added.
- [x] Every new distributed-coordination path has a seeded `SimRuntime` test; none is added.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate is introduced by v0.57.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] `rtk make e2e` passes.
- [x] No item has been deferred from v0.57.
- [x] `NEW_ROADMAP.md` marks v0.57 `✅ Done`.

## Reproduction

```text
rtk bash scripts/check-capability-contract.test.sh
rtk bash scripts/check-exit-criteria.test.sh
rtk cargo test -p rockstream-sql --test docs_conformance_tests test_language_features_doc_keywords_are_parseable -- --exact
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test --workspace
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.57 is signed off. No v0.57 item has been deferred.
