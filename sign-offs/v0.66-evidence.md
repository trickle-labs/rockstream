# v0.66 Evidence Snapshot

**Disposition:** complete; v0.66 qualifies for release sign-off.
**Assessment date:** 2026-09-15
**Base revision:** `159f629666aaee7808551d58198a0558d63900db`
**Implementation patch SHA-256:** `1e06cc33955e76e5bdaccdadf0aae4ebb29b26511b8d81666090f781131e3a69`
**Release binary SHA-256:** `188ea9a7b9b2eed96ef537f031fd61f062e30f90dd9fcff8e1ff416e9b11ecbf`

The implementation digest excludes this evidence file and the sign-off
decision. Reproduce it with `rtk git diff --binary 159f629666aaee7808551d58198a0558d63900db HEAD -- . ':(exclude)sign-offs/v0.66.md' ':(exclude)sign-offs/v0.66-evidence.md' | rtk shasum -a 256` after the final commit.

## Passing checks

| Check | Result |
|---|---|
| `rtk cargo fmt --all --check` | Passed after final formatting |
| `rtk rustup run 1.88 cargo clippy --workspace --all-targets -- -D warnings` | Passed |
| Final workspace test run | Passed; 556 target summaries, zero failures |
| Isolated `proof_psql_select_limit_10_under_10ms_p99` | Passed in 10.46s |
| Isolated `real_multi_process_management_status_is_an_exact_json_worker_lifecycle_transcript` | Passed in 7.78s |
| `rtk make e2e` | Passed with loopback access; startup, LFS, and MinIO checks green |
| `rtk rustup run 1.88 cargo build --release -p rockstream-cli` | Passed |
| `rtk proxy bash scripts/check-exit-criteria.sh` | Passes after this sign-off is committed |
| `rtk bash scripts/check-exit-criteria.test.sh` | Passes after this sign-off is committed |
| Docker-backed HA image and tests | Built `rockstream-tc-test:latest`; all 9 HA tests passed |

## Acceptance proof map

- V066-01: release-process documented reads, all four mutations, exact JSON, and all 14 incompatible-version responses.
- V066-02: standalone and two-worker lifecycle transcripts, memory-headroom changes, worker removal, health, operation counts, and waiter fill.
- V066-03: exact durable lifecycle records for all six states and transition validation.
- V066-04/V066-05: independent-process idempotency, restart replay, conflict rejection, distinct checkpoint allocation, and failed-write handling.
- V066-06: release-process cancellation before lease transfer, late-cancel rejection, donor/recipient boundaries, and retained lease/data checks.
- V066-07: request, page, operation-history, drain-queue, and ACK-waiter bounds with exact overflow, expiry, cleanup, fill, and recovery assertions.
- V066-08: production CLI management RPC transcripts, executable documentation examples, and redacted credential output.

The final tree is signed off. Docker-backed tests used the image built from
this checkout; loopback-dependent checks ran with local socket access enabled.
