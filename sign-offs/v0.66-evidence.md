# v0.66 Evidence Snapshot

**Disposition:** incomplete; this file does not qualify v0.66 for release.
**Assessment date:** 2026-09-14
**Base revision:** `159f629666aaee7808551d58198a0558d63900db`
**Implementation patch SHA-256:** `39358cf600106cf76af8451ed246823f598a5c9efdd326051834477c714fecd6`
**Release binary SHA-256:** `a1f0b5cb391126cd94523b54de4629f0418887edf51b2a7ffc48f1581e045b2e`

The patch digest identifies the staged implementation, tests, generated
references, and CI wiring relative to the base revision. It excludes this
evidence file and the sign-off decision. The release binary was rebuilt from
that implementation before these two evidence-only files were written.
After commit, reproduce the digest with `rtk git diff --binary 159f629666aaee7808551d58198a0558d63900db HEAD -- . ':(exclude)sign-offs/v0.66.md' ':(exclude)sign-offs/v0.66-evidence.md' | rtk shasum -a 256`.

## Passing checks

| Command | Result |
|---|---|
| `rtk cargo fmt --all --check` | Passed on the current tree |
| `rtk git diff --check` | Passed |
| `rtk rustup run 1.88 cargo test --quiet -p rockstream-control` | Passed; all 42 test-target summaries passed with local socket access enabled |
| `rtk cargo test --release --quiet -p rockstream-cli --test management_backup_process_tests --test product_truth_lifecycle_tests --test r1_real_worker_data_plane_tests -- --test-threads=1` | 6 passed across 3 suites; backup create, restore, restart replay, and exact CLI output |
| `rtk rustup run 1.88 cargo test --release --quiet -p rockstream-cli --test management_backup_process_tests release_management_backup_commits_and_restores_real_shard_data -- --exact` | Passed; executed all 10 documented read commands against the release process and checked full JSON output |
| `rtk rustup run 1.88 cargo test -p rockstream-control --test management_service_tests management_rejects_incompatible_versions_before_reading_state -- --exact` | Passed; all 14 RPC methods return the exact incompatible-version error |
| `rtk rustup run 1.88 cargo test --release -p rockstream-cli --test management_backup_process_tests release_management_backup_idempotency_survives_role_all_restart -- --exact` | Passed; exact operation replay and changed-payload conflict after a real process restart |
| `rtk rustup run 1.88 cargo test --release -p rockstream-cli --test management_backup_process_tests release_management_backup_idempotency_is_atomic_across_processes -- --exact` | Passed; concurrent requests to two release processes yield one operation, one committed backup generation, and an exact conflicting-payload error |
| `rtk rustup run 1.88 cargo test --release -p rockstream-cli --test management_backup_process_tests release_management_backups_allocate_distinct_checkpoints_concurrently -- --exact` | Passed; two concurrent distinct backup requests complete with separate checkpoint IDs and commit markers |
| `rtk rustup run 1.88 cargo test --release -p rockstream-cli --test r1_real_worker_data_plane_tests real_multi_process_management_migration_moves_a_shard_and_keeps_its_data -- --exact --test-threads=1` | Passed; standalone control restores a persisted lease, real workers move the MinIO-backed shard, exact operation JSON reports success, and the stored value remains readable |
| `rtk rustup run 1.88 cargo test --quiet -p rockstream-control management::tests::cluster_status_reports_request_and_ack_waiter_fill_and_recovery -- --exact` | Passed; request-limit error and request/ACK waiter fill recovery |
| `rtk rustup run 1.88 cargo test --quiet -p rockstream-runtime --test worker_graceful_drain_tests test_worker_epoch_flush_lease_release_durability -- --exact --test-threads=1` | 1 passed with recipient-open acknowledgement and a live recipient connection |
| `rtk rustup run 1.88 cargo test --quiet --workspace` | Final-tree rerun cancelled after 24 passing test-target summaries and zero failures during a slow link step; the prior 422-target pass was on the preceding tree |
| `rtk rustup run 1.88 cargo clippy --workspace --all-targets -- -D warnings` | Passed; no issues found |
| `rtk make e2e` | Passed: startup check, 14 LFS tests, and 7 MinIO tests |
| `rtk rustup run 1.88 cargo build --release -p rockstream-cli` | Passed; binary SHA-256 recorded above |
| `rtk proxy bash scripts/check-exit-criteria.sh` | Passed for versions already marked Done; v0.66 remains Planned and is not qualified by this gate |
| `rtk bash scripts/check-exit-criteria.test.sh` | Passed |

The focused CLI process suites ran in release mode, including a post-lint
rerun of the exact backup transcript test. The full control package passed
with local socket access enabled. A prior complete workspace suite passed on
the preceding tree; the final-tree workspace rerun was cancelled after 24
passing targets. Docker-backed migration/drain tests ran against a test image
built from this checkout.

## Incomplete or failed proof

- A Docker-backed process test proves successful worker drain, actual shard
  movement, recipient-open acknowledgement, and terminal operation status.
  A donor-loss test proves the operation waits at the worker-flush boundary.
  A release-process migration now restores a persisted shard lease, moves it
  between real workers, and preserves its MinIO-backed value. Incompatible-
  version rejection covers all 14 RPCs; real cancellation effects remain
  incomplete.
- Backup, status, and migration have release-process transcripts, and all ten
  documented management read commands now run against a release process. The
  full secret-free output matrix remains incomplete.
- The status field-to-owner matrix is incomplete for health, lifecycle,
  headroom, and operation counts; process health remains explicitly unknown.
- Concurrent same-key backup retries across independent processes produce one
  committed effect; changed-payload retries after restart are rejected.
  Kill-before/during-effect reconciliation, executor cancellation races, and
  complete saturation and expiry-recovery evidence remain absent.
- Each attached migration and backup ACK registry now admits at most 64
  waiters. Status reports waiter fill and capacity. Integrated executor
  saturation and expiry recovery within a frozen resource profile remain
  unproved.
- No durable CI artifact or committed raw test log exists for this local run.
- The final-tree full workspace run is incomplete: it was cancelled after 24 passing targets and zero failures because linking advanced too slowly. The prior 422-target run passed on the preceding tree.

See [`v0.66.md`](v0.66.md) for the unchecked acceptance criteria. These results
support an implementation checkpoint, not a release sign-off.
