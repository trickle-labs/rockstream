# v0.66 Evidence Snapshot

**Disposition:** incomplete; this file does not qualify v0.66 for release.
**Assessment date:** 2026-09-13
**Base revision:** `159f629666aaee7808551d58198a0558d63900db`
**Implementation patch SHA-256:** `2600750cd48d24fbe625e1fa3cc8d1d59b95d0183fe031318c12e8db706f3014`
**Release binary SHA-256:** `97420ecad1ac945b7fa771b34079f1ee6074e06de98b36047f0a4c2093b298a4`

The patch digest identifies the staged implementation, tests, generated
references, and CI wiring relative to the base revision. It excludes this
evidence file and the sign-off decision. The release binary was rebuilt from
that implementation before these two evidence-only files were written.
After commit, reproduce the digest with `rtk git diff --binary 159f629666aaee7808551d58198a0558d63900db HEAD -- . ':(exclude)sign-offs/v0.66.md' ':(exclude)sign-offs/v0.66-evidence.md' | rtk shasum -a 256`.

## Passing checks

| Command | Result |
|---|---|
| `rtk cargo fmt --all --check` | Passed |
| `rtk cargo clippy --workspace --all-targets -- -D warnings` | Passed |
| `rtk cargo test -p rockstream-control` | 293 passed across 40 suites |
| `rtk cargo test -p rockstream-cli` | 406 passed across 73 suites |
| `rtk cargo test -p rockstream-control --test management_operation_durability_tests` | 16 passed |
| `rtk cargo test -p rockstream-control --test management_service_tests` | 6 passed |
| `rtk cargo test -p rockstream-cli --test product_truth_lifecycle_tests` | 1 passed against a live management endpoint |
| `rtk cargo test -p rockstream-docgen --test reference_render_tests` | 2 passed |
| `rtk cargo test -p rockstream-gateway --test conformance_doc_tests` | 15 passed |
| `rtk rustup run 1.88 cargo llvm-cov -p rockstream-management-proto --lib --tests --fail-under-lines 70 --fail-under-regions 70` | 4 tests passed; 100% lines and regions |
| `rtk make e2e` | Passed: standalone startup, 14 LFS tests, 7 MinIO tests |
| `rtk cargo build --release -p rockstream-cli` | Passed |
| `rtk proxy bash scripts/check-exit-criteria.sh` | Passed for versions already marked Done; v0.66 remains Planned and is not qualified by this gate |
| `rtk bash scripts/check-exit-criteria.test.sh` | Passed |

The CLI and control package suites ran with the host compiler. The full
workspace attempt used the repository's pinned Rust 1.88 toolchain.

## Incomplete or failed proof

- `rtk rustup run 1.88 cargo test --workspace` had one failure in
  `rockstream-connectors::source_durability_minio_tests` (12 passed, 1 failed).
  The same target passed twice when rerun alone (13/13 each time). The complete
  workspace command therefore remains unverified as green.
- No real management `CreateBackup` executor or committed checkpoint-manifest
  path is attached; the RPC returns `UNAVAILABLE`.
- Drain does not await recipient-open acknowledgement and can report completion
  without proving the recipient owns the opened shard. Migration tests do not
  qualify a real release-process data move.
- No management release transcript proves every displayed field on both
  standalone and multi-process deployments.
- Process-kill/restart tests around production mutation effects, leader fencing,
  full cancellation-interruption checks, and complete limit-saturation/expiry
  recovery proof remain absent.
- No durable CI artifact or committed raw test log exists for this local run.

See [`v0.66.md`](v0.66.md) for the unchecked acceptance criteria. These results
support an implementation checkpoint, not a release sign-off.
