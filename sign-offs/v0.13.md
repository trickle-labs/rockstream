# v0.13 Sign-off

**Signed off**: 2026-06-10

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.13 have been verified.

- [x] `cargo test --workspace` passes
- [x] ROADMAP.md status updated to `✅ Done`
- [x] **Proof Claim 1**: 100M-row-equivalent snapshot view matches the batch result
  - *Evidence*: `cargo test -p rockstream-ops --test lfs_snapshot lfs_snapshot_bootstrap_large_scale` and `cargo test -p rockstream-ops --test minio_snapshot minio_snapshot_bootstrap_large_scale` pass.
- [x] **Proof Claim 2**: A mid-bootstrap connector restart neither duplicates nor skips rows
  - *Evidence*: `cargo test -p rockstream-ops --test lfs_snapshot lfs_snapshot_bootstrap_restart_resilience` and `cargo test -p rockstream-ops --test minio_snapshot minio_snapshot_bootstrap_restart_resilience` pass.
- [x] **Proof Claim 3**: A 5-level view-on-view DAG and a diamond topology converge under continuous input
  - *Evidence*: `cargo test -p rockstream-sql --test lfs_catalog test_5_level_view_chain_convergence` and `cargo test -p rockstream-sql --test lfs_catalog test_diamond_topology_convergence` pass.
- [x] **Proof Claim 4**: A cycle is rejected at compile time
  - *Evidence*: `cargo test -p rockstream-sql --test lfs_catalog test_cycle_rejection` passes.
- [x] **Oracle / Property**: `SnapshotOp` output equals batch query reference
  - *Evidence*: `cargo test -p rockstream-ops --lib oracle_snapshot_matches_batch` passes.
- [x] **Oracle / Property**: `ViewRefOp` tail output matches batch CDC reference
  - *Evidence*: `cargo test -p rockstream-ops --lib oracle_view_ref_matches_batch` passes.

## Notes

Verified all tests locally. All checkpoint boundaries, bootstrap offsets, and cyclic dependency compile-time checks are active and correct.
