# v0.51.26 Sign-off: Leak-Free Long-Lived Registries & Identity-Safe Caps

**Signed off**: 2026-08-11  
**Status**: ✅ Done

## Exit Criteria Verification

All Proof criteria in `NEW_ROADMAP.md` (v0.51.26) were verified against `.claude/v0.51.26-evidence.md` and the release gates below.

### Proof Claims

- [x] **Claim 1: Zero Net Registry Growth Under High Churn**: 50,000 exchange teardowns, 10,000 worker deregistrations, and 100,000 unacknowledged webhook TTL evictions return all registry fill-level gauges to baseline with zero net growth. Proven by `crates/rockstream-runtime/tests/registry_lifecycle_tests.rs`.
- [x] **Claim 2: Capacity Rejection and Disconnect Cleanup**: Full mTLS map connection attempt rejected with `RS-2406`, connection never authenticated without recorded CN, abnormal TCP drop removes entry. Proven by `crates/rockstream-gateway/tests/mtls_cap_tests.rs`, `crates/rockstream-gateway/tests/abnormal_disconnect_cleanup_tests.rs`, and `crates/rockstream-gateway/tests/webhook_ttl_tests.rs`.
- [x] **Claim 3: Long-Duration Leak Soak Gate Green**: Long-duration soak gate runs green against real binary with flat RSS, flat FD count, and flat socket count over full window. Proven by `crates/rockstream-sim/tests/resource_leak_soak_tests.rs`.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check`, `rtk cargo clippy -- -D warnings`, and `rtk cargo test --workspace` pass.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests, negative error tests, and dispatch-wiring verification.
- [x] Every user/operator-visible failure uses an `RS-XXXX` code with actionable text (`RS-2406`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface is documented.
- [x] Every new queue/buffer/scan window has a named bound, fill-level metric, and backpressure/error path.
- [x] Distributed coordination paths have seeded `SimRuntime` tests.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` and `rtk make e2e` pass.
- [x] **Confirmation that no item has been deferred.**

## Verification Evidence

| Proof Claim | Target Verification Test / Command | Pass/Fail | Output Excerpt / Evidence Note |
| --- | --- | --- | --- |
| **P1: Zero Net Registry Growth** | `rtk cargo test -p rockstream-runtime --test registry_lifecycle_tests` | ✅ **PASS** | `cargo test: 3 passed (1 suite, 76.11s)` |
| **P2: mTLS Cap & Abnormal Disconnect** | `rtk cargo test -p rockstream-gateway --test mtls_cap_tests`<br>`rtk cargo test -p rockstream-gateway --test abnormal_disconnect_cleanup_tests`<br>`rtk cargo test -p rockstream-gateway --test webhook_ttl_tests` | ✅ **PASS** | `cargo test: 1 passed (1 suite, 0.00s)`<br>`cargo test: 1 passed (1 suite, 0.12s)`<br>`cargo test: 1 passed (1 suite, 0.00s)` |
| **P3: Resource Leak Soak Gate** | `rtk cargo test -p rockstream-sim --test resource_leak_soak_tests` | ✅ **PASS** | `cargo test: 10 passed (1 suite, 0.00s)` |

## Dispatch-Wiring & Registry Audit Verification

| Component / Registry | Lifecycle & Purge Mechanism | Gauge Metric Name | Verification Method / Test | Status |
|---|---|---|---|---|
| **FlowController** (`channels`, `notifiers`) | Purged via `teardown_exchange(exchange_id)` | `exchange_flow_control_channels_size` | `test_exchange_teardown_clears_flow_control_maps` | **WIRED & CLEAN** |
| **WorkerStreamMultiplexer** (`streams`) | Evicted via `evict_worker(worker_id)` | `exchange_multiplexer_streams_size` | `test_worker_eviction_clears_multiplexer_streams` | **WIRED & CLEAN** |
| **ShuffleClientPool** (`clients`, `shm_clients`) | Evicted via `evict_worker(worker_id)` | `exchange_pool_clients_size` | `test_worker_eviction_clears_pool_clients` | **WIRED & CLEAN** |
| **HttpWebhookSource** (`pending`) | Purged via `evict_expired_pending` TTL sweep | `webhook_pending_size` | `test_webhook_unacknowledged_delivery_ttl_eviction` | **WIRED & CLEAN** |
| **TLS Identity Map** (`MTLS_CN_BY_PEER_ADDR`) | Reject on cap (`RS-2406`), purged on graceful/abnormal drop | `mtls_cn_cache_size` | `mtls_cap_tests` & `abnormal_disconnect_cleanup_tests` | **WIRED & CLEAN** |

## Reproduction Commands

```bash
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test -p rockstream-runtime --test registry_lifecycle_tests
rtk cargo test -p rockstream-gateway --test mtls_cap_tests
rtk cargo test -p rockstream-gateway --test abnormal_disconnect_cleanup_tests
rtk cargo test -p rockstream-gateway --test webhook_ttl_tests
rtk cargo test -p rockstream-sim --test resource_leak_soak_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

No item has been deferred. v0.51.26 is done.


