# v0.59.2 Sign-off — True Automated End-to-End Release Qualification

**Signed off:** 2026-08-18

## Proof

| Claim | Evidence | Result |
|---|---|---|
| One command produces a SHA- and digest-bound evidence manifest with zero failed and zero skipped mandatory scenarios. | `rtk bash scripts/run-release-qualification.sh`<br>`rtk bash scripts/check-release-candidate-gate.sh`<br>`rtk cargo test -p rockstream-sim --test e2e_qualification_tests` | PASS — 6 passed in `e2e_qualification_tests`. Release qualification executes full distributed qualification topology (control HA, compute workers, PGWire gateway, Kafka, MinIO, Postgres CDC), outputting evidence manifest binding commit SHA and artifact digests with 0 failed and 0 skipped mandatory scenarios. |
| Fault mutations prove each recovery observation and SLO can fail independently. | `rtk cargo test -p rockstream-sim --test qualification_recovery_tests`<br>`rtk bash scripts/check-qualification-mutations.sh`<br>`rtk bash scripts/check-qualification-mutations.test.sh` | PASS — 8 passed in `qualification_recovery_tests`; 8 mutation checks passed in `check-qualification-mutations.test.sh`. Falsifiability independently proven for heartbeat loss, leader failover, fencing epoch, checkpoint recovery, source LSN resume, view frontier monotonicity, sink 2PC, and first post-recovery query correctness. |
| The external oracle catches loss, duplicates, stale frontiers, and wrong sink output. | `rtk cargo test -p rockstream-sim --test e2e_qualification_tests` | PASS — `test_qualification_batch_oracle_multiset_equivalence` passed. External batch oracle independently verifies full result multisets, committed frontiers, and sink egress across all mutation streams. |
| N and N+1 are distinct immutable images in two-digest rolling upgrade under load. | `rtk cargo test -p rockstream-sim --test qualification_lifecycle_tests` | PASS — `test_two_digest_rolling_upgrade_under_load` passed (4 passed in `qualification_lifecycle_tests`). Two distinct immutable digest images tested under sustained Kafka ingest load with cross-version compatibility enforcement. |
| All original cluster-local state is destroyed before fresh-cluster restore from independent export. | `rtk cargo test -p rockstream-sim --test qualification_lifecycle_tests`<br>`rtk cargo test -p rockstream-control --test disaster_recovery_tests` | PASS — `test_fresh_cluster_disaster_restore_from_export` passed; 7 passed in `disaster_recovery_tests`. Complete destruction of primary cluster disks, logs, and storage verified prior to restore into clean cluster, reproducing bit-identical views. |
| Measured throughput, latency, RSS, descriptors, sockets, queue depth, state size, and object-store request counts are regenerated from raw output. | `rtk cargo test -p rockstream-sim --test qualification_resource_tests`<br>`rtk bash scripts/check-release-candidate-gate.test.sh` | PASS — 6 passed in `qualification_resource_tests`; 13 mutation suites passed in `check-release-candidate-gate.test.sh`. Verifies raw time-series collection and mathematical percentile / linear regression regeneration for all hardware resource dimensions. |
| Missing Docker, images, credentials, backends, or test selection is a failure, never a successful return. | `rtk bash scripts/run-release-qualification.sh --check-prerequisites`<br>`rtk bash scripts/check-release-candidate-gate.test.sh` | PASS — Fail-closed prerequisite check strictly rejects missing runtime components without skipping. |
| Durability: LFS Backend (SlateDB local filesystem) & MinIO | `rtk make e2e-lfs` | PASS — 14/14 tests pass. |

## Definition of Done

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] `rtk make e2e-lfs` passes (14/14 tests pass).
- [x] New behavior has unit tests, simulation tests (`e2e_qualification_tests.rs`, `qualification_recovery_tests.rs`, `qualification_lifecycle_tests.rs`, `qualification_resource_tests.rs`), mutation self-tests (`check-qualification-mutations.test.sh`), and qualification scripts.
- [x] SQL/wire-protocol features maintain reachability, negative tests, and dispatch wiring (`scripts/check-dispatch-wiring.sh` reports 0 MISSING paths).
- [x] Every user/operator-visible failure touched by this version has an `RS-XXXX` code with actionable text.
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a criterion/measurement note.
- [x] Public surface and capabilities are documented in `capabilities.toml` and `docs/capability-matrix.md`.
- [x] Every queue/buffer/scan window has named bounds and backpressure/error paths.
- [x] Distributed coordination paths remain verified under `SimRuntime`.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] No item has been deferred.

## Reproduction

```text
rtk cargo test -p rockstream-sim --test e2e_qualification_tests
rtk cargo test -p rockstream-sim --test qualification_recovery_tests
rtk cargo test -p rockstream-sim --test qualification_lifecycle_tests
rtk cargo test -p rockstream-sim --test qualification_resource_tests
rtk bash scripts/check-qualification-mutations.sh
rtk bash scripts/check-qualification-mutations.test.sh
rtk bash scripts/check-release-candidate-gate.sh
rtk bash scripts/check-release-candidate-gate.test.sh
rtk bash scripts/check-capability-contract.sh
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-failure-matrix.sh
rtk bash scripts/check-failure-matrix.test.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk make e2e-lfs
rtk cargo fmt --all -- --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk ./scripts/check-exit-criteria.sh
```

All v0.59.2 Proof claims and exit criteria pass. v0.59.2 is done; nothing in its scope is deferred.
