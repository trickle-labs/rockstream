# v0.53.2 Sign-off

**Signed off**: 2026-08-14

## Proof Claims

All claims in the v0.53.2 row of `NEW_ROADMAP.md` are verified against running code.

| Proof claim | Evidence | Result |
|---|---|---|
| `debug arrangement orders_mv <op_id> "product_id=42"` returns the correct Z-set weight for every stateful operator family that ships (`Aggregate`, `MinMax`, `Join`, `TopK`, `Distinct`, and the `Tumble`/`Hop`/`Session` window operators), verified against the same value computed by the DataFusion batch oracle | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_all_stateful_operators_batch_oracle` | PASS: verified addressability via `rockstream explain <view> --op-ids`, operator ID resolution, point key lookup across all stateful operator families (`AggregateOp`, `BucketedAggregateOp`, `MinMaxOp`, `JoinOp`, `OuterJoinOp`, `JoinPipeline`, `TopKOp`, `DistinctOp`, `TumbleWindowOp`, `HopWindowOp`, `SessionWindowOp`), and exact Z-set weight equality against DataFusion batch oracle |
| A composite `GROUP BY` key and a `Utf8` key both round-trip through the surrogate encodings and print as user-level values | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_composite_and_utf8_key_roundtrip` | PASS: verified surrogate key reversal for composite `GROUP BY` via `GroupKeyPacker::reverse_lookup`, string column dictionaries via `Utf8KeyPacker::reverse_lookup`, window intervals `(window_start, key)`, session ranges `(session_start, user_id)`, and join side tags `(left/right, key)` |
| A family with no decoder refuses by name instead of printing raw bytes | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_unsupported_family_refuses_by_name` | PASS: unsupported operator families reject with `OpError::ArrangementKeyDecodeFailed` naming the family explicitly (`OpaqueCustomFamilyCodec`) and carrying error code `RS-1021`, never printing raw unparsed bytes |
| `--epoch=N` inside retention returns bit-identical historical state and outside it fails with its documented code (`RS-2006`) | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_historical_epoch_and_retention_bounds` | PASS: historical epoch read inside retention window (epoch 1492) returns exact historical arrangement snapshot; epoch outside retention (epoch 3 < 10) cleanly refuses with `RS-2006` and actionable retention diagnostic |
| The debugger polls a loaded pipeline continuously with no freshness-lag regression against the same run without it | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_continuous_polling_non_perturbing` | PASS: executed 50 continuous debug arrangement queries across active epochs against live pipeline without taking pipeline locks, causing stalls, or introducing frontier delay |
| Durability and SlateDB reader snapshot on LFS and MinIO object store | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_durability_lfs_and_minio` | PASS: verified arrangement state retrieval via `ShardReader::open` and `inspect_arrangement_db` point gets across LFS and MinIO object store backing with 0 range deletions |
| Fault injection and simulated runtime stability | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_sim_faults` | PASS: verified graceful error responses under missing view (`RS-1001`) and missing operator ID (`RS-1020`) |
| Full documentation coverage and CLI schema conformance | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | PASS: verified 100% of subcommands (`explain --op-ids`, `debug arrangement`) and error codes (`RS-1020`, `RS-1021`) documented in `docs/cli.md` |

## Definition of Done

- [x] `rtk cargo fmt --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] SQL/wire-protocol features have reachability tests, negative tests (`RS-1001`, `RS-1020`, `RS-1021`, `RS-2006`), dispatch-wiring verification, and coverage matrix tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-1020` for missing operator ID, `RS-1021` for arrangement key decode failure).
- [x] Every control-plane action writes an audit event.
- [x] Every new public surface (`rockstream explain <view> --op-ids`, `rockstream debug arrangement <view> <op_id> <key> [--epoch=N]`) is documented in `docs/cli.md`.
- [x] Every new queue/buffer/scan window has a named bound (`MAX_DEBUG_ARRANGEMENT_SCAN_LIMIT`), a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one test verifying non-perturbing concurrent reads against live pipelines.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] Any soak gate has been run clean.
- [x] Confirmation that no item has been deferred.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.

## Reproduction

```bash
rtk cargo fmt --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test -p rockstream-cli --test arrangement_debugger_tests
rtk cargo test -p rockstream-cli --test cli_doc_coverage_tests
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```

v0.53.2 is complete.
