# v0.52.4 Sign-off

**Signed off**: 2026-08-13

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.52.4 have been verified.

- [x] `rockstream-connectors` exports exactly the retained modules; `connector_surface_contract_tests` passes (5/5).
- [x] Workspace builds with `-D warnings`; `cargo clippy --workspace --all-targets -- -D warnings` passes with no issues and no new `#[allow(dead_code)]`.
- [x] `cargo tree -i iceberg`, `deltalake`, `aws-sdk-s3`, `aws-config`, and `csv` find no workspace paths; manifest scan is empty.
- [x] Full retained workspace suite passes in `cargo test --target-dir /private/tmp/rockstream-v0524-proof --workspace`, including LFS, MinIO/Testcontainers, simulation, Nexmark, and pgwire suites.
- [x] Removed-connector tests enumerate only removed connector tests and pass; deleted test files are exclusively removed-connector coverage.
- [x] `make verify` and `make verify-relaxed` pass with M5 retired; M1–M4, M6, and M7 remain unchanged in the formal gate.
- [x] Repo-wide live-tree reference scan finds no deleted module references; `formal/m5_cold_tier_sink.fizz` is retired with rationale.
- [x] `cargo fmt --check`, `git diff --check`, and `cargo run -p rockstream-cli -- --help` pass; CLI remains runnable.
- [x] `scripts/check-invariant-pairs.test.sh` passes and proves retired M5 IDs are neither required nor silently ignored.
- [ ] `NEW_ROADMAP.md` status updated to `✅ Done` (held until performance evidence is complete).

## Proof Evidence

| Proof claim | Evidence | Result |
|---|---|---|
| Exact retained connector surface | `cargo test -p rockstream-connectors --test connector_surface_contract_tests` | 5 passed; 0 failed |
| Removed connectors fail closed over pgwire | `cargo test -p rockstream-gateway --test connector_removal_tests` | 7 passed; 0 failed; exact `RS-4017` checks |
| Durable removed state | `connector_removal_lfs_tests`, `connector_removal_minio_tests` | 1 passed; 0 failed in each store |
| Retained source reachability | `cargo test -p rockstream-gateway --test source_ddl_postgres_kafka_tests` | 3 passed; 0 failed |
| CDC failure preservation | `cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests` | 6 passed; 0 failed; exact `RS-1002` |
| Decimal/operator backfill matrix | `cargo test -p rockstream-gateway --test backfill_operator_matrix_tests` | 28 passed; 0 failed |
| Nexmark exactness | oracle `nexmark_retraction` and SQL `nexmark` tests | 6 and 12 passed; 0 failed |
| Formal/static gates | `make verify`, `make verify-relaxed`, clippy, fmt, diff check | All passed |
| Dependency removal | `cargo tree -i` checks and manifest scan | No matching paths/references |
| Complete workspace proof | `cargo test --target-dir /private/tmp/rockstream-v0524-proof --workspace` | Exit 0; no FAILED/error summaries |

Dependency count: Cargo.lock package entries decreased from 842 to 701. Release-build and CI wall-time before/after measurements are not available in the Phase 4 evidence; no performance improvement is claimed.

## Notes

The deleted tests and files are limited to S3, webhook, Iceberg, Delta, object-store,
cold-tier, and their dead abstractions. No item is deferred.
