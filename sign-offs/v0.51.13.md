# v0.51.13 Sign-off: Full Ad Hoc Query Support

**Signed off**: 2026-08-05  
**Status**: ✅ Done

## Exit Criteria Verification

All Proof criteria in `NEW_ROADMAP.md` (v0.51.13, line 1058) were verified against `.claude/v0.51.13-evidence.md` and the release gates below.

### Proof Claims

- [x] **P1 — Complete multi-shard ad hoc queries**: A three-shard topology returns exact single-shard-oracle results for raw-SQL `WHERE`, `INNER JOIN`, and `GROUP BY`/`SUM`/`COUNT` queries. `query_time_multi_shard_scatter_tests` covers `BIGINT`, `TEXT`, and `DOUBLE PRECISION` matrices, dispatch through `tokio_postgres::Client::simple_query`, LFS-backed shards, and `RS-2028` topology failure handling. `query_time_topology_provider_tests` proves the production CLI refreshes every configured owner at one frontier and reports stale frontiers as `RS-2030` with `next_steps`.
- [x] **P2 — Result sets beyond retired caps complete correctly**: `query_time_large_relation_tests` returns exact aggregates over 1,000,001 rows and 65,537 rows above 64 MiB, respectively, instead of `RS-2025`. The pathological-budget case returns `RS-2029`, `3 rows or 1024 bytes`, and `next_steps` before emitting a partial response.
- [x] **P3 — Existing query behavior remains correct**: `create_index_query_planning_tests` passes unchanged. `reader_scan_prefix_pages_obeys_exact_row_and_byte_bounds` verifies exact paged-reader bounds. The bounded scatter test returns exact `["200010000", "20000"]` output while terminal fill levels are exactly `{ rows: 0, bytes: 0, batches: 0 }` and all peaks remain within named limits.

### Additional Required Coverage

- [x] **MinIO/TestContainers**: `query_time_multi_shard_scatter_minio_tests` starts and tears down MinIO through TestContainers and verifies exact `WHERE`, `JOIN`, and grouped-aggregate rows across three S3-backed shards.
- [x] **Seeded distributed simulation**: `scatter_frontier_buggify_preserves_complete_exact_result` passes seeds `0x51_13_0001`, `0x51_13_0002`, and `0x51_13_0003`; each injects an adversarial scatter fault and returns exactly `[(1, west), (2, east), (3, north)]` at frontier `17`.
- [x] **SQL reachability and negative behavior**: Raw SQL reaches parser, dispatcher, topology-aware executor, paged readers, and response encoder. Invalid SQL returns `RS-2026` with `next_steps`; empty topology returns `RS-2028`; stale topology returns `RS-2030`; pathological query budget returns `RS-2029` without truncation.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check`, `rtk cargo clippy -- -D warnings`, and `rtk cargo test --workspace` pass.
- [x] New behavior has unit, exact-oracle, LFS, MinIO, and TestContainers coverage.
- [x] SQL/wire reachability, negative error behavior, dispatch wiring, and `BIGINT`/`TEXT`/`DOUBLE PRECISION` query matrices are tested.
- [x] Every new user-visible failure uses actionable `RS-2026`, `RS-2028`, `RS-2029`, or `RS-2030` text with recovery guidance.
- [x] No new control-plane action was added.
- [x] Bounded scatter batches have row, byte, and batch limits; fill-level metrics; and `RS-2029` backpressure/error behavior.
- [x] The distributed scatter path has seeded `SimRuntime` coverage.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No soak gate applies to this version.
- [x] `NEW_ROADMAP.md` status is updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` and `rtk make e2e` pass.
- [x] Nothing is deferred to a later version.

## Verification Evidence

| Proof claim | Test / command | Result | Evidence |
| --- | --- | --- | --- |
| P1 | `rtk cargo test -p rockstream-gateway --test query_time_multi_shard_scatter_tests` | PASS | Exact `WHERE`, `JOIN`, and aggregate oracle results for all three types; LFS coverage. |
| P1 | `rtk cargo test -p rockstream-cli --test query_time_topology_provider_tests` | PASS | All configured owners refresh at one frontier; stale frontier fails closed with `RS-2030`. |
| P2 | `rtk cargo test -p rockstream-gateway --test query_time_large_relation_tests` | PASS | Exact 1,000,001-row and >64-MiB aggregates; pathological work fails with `RS-2029` before partial output. |
| P3 | `rtk cargo test -p rockstream-gateway --test create_index_query_planning_tests` | PASS | v0.51.2 index-planning regression remains green. |
| MinIO | `rtk cargo test -p rockstream-gateway --features testcontainers --test query_time_multi_shard_scatter_minio_tests` | PASS | Exact multi-shard S3-backed query results. |
| Simulation | `rtk cargo test -p rockstream-sim --features simulation --test query_time_scatter_sim_tests` | PASS | All three seeded scatter-fault cases preserve the exact result. |

## Dispatch Wiring

`rtk rg -n "analyze_select_query\\(&self\\.catalog|query_time_datafusion_response\\(|query_time_datafusion_select\\(|StreamingTable::try_new|datafusion_batches_to_query_response\\(|with_shard_db_and_query_time_shard_topology|scan_prefix_pages\\(" crates/rockstream-gateway/src/server.rs crates/rockstream-storage/src/reader.rs`

PASS — relation analysis (`server.rs:3618`) flows into query-time dispatch (`:3703`), authorization before topology selection (`:4099`), the topology-aware executor and response encoding (`:4112`/`:4115`), `StreamingTable` registration (`:9834`), and `ShardReader::scan_prefix_pages` (`:9997`). `EXPLAIN` uses the same topology-aware helper (`:2978`).

## Reproduction

`rtk cargo fmt --check`  
`rtk cargo clippy -- -D warnings`  
`rtk cargo test --workspace`  
`rtk cargo test -p rockstream-gateway --test query_time_multi_shard_scatter_tests`  
`rtk cargo test -p rockstream-gateway --features testcontainers --test query_time_multi_shard_scatter_minio_tests`  
`rtk cargo test -p rockstream-gateway --test query_time_large_relation_tests`  
`rtk cargo test -p rockstream-cli --test query_time_topology_provider_tests`  
`rtk cargo test -p rockstream-sim --features simulation --test query_time_scatter_sim_tests`  
`rtk cargo test -p rockstream-gateway --test create_index_query_planning_tests`  
`rtk ./scripts/check-exit-criteria.sh`  
`rtk make e2e`

No new docs pages, metrics, regression seeds, or RS codes were added by this sign-off; the implementation adds regression seeds `0x51_13_0001` through `0x51_13_0003` and uses `RS-2026`, `RS-2028`, `RS-2029`, and `RS-2030`. No item has been deferred. v0.51.13 is done.
