# v0.59.13 Sign-off — Single-Source Product Surface

**Signed off**: 2026-08-24

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Authoritative SQL type compatibility contract declaring all type families, operations, support tiers, and mandatory rejection codes (`DOC-001`) | `sql_matrix_contract_tests::*`, `contracts/sql-type-matrix.toml` | PASS (11) |
| Normalized `ProductSurfaceManifest` data structures with deterministic RFC 8785 key ordering, stable array sorting, and zero dangling references (`DOC-001`) | `manifest_determinism_tests::*` | PASS (4) |
| Modular product surface extraction across CLI, runtime configuration, functions, catalog, telemetry metrics, error catalog, and SQL matrix (`DOC-001`) | `contributor_tests::*` | PASS (7) |
| Deterministic generated JSON manifest matching canonical schema byte-for-byte (`DOC-001`, `DOC-004`) | `generator_e2e_tests::test_generated_manifest_matches_docs_json`, `docs/product-surface.json` | PASS (1) |
| Automated drift gate and CI verification with zero drift across source code and manifest (`DOC-004`) | `scripts/check-product-surface.sh`, `scripts/check-product-surface.py` | PASS |
| Mutation gate robustness detecting unauthorized modifications to CLI flags, config keys, function signatures, error codes, and SQL matrix cells (`DOC-004`) | `scripts/check-product-surface.test.sh` | PASS |
| Deterministic manifest integrity and ID resolution under SimRuntime fault chaos across 50 simulation seeds | `v05913_product_surface_sim_tests` | PASS (1) |

## Definition of Done

- [x] Targeted proof suites pass.
- [x] `cargo fmt --all --check` passes.
- [x] `NEW_ROADMAP.md` marks v0.59.13 `✅ Done`.
- [x] Workspace package version is `0.59.13`.
- [x] `./scripts/check-exit-criteria.sh` passes.
- [x] `./scripts/check-dispatch-wiring.sh` passes.
- [x] `./scripts/check-capability-contract.sh` passes.
- [x] `./scripts/check-error-codes.sh` passes.
- [x] `./scripts/check-error-catalog.sh` passes.
- [x] `./scripts/check-product-surface.sh` passes.
- [x] `./scripts/check-invariant-pairs.sh` passes.

## Reproduction

```text
rtk cargo test -p rockstream-docgen
rtk cargo test -p rockstream-sim --test v05913_product_surface_sim_tests
rtk cargo fmt --all --check
rtk cargo clippy --all-targets -- -D warnings
rtk bash scripts/check-exit-criteria.sh
rtk bash scripts/check-dispatch-wiring.sh
rtk bash scripts/check-capability-contract.sh
rtk bash scripts/check-error-codes.sh
rtk bash scripts/check-error-catalog.sh
rtk bash scripts/check-error-catalog.test.sh
rtk bash scripts/check-product-surface.sh
rtk bash scripts/check-product-surface.test.sh
rtk bash scripts/check-invariant-pairs.sh
```
