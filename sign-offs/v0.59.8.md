# v0.59.8 Sign-off — Shared Windows & Skew-Aware Execution

**Signed off**: 2026-08-23

## Proof obligations

| Proof claim | Evidence | Result |
|---|---|---|
| Correlated windows share physical slices, preserve exact results under retractions, and stay bounded | `shared_window_tests` | PASS (4) |
| Composable hot-key plans normalize to power-of-two splits; non-composable laws retain spill fallback | `hot_key_planning_tests` | PASS (2) |
| Power-of-two routing is deterministic, bounded, and durable | `virtual_bucket_routing_tests`; `virtual_bucket_state_durability_tests` | PASS (8) |
| Snapshot-pinned bounded migration copy, committed-frontier cutover, and delayed reclamation preserve ownership safety | `shard_migration_state_machine_tests` | PASS (13) |
| Migration-epoch dual routing delivers each write exactly once to authoritative destinations | `dual_write_zero_loss_tests` | PASS (2) |
| Decayed lease heat survives worker reassignment | `worker_death_causes_reassignment`; `heat_hint_decays_and_survives_reassignment` | PASS (2) |
| Shared-window and migration types preserve serialization and transition invariants | `rockstream-types` | PASS (278) |
| R1 architecture-value prerequisite is satisfied | `evidence/r1-local/decision.json` | GREEN (15 raw samples, 3 structural results) |

## Definition of Done

- [x] Targeted proof suites pass.
- [x] `cargo fmt --check` passes.
- [x] `NEW_ROADMAP.md` marks v0.59.8 `✅ Done`.
- [x] Workspace package version is `0.59.8`.
- [x] No SQL, CLI, or wire-protocol surface was added.
- [x] All migration copy, query, and routing bounds have explicit enforcement.
- [x] `./scripts/check-exit-criteria.sh` passes.

## Reproduction

```text
rtk cargo test -p rockstream-ops --test shared_window_tests --test virtual_bucket_state_durability_tests
rtk cargo test -p rockstream-control --test hot_key_planning_tests --test shard_migration_state_machine_tests
rtk cargo test -p rockstream-plan --test virtual_bucket_routing_tests
rtk cargo test -p rockstream-runtime --test dual_write_zero_loss_tests
rtk cargo test -p rockstream-control --lib worker_death_causes_reassignment
rtk cargo test -p rockstream-control --lib heat_hint_decays_and_survives_reassignment
rtk cargo test -p rockstream-types
rtk cargo fmt --check
rtk ./scripts/check-exit-criteria.sh
```
