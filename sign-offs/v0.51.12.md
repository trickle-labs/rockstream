# v0.51.12 Sign-off: Operational Edge-Case Validation Specs & Real-Cluster Chaos/SLO Soak

**Signed off**: 2026-08-04
**Status**: ✅ Done

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` (line 1057) for v0.51.12 have been verified against `.claude/v0.51.12-evidence.md` and the release gates below.

### Proof Claims

- [x] **P1 — Operational edge-case specifications and paired assertions**: Quota exhaustion, source failure, object-store brownout, misconfiguration rejection, and late data each have a machine-checkable scenario/model and paired runtime assertion. Verified by `formal/edge_quota_exhaustion.fizz`, `formal/edge_late_data.fizz`, all five `EDGE-*` cases in `crates/rockstream-sim/tests/edge_case_recovery_tests.rs`, and `scripts/check-invariant-pairs.test.sh`. Evidence: `.claude/v0.51.12-evidence.md`.
- [x] **P2 — Formal-verify path coverage**: Changes to every edge-covered crate trigger `formal-verify`. Verified by `formal_verify_path_filter_runs_for_each_edge_covered_crate` and `formal_verify_path_filter_ignores_uncovered_path` in `crates/rockstream-gateway/tests/conformance_doc_tests.rs`. Evidence: `.claude/v0.51.12-evidence.md`.
- [x] **P3 — Real-cluster chaos and absolute SLO soak**: A real multi-worker Kafka/MinIO cluster survives the fixed chaos schedule with an exact batch-oracle match and all absolute SLO targets met. Verified by `real_cluster_chaos_soak_kafka_minio_absolute_slos_and_exact_oracle` in `crates/rockstream-sim/tests/real_cluster_chaos_soak_tests.rs`; artifact seed `1360183461`, 128/128 rows committed, p99 detection/reassignment/freshness 119/222/604 ms, and 68 rows/s. Evidence: `.claude/v0.51.12-evidence.md`.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check`, `rtk cargo clippy -- -D warnings`, and `rtk cargo test --workspace` pass.
- [x] New behavior has unit, oracle/property, LFS, MinIO, and TestContainers coverage.
- [x] No SQL/wire-protocol surface was added; reachability and dispatch-wiring requirements are not applicable.
- [x] Misconfiguration rejection returns actionable `RS-1005 connector.watermark_required`.
- [x] No new control-plane action or public surface was added.
- [x] The bounded object-store pending-epoch buffer has a named cap, fill metric, and backpressure path.
- [x] Distributed quota and late-data paths have seeded `SimRuntime` coverage.
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] The fixed real-cluster chaos soak ran cleanly.
- [x] `NEW_ROADMAP.md` status is updated to `✅ Done`.
- [x] `rtk ./scripts/check-exit-criteria.sh` and `rtk make e2e` pass.
- [x] Nothing is deferred to a later version.

## Verification Evidence

| Proof claim | Test/model | Result | Evidence |
| --- | --- | --- | --- |
| P1 | `edge_case_recovery_tests`, `edge_*.fizz`, invariant-pair checker | PASS | All five `EDGE-*` invariants paired; every late-data policy cell passes. |
| P2 | Formal-verify CI path-filter tests | PASS | Each covered crate path enables the workflow; unrelated path does not. |
| P3 | `real_cluster_chaos_soak_kafka_minio_absolute_slos_and_exact_oracle` | PASS | Exact digest `498f756bd236396d5adec3aac119485ce4cc3bb3deb0de5d823d3361420eebf5`; all SLO targets met. |

## Reproduction

`rtk python3 scripts/check_invariant_pairs.py`  
`rtk bash scripts/check-invariant-pairs.test.sh`  
`rtk cargo test -p rockstream-sim --test edge_case_recovery_tests`  
`rtk cargo test -p rockstream-gateway --test conformance_doc_tests`  
`rtk cargo test -p rockstream-sim --test real_cluster_chaos_soak_tests`  
`rtk ./scripts/check-exit-criteria.sh`  
`rtk make e2e`

No item has been deferred. v0.51.12 is done.
