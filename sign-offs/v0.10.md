# v0.10 Sign-off

**Signed off**: 2026-06-09

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.10 have been verified.

- [x] `cargo test --workspace` passes — 659 tests pass (42 suites, 225.60s)
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Obligations

### IVM-6: Distinct / Set Ops / Storage Budget Gate

| # | Criterion | Evidence |
|---|---|---|
| 1 | Weight-based `DistinctOp` with zero-crossing emission | `crates/rockstream-ops/src/distinct.rs` — `DistinctOp`; emits `+1` on `0→+n`, `−1` on `+n→0` |
| 2 | `PlanNode::Distinct` / `OpKind::Distinct` IR | `crates/rockstream-plan/src/lib.rs` |
| 3 | `Intersect {all}` / `Except {all}` IR and operators | `crates/rockstream-plan/src/lib.rs`; `IntersectOp`, `ExceptOp` in `distinct.rs` |
| 4 | Set semantics: Intersect outputs `min(wL, wR)` | `DualArrangement` in `distinct.rs`; oracle property test `oracle_intersect_set_50k` (50k cases) |
| 5 | Bag semantics: Intersect outputs `min(wL, wR)` per bag | oracle property test `oracle_intersect_bag_50k` (50k cases) |
| 6 | Set semantics: Except outputs `max(0, wL − wR)` | oracle property test `oracle_except_set_50k` (50k cases) |
| 7 | Bag semantics: Except propagates negative weights | oracle property test `oracle_except_bag_50k` (50k cases) |
| 8 | Oracle property — Distinct zero-crossing correctness | `oracle_distinct_50k` — 50k randomized sequences, incremental == batch at every epoch |
| 9 | SQL lowering: `SELECT DISTINCT`, `INTERSECT`, `EXCEPT` | `crates/rockstream-sql/src/lower.rs` — 4 unit tests pass |
| 10 | EXPLAIN INCREMENTAL renders new operators | `explain_incremental.rs` — `explain_distinct_shows_zero_crossing`, `explain_intersect_set_shows_semantics`, `explain_except_all_shows_semantics` all pass |
| 11 | Distribution pass handles new operators | `distribution.rs` — `distinct_inserts_loopback_exchange`, `intersect_inserts_loopback_on_both_sides`, `except_inserts_loopback_on_both_sides` all pass |
| 12 | LFS: `DistinctOp` state persists across close/reopen | `lfs_distinct_state_persists_across_reopen` — 2 live rows reload; weight transitions correct |
| 13 | LFS: WAL-only write recovers bit-identically (crash replay) | `lfs_distinct_crash_replay_bit_identical` — drop-without-flush, reopen via WAL; 3 rows bit-identical |
| 14 | No SlateDB range deletion used | `lfs_distinct_no_range_delete` — 27-byte distinct keys (`0x01` prefix + `[0x44,0x53]` discriminator); `delete_range` absent from API (structural guarantee) |
| 15 | Storage keys: distinct arrangement key format | `crates/rockstream-storage/src/keys.rs` — `DISTINCT_DISCRIMINATOR`, `distinct_key`, `distinct_op_prefix`; format `[0x01][0x44,0x53][op_id:8][row_hash:16]` = 27 bytes |
| 16 | SQL Engine milestone: `CREATE VIEW … JOIN … GROUP BY …` | `sql_engine_create_view_join_group_by` — compiles, deploys, 4 epochs incremental output matches DataFusion batch oracle |
| 17 | Storage budget: WAL listing-cache hit ratio >99% | `minio_wal_listing_cache_hit_ratio` — 1 populate + 1000 reads; >99% cache hits asserted (PASS, 12.46s) |
| 18 | Storage budget: manifest cadence bounded | `minio_manifest_cadence_bounded` — 50 epochs; shard-meta entries ≤ 52 asserted (PASS, 6.73s) |
| 19 | Storage budget: PUT/GET/LIST p99 at 1 GB | `minio_latency_p99_1gb` — 1000 PUT/GET ops; RS-5022 not triggered (PASS, 114.11s) |
| 20 | Storage budget: write amplification ≤ 20× | `minio_write_amplification` — 50 epochs; logical/physical ratio ≤ 20× asserted; RS-5022 not triggered (PASS, 7.42s) |
| 21 | Budget exceeded >2× → tracked mitigation (RS-5022) | No budget breach detected — no RS-5022 mitigation items required |

### Definition-of-Done Checklist

- [x] `cargo fmt --all --check` passes — no diff
- [x] `cargo clippy --workspace -- -D warnings` passes — no warnings or errors
- [x] `cargo test --workspace` passes — 659 tests, 0 failed, 0 ignored
- [x] New behavior has unit tests + oracle/property tests + LFS tests + MinIO tests
  - 5 oracle property tests × 50k cases each (distinct, intersect set/bag, except set/bag)
  - 3 LFS durability tests (persist, crash-replay, no-range-delete)
  - 4 MinIO storage budget tests (WAL cache, manifest cadence, p99 latency, write amplification)
  - 10 SQL lowering / explain / distribution unit tests
  - 1 SQL e2e milestone test
- [x] Every user/operator-visible failure has an `RS-XXXX` code — `RS-5022` for storage budget breach
- [x] Every new performance claim has a measurement — MinIO tests record p99 latency and write amplification ratio
- [x] Every new public SQL surface documented — `SELECT DISTINCT`, `INTERSECT [ALL]`, `EXCEPT [ALL]`
- [x] Every new arrangement has a named bound and `fill_level` metric — `DistinctOp::fill_level()` verified in LFS tests
- [x] No code path depends on SlateDB range deletion — structural guarantee + `lfs_distinct_no_range_delete`
- [x] `main` remains runnable through the single `rockstream` binary
- [x] No item deferred — all 13 vertical slices implemented

### Reproduce Locally

```bash
# Oracle property tests (5 × 50k cases)
rtk cargo test -p rockstream-oracle

# LFS durability tests
rtk cargo test -p rockstream-ops lfs_distinct

# SQL lowering / explain / distribution
rtk cargo test -p rockstream-sql

# SQL e2e milestone
rtk cargo test -p rockstream-sql sql_engine_create_view_join_group_by

# Storage budget gate (requires Docker for MinIO)
rtk cargo test -p rockstream-storage minio

# Full workspace
rtk cargo test --workspace
```

### New RS-XXXX Codes

| Code | Trigger | Text |
|---|---|---|
| RS-5022 | Storage budget exceeded >2× threshold | `Storage operational budget exceeded: {metric} is {actual}× over limit ({threshold}×). Mitigation required before v0.11.` |

### Files Added / Modified (19 files, 2903 insertions)

- `crates/rockstream-plan/src/lib.rs` — `PlanNode::{Distinct,Intersect,Except}`, `OpKind::{Distinct,Intersect,Except}`
- `crates/rockstream-plan/src/dag.rs` — new variant arms in `collect_refs`
- `crates/rockstream-plan/src/explain.rs` — new arms in `explain_op_node`
- `crates/rockstream-diff/src/lib.rs` — new diff arms for Distinct/Intersect/Except
- `crates/rockstream-ops/src/distinct.rs` — `DistinctOp`, `IntersectOp`, `ExceptOp`, persistence
- `crates/rockstream-ops/src/lib.rs` — pub re-exports
- `crates/rockstream-ops/tests/lfs_distinct.rs` — 3 LFS durability tests
- `crates/rockstream-oracle/src/distinct_oracle.rs` — 5 oracle property tests × 50k cases
- `crates/rockstream-oracle/src/lib.rs` — module registration
- `crates/rockstream-sql/src/explain_incremental.rs` — 3 new render arms + 3 unit tests
- `crates/rockstream-sql/src/distribution.rs` — 3 distribution rules + 3 unit tests
- `crates/rockstream-sql/src/lower.rs` — SELECT DISTINCT, UNION, INTERSECT, EXCEPT lowering + 4 tests
- `crates/rockstream-sql/Cargo.toml` — `rockstream-ops` dev-dependency
- `crates/rockstream-sql/tests/sql_engine_e2e.rs` — e2e SQL milestone test
- `crates/rockstream-sql/tests/tpch_plans.rs` — new variants in `count_*_joins`
- `crates/rockstream-storage/src/keys.rs` — `DISTINCT_DISCRIMINATOR`, `distinct_key`, `distinct_op_prefix`
- `crates/rockstream-storage/src/lib.rs` — re-export
- `crates/rockstream-storage/tests/minio_budget.rs` — 4 MinIO storage budget tests

## Notes

- Distinct arrangement is bounded by relation cardinality — `fill_level` API verified in LFS tests.
- MinIO tests use TestContainers (Docker); they skip gracefully if Docker is unavailable.
- RS-5022 was not triggered during testing — no budget breach detected, no mitigation items required before v0.11.
- `WalListingCache` eliminates LIST calls on the hot path; all reads after first populate hit the cache.
- Storage key format for distinct arrangements: `[0x01][0x44,0x53][op_id:8][row_hash:16]` = 27 bytes (no range-delete footprint).
