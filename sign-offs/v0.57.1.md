# v0.57.1 Sign-off — BLOCKED

**Signed off**: 2026-08-17

**Status**: BLOCKED — the full workspace test gate did not complete cleanly.

## Proof Claims

| Proof claim | Proof/evidence | Status |
|---|---|---|
| Full semantics mode rejects a missing Core behavior or proof link. | `rtk bash scripts/check-capability-contract.test.sh` passed the `missing Core semantic behavior` and `missing Core semantic proof` mutation cases; `rtk bash scripts/check-capability-contract.sh --full-semantics` passed on the pristine tree. | PASS |
| At least one tier change is recorded with a reason. | `scripts/check-capability-contract.test.sh` passed the `missing tier decision reason` rejection and `core tier decision ledger accepted` cases; the checked-in `capabilities.toml` decision ledger records the evidence-backed tier change and reason. | PASS |
| `docs/language-features.md` and the generated capability matrix agree byte-for-byte after regeneration. | `scripts/check-capability-contract.test.sh` passed the `generated Core semantics drift` and `generated matrix drift` mutations; full semantics validation passed after regeneration. | PASS |

## Exit Criteria Verification

- [x] `rtk cargo fmt --all -- --check` passes.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [ ] `rtk cargo test --workspace` passes.
- [x] All Proof criteria verified against running code or CI output.
- [ ] `NEW_ROADMAP.md` status updated to `✅ Done` (pending the workspace gate).
- [x] `rtk ./scripts/check-exit-criteria.sh` passes.
- [x] `rtk make e2e` passes.

## Definition of Done

- [x] New behavior has unit, oracle/property, LFS, and MinIO coverage where applicable; the evidence suite passed, with one unrelated optional TestContainers ORM test ignored by its existing feature gate.
- [x] SQL/wire reachability, negative RS-code, dispatch-wiring, and coverage-matrix checks pass; the raw `SUBSCRIBE` pgwire proof and all six dispatch anchors are recorded in `.claude/0.57.1-evidence.md`.
- [x] Every user/operator-visible failure touched by this version has actionable `RS-XXXX` text; no new error code was required.
- [x] No control-plane action, queue, buffer, scan window, distributed-coordination path, or soak gate was added; therefore no new audit event, named bound/metric, seeded `SimRuntime`, or soak obligation applies.
- [x] No new performance claim, public surface, or documentation page was added; generated capability documentation and existing docs were validated.
- [x] No code path depends on SlateDB range deletion; existing no-range-delete proofs remain passing.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] No item has been deferred.

## Blocking Failure

The audit-log corruption, `gateway_serve_tests` hang, and DST fuzz harness
runtime issue are fixed: the CLI audit test, complete gateway serve suite,
subscribe dispatch proof, and 100,000-seed DST fuzz test pass. The required
`rtk cargo test --workspace -- --test-threads=1` gate was run after the fixes
for more than 20 minutes without completing or reporting a failure, so this
sign-off remains blocked pending a completed workspace run.

## Reproduction Commands

```text
rtk bash scripts/check-capability-contract.test.sh
rtk bash scripts/check-capability-contract.sh --full-semantics
rtk cargo test -p rockstream-gateway --test core_operator_semantics_e2e_tests
rtk cargo test -p rockstream-gateway --test core_operator_semantics_e2e_tests --test live_exec_aggregate_durability_lfs_tests --test live_exec_join_durability_lfs_tests --test live_exec_tumble_window_durability_lfs_tests --test gateway_proof_tests --test core_operator_durability_minio_tests --no-fail-fast
rtk ./scripts/check-exit-criteria.sh
rtk make e2e
```
