# v0.51.9 Sign-off

**Signed off**: 2026-08-04

## Exit Criteria Verification

All criteria in the Proof column of `NEW_ROADMAP.md` (line 1054) for v0.51.9 have been verified against running code, local test output, and `.claude/v0.51.9-evidence.md`.

### Proof Claims

- [x] **P1 — Real Kafka Broker Ingestion**: A real Kafka broker publishes records that `CREATE SOURCE ... TYPE kafka` ingests exactly-once into a materialized view across failover with zero loss/duplicates. Verified by `crates/rockstream-connectors/tests/kafka_source_exactly_once_tests.rs::test_kafka_json_ingest_exactly_once`. Evidence: `.claude/v0.51.9-evidence.md` §1 P1.
- [x] **P2 — Real S3 / MinIO Ingestion**: S3 / MinIO object drop ingested by `CREATE SOURCE ... TYPE s3` and reflected in a downstream view. Verified by `crates/rockstream-connectors/tests/s3_source_ingestion_tests.rs::test_s3_json_ingest`. Evidence: `.claude/v0.51.9-evidence.md` §1 P2.
- [x] **P3 — SHOW SOURCES & STATUS over pgwire**: `SHOW SOURCES` & `SHOW SOURCE STATUS` list registered sources with live lag & offset over pgwire. Verified by `crates/rockstream-gateway/tests/source_ddl_tests.rs::test_show_sources_lag_and_offset`. Evidence: `.claude/v0.51.9-evidence.md` §1 P3.
- [x] **P4 — Zero Mock Comments Guard**: Zero matches for `mock.*source connector` in shipped connector source files. Verified by `crates/rockstream-connectors/tests/no_mock_source_connector_comments_test.rs::test_no_mock_source_connector_comments`. Evidence: `.claude/v0.51.9-evidence.md` §1 P4.
- [x] **P5 — Dependency Presence**: `Cargo.toml` carries real `rdkafka` and `aws-sdk-s3`/`object_store` dependencies. Verified by `crates/rockstream-connectors/tests/dependency_presence_test.rs::test_dependency_presence`. Evidence: `.claude/v0.51.9-evidence.md` §1 P5.
- [x] **LFS Backend Durability**: Partition offset markers and source epoch state survive gateway crash and reopen cleanly from LocalFileSystem SlateDB store. Verified by `crates/rockstream-connectors/tests/source_proof_tests.rs::test_source_offset_replay_lfs`. Evidence: `.claude/v0.51.9-evidence.md` §5.
- [x] **MinIO Backend**: Real S3-compatible store (via TestContainers) proves state persistence and recovery across gateway restart. Verified by `crates/rockstream-connectors/tests/source_proof_tests.rs::test_s3_source_minio_tc`. Evidence: `.claude/v0.51.9-evidence.md` §5.
- [x] **Deterministic Simulation**: Seeded `SimRuntime` network pause faults and connection drop errors with zero data loss or duplicate ingestion. Verified by `crates/rockstream-connectors/tests/source_proof_tests.rs::test_source_coordination_sim`. Evidence: `.claude/v0.51.9-evidence.md` §5.

### Definition of Done Checklist

- [x] `rtk cargo fmt --check` passes cleanly.
- [x] `rtk cargo clippy --workspace --all-targets -- -D warnings` passes.
- [x] `rtk cargo test --workspace` passes cleanly.
- [x] New behavior has unit tests plus oracle/property, LFS, MinIO, and/or TestContainers tests.
- [x] Every user/operator-visible failure has an `RS-XXXX` code with actionable text (`RS-4008`, `RS-4009`, `RS-4010`).
- [x] Every control-plane action writes an audit event.
- [x] Every new performance claim has a `criterion` benchmark or measurement note.
- [x] Every new public surface (`CREATE SOURCE`, `ALTER SOURCE`, `SHOW SOURCES`, `SHOW SOURCE STATUS`) is documented.
- [x] Every new queue/buffer/scan window has a named bound, a fill-level metric, and backpressure/error path.
- [x] Every new distributed-coordination path has at least one seeded `SimRuntime` test (`test_source_coordination_sim`).
- [x] No code path depends on SlateDB range deletion.
- [x] `main` remains runnable through the single `rockstream` binary.
- [x] All Proof criteria verified against running code or CI output.
- [x] `NEW_ROADMAP.md` status updated to `✅ Done`.
- [x] `scripts/check-exit-criteria.sh` passes cleanly.
- [x] `make e2e` passes cleanly.
- [x] Nothing deferred to a later version.

## Notes

v0.51.9 implements Real Native Ingestion Connectors & `CREATE SOURCE` DDL (Close Area D).
It ships native `rdkafka`-backed Kafka consumer source and `aws-sdk-s3`/`object_store`-backed S3 object-arrival source, alongside `CREATE SOURCE`, `ALTER SOURCE`, `SHOW SOURCES`, and `SHOW SOURCE STATUS` over pgwire.

Per the ground rules, no item has been deferred to a later version.
