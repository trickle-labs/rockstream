# v0.36 Sign-off

**Signed off**: 2026-06-25

## Exit Criteria Verification

All criteria in the Proof column of NEW_ROADMAP.md for v0.36 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
- [x] ROADMAP.md status updated to `✅ Done`

## Proof Claims Verification

- [x] **P1: Every Nexmark view (q0–q9, q12–q22) maintains `incremental == batch` after mixed INSERT+UPDATE+DELETE sequences**
  - **Evidence**: Verified by tests `test_nexmark_q0_q3_retraction_lfs`, `test_nexmark_q4_q9_retraction_lfs`, and `test_nexmark_q12_q22_retraction_lfs` for the local filesystem (LFS) backend, and `test_nexmark_q0_q9_retraction_minio` and `test_nexmark_q12_q22_retraction_minio` for the MinIO backend in `crates/rockstream-oracle/tests/nexmark_retraction.rs`.
  - **Command**: `rtk proxy cargo test -p rockstream-oracle --test nexmark_retraction`
- [x] **P2: Correctness maintains under 100 randomised seeds**
  - **Evidence**: Verified by test `test_nexmark_retraction_100_seeds_lfs` in `crates/rockstream-oracle/tests/nexmark_retraction.rs`, running 100 distinct random seed iterations of retraction sequences in parallel.
  - **Command**: `rtk proxy cargo test -p rockstream-oracle --test nexmark_retraction -- test_nexmark_retraction_100_seeds_lfs`
- [x] **P3: Delta amplification factor ≤10× for all stateful queries**
  - **Evidence**: Verified by `bench_nexmark` execution where stateful queries `q6` and `q8` exhibited delta amplification factors of 8.61x and 0.60x respectively, and all other queries exhibited amplification factors ≤0.58x, fully satisfying the ≤10x requirement.
  - **Command**: `rtk proxy cargo bench -p rockstream-ops --bench nexmark`
- [x] **P4: Criterion benchmarks record baseline numbers for delta propagation cost at 0.1%/1%/10% change rates**
  - **Evidence**: Verified by `bench_nexmark` execution recording baseline delta propagation latencies of 306.78 ms at 0.1% change rate, 2.1706 s at 1.0% change rate, and 2.1518 s at 10% change rate.
  - **Command**: `rtk proxy cargo bench -p rockstream-ops --bench nexmark`
- [x] **P5: All tests pass on LFS and MinIO**
  - **Evidence**: Verified by running the integration test suite over both the local filesystem (LFS) and MinIO object store (via TestContainers docker module).
  - **Command**: `rtk proxy cargo test -p rockstream-oracle --test nexmark_retraction`

## Notes

All correctness, verification, and performance claims for retraction handling have been met. Retractions (INSERT+UPDATE+DELETE sequences) propagate correctly through all Nexmark views (q0-q9, q12-q22) with incremental output bit-identical to the batch DataFusion oracle. All workspace formatting, linting, and unit/integration tests pass.
