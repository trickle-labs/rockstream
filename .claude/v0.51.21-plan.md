# v0.51.21 Plan — Real Kafka & Object-Store Connector Clients

## Contract

| Field | Commitment |
|---|---|
| Focus | Retire the last in-memory Kafka source, Kafka sink, and object-store sink implementations. |
| Scope | Real `rdkafka` consumer-group source, transactional `rdkafka` sink, and conditional real object-store/MinIO sink. Preserve the existing exactly-once state machines, runtime assertions, and M3/M5 models. Delete the manifest-string test and prevent mock connector prose from returning. |
| Proof | Unchanged M3/M5 and sink assertions; real Kafka transaction recovery; real MinIO conditional/idempotent finalization; compile-time dependency-use gate; mock-connector checker self-test. |
| Backends | Unit, LFS, MinIO, TestContainers. |

## Preconditions and deferred-item audit

- `sign-offs/v0.51.20.md:5` is `✅ Done`; its line 18 confirms no required work was deferred.
- No deferred, postponed, TODO, stub, placeholder, or out-of-scope item in prior sign-offs overlaps real Kafka/object-store connector clients. Historic v0.51.9 claims of real connector coverage are contradictory to the current implementations and are corrective work in this version, not a deferral to absorb.
- Existing overlaps that **must be replaced**: `crates/rockstream-connectors/src/kafka_source.rs:56,182`, `kafka_sink.rs:36-45,123`, and `object_store_sink.rs:42,48-55` describe or implement mocks; `tests/dependency_presence_test.rs` is the text-only dependency assertion; `scripts/check-no-mock-connectors.sh` is absent.
- Out of scope: no new SQL/wire surface, no change to M3/M5 FizzBee models, no general connector factory, and no in-memory production fallback.

## Wiring audit

SQL parser → dispatcher → executor → response encoder is not applicable: this version adds no SQL or wire command. The connector paths below are the equivalent required wiring; every `MUST ADD` link is required before commit.

| Path | Current wiring | Required result |
|---|---|---|
| Kafka source construction → broker client | `kafka_source.rs:66` constructs `BTreeMap` partitions; no `rdkafka` import | **MUST ADD** explicit real-client constructor/configuration using `rdkafka::consumer::StreamConsumer`; no in-memory constructor remains. |
| Consumer group assignment/revocation → `poll_delta` | `kafka_source.rs:172-248` polls synthetic partitions and emits `OffsetToken` | **MUST ADD** rebalance-aware assignment/revocation and bounded, credit- and `max_bytes`-limited polling from the assigned real partitions. |
| M3 `OffsetToken` → Kafka commit | `kafka_source.rs:249-252` records `last_committed` only | **MUST ADD** token-to-partition-offset commit through the real consumer, preserving the lifecycle call from `source_connector.rs:208-235`. |
| Kafka sink construction → transactional producer | `kafka_sink.rs:60-106` uses staged/delivered `BTreeSet`s; no `rdkafka` import | **MUST ADD** explicit transactional producer configuration and `init_transactions`; retain `SinkConnector` invariants at `sink_connector.rs:122,142,168,206,246`. |
| `pre_commit` → `commit` → recovery | `kafka_sink.rs:113-224` stages/inserts in memory | **MUST ADD** `begin_transaction`, produce, `send_offsets_to_transaction`, `commit_transaction`; recovery independently reads the real epoch topic, not an in-process set. |
| Object-store sink construction → bucket client | `object_store_sink.rs:69-145` mutates pending/final maps; no `object_store` or `aws_sdk_s3` import | **MUST ADD** real bucket configuration/client with a bounded `_pending/{epoch}/` staging prefix. |
| Object pending → final | `object_store_sink.rs:151-302` simulates rename and recovery | **MUST ADD** conditional final-object PUT/copy (`If-None-Match`), byte comparison on re-drive, and recovery against bucket state. |
| Test callers → real clients | `KafkaSource::new`, `KafkaSink::new`, and `ObjectStoreSink::new` callers are tests/simulation only | **MUST ADD** real broker/bucket fixture configuration at each caller; do not retain injected-record or `BTreeSet` state-machine helpers as a hidden mock. |
| Dependency proof | `Cargo.toml:43,86-88` and connector manifest declare all libraries, but connector source does not import them | **MUST ADD** a compile-time Rust client-use test that references the actual `rdkafka`, `object_store`, and `aws-sdk-s3` APIs; delete `dependency_presence_test.rs`. |
| Regression gate | CI currently runs `check-no-hardcoded-query-rewrites` at `.github/workflows/ci.yml:47-50` | **MUST ADD** `scripts/check-no-mock-connectors.sh`, its mutated-tree self-test, and adjacent CI steps. |

Pre-commit rule: any remaining `MUST ADD` path means the feature is incomplete. The synchronous `SourceConnector`/`SinkConnector` traits are an implementation constraint to resolve at the concrete connector boundary; a trait-wide async refactor is not in scope unless no safe bounded bridge can preserve the existing interface.

## Coverage matrices

Aggregate, type, join, and window matrices are not applicable: this version introduces none of those SQL/operator surfaces.

| Connector behavior | Test commitment |
|---|---|
| Kafka source: assignment, revocation, bounded credit polling, per-partition `OffsetToken` commit | Convert `crates/rockstream-connectors/tests/kafka_source_exactly_once_tests.rs` from injected records to a real Kafka TestContainers broker; assert the complete consumed/committed offset sequence. |
| Kafka sink: fresh transaction, recovered committed epoch, kill during commit | New `crates/rockstream-connectors/tests/kafka_sink_exactly_once_tests.rs`; an independent consumer must assert the exact epoch payload sequence with no duplicate and no loss. |
| Kafka sink: cluster checkpoint before transaction commit, timeout/abort, retry | Preserve and run `kafka_sink.rs` tests plus `tests/kafka_tx_timeout_tests.rs` against the client boundary; assertions remain exact and unchanged. |
| Object sink: first finalization, same-byte re-drive, conflicting-byte conditional failure, recovery cleanup | New `crates/rockstream-connectors/tests/object_store_sink_real_bucket_tests.rs` using the existing generic MinIO TestContainers fixture pattern from `tests/partial_write_recovery_tests.rs:316-326`; assert exact object keys and bytes. |
| Runtime invariants/models | Existing `sink_connector.rs::test_m3_runtime_asserts_all_paths`; `.github/workflows/ci.yml:143,145` M3/M5 FizzBee commands unchanged. |
| Dependency linkage | New compile-time client-use test in `crates/rockstream-connectors/tests/`; the test imports/constructs the real client APIs and reports the exact dependency names, replacing the deleted manifest text test. |
| Anti-mock gate | New `scripts/check-no-mock-connectors.test.sh` creates a temporary connector source file with a forbidden mock docstring and asserts checker failure naming that file; the unmodified tree must pass. |

## Vertical, test-first slices

| Slice | Owned files | Dependencies | Failing test written first | Proof claims |
|---|---|---|---|---|
| S1 — real-client configuration and compile gate | `Cargo.toml` only if feature correction is necessary; connector client constructors; delete `tests/dependency_presence_test.rs`; new compile-use test | Existing workspace dependencies | Compile-use test fails until all three real API imports are linked | P4 |
| S2 — broker source | `src/kafka_source.rs`, `tests/kafka_source_exactly_once_tests.rs`, focused source-proof callers | S1, real Kafka TC fixture | Real broker assignment/poll/revoke/commit test fails with current injected source | Scope source replacement; P1 support |
| S3 — transactional broker sink | `src/kafka_sink.rs`, new `tests/kafka_sink_exactly_once_tests.rs`, focused sink callers | S1, real Kafka TC fixture | Mid-commit kill/recovery test fails with the current `BTreeSet` model | P1 |
| S4 — real object-store sink | `src/object_store_sink.rs`, new `tests/object_store_sink_real_bucket_tests.rs`, focused recovery callers | S1, existing MinIO TC fixture | Conditional finalization/re-drive test fails with current maps | P2 |
| S5 — invariant and anti-mock integration | `scripts/check-no-mock-connectors.sh`, its self-test, `.github/workflows/ci.yml`, relevant connector tests | S2–S4 | Mutated-tree mock-docstring self-test and unchanged M3/M5/runtime assertion suite | P1, P3, P5 |

Each slice keeps queues and poll windows bounded: source reads stop at credits, `max_bytes`, and a named finite record window; staging is per epoch and cleaned/rejected on bounded failure. No SlateDB range deletion is introduced.

## Proof mapping and completion commands

| Claim | Evidence-producing test/command |
|---|---|
| P1 — unchanged M3/M5 and sink assertions hold against real clients | Focused Kafka TC sink test, `rtk cargo test -p rockstream-connectors sink_connector::tests::test_m3_runtime_asserts_all_paths -- --exact`, and existing M3/M5 formal CI commands. |
| P2 — real Kafka transaction survives mid-commit kill exactly once | `rtk cargo test -p rockstream-connectors --features testcontainers --test kafka_sink_exactly_once_tests -- --exact` with independent-consumer exact payload assertion. |
| P3 — real MinIO transition is conditional/idempotent | `rtk cargo test -p rockstream-connectors --features testcontainers --test object_store_sink_real_bucket_tests -- --exact` with exact key/byte assertions. |
| P4 — no declared-but-unimported connector dependency | Focused compile-use integration test; `rtk cargo tree -p rockstream-connectors` is recorded as resolution evidence only, never as import proof. |
| P5 — anti-mock gate catches drift | `rtk ./scripts/check-no-mock-connectors.sh` and `rtk ./scripts/check-no-mock-connectors.test.sh`. |

Final version proof additionally runs `rtk cargo fmt --all -- --check`, `rtk cargo clippy --workspace -- -D warnings`, the focused and workspace tests, `rtk ./scripts/check-exit-criteria.sh`, and `rtk make e2e`. No required item is deferred.
