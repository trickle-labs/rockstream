# Version Plan: v0.51.27 — Honest Failure Semantics: No Silent-Wrong-Answer or Acknowledged-and-Discarded Branches

## 1. Deferred-Item Audit

Scanned all prior sign-offs (`sign-offs/v0.1.md` through `sign-offs/v0.51.26.md`) and previous `.claude/*-plan.md` files:
- **v0.51.26**: Delivered leak-free registries and identity-safe caps; noted that `WorkerMessage::LifecycleState` delivery from control plane (needed for worker death eviction) is delivered by v0.51.27. Included in v0.51.27 scope.
- **v0.51.3**: Noted view refresh incremental maintenance deferred to v0.51.4 (completed in v0.51.4).
- **All other prior sign-offs**: Confirmed "Nothing deferred". No outstanding deferred or postponed items overlap v0.51.27 scope.

---

## 2. Dispatch-Wiring Audit

| SQL / Control Feature | Code Path (Parser → Dispatcher → Executor → Response / Registry) | Current File & Line Status | Required Wiring |
| :--- | :--- | :--- | :--- |
| **Tiered Store Fallback Race** | `TieredObjectStore::get_with_fallback` fallback loop → primary store re-check on missing secondary | `crates/rockstream-storage/src/tiered_store.rs:93` (`primary.get(location).await.map(\|_\| unreachable!())`) | **MUST FIX**: Replace `unreachable!()` panic with object return or `NotFound` error. |
| **`NTILE` Operator Rejection** | SQL lowering (`crates/rockstream-sql/src/lower.rs:541`) → Window operator evaluation (`crates/rockstream-ops/src/window.rs:241`) | `crates/rockstream-ops/src/window.rs:241` (`WindowFunc::Ntile(_) => { out[i] = 0; }`) | **MUST FIX**: Return `OpError` (`RS-1016`) from operator layer instead of returning `0`. |
| **Composite Index Acceleration** | `CREATE INDEX` SQL statement → `IndexArrangement` builder → `point_lookup` / `point_lookup_values` | `crates/rockstream-ops/src/index_arrange.rs:188` (`first index column only for now`) | **MUST FIX**: Implement multi-column key encoding in `point_lookup_values`; update `EXPLAIN` and `docs/language-features.md`. |
| **Control Plane `DrainAck` Handling** | Control service TCP message receiver → `WorkerMessage::DrainAck` handler → `DrainState` queue completion | `crates/rockstream-control/src/service.rs:903` (logging only) | **MUST FIX**: Advance `DrainState` state machine to completed on receiving `DrainAck`. |
| **Control Plane `LifecycleState` Handling** | Control service TCP message receiver → `WorkerMessage::LifecycleState` handler → `ShardManager` update & worker-death signal | `crates/rockstream-control/src/service.rs:913` (only catalog update, no ShardManager or death signal) | **MUST FIX**: Update `ShardManager` active worker leases and emit worker death signal for v0.51.26 multiplexer/pool eviction. |
| **Input-Reachable `unreachable!()` Branches** | 7 instances in production crates (`tiered_store.rs`, `expr.rs`, `recursion.rs`) | Listed in Section 3 Matrix | **MUST FIX**: Replace all 7 `unreachable!()` calls with explicit enum match arms or coded errors. |

---

## 3. Coverage Matrices

### A. Window Function Support Matrix
| Window Function | Key Types | Value Types | Operator Behavior | Test Commitment |
| :--- | :--- | :--- | :--- | :--- |
| `ROW_NUMBER` | `Int64`, `Utf8` | N/A | Evaluates row sequence | Existing window tests |
| `RANK` / `DENSE_RANK` | `Int64`, `Utf8` | `Int64` | Evaluates rank | Existing window tests |
| `LAG` / `LEAD` | `Int64`, `Utf8` | `Int64` | Evaluates lag/lead | Existing window tests |
| `NTILE` | `Int64`, `Utf8` | `Int64` | **Rejects with RS-1016** | `operator_ntile_honest_rejection_tests.rs` |

### B. Composite Index Point Lookup Matrix
| Index Column Count | Lookup Key Provided | Operator Behavior | EXPLAIN & Doc Status | Test Commitment |
| :--- | :--- | :--- | :--- | :--- |
| 1 Column | Single Column Value | Exact prefix scan | Documented accelerated | `composite_index_point_lookup_tests.rs` |
| 2 Columns (Composite) | Both Columns (`(col1, col2)`) | Full composite key scan | Documented accelerated | `composite_index_point_lookup_tests.rs` |
| 2 Columns (Composite) | Prefix Column (`col1`) | Prefix key scan | Documented accelerated | `composite_index_point_lookup_tests.rs` |

### C. `unreachable!()` Elimination Sweep Matrix
| File | Line | Current Construct | Target Replacement | Test Commitment |
| :--- | :--- | :--- | :--- | :--- |
| `crates/rockstream-storage/src/tiered_store.rs` | 93 | `primary.get().map(\|_\| unreachable!())` | Retry/Return object or `NotFound` | `tiered_store_fallback_race_tests.rs` |
| `crates/rockstream-ops/src/expr.rs` | 81 | `_ => unreachable!()` in arithmetic binop | Explicit match arms or `OpError` | `crates/rockstream-ops` unit tests |
| `crates/rockstream-ops/src/expr.rs` | 139 | `_ => unreachable!()` in comparison binop | Explicit match arms or `OpError` | `crates/rockstream-ops` unit tests |
| `crates/rockstream-ops/src/recursion.rs` | 569 | `PlanNode::Window => unreachable!()` | Return `OpKind::Window` / `OpError` | `crates/rockstream-ops` unit tests |
| `crates/rockstream-ops/src/recursion.rs` | 572 | `PlanNode::SessionWindow => unreachable!()` | Return `OpKind::SessionWindow` / `OpError` | `crates/rockstream-ops` unit tests |
| `crates/rockstream-ops/src/recursion.rs` | 573 | `PlanNode::TopK => unreachable!()` | Return `OpKind::TopK` / `OpError` | `crates/rockstream-ops` unit tests |
| `crates/rockstream-ops/src/recursion.rs` | 613 | `PlanNode::IndexArrange => unreachable!()` | Return `OpKind::IndexArrange` / `OpError` | `crates/rockstream-ops` unit tests |

---

## 4. Vertical Slices

- **Slice 1: Tiered Store Fallback Race Fix**
  - Replace `primary.get(location).await.map(|_| unreachable!())` in `rockstream-storage/src/tiered_store.rs`.
  - Handle race where object reappears during fallback scan cleanly.
  - End-state: `tiered_store_fallback_race_tests.rs` passing under `SimRuntime` seed injection.

- **Slice 2: NTILE Operator Honest Rejection**
  - Update `rockstream-ops/src/window.rs` to return `OpError` (`RS-1016`) on `WindowFunc::Ntile(_)`.
  - End-state: Direct operator call test returning `RS-1016` green.

- **Slice 3: Composite Index Acceleration & Documentation**
  - Add `point_lookup_values(&self, values: &[i64])` in `rockstream-ops/src/index_arrange.rs`.
  - Update `EXPLAIN` output in `rockstream-sql` and `docs/language-features.md` to document composite key index acceleration.
  - End-state: `composite_index_point_lookup_tests.rs` passing and doc-conformance test passing.

- **Slice 4: Control Plane Drain & Worker Lifecycle Consumption**
  - Update `WorkerMessage::DrainAck` handler in `rockstream-control/src/service.rs` to advance `DrainState` to completion.
  - Update `WorkerMessage::LifecycleState` handler to sync `ShardManager` active worker state and trigger worker death eviction in `exchange/multiplexer.rs` & `exchange/pool.rs`.
  - End-state: `drain_lifecycle_handling_tests.rs` passing against multi-worker TC drill.

- **Slice 5: Unreachable Sweep & Grep CI Gate**
  - Eliminate all 7 `unreachable!()` calls across `crates/*/src/`.
  - Add `scripts/check-no-unreachable.sh` CI gate.
  - End-state: `scripts/check-no-unreachable.sh` exits 0 with zero matching `unreachable!()` lines in production crates.

---

## 5. Test Commitments

1. **`tiered_store_fallback_race_tests.rs`**:
   - `SimRuntime` test with `buggify!()` simulating object reappearance during fallback scan.
   - Asserts object is returned without panicking.
2. **`operator_ntile_honest_rejection_tests.rs`**:
   - Unit test invoking `eval_window` / `WindowOperator` directly with `WindowFunc::Ntile`.
   - Asserts error code is `RS-1016` and result is `Err`, never `0`.
3. **`composite_index_point_lookup_tests.rs`**:
   - Tests `IndexArrangement` point lookup on a 2-column composite index `(col1, col2)`.
   - Asserts exact composite match returns matching rows.
   - Verifies `EXPLAIN` and `docs/language-features.md` doc-conformance test.
4. **`drain_lifecycle_handling_tests.rs`**:
   - Multi-worker TestContainers (TC) integration test.
   - Issues worker drain, sends `DrainAck`, asserts drain completes and worker death evicts multiplexer/pool entries.
5. **`scripts/check-no-unreachable.sh`**:
   - Shell script checking `rtk grep -n "unreachable!" crates/*/src/`.
   - Fails build if any `unreachable!()` remains in production source code.

---

## 6. Durability Slices

- **SlateDB LFS & MinIO Test**:
  - `tiered_store_fallback_race_tests.rs` will run against local filesystem object store and MinIO object store to verify fallback behavior across real S3-compatible storage tiers.

---

## 7. Coordination Slices

- **`SimRuntime` Seeded Fault Injection**:
  - `tiered_store_fallback_race_tests.rs` utilizes `SimRuntime` with seeded execution to deterministically trigger race condition.
  - `drain_lifecycle_handling_tests.rs` utilizes `SimRuntime` / `buggify!("exchange.domain_rebuild_during_drain")` annotations to verify drain state progression during re-configurations.

---

## 8. Proof Mapping

| Proof Claim | Mapped Verification Test(s) | Target Location |
| :--- | :--- | :--- |
| **Proof 1**: Tiered store fallback race returns object instead of panicking on identical `SimRuntime` seed. | `tiered_store_fallback_race_tests.rs` | `crates/rockstream-storage/tests/tiered_store_fallback_race_tests.rs` |
| **Proof 2**: `NTILE` request against operator layer returns `RS-1016`, never `0`. | `operator_ntile_honest_rejection_tests.rs` | `crates/rockstream-ops/tests/operator_ntile_honest_rejection_tests.rs` |
| **Proof 3**: Two-column `CREATE INDEX` point lookup accelerated; `EXPLAIN` and `docs/language-features.md` doc-conformance verified. | `composite_index_point_lookup_tests.rs` & `v0.51.22` doc-conformance test | `crates/rockstream-ops/tests/composite_index_point_lookup_tests.rs` |
| **Proof 4**: Multi-worker TC drain completes via `DrainAck` action and worker death evicts multiplexer/pool entries. | `drain_lifecycle_handling_tests.rs` | `crates/rockstream-control/tests/drain_lifecycle_handling_tests.rs` |
| **Proof 5**: Zero `unreachable!()` remain on input-reachable branches anywhere in `crates/*/src/`. | `scripts/check-no-unreachable.sh` (CI gate) | `scripts/check-no-unreachable.sh` |
