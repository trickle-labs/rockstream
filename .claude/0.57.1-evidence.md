# v0.57.1 Phase 4 Evidence

## Validation

| Proof claim | Command | Result |
|---|---|---|
| Feature-specific semantic scenarios, raw `SUBSCRIBE` dispatcher proof, and bounded-state outcomes | `rtk cargo test -p rockstream-gateway --test core_operator_semantics_e2e_tests` | PASS — 119 passed, 0 failed |
| Contract mutation tests, proof resolution, and generated-document equality | `rtk bash scripts/check-capability-contract.test.sh && rtk bash scripts/check-capability-contract.sh --full-semantics` | PASS — both checks passed |
| LFS durability, gateway proofs, MinIO recovery, and semantic tests | `rtk cargo test -p rockstream-gateway --test core_operator_semantics_e2e_tests --test live_exec_aggregate_durability_lfs_tests --test live_exec_join_durability_lfs_tests --test live_exec_tumble_window_durability_lfs_tests --test gateway_proof_tests --test core_operator_durability_minio_tests --no-fail-fast` | PASS — all result lines passed; 1 pre-existing optional test ignored |
| Rust formatting | `rtk cargo fmt --all -- --check` | PASS after `rtk cargo fmt --all` |
| Dispatch-wiring anchors | `rtk rg -n 'dispatch_async_with_conn\|dispatch_sync\|sql_to_plan_node\|handle_create_view\|compile_node\|DataRowEncoder' crates/rockstream-gateway/src/server.rs crates/rockstream-sql/src/frontend.rs crates/rockstream-ops/src/compile.rs` | PASS — all six anchors resolve |

## Exact gap closures

- `core_subscribe_pgwire_dispatch` calls `GatewayHandler::dispatch_async_with_conn` with raw `SUBSCRIBE`, verifies the encoded schema/row, and exercises the gateway-owned registry.
- `core_query_read_state_growth`, `core_scalar_state_growth`, and `core_freshness_state_growth` assert RS-2029 query-budget failures.
- DML, transaction, and view state-growth proofs assert RS-2019 write-buffer backpressure.
- `core_subscribe_state_growth` fills the subscription backlog, asserts RS-2020, and verifies the bound-hit metric.

## Remaining gaps

None for the Phase 4 proof obligations. One unrelated optional TestContainers ORM test remains ignored by its existing feature gate.
