# Version v0.15 Proof Evidence

All proof obligations for version v0.15 have been verified successfully.

## Proof Evidence Table

| Proof Claim | Target Test(s) / Proving Command | Status | Output / Evidence Excerpt |
|---|---|---|---|
| A two-writer fence test proves only one writer can commit to a shard | `fencing_lfs` in `crates/rockstream-storage/tests/lfs_backend.rs`<br>`fencing_minio` in `crates/rockstream-storage/tests/minio_backend.rs` | **PASSED** | Checked SlateDB local filesystem and MinIO S3 backend fencing. The second writer successfully fences out subsequent writes from the first writer, raising mapped fencing errors. |
| Killing a worker releases its leases and they are reassigned cleanly | `sim_control_plane_leases` in `crates/rockstream-sim/tests/control_sim.rs`<br>`shard_leases_released_on_worker_disconnect` in `crates/rockstream-control/src/service.rs` | **PASSED** | Deterministic simulation uses `buggify!` to inject partitions/failures. Disconnecting a worker triggers immediate lease revocation and clean reassignment via `ShardManager` and `ControlService`. |
| Topology changes are audited | `run_start_writes_audit_log_and_support_bundle` in `crates/rockstream-cli/src/lib.rs` | **PASSED** | Verifies that starting and running nodes generates JSONL-formatted `AuditEvent` entries for `server.started`, `worker.registered`, `shard.lease_granted`, `pipeline.created`, etc. |
| Unified entrypoint supports roles and control address | CLI unit tests in `crates/rockstream-cli/src/lib.rs` | **PASSED** | Verifies that `--role` checks are enforced (`unknown_role_is_rejected_with_rs_0002`), known roles are accepted, and `worker`/`gateway` roles fail cleanly if `--control` is omitted. |

## Verification Run Summary

Tests were run successfully with `--all-features` to enable deterministic simulation features.

```bash
rtk cargo test --all-features
```

Output:
```
cargo test: 742 passed (52 suites, 1056.90s)
```
