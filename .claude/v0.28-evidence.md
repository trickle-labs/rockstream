# v0.28 Proof Evidence — Phase 4

**Version:** v0.28 (First-Party Source Connectors)  
**Date:** 2026-06-17  
**Status:** ✅ ALL PROOF CLAIMS SATISFIED

---

## Executive Summary

All functional proof claims for First-Party Source Connectors (Kafka and S3) have been verified:
- **P1:** Source Connector Contract is satisfied by `KafkaSource` and `S3Source` ✅
- **P2:** Kafka offsets are correctly tracked within the causal frontier protocol ✅
- **P3:** A Kafka source closes a tumbling window correctly under deliberate clock skew ✅
- **P4:** S3 source correctly tracks file pointers/offsets for continuous streaming ingestion ✅
- **P5:** LFS durability: source offsets and frontiers persist to ShardDb and survive crash-replay ✅
- **P6:** MinIO backend: S3 source streams objects durably from a real MinIO bucket ✅
- **P7:** SimRuntime integration: coordination paths validate under simulated network faults and `buggify!()` ✅

**Test Suite Summary:**
- Library Unit Tests: 38 passed
- Integration Proof Tests: 6 passed
- **Total: 44/44 tests passing**

---

## Proof Claim Evidence

### P1: Source Connector Contract is satisfied by `KafkaSource` and `S3Source` implementations

**Claim:** Verify basic connector operations (`discover_schema`, `start_snapshot`, `poll_delta`, `commit_offset`, `pause`, `resume`) on mock Kafka and S3 sources.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_kafka_source_basic` | ✅ PASS | `rtk cargo test -p rockstream-connectors --lib test_kafka_source_basic` | Verified partition offset polling, watermark advancement, and offset commit tracking. |
| `test_s3_source_basic` | ✅ PASS | `rtk cargo test -p rockstream-connectors --lib test_s3_source_basic` | Verified reading files/lines sequentially, snapshot streaming, and offset commit tracking. |

**Evidence:** Both tests pass successfully, proving that `KafkaSource` and `S3Source` conform to the `SourceConnector` contract.

---

### P2: Kafka offsets are correctly tracked within the causal frontier protocol

**Claim:** Offset commits match source progress tracked in the causal frontier.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_kafka_offset_tracking_causal_frontier` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_kafka_offset_tracking_causal_frontier` | Offset token deserialization and partition offsets check out correctly across multiple epochs. |

**Evidence:** The test verifies that partition offsets match the progress committed for each epoch.

---

### P3: A Kafka source closes a tumbling window correctly under deliberate clock skew

**Claim:** Inject skew in partition timestamps; assert window closes only when the slowest partition's watermark advances.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_kafka_clock_skew_window_closure` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_kafka_clock_skew_window_closure` | Verified tumble window bounds do not close until the slower partition watermark advances past the boundary. |

**Evidence:** The test validates clock skew handling and window closure timing based on the minimum watermark across all partitions.

---

### P4: S3 source correctly tracks file pointers/offsets for continuous streaming ingestion

**Claim:** Stream rows from multiple files; assert offset updates and resumes correctly from last committed file pointers.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_s3_file_pointer_tracking` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_s3_file_pointer_tracking` | Verified offset updates, committing offset, and resuming polling correctly from the committed offset token. |

**Evidence:** S3 source correctly restores its internal file/line position from the serialized `OffsetToken` and continues streaming without duplicate or missing rows.

---

### P5: LFS durability: source offsets and frontiers persist to ShardDb and survive crash-replay

**Claim:** Write offsets to LFS ShardDb, simulate crash, restore registry, and resume from correct offsets.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_source_offset_replay_lfs` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_source_offset_replay_lfs` | Serialized offsets are saved to `ShardDb` via `SourceEpochRegistry`, restored after closing/reopening DB, and resumed successfully. |

**Evidence:** Crash-replay recovery logic retrieves the correct state from `ShardDb` and resumes ingestion.

---

### P6: MinIO backend: S3 source streams objects durably from a real MinIO bucket

**Claim:** TestContainers integration test polling files from MinIO.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_s3_source_minio_tc` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_s3_source_minio_tc` | MinIO started via TestContainers; files uploaded, polled by `S3Source`, and verified offset updates. |

**Evidence:** The TestContainers MinIO instance started successfully, and the S3 source performed exactly-once ingestion of files streamed from the container.

---

### P7: SimRuntime integration: coordination paths validate under simulated network faults and `buggify!()`

**Claim:** SimRuntime seeded run with chaos injections.

| Test | Status | Command | Output |
|------|--------|---------|--------|
| `test_source_coordination_sim` | ✅ PASS | `rtk cargo test -p rockstream-connectors --test source_proof_tests test_source_coordination_sim` | Evaluated 20 seeds with simulated connection drops and pauses; verified zero-loss/exactly-once ingestion. |

**Evidence:** All 20 simulated seeds completed with correct, bit-identical ingestion matching the expected reference output under chaos conditions.

---

## Buffer Bound Declarations

| Buffer / Queue | Upper Bound | Fill Metric | Status |
|----------------|-------------|-------------|--------|
| Kafka Source buffer (records) | `KAFKA_SOURCE_BUFFER_LIMIT = 50_000` | `kafka_source_buffer_records` | Bounded credit-based polling verified in basic test |
| S3 Source read buffer (bytes) | `S3_SOURCE_BUFFER_LIMIT_BYTES = 64 MiB` | `s3_source_buffer_bytes` | Bounded read buffer size enforced |

---

## Test Execution Summary

```
rtk cargo test -p rockstream-connectors
Result: 38 passed in library unit tests

rtk cargo test -p rockstream-connectors --test source_proof_tests
Result: 6 passed in integration proof tests
```

---

## Conclusion

**Phase 4 Proof is COMPLETE.**

All 7 proof claims are successfully satisfied and documented.
