# v0.56 Proof Evidence

All commands below passed. Focused test commands exited 0 with no `FAILED` or
`error` summary line after RTK filtering; `rtk make e2e` emitted the excerpts
recorded below.

## Proof claims

| Claim | Proof | Result / excerpt |
|---|---|---|
| Three-worker N→N+1 rolling upgrade has no epoch loss or in-flight downtime. | `rtk cargo test -p rockstream-sim --test rolling_upgrade_tests --features docker_tests` | PASS — `three_worker_n_to_n_plus_1_zero_loss_tc` passed. |
| Out-of-range or malformed shard format is rejected as `RS-5001`. | `rtk cargo test -p rockstream-storage --test storage_format_tests` | PASS — `n_binary_refuses_v2_shard_rs5001` and `malformed_format_marker_refused_rs5001` passed. |
| Incompatible peer is rejected as `RS-5021`, never `RS-5002`. | `rtk cargo test -p rockstream-runtime --test protocol_version_tests` | PASS — `newer_protocol_refused_rs5021_not_rs5002` passed. |
| Root offline migration preserves populated-shard results across 1→2. | `rtk cargo test -p rockstream-storage --test format_migration_lfs_tests`; `rtk cargo test -p rockstream-storage --test format_migration_minio_tests`; `rtk cargo test -p rockstream-cli --test format_migrate_cli_tests` | PASS — `migrates_populated_v1_shards_to_v2_bit_identically`, `migrates_populated_v1_shards_to_v2_bit_identically_tc`, and `root_migrate_dispatches_and_reports_exact_results` passed. |
| Interrupted migration is rerunnable and keeps every shard openable. | `rtk cargo test -p rockstream-storage --test format_migration_lfs_tests`; `rtk cargo test -p rockstream-storage --test format_migration_minio_tests` | PASS — `interrupted_migration_is_rerunnable_and_never_leaves_unopenable_shard` and `interrupted_migration_reruns_exactly_tc` passed. |
| Control never assigns until the mixed-version compatibility floor is met. | `rtk cargo test -p rockstream-sim --test rolling_upgrade_sim_tests --features simulation` | PASS — seeded tests `mixed_versions_withhold_cross_version_assignment_seeded` (`0x56_0001`) and `mixed_versions_assign_after_compatibility_floor_seeded` (`0x56_0002`) passed. |
| Migration never uses SlateDB range delete. | `rtk cargo test -p rockstream-storage --test format_migration_tests` | PASS — `migration_uses_scan_copy_verify_and_point_delete_only` passed. |

## Compatibility coverage

| Matrix | Tests | Result |
|---|---|---|
| Storage formats: legacy/missing, v1, rolling v1/v2, v2 refusal, malformed marker. | `legacy_missing_marker_opens_as_v1`; `v1_binary_opens_v1_shard`; `n_plus_1_opens_n_shard`; `n_plus_1_opens_migrated_v2_shard`; `n_binary_refuses_v2_shard_rs5001`; `malformed_format_marker_refused_rs5001` in `storage_format_tests`. | PASS |
| Protocol headers: N→N, N+1→N, N→N+1, unsupported peer. | `v1_header_delivers_shuffle_frame`; `n_plus_1_to_n_is_accepted`; `n_to_n_plus_1_is_accepted`; `newer_protocol_refused_rs5021_not_rs5002` in `protocol_version_tests`. | PASS |
| Migration durability: LFS success/restart and MinIO/TestContainers success/restart. | `format_migration_lfs_tests`; `format_migration_minio_tests`. | PASS |

## Dispatch-wiring and reachability audit

| Path | Verification | Result |
|---|---|---|
| Storage marker → range-aware shard open → worker attach → structured `RS-5001`. | `rtk rg -n "format_version_key|with_supported_format_range|IncompatibleFormat" crates/rockstream-storage/src crates/rockstream-runtime/src/client.rs` found `keys.rs`, `shard_db.rs`, and `client.rs` links. | CONNECTED |
| Shuffle sender metadata → receiver pre-consumption validation → `RS-5021`. | `rtk rg -n "protocol_version|validate_protocol_version|RS-5021" crates/rockstream-runtime/src/exchange` found metadata insertion in `multiplexer.rs` and validation before frame consumption in `service.rs`; `protocol_version_tests` covers live delivery and rejection. | CONNECTED |
| Worker registration/persistence → shared compatibility predicate → withheld assignment response. | `rtk rg -n "with_compatibility|assignment_compatible|compatibility_withheld" crates/rockstream-types/src/topology.rs crates/rockstream-runtime/src/client.rs crates/rockstream-control/src` found registration, scheduler, and service connections; seeded SimRuntime tests cover withholding and later assignment. | CONNECTED |
| Root CLI parser → dispatcher → offline executor → migration engine. | `root_migrate_dispatches_and_reports_exact_results` in `format_migrate_cli_tests`; `rockstream --help` output includes `migrate  Migrate shard storage formats offline`. | CONNECTED |

No SQL feature changed in v0.56, so e2e pgwire raw-SQL reachability and SQL error tests are not applicable. The live gRPC feature has explicit request reachability and negative coverage above.

## End-to-end and repository gates

| Command | Result / excerpt |
|---|---|
| `rtk make e2e` | PASS — LFS: `14 passed; 0 failed`; MinIO/TestContainers: `7 passed; 0 failed`; `=== e2e PASSED ===`. |
| `rtk cargo test --workspace` | PASS — clean exit after allowing the suite's required loopback listeners. |
| `rtk cargo clippy --workspace --all-targets -- -D warnings` | PASS — `cargo clippy: No issues found`. |
| `rtk cargo run -p rockstream-cli -- --help` | PASS — exposes root `migrate` and preserves `shard`. |
