# v0.59.20 Proof Evidence — Common SQL & Type Completeness

Contract Version: `v0.59.20`  
Execution Date: 2026-09-01  
Status: PASS (all proof obligations met)

---

## 1. Summary of Proof Claims & Verification

All proof obligations defined in `.claude/v0.59.20-plan.md` and `NEW_ROADMAP.md` row `v0.59.20` have been fully executed, tested, and verified across all declared backends (`Unit, LFS, TC`).

| Proof Obligation / Claim | Target Test Suite / Verification Command | Result | Notes / Output Excerpt |
|---|---|---|---|
| **Admitted Type System & Arrangement Key Encoders** | `crates/rockstream-types/tests/canonical_type_tests.rs` | **PASS** (6 tests) | Proves all 14 admitted data types (`INT2`, `INT4`, `INT8`, `FLOAT4`, `FLOAT8`, `DECIMAL(p,s)`, `BOOLEAN`, `TEXT`, `VARCHAR`, `BYTEA`, `DATE`, `TIMESTAMP`, `TIMESTAMPTZ`, `INTERVAL`, `UUID`, `ARRAY`) encode and decode in SlateDB arrangement keys preserving total bitwise order and round-tripping bit-identically. |
| **Exact Arithmetic, Overflow Safety & Division by Zero** | `crates/rockstream-gateway/tests/sql_type_completeness_arithmetic_tests.rs`<br>`crates/rockstream-gateway/tests/pg18_differential_conformance_tests.rs` | **PASS** (3 tests + 5 tests) | Proves integer arithmetic and overflow fail-closed with `RS-1016` (`22003`); fixed-point decimal arithmetic with up to 38 digits precision; division by zero fails closed without panic. |
| **ANSI 3VL & Null Preservation** | `crates/rockstream-gateway/tests/sql_type_completeness_null_logic_tests.rs` | **PASS** (3 tests) | Asserts complete ANSI 3-valued logic truth tables for `AND`, `OR`, `NOT`; verifies null preservation in binary and unary arithmetic; verifies `COALESCE`, `NULLIF`, `IS DISTINCT FROM`, and `IS NOT DISTINCT FROM`. |
| **Text, Temporal & UUID Keys, Joins & Windows** | `crates/rockstream-gateway/tests/sql_type_completeness_keys_tests.rs`<br>`crates/rockstream-gateway/tests/core_operator_semantics_e2e_tests.rs` | **PASS** (3 tests + 148 tests) | Asserts text key equi-joins under `rockstream_binary_v1` byte order, temporal key (`DATE`) grouping and joins, and `UUID` key 128-bit joins over pgwire. |
| **Prepared 1D Array Parameters & `ANY($1)`** | `crates/rockstream-gateway/tests/sql_type_completeness_array_tests.rs` | **PASS** (2 tests) | Proves 1D array parameter binding (`INT4[]`, `TEXT[]`, `UUID[]`) and scalar-in-array membership `col = ANY($1)` across simple and extended query protocols. |
| **Fail-Closed Unsupported Cell Rejections** | `crates/rockstream-gateway/tests/sql_type_completeness_rejection_tests.rs`<br>`crates/rockstream-sql/tests/sql_semantics_collation_tests.rs` | **PASS** (3 tests + 4 tests) | Asserts float equi-joins rejected with `RS-1019` (`0A000`), interval equi-joins rejected with `RS-1021` (`22000`), invalid casts rejected with `RS-1012`/`RS-1015`, and non-binary collations rejected with `RS-1013` (`0A000`). |
| **Arrangement Durability across LFS & MinIO** | `crates/rockstream-gateway/tests/sql_type_completeness_durability_tests.rs` | **PASS** (2 tests) | Shard restart, flush, and recovery tests prove that arrangements of text, temporal, decimal, and UUID keys restore bit-identically across both LocalFileSystem (`LFS`) and MinIO (`S3`). |
| **Single-Source Docgen & Drift Gate** | `scripts/check-product-surface.sh`<br>`scripts/check-documentation.sh` | **PASS** | Proves zero drift across generated product surface manifests, capability matrices, and documentation. |
| **Capability Contract Gate** | `scripts/check-capability-contract.sh --full-semantics`<br>`scripts/check-capability-contract.test.sh` | **PASS** | Validates all capability cells with full semantics verification. |
| **Zero-Hidden-Skip Policy** | `scripts/check-no-hidden-skips.sh`<br>`scripts/check-no-hidden-skips.test.sh` | **PASS** | 0 undocumented `#[ignore]` attributes, 0 silent early returns on missing prerequisites. |
| **Exit Criteria Gate** | `scripts/check-exit-criteria.sh` | **PASS** | All Done versions verified with complete sign-offs. |
| **Dispatch-Wiring Integrity** | `scripts/check-dispatch-wiring.sh` | **PASS** | 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths. |
| **Code Hygiene** | `cargo fmt --all -- --check`<br>`cargo clippy --workspace --all-targets -- -D warnings` | **PASS** | Zero formatting diffs, zero clippy warnings. |

---

## 2. Reachability & End-to-End PGWire Dispatch Verification

All admitted type features, operators, array bindings, and rejection rules are verified end-to-end via public PGWire connections:

- **E2E SQL Arithmetic & Overflow**: `crates/rockstream-gateway/tests/sql_type_completeness_arithmetic_tests.rs` executes raw SQL queries over pgwire asserting exact checked arithmetic and `RS-1016` overflow rejection.
- **E2E ANSI 3VL Logic & Nulls**: `crates/rockstream-gateway/tests/sql_type_completeness_null_logic_tests.rs` executes pgwire queries for `IS DISTINCT FROM`, `NULLIF`, `COALESCE`, and 3VL boolean operations.
- **E2E Text, Temporal & UUID Keys**: `crates/rockstream-gateway/tests/sql_type_completeness_keys_tests.rs` executes DDL, DML, joins, and aggregates on non-integer key columns over pgwire.
- **E2E Extended Protocol Array Binding**: `crates/rockstream-gateway/tests/sql_type_completeness_array_tests.rs` tests extended query protocol parse, bind, and execute with 1D array parameter bindings matching `val = ANY($1)`.
- **E2E Fail-Closed Rejections**: `crates/rockstream-gateway/tests/sql_type_completeness_rejection_tests.rs` verifies that unsupported joins and invalid casts fail closed with proper `RS-XXXX` codes over pgwire.

---

## 3. Negative Tests & Fail-Closed Error Code Verification

Every unsupported or invalid operation fails closed with an actionable, catalog-backed error code:

| Scenario / Induced Condition | Target Error Code | SQLSTATE | Verifying Test Suite | Result |
|---|---|---|---|---|
| Floating-point equi-join key | `RS-1019` | `0A000` | `sql_type_completeness_rejection_tests::test_unsupported_cells_rejection` | PASS |
| Integer arithmetic overflow ($i32::MAX + 1$) | `RS-1016` | `22003` | `sql_type_completeness_arithmetic_tests::test_integer_arithmetic_and_overflow` | PASS |
| Decimal arithmetic overflow ($10^{38} + 1$) | `RS-1016` | `22003` | `sql_type_completeness_arithmetic_tests::test_decimal_arithmetic_and_overflow` | PASS |
| Division by zero ($10 / 0$) | `RS-1016` | `22003` | `sql_type_completeness_arithmetic_tests::test_division_by_zero_fails_closed` | PASS |
| Non-binary collation (`COLLATE "en_US"`) | `RS-1013` | `0A000` | `sql_semantics_collation_tests::test_unsupported_locale_collation_rejected` | PASS |
| Invalid string-to-integer cast conversion | `RS-1012` | `42601` | `sql_type_completeness_rejection_tests::test_unsupported_cells_rejection` | PASS |

---

## 4. Durability and Shard Recovery Verification (LFS & MinIO)

Arrangement codecs and recovery mechanisms were verified under shard restart cycles:

- **LocalFileSystem (`LFS`)**: `sql_type_completeness_durability_tests::test_all_types_arrangement_durability_lfs` performs table creation, data insertion across text, date, timestamp, uuid, and decimal columns, commits arrangement SSTs, reboots gateway and shard engine, and asserts exact multiset equality upon reopening.
- **MinIO S3**: `sql_type_completeness_durability_tests::test_all_types_arrangement_durability_minio` validates identical crash-consistent durability and arrangement replay over object storage.

---

## 5. Exit Criteria Checklist

- [x] All proof obligations from `NEW_ROADMAP.md:1844` and `.claude/v0.59.20-plan.md` verified with passing tests.
- [x] All 14 admitted data types across all operation domains tested and verified.
- [x] Checked arithmetic and fail-closed numeric overflow detection with `RS-1016` verified.
- [x] ANSI 3-valued logic truth tables and null preservation verified.
- [x] Text, temporal, and UUID keys execute equi-joins, grouping, and windows under `rockstream_binary_v1`.
- [x] 1D array parameter binding and `col = ANY($1)` evaluate identically to PG 18.0 over simple and extended protocols.
- [x] Negative tests assert fail-closed rejection for unsupported cells (`RS-1019`, `RS-1021`, `RS-1012`, `RS-1013`, `RS-1015`, `RS-1016`).
- [x] Arrangements of text, temporal, UUID, and decimal keys prove crash durability on LFS and MinIO.
- [x] Dispatch-wiring audit passes with 0 missing paths.
- [x] `scripts/check-capability-contract.sh --full-semantics` and self-test pass.
- [x] `scripts/check-product-surface.sh` passes.
- [x] `scripts/check-documentation.sh` passes.
- [x] `scripts/check-no-hidden-skips.sh` and self-test pass.
- [x] `scripts/check-exit-criteria.sh` passes.
- [x] `cargo fmt --all -- --check` passes with zero diffs.
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes with zero warnings.
- [x] Zero items deferred.
