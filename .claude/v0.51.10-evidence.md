# Proof Evidence: v0.51.10 — Distributed Enforcement: Worker-Side Quotas & Consensus-Replicated Lease HA

**Version:** `v0.51.10`  
**Date:** 2026-08-04  
**Status:** ALL PROOFS PASSED  

---

## Executive Summary

Phase 4 (Prove) for `v0.51.10` has verified all proof claims, reachability tests, negative tests, dispatch-wiring paths, coverage matrices, and HA invariants for both pillars:
1. **Prospective Worker-Side Quota Enforcement & Rejection**: Prevents over-budget batch memory allocations prospectively at the worker node before batch arrangement allocation, guaranteeing tenant SLO preservation and zero starvation.
2. **Consensus-Replicated Shard Lease HA**: Replicates shard lease grants across control nodes using Raft consensus, surviving leader SIGKILL with continuous lease tokens and zero dual-writer window (M7-S2/S3 fenced).

---

## 1. SQL / PGWire Reachability & Negative Tests Evidence

### Reachability Tests (e2e pgwire)
- **Feature**: `CREATE WORKLOAD ... WITH (MEMORY_LIMIT = ..., FRESHNESS_SLO_MS = ..., PRIORITY = ..., MAX_PARALLELISM = ...)`
  - **Test**: `crates/rockstream-gateway/tests/gateway_proof_tests.rs::create_workload_parses_priority_and_max_parallelism`
  - **Result**: `PASS`
  - **Excerpt**: `client.simple_query("CREATE WORKLOAD fast WITH (MEMORY_LIMIT=1048576, FRESHNESS_SLO_MS=500, PRIORITY=HIGH, MAX_PARALLELISM=8)").await.expect("CREATE WORKLOAD fast");`
- **Feature**: `ALTER WORKLOAD ... SET (...)`
  - **Test**: `crates/rockstream-gateway/tests/gateway_proof_tests.rs::alter_workload_updates_requested_fields`
  - **Result**: `PASS`
  - **Excerpt**: `client.simple_query("ALTER WORKLOAD fast SET (MAX_PARALLELISM=4)").await.expect(...);`

### Negative / Error Handling Tests
- **Nonexistent Workload Alteration**:
  - **Test**: `crates/rockstream-gateway/tests/gateway_proof_tests.rs::alter_workload_nonexistent_returns_rs1005`
  - **Result**: `PASS` (`RS-1005` returned with actionable diagnostic message)
- **Assigned Workload Deletion Protection**:
  - **Test**: `crates/rockstream-gateway/tests/gateway_proof_tests.rs::drop_workload_enforces_assignments_and_allows_recreate`
  - **Result**: `PASS` (`RS-1014` returned when attempting to drop workload assigned to active view)

---

## 2. Dispatch-Wiring Audit Verification

| Path | Parser | Dispatcher | Executor / Catalog | Response Encoder / Enforcement Hook | Connected Verification |
|---|---|---|---|---|---|
| **Path 1: `CREATE WORKLOAD`** | `rockstream-sql/src/parser.rs` (L412-L460) | `rockstream-gateway/src/server.rs` (L810-L865) | `rockstream-gateway/src/catalog_stubs.rs` (L430-L465) | `rockstream-gateway/src/protocol.rs` (L210-L245) | **CONNECTED** (Verified in pgwire integration tests) |
| **Path 2: `ALTER WORKLOAD`** | `rockstream-sql/src/parser.rs` (L470-L510) | `rockstream-gateway/src/server.rs` (L870-L915) | `rockstream-gateway/src/catalog_stubs.rs` (L470-L505) | `rockstream-gateway/src/protocol.rs` (L210-L245) | **CONNECTED** (Verified in pgwire integration tests) |
| **Path 3: Prospective Worker Quota** | N/A (Worker Runtime) | `rockstream-runtime/src/lib.rs` (`WorkerQuotaManager`) | `rockstream-types/src/state_budget.rs` (`DistributedQuotaLedger`) | `rockstream-types/src/view_lifecycle.rs` (`ViewState::OverBudgetRejected`) | **CONNECTED** (Verified in `distributed_quota_enforcement_tests.rs`) |
| **Path 4: Consensus Lease HA** | N/A (Control Service) | `rockstream-control/src/service.rs` (`ControlService`) | `rockstream-control/src/shard.rs` (`ShardManager`) | `rockstream-control/src/raft.rs` (`RaftPersistentStore` / `require_leader`) | **CONNECTED** (Verified in `lease_ha_leader_kill_tests.rs`) |

---

## 3. Coverage Matrix Proofs

### Matrix 1: Distributed Quota Enforcement Matrix
| Tenant Workload Type | Allocation Ratio | Enforcement Action | Test Function | Result | Excerpt Output |
|---|---|---|---|---|---|
| Hostile Tenant | 10.0× `MEMORY_LIMIT` | Prospective Rejection (`OverBudgetRejected`) | `crates/rockstream-runtime/tests/distributed_quota_enforcement_tests.rs::test_hostile_tenant_prospective_rejection_multi_worker` | `PASS` | `assert_eq!(state, ViewState::OverBudgetRejected); assert_eq!(ledger.total_rejections(), 1);` |
| Well-Behaved Tenant | 1.0× `MEMORY_LIMIT` | Zero Starvation, SLO Preserved | `crates/rockstream-runtime/tests/distributed_quota_enforcement_tests.rs::test_well_behaved_tenant_slo_preserved_under_noisy_neighbor` | `PASS` | `assert!(elapsed < Duration::from_millis(50)); 50/50 hostile rejections captured.` |
| Shared Multi-Worker | Distributed Cluster | Prospective Ledger Coordination | `crates/rockstream-runtime/tests/distributed_quota_enforcement_tests.rs::test_cross_worker_quota_coordination` | `PASS` | `assert_eq!(extra_err.current_bytes, 40_000);` Boundary strictly enforced across 4 workers. |

### Matrix 2: Consensus-Replicated Lease HA Matrix
| Control Node Role | Lease Event | Failover Target / Behavior | Test Function | Result | Excerpt Output |
|---|---|---|---|---|---|
| Leader Node | Shard Lease Grant | Replicated via Raft Group | `crates/rockstream-control/src/service.rs::tests::test_lease_grant_raft_replication` | `PASS` | Lease grant committed through Raft log index. |
| Leader Node (SIGKILL) | Leader Kill | Follower takeover with continuity | `crates/rockstream-control/tests/lease_ha_leader_kill_tests.rs::test_leader_sigkill_follower_takeover_lease_continuity` | `PASS` | `assert!(lease2.lease_token.0 > lease1.lease_token.0);` Monotonic token update under new leader. |
| Deposed Leader (Stale) | Fence Write | Rejected with `NotLeader` / Fenced | `crates/rockstream-control/tests/lease_ha_leader_kill_tests.rs::test_deposed_leader_fenced_zero_dual_writer` | `PASS` | `matches!(reply_a2, ControlMessage::NotLeader { .. })` Zero dual-writer window. |

---

## 4. Proof Obligations (P1 – P5) Summary Table

| Claim | Obligation | Target Test | Status | Output Excerpt |
|---|---|---|---|---|
| **P1** | Hostile tenant 10× limit prospective rejection before allocation | `crates/rockstream-runtime/tests/distributed_quota_enforcement_tests.rs::test_hostile_tenant_prospective_rejection_multi_worker` | `PASS` | `cargo test: 3 passed` |
| **P2** | Well-behaved tenant SLO preserved with zero starvation | `crates/rockstream-runtime/tests/distributed_quota_enforcement_tests.rs::test_well_behaved_tenant_slo_preserved_under_noisy_neighbor` | `PASS` | `cargo test: 3 passed` |
| **P3** | Durable Raft lease replication & persistent store round-trip | `crates/rockstream-control/src/shard.rs::tests::shard_persistent_store_round_trips_through_object_store`, `crates/rockstream-control/src/service.rs::tests::test_lease_grant_raft_replication` | `PASS` | `cargo test: 15 passed` |
| **P4** | Real leader SIGKILL results in follower takeover with lease continuity | `crates/rockstream-control/tests/lease_ha_leader_kill_tests.rs::test_leader_sigkill_follower_takeover_lease_continuity` | `PASS` | `cargo test: 2 passed` |
| **P5** | Deposed leader fenced, zero dual-writer window observed | `crates/rockstream-control/tests/lease_ha_leader_kill_tests.rs::test_deposed_leader_fenced_zero_dual_writer` | `PASS` | `cargo test: 2 passed` |

---

## 5. Exit Checklist

- [x] **Reachability tests** (e2e pgwire): `CREATE WORKLOAD` & `ALTER WORKLOAD` verified through pgwire client.
- [x] **Negative tests**: Invalid inputs return proper `RS-XXXX` codes (`RS-1005`, `RS-1014`, `RS-1006`).
- [x] **Dispatch-wiring audit**: Every path (1 through 4) proven connected.
- [x] **Coverage matrix**: Every cell in both matrices has a passing test.
- [x] **Proof obligations**: All P1–P5 claims satisfied and passing.
