# Phase 4 Proof Evidence: v0.55 — Internal mTLS & Node Identity

## 1. Proof Obligations & Evidence Table

| Proof Claim from `NEW_ROADMAP.md` & `v0.55-plan.md` | Mapped Test File & Function | Command Executed | Result & Excerpt | Status |
| :--- | :--- | :--- | :--- | :--- |
| **Claim 1: Rejected Peer Audited Denial & Zero Data Processed**: A worker or peer connection presenting an invalid, expired, or absent mTLS client certificate is rejected with an audited denial and never processes shard data. | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_peer_invalid_expired_absent_cert_rejected_with_audit_denial_no_data` & `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_missing_cert_rejected` | `rtk cargo test -p rockstream-control --test internal_mtls_tests test_peer_invalid_expired_absent_cert_rejected_with_audit_denial_no_data` | `cargo test: 1 passed (1 suite, 0.35s)`; asserts untrusted, expired, and absent cert peers fail handshake, topology catalog remains empty (`catalog.len() == 0`), zero leases granted, and audited denial events `security.internal_mtls_denied` emitted with `RS-2410`/`RS-2411`. | **PASS** |
| **Claim 1 (Control/Worker Untrusted CA)**: Connecting a worker with untrusted CA certificate is rejected with audited denial `RS-2411`. | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_untrusted_ca_rejected` | `rtk cargo test -p rockstream-control --test control_worker_mtls_tests test_control_worker_mtls_untrusted_ca_rejected` | `cargo test: 1 passed (1 suite, 0.34s)`; asserts rejected handshake and audited denial `security.internal_mtls_denied` with `RS-2411`. | **PASS** |
| **Claim 1 (Control/Worker Expired Cert)**: Connecting a worker with expired certificate is rejected with audited denial `RS-2411`. | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_expired_cert_rejected` | `rtk cargo test -p rockstream-control --test control_worker_mtls_tests test_control_worker_mtls_expired_cert_rejected` | `cargo test: 1 passed (1 suite, 0.34s)`; asserts rejected handshake and audited denial `security.internal_mtls_denied` with `RS-2411`. | **PASS** |
| **Claim 1 (Control/Worker Identity Mismatch)**: Worker registration with mismatch between certificate CN and message `worker_id` is rejected with `RS-2412`. | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_identity_mismatch_rejected` | `rtk cargo test -p rockstream-control --test control_worker_mtls_tests test_control_worker_mtls_identity_mismatch_rejected` | `cargo test: 1 passed (1 suite, 0.34s)`; asserts registration rejected with `RS-2412` and audited denial recorded. | **PASS** |
| **Claim 1 (Worker Shuffle gRPC Unauthenticated)**: Worker shuffle stream without client certificate is rejected with gRPC unauthenticated error. | `crates/rockstream-runtime/tests/shuffle_mtls_tests.rs::test_worker_shuffle_grpc_mtls_unauthenticated_rejected` | `rtk cargo test -p rockstream-runtime --test shuffle_mtls_tests test_worker_shuffle_grpc_mtls_unauthenticated_rejected` | `cargo test: 1 passed (1 suite, 0.14s)`; asserts plaintext connection rejected by mTLS shuffle server. | **PASS** |
| **Claim 1 (Worker Shuffle gRPC Untrusted CA)**: Worker shuffle stream with untrusted CA is rejected during TLS handshake. | `crates/rockstream-runtime/tests/shuffle_mtls_tests.rs::test_worker_shuffle_grpc_mtls_untrusted_rejected` | `rtk cargo test -p rockstream-runtime --test shuffle_mtls_tests test_worker_shuffle_grpc_mtls_untrusted_rejected` | `cargo test: 1 passed (1 suite, 0.14s)`; asserts handshake failure for untrusted certificate client. | **PASS** |
| **Claim 2: Zero-Downtime Certificate Rotation Under Load**: Certificate rotation completes on a 3-worker cluster under sustained load with zero epoch loss, zero pipeline restarts, and no window in which an already-authenticated peer is spuriously refused. | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_certificate_rotation_3_worker_cluster_under_load_zero_loss_zero_restart` | `rtk cargo test -p rockstream-control --test internal_mtls_tests test_certificate_rotation_3_worker_cluster_under_load_zero_loss_zero_restart` | `cargo test: 1 passed (1 suite, 0.40s)`; asserts 3 workers streaming lease requests during Gen2 CA + cert reload maintain continuous active status with `catalog.len() == 3` and 0 lost epochs. | **PASS** |
| **Claim 2 (Control Dynamic Reload Under Load)**: In-flight control plane certificate swap during active worker traffic stream completes without disconnects. | `crates/rockstream-control/tests/mtls_rotation_load_tests.rs::test_mtls_rotation_control_under_load_zero_loss` | `rtk cargo test -p rockstream-control --test mtls_rotation_load_tests test_mtls_rotation_control_under_load_zero_loss` | `cargo test: 1 passed (1 suite, 0.49s)`; asserts sustained traffic streams without interruption or spurious rejection. | **PASS** |
| **Claim 2 (Stale Peer Rejection Post-Grace Window)**: Connecting a stale Gen1 peer after grace window expiration is rejected with `RS-2411`, while Gen2 peer is admitted. | `crates/rockstream-control/tests/mtls_rotation_load_tests.rs::test_mtls_rotation_stale_cert_rejection_after_grace` | `rtk cargo test -p rockstream-control --test mtls_rotation_load_tests test_mtls_rotation_stale_cert_rejection_after_grace` | `cargo test: 1 passed (1 suite, 0.49s)`; asserts stale certificate rejected and fresh certificate admitted. | **PASS** |
| **Claim 3: CLI Transport Seam Integrity & Refusal**: The v0.53 CLI reaches the control plane with a client certificate and is refused without one, with no change to its command surface — proving the transport seam held. | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_cli_control_mtls_transport_seam_and_refusal` | `rtk cargo test -p rockstream-control --test internal_mtls_tests test_cli_control_mtls_transport_seam_and_refusal` | `cargo test: 1 passed (1 suite, 0.43s)`; asserts valid cert CLI executes status command (`cli.authenticated` emitted), unauthenticated CLI refused with `RS-2410`, untrusted CLI refused with `RS-2411`. | **PASS** |
| **Claim 3 (CLI Valid Cert Command Execution)**: CLI commands (`cluster status`, etc.) execute transparently over mTLS transport. | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_valid_cert_executes` | `rtk cargo test -p rockstream-cli --test cli_mtls_transport_tests test_cli_control_mtls_valid_cert_executes` | `cargo test: 1 passed (1 suite, 0.35s)`; asserts JSON and text output match expected models. | **PASS** |
| **Claim 3 (CLI Missing Cert Refusal RS-2410)**: CLI without certificate connecting to mTLS control plane is refused with actionable `RS-2410`. | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_missing_cert_refused` | `rtk cargo test -p rockstream-cli --test cli_mtls_transport_tests test_cli_control_mtls_missing_cert_refused` | `cargo test: 1 passed (1 suite, 0.35s)`; asserts `err.code == RS_2410` and `security.internal_mtls_denied` audit event. | **PASS** |
| **Claim 3 (CLI Untrusted Cert Refusal RS-2411)**: CLI with untrusted certificate is refused with `RS-2411`. | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_invalid_cert_refused` | `rtk cargo test -p rockstream-cli --test cli_mtls_transport_tests test_cli_control_mtls_invalid_cert_refused` | `cargo test: 1 passed (1 suite, 0.35s)`; asserts `err.code == RS_2411` and `security.internal_mtls_denied` audit event. | **PASS** |
| **Claim 4: Distributed Coordination (SimRuntime)**: Coordination under simulated network delay, packet drops, and rotation races with `buggify!()` annotations. | `crates/rockstream-sim/tests/internal_mtls_sim_tests.rs::test_mtls_rotation_simruntime_faults` | `rtk cargo test -p rockstream-sim --test internal_mtls_sim_tests test_mtls_rotation_simruntime_faults` | `cargo test: 1 passed (1 suite, 0.52s)`; seeded `SimRuntime` proves fault injection during worker join jitter and live rotation keeps all 3 workers operating. | **PASS** |
| **Claim 4 (Concurrent Worker Joins During Rotation)**: Concurrently joining Gen1 and Gen2 workers during active rotation in `SimRuntime` succeeds. | `crates/rockstream-sim/tests/internal_mtls_sim_tests.rs::test_mtls_concurrent_worker_joins_under_rotation` | `rtk cargo test -p rockstream-sim --test internal_mtls_sim_tests test_mtls_concurrent_worker_joins_under_rotation` | `cargo test: 1 passed (1 suite, 0.52s)`; asserts concurrent worker joins admitted without race conditions. | **PASS** |
| **Claim 5: Storage Backend Durability (LFS & MinIO)**: Topology, shard leases, and audit trail under mTLS durably persist across node restarts with 0 range deletions. | `crates/rockstream-control/tests/internal_mtls_tests.rs::test_mtls_cluster_durability_lfs_and_minio` | `rtk cargo test -p rockstream-control --test internal_mtls_tests test_mtls_cluster_durability_lfs_and_minio` | `cargo test: 1 passed (1 suite, 0.43s)`; asserts worker registration, lease assignments, and audit records persist identically. | **PASS** |

---

## 2. SQL / Wire Feature Reachability & Error Handling (Negative Tests)

| Channel / Interface | Reachability Test Function | Negative / Error Test Function | Error Code Verification | Status |
| :--- | :--- | :--- | :--- | :--- |
| `control ↔ worker` | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_valid_cert_admitted` | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_missing_cert_rejected` | Plaintext / absent client cert returns `RS-2410` audited denial | **PASS** |
| `control ↔ worker` (expired) | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_valid_cert_admitted` | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_expired_cert_rejected` | Expired cert rejected with `RS-2411` audited denial | **PASS** |
| `control ↔ worker` (identity mismatch) | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_valid_cert_admitted` | `crates/rockstream-control/tests/control_worker_mtls_tests.rs::test_control_worker_mtls_identity_mismatch_rejected` | Cert CN != `WorkerId` payload rejected with `RS-2412` audited denial | **PASS** |
| `worker ↔ worker` (gRPC shuffle) | `crates/rockstream-runtime/tests/shuffle_mtls_tests.rs::test_worker_shuffle_grpc_mtls_valid_cert_admitted` | `crates/rockstream-runtime/tests/shuffle_mtls_tests.rs::test_worker_shuffle_grpc_mtls_unauthenticated_rejected` | Unauthenticated gRPC stream returns unauthenticated rejection | **PASS** |
| `CLI ↔ control` | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_valid_cert_executes` | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_missing_cert_refused` | Absent cert refused with `RS-2410` | **PASS** |
| `CLI ↔ control` (untrusted) | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_valid_cert_executes` | `crates/rockstream-cli/tests/cli_mtls_transport_tests.rs::test_cli_control_mtls_invalid_cert_refused` | Untrusted cert refused with `RS-2411` | **PASS** |

---

## 3. Dispatch-Wiring Audit Verification

| Feature / Path | Code Path (Parser → Dispatcher → Executor → Response Encoder) | Verification Evidence | Status |
| :--- | :--- | :--- | :--- |
| **Control ↔ Worker TLS Transport** | `crates/rockstream-types/src/config.rs:410-435` (CLI/Config flags) → `crates/rockstream-control/src/service.rs:275-316` (`ControlService::with_internal_tls`, `TlsAcceptor`) → `crates/rockstream-control/src/tls.rs` (`extract_peer_identity`, `NodeIdentity::matches_worker_id`) → `crates/rockstream-runtime/src/client.rs:145-200` (`start_worker_client_with_tls`) | Proven connected via end-to-end mTLS tests (`control_worker_mtls_tests.rs`, `internal_mtls_tests.rs`). Verified client identity extracted from certificate DER, asserted against registration payload, and rejected with `RS-2410`/`RS-2411`/`RS-2412`. | **VERIFIED** |
| **Worker ↔ Worker gRPC Shuffle Channel** | `crates/rockstream-types/src/identity.rs` (`InternalTlsConfig`) → `crates/rockstream-runtime/src/exchange/service.rs:215-245` (`ExchangeService::with_internal_tls`, `ServerTlsConfig`) → `crates/rockstream-runtime/src/exchange/pool.rs:160-200` (`ShuffleClientPool::with_internal_tls`, `ClientTlsConfig`) | Proven connected via `shuffle_mtls_tests.rs`. TLS-encrypted gRPC shuffle streams authenticate connecting peer against cluster CA root. | **VERIFIED** |
| **CLI ↔ Control-Plane Channel** | `crates/rockstream-cli/src/main.rs:28-45` (`--tls-ca-cert-path`, `--tls-cert-path`, `--tls-key-path`) → `crates/rockstream-cli/src/transport.rs:130-240` (`ControlClient::with_internal_tls`) → `crates/rockstream-control/src/service.rs` | Proven connected via `cli_mtls_transport_tests.rs`. CLI commands (`cluster status`, `workers list`, etc.) execute transparently over mTLS transport with zero changes to CLI command surface. | **VERIFIED** |
| **Dynamic Certificate Rotation & Reloader** | `crates/rockstream-control/src/tls.rs:65-175` (`TlsCertificateReloader`, `add_trusted_ca`, `reload`) → `crates/rockstream-control/src/service.rs:160-170` (`ControlServiceHandle::reload_tls`) | Proven connected via `mtls_rotation_load_tests.rs`. In-flight certificate swapping and dual-generation CA trust proven under continuous traffic with 0 dropped frames and 0 lost epochs. | **VERIFIED** |

---

## 4. Exit Checklist

- [x] **Reachability tests**: Every internal channel (`control ↔ worker`, `worker ↔ worker` gRPC shuffle, `CLI ↔ control`) has an end-to-end test executing over actual TLS streams.
- [x] **Negative tests**: Invalid, expired, untrusted, or mismatched identity peers return proper `RS-2410`, `RS-2411`, or `RS-2412` error codes and audited denials.
- [x] **Dispatch-wiring audit**: Every path in Phase 2 audit is proven connected with test evidence and exact code references.
- [x] **Coverage matrix**: Every cell across Channel × Peer Role × Condition, Rotation × Load State, and Durability matrices is tested and passing.
- [x] **All other Proof obligations met**:
  - Internal mTLS wrapped on every internal channel.
  - Node identity derived cryptographically from X.509 CN / SAN.
  - Audited denial `security.internal_mtls_denied` emitted for all rejected peers with zero data processed.
  - Dynamic in-flight certificate rotation proven under sustained load with zero lost epochs.
  - CLI transport seam preserved with zero syntax changes.
  - Durability across SlateDB LFS and MinIO verified with 0 range deletions.
  - Seeded SimRuntime multi-schedule fault injection verified (`test_mtls_rotation_simruntime_faults`, `test_mtls_concurrent_worker_joins_under_rotation`).
  - Documentation updated in `docs/security.md`, `docs/sre-operations.md`, and `docs/cli.md`.
  - All logged and returned errors carry registered `RS-XXXX` codes (`scripts/check-error-codes.sh` passed).
  - Formal verification specs passed (`make verify`).
  - Workspace clippy clean with zero warnings (`cargo clippy: No issues found`).
  - Workspace rustfmt clean (`cargo fmt --check` passed).
