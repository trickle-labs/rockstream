# v0.53.2 Proof Evidence — IVM Arrangement Debugger: Addressability, User-Level Key Decoding & Non-Perturbing State Inspection

**Execution Date**: 2026-08-14  
**Scope**: IVM arrangement debugger CLI surface (`rockstream explain <view> --op-ids` and `rockstream debug arrangement <view> <op_id> <key> [--epoch=N]`), addressability, user-level key decoding and encoding (`GroupKeyPacker`, `Utf8KeyPacker`, `Utf8ColumnPacker`, window and join keys), clean refusal by family name (`RS-1021`), non-perturbing snapshot and historical epoch inspection (`RS-2006`), batch oracle equivalence across stateful operator families, and LFS / MinIO durability.

---

## 1. Roadmap Proof Claims & Proof Matrix

| Proof Claim from `NEW_ROADMAP.md` | Proving Test / Mechanism | Command | Status | Evidence Excerpt |
|---|---|---|---|---|
| `debug arrangement orders_mv <op_id> "product_id=42"` returns the correct Z-set weight for every stateful operator family that ships (`Aggregate`, `MinMax`, `Join`, `TopK`, `Distinct`, and the `Tumble`/`Hop`/`Session` window operators), verified against the same value computed by the DataFusion batch oracle | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_all_stateful_operators_batch_oracle` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** | `test test_debug_arrangement_all_stateful_operators_batch_oracle ... ok` — verified addressability, compiled execution plan, operator ID lookup, and exact Z-set weight equality (`weight: 1`) against batch oracle across all stateful operator families. |
| A composite `GROUP BY` key and a `Utf8` key both round-trip through the surrogate encodings and print as user-level values | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_composite_and_utf8_key_roundtrip` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** | `test test_debug_arrangement_composite_and_utf8_key_roundtrip ... ok` — verified composite `GROUP BY` surrogate roundtrips via `GroupKeyPacker::reverse_lookup`, string columns via `Utf8KeyPacker::reverse_lookup`, window ranges `(window_start, key)`, session ranges `(session_start, user_id)`, and join side tags `(left/right, key)`. |
| A family with no decoder refuses by name instead of printing raw bytes | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_unsupported_family_refuses_by_name` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_unsupported_family_refuses_by_name` | **PASS** | `test test_debug_arrangement_unsupported_family_refuses_by_name ... ok` — unsupported family rejects with `OpError::ArrangementKeyDecodeFailed` naming the family explicitly (`OpaqueCustomFamilyCodec`) with `RS-1021`, never printing raw unparsed bytes. |
| `--epoch=N` inside retention returns bit-identical historical state and outside it fails with its documented code (`RS-2006`) | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_historical_epoch_and_retention_bounds` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_historical_epoch_and_retention_bounds` | **PASS** | `test test_debug_arrangement_historical_epoch_and_retention_bounds ... ok` — historical epoch read inside retention window (epoch 1492) returns snapshot with exact weight; epoch outside retention (epoch 3 < 10) cleanly refuses with `RS-2006` and actionable retention diagnostic. |
| The debugger polls a loaded pipeline continuously with no freshness-lag regression against the same run without it | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_continuous_polling_non_perturbing` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_continuous_polling_non_perturbing` | **PASS** | `test test_debug_arrangement_continuous_polling_non_perturbing ... ok` — executed 50 continuous debug arrangement queries across epochs against live pipeline without pipeline locks, stalls, or frontier delay. |
| Durability and SlateDB reader snapshot on LFS and MinIO object store | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_durability_lfs_and_minio` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_durability_lfs_and_minio` | **PASS** | `test test_debug_arrangement_durability_lfs_and_minio ... ok` — verified arrangement state retrieval via `ShardReader::open` and `inspect_arrangement_db` point gets across LFS and MinIO object store backing with 0 range deletions. |
| Fault injection and simulated runtime stability | `crates/rockstream-cli/tests/arrangement_debugger_tests.rs::test_debug_arrangement_sim_faults` | `rtk cargo test -p rockstream-cli --test arrangement_debugger_tests test_debug_arrangement_sim_faults` | **PASS** | `test test_debug_arrangement_sim_faults ... ok` — verified graceful error responses under missing view (`RS-1001`) and missing operator ID (`RS-1020`). |
| Full documentation coverage and CLI schema conformance | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | `rtk cargo test -p rockstream-cli --test cli_doc_coverage_tests` | **PASS** | `test test_docs_cli_conformance_and_coverage ... ok` — verified 100% of subcommands (`explain --op-ids`, `debug arrangement`) and error codes (`RS-1020`, `RS-1021`) documented in `docs/cli.md`. |

---

## 2. Dispatch-Wiring Verification (Phase 2 Audit)

Every arrangement debugger CLI path and internal decoder identified in the Phase 2 audit is verified as connected end-to-end:

| Feature / Command | Parser (`main.rs`) | Dispatcher (`lib.rs`) | Executor / Engine (`rockstream-ops` / `storage`) | Response Encoder & Output Model (`output.rs`) | Status |
|---|---|---|---|---|---|
| `explain <view> --op-ids` | `crates/rockstream-cli/src/main.rs:254` | `crates/rockstream-cli/src/lib.rs:1586` (`run_explain_view`) | `rockstream_ops::debugger::explain_view_op_ids` (`debugger.rs:94`) | `ExplainOpIdInfo` / `format_explain_op_ids` (`output.rs:816`) | **CONNECTED** |
| `debug arrangement <view> <op_id> <key> [--epoch=N]` | `crates/rockstream-cli/src/main.rs:271` | `crates/rockstream-cli/src/lib.rs:1639` (`run_debug_arrangement`) | `rockstream_ops::debugger::inspect_arrangement_for_view` (`debugger.rs:343`) | `ArrangementDebugInfo` / `format_debug_arrangement` (`output.rs:830`) | **CONNECTED** |
| `GroupKeyPacker` surrogate key decoder | N/A (Internal) | `debugger.rs:172` (`decode_user_key`) | `GroupKeyPacker::reverse_lookup` / `reverse_slices` (`live_exec.rs:161`) | User-level composite text (e.g. `category_id=42, region_id=100`) | **CONNECTED** |
| `Utf8ColumnPacker` / `Utf8KeyPacker` key decoder | N/A (Internal) | `debugger.rs:188` (`decode_user_key`) | `Utf8KeyPacker::reverse_lookup` (`live_exec.rs:107`) | User text representation (e.g. `name=electronics`) | **CONNECTED** |
| Window key decoder (Tumble / Hop / Session) | N/A (Internal) | `debugger.rs:205` (`decode_user_key`) | `time_window::decode_window_slice` / window parser | Window range & key text (e.g. `window_start=1000, key=5`) | **CONNECTED** |
| Join-side row key decoder | N/A (Internal) | `debugger.rs:238` (`decode_user_key`) | `join::decode_join_entry` / join side parser | Side tag & key text (e.g. `left: product_id=42`) | **CONNECTED** |
| Historical epoch snapshot reader (`--epoch=N`) | `crates/rockstream-cli/src/main.rs:280` | `crates/rockstream-cli/src/lib.rs:1665` (`run_debug_arrangement`) | `ShardReader::open_with_epoch` / `inspect_arrangement_reader` | Validated historical snapshot or `RS-2006` | **CONNECTED** |

---

## 3. Stateful Operator Family × Key Type × Result Coverage Matrix

Verified in `crates/rockstream-cli/tests/arrangement_debugger_tests.rs`:

| Operator Family | Int64 Key Test | Composite Group Key (`GroupKeyPacker`) Test | String Key (`Utf8KeyPacker`) Test | Batch Oracle Equality Test (`incremental == batch`) | Status |
|---|---|---|---|---|---|
| `Aggregate` (`AggregateOp`, `BucketedAggregateOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `MinMax` (`MinMaxOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `Join` (`JoinOp`, `OuterJoinOp`, `JoinPipeline`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `TopK` (`TopKOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `Distinct` (`DistinctOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `TumbleWindow` (`TumbleWindowOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `HopWindow` (`HopWindowOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| `SessionWindow` (`SessionWindowOp`) | `test_debug_arrangement_all_stateful_operators_batch_oracle` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |

---

## 4. Key Decoding, Unsupported Refusal & Error Code Matrix

| Scenario / Codec | User Input Key Syntax | Decoded Representation | Documented Code | Test Commitment | Result |
|---|---|---|---|---|---|
| Single Int64 / Numeric Key | `"product_id=42"` | `[0x01][op_id][be_bytes(42)]` | Success | `test_debug_arrangement_all_stateful_operators_batch_oracle` | **PASS** |
| Composite `GROUP BY` Key | `"category_id=42, region_id=100"` | Interned surrogate `i64` via `GroupKeyPacker` | Success | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** |
| `Utf8` String Column / Key | `"name=electronics"` | Interned surrogate `i64` via `Utf8KeyPacker` | Success | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** |
| Window Key (`Tumble` / `Hop`) | `"window_start=1000, key=5"` | Encoded window ID + group key | Success | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** |
| Session Window Key | `"session_start=5000, user_id=99"` | Encoded session ID + group key | Success | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** |
| Join Side State Key | `"left: product_id=42"` | `[OpState][JL/JR][op_id][key]` | Success | `test_debug_arrangement_composite_and_utf8_key_roundtrip` | **PASS** |
| Unsupported Family Codec | Custom opaque family | Rejection by family name (NO raw bytes) | `RS-1021` | `test_debug_arrangement_unsupported_family_refuses_by_name` | **PASS** |
| Missing View | `"nonexistent_view_404"` | Entity not found | `RS-1001` | `test_debug_arrangement_sim_faults` | **PASS** |
| Missing Operator ID | `"op-invalid-999999"` | Operator not found in view pipeline | `RS-1020` | `test_debug_arrangement_sim_faults` | **PASS** |
| Epoch Inside Retention | `--epoch=1492` | Exact historical SlateDB snapshot | Success | `test_debug_arrangement_historical_epoch_and_retention_bounds` | **PASS** |
| Epoch Outside Retention | `--epoch=3` | Historical snapshot unavailable | `RS-2006` | `test_debug_arrangement_historical_epoch_and_retention_bounds` | **PASS** |

---

## 5. Durability & Backend Proofs

- **SlateDB LFS & MinIO Backend**: Point reads and prefix scans execute directly on SlateDB storage layer across both local filesystem and MinIO (S3) test configurations (`test_debug_arrangement_durability_lfs_and_minio`).
- **Zero SlateDB Range Deletions**: Storage inspection uses point gets (`db.get`) and bounded prefix scans (`scan_prefix_bounded`) with 0 range deletions.
- **Continuous Polling Invariance**: 50 consecutive debug inspections verify non-perturbing reads with zero pipeline locks or freshness delay (`test_debug_arrangement_continuous_polling_non_perturbing`).
- **Clippy & Lints**: Checked clean workspace with `rtk cargo clippy --all-targets` (0 warnings).
