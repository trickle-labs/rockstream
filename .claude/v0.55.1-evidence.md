# v0.55.1 Proof Evidence

All commands below exited successfully on 2026-08-15. Passing Cargo output was
filtered to summary/error lines as required; no error or failure lines were emitted.

| Proof claim | Test / command | Result / excerpt |
|---|---|---|
| Secret plaintext never reaches emitted artifacts | `rtk cargo test -p rockstream-control --test secrets_redaction_tests` | PASS — no error or failure summary lines. `secret_literal_is_absent_from_audit_and_metadata_artifacts` scans literal bytes. |
| Live credential rotation has no restart or failed batch | `rtk cargo test -p rockstream-gateway --test live_secret_rotation_tests` | PASS — no error or failure summary lines. `alter_secret_notifies_and_connector_applies_next_epoch_token` asserts rotation, zero restarts, and zero failed batches. |
| Worker secrets resolve only from short-lived tokens | `rtk cargo test -p rockstream-runtime --test worker_secret_resolution_tests` | PASS — no error or failure summary lines. Tests cover valid identity, wrong identity, and expiry eviction. |
| KEK providers conform and rotation preserves plaintext | `rtk cargo test -p rockstream-control --test secrets_kek_conformance_tests` | PASS — no error or failure summary lines. `test_kek_providers_interchangeable_conformance_and_rotation` covers `env`, `aws_kms`, and re-wrapping. |
| Secret durability survives LFS and MinIO with KEK rotation | `rtk cargo test -p rockstream-control --test secrets_management_durability_tests` | PASS — no error or failure summary lines. Covers LFS and `secrets_survive_minio_tc_reopen_and_kek_rotation`, which starts and tears down MinIO through TestContainers. |
| Token/rotation scheduling remains bounded under seeded simulation | `rtk cargo test -p rockstream-sim --features simulation --test secrets_sim_tests` | PASS — no error or failure summary lines. `secret_resolution_remains_bounded_under_seeded_rotation_jitter` uses seed `55_001`. |

## SQL reachability, negative handling, and dispatch wiring

| Requirement | Test / verification | Result / excerpt |
|---|---|---|
| Raw pgwire reachability for `CREATE`, `ALTER`, `DROP`, and `SHOW SECRETS` | `rtk cargo test -p rockstream-gateway --test secret_ddl_tests` | PASS — no error or failure summary lines. `secret_ddl_is_reachable_over_pgwire_and_returns_metadata_only` sends raw SQL through `tokio_postgres`. |
| Actionable errors | Same pgwire target | PASS — `secret_ddl_negative_paths_return_actionable_codes` asserts `RS-2424`, `RS-2420`, and `RS-2426`. Lifecycle tests additionally assert duplicate and missing-secret errors. |
| Parser → dispatcher → store → response | `rtk rg "handle_(create|alter|drop|show)_secret|parse_(create|alter|drop)_secret_ddl|with_secret_store" crates/rockstream-gateway/src/server.rs` | PASS — handlers, parser functions, and the injected `SecretStore` are present; raw pgwire test proves the connected path. |
| Source/sink secret validation and dependency protection | `rtk cargo test -p rockstream-gateway --test secret_ddl_tests` | PASS — raw `CREATE SOURCE ... secret = ...` validates `RS-2420`; referenced-secret deletion returns `RS-2426`. |

## Completion checks

| Check | Command | Result / excerpt |
|---|---|---|
| Format | `rtk cargo fmt --check` | PASS — no output. |
| Lints | `rtk cargo clippy --workspace --all-targets -- -D warnings` | PASS — `cargo clippy: No issues found`. |
| Workspace | `rtk cargo test --workspace` | PASS — no error or failure summary lines. |
| LFS and MinIO e2e | `rtk run 'make e2e'` | PASS — no `FAILED` or `error` lines. |
