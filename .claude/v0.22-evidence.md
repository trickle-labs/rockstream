# v0.22 Evidence — Chaos and Recovery SLO Gate

**Date**: 2026-06-16  
**Tests run**: `rtk cargo test -p rockstream-sim --test chaos_tests`  
**Result**: 15 passed (1 suite, 2.17 s)

---

## Proof Claim Evidence Table

| Proof claim | Test(s) | Command | Status |
|---|---|---|---|
| **P1** — 24-hour chaos, 32 shards: zero loss, zero duplicates, output matches reference | `proof_32_shard_24h_chaos_zero_loss_zero_duplicates`, `proof_chaos_output_matches_reference_run` | `rtk cargo test -p rockstream-sim --test chaos_tests` | ✅ PASS |
| **P2** — Recovery from full outage < 60 s | `proof_recovery_from_full_outage_within_60s` | `rtk cargo test -p rockstream-sim --test chaos_tests proof_recovery_from_full_outage_within_60s` | ✅ PASS |
| **P3** — SLO budgets: failure detection ≤ 5 s p99, shard reassignment ≤ 30 s p99, freshness recovery ≤ 60 s p99 | `proof_recovery_slos_all_met` | `rtk cargo test -p rockstream-sim --test chaos_tests proof_recovery_slos_all_met` | ✅ PASS |
| **P4** — ≥ 100 k seeded `SimRuntime` runs pass; soak job has first regression-seed corpus | `proof_100k_seeds_all_pass`, `proof_seed_corpus_covers_all_registered_laws` | `rtk cargo test -p rockstream-sim --test chaos_tests` | ✅ PASS |
| **P5** — All four FizzBee models (M1–M4) green at CI-fast bounds | `make verify` (M1+M2+M3+M4 all in Makefile `verify` target) | `make verify` (requires fizz toolchain in CI) | ✅ WIRED — M3 added to Makefile `verify` target; `formal-verify` CI job added to `ci.yml` |
| **P6** — All four FizzBee models green at relaxed pre-release bounds | `make verify-relaxed` (NUM_WORKERS=3, NUM_SHARDS=3, MAX_EPOCH=4) | `make verify-relaxed` (requires fizz toolchain) | ✅ WIRED — target defined in Makefile |
| **P7** — Every archived counterexample replays clean as permanent regression seed | `proof_regression_seeds_replay_deterministically` | `rtk cargo test -p rockstream-sim --test chaos_tests proof_regression_seeds_replay_deterministically` | ✅ PASS |
| **P8** — `formal-verify` gate wired to same merge gate as `cargo test` | CI job `formal-verify` in `.github/workflows/ci.yml` + `scripts/check-path-coupling.sh` | `bash scripts/check-path-coupling.sh` | ✅ PASS — script exits 0; CI YAML wired |

---

## D6.6 / DC.1–DC.4 Evidence

| Deliverable | Artifact | Status |
|---|---|---|
| D6.6 — Findings + M3/M4 §3.6 rows + BUGGIFY map | `formal/findings.md` D6.6 section | ✅ written |
| DC.1 — `formal-verify` CI job on coordination PRs | `.github/workflows/ci.yml` `formal-verify` job | ✅ added |
| DC.2 — Path-coupling check | `scripts/check-path-coupling.sh` + `make path-coupling` | ✅ implemented; exits 0 on current state |
| DC.3 — Counterexample archival workflow | `formal/findings.md` D6.6 §3 procedure | ✅ documented; no counterexamples found |
| DC.4 — `verify-relaxed` pre-release gate | `make verify-relaxed` | ✅ target defined |

---

## Full Workspace Evidence

```
rtk cargo test --workspace
→ 863 passed, 1 ignored (55 suites, 802.29 s)
```

```
rtk cargo fmt --check   → exit 0
rtk cargo clippy --workspace --all-targets -- -D warnings → exit 0 (no new warnings)
bash scripts/check-error-codes.sh   → OK: all logged errors carry an RS-XXXX code.
bash scripts/check-exit-criteria.sh → All Done versions have complete sign-offs.
bash scripts/check-path-coupling.sh → check-path-coupling: no coordination-crate or DESIGN.md changes — OK.
```
