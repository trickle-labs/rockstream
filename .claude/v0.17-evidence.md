# Version v0.17 Proof Evidence

## Proof Claims

| Proof Claim | Target Test(s) | Status | Command / Details |
|---|---|---|---|
| Distributed results are **bit-identical** to single-shard runs across the entire oracle/TPC-H suite | `test_distributed_fuzz_parity` | PASS | `rtk cargo test -p rockstream-runtime --test exchange_integration test_distributed_fuzz_parity` |
| An injected receiver failure forces durable fallback and the receiver catches up with no duplicates | `test_durable_shuffle_fallback` | PASS | `rtk cargo test -p rockstream-runtime --test exchange_integration test_durable_shuffle_fallback` |
| A 1,000-shard exchange stress test stays within connection and shuffle-object budgets against MinIO | `test_thousand_shard_exchange_stress` | PASS | `rtk cargo test -p rockstream-runtime --test exchange_integration test_thousand_shard_exchange_stress` |
| Rendezvous Hashing Minimality and uniform distribution | `test_rendezvous_rebalance_minimality` | PASS | `rtk cargo test -p rockstream-types --lib rendezvous` |
| Deterministic control plane simulation test under fault injection | `sim_control_plane_leases` | PASS | `rtk cargo test -p rockstream-sim --features simulation --test control_sim` |

### Test Run Summary

```
cargo test: 1 passed (1 suite, 0.51s) [-p rockstream-sim --features simulation --test control_sim]
cargo test: 3 passed, 152 filtered out (1 suite, 1.34s) [-p rockstream-types --lib rendezvous]
cargo test: 4 passed (1 suite, 1.76s) [-p rockstream-runtime --test exchange_integration]
```
