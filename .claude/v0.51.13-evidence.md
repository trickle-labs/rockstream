# v0.51.13 Phase 4 Evidence — Full Ad Hoc Query Support

All commands below completed successfully. The test fixtures compare complete,
ordered pgwire rows (not counts alone) and send raw SQL through the gateway
dispatcher.

| Proof claim | Test / command | Result and output excerpt |
| --- | --- | --- |
| `WHERE` matrix: `BIGINT`, `TEXT`, and `DOUBLE PRECISION` scatter results equal the complete oracle | `rtk cargo test -p rockstream-gateway --test query_time_multi_shard_scatter_tests` — `multi_shard_where_bigint_matches_single_shard_oracle`, `multi_shard_where_text_matches_single_shard_oracle`, `multi_shard_where_float_matches_single_shard_oracle` | PASS — `test result: ok` |
| `INNER JOIN` matrix for `BIGINT`, `TEXT`, and `DOUBLE PRECISION` | Same command — `multi_shard_inner_join_bigint_matches_single_shard_oracle`, `multi_shard_inner_join_text_matches_single_shard_oracle`, `multi_shard_inner_join_float_matches_single_shard_oracle` | PASS — `test result: ok` |
| `GROUP BY` / `SUM` / `COUNT` matrix for `BIGINT`, `TEXT`, and `DOUBLE PRECISION` keys | Same command — `multi_shard_group_by_bigint_sum_count_matches_single_shard_oracle`, `multi_shard_group_by_text_key_sum_count_matches_single_shard_oracle`, `multi_shard_group_by_float_sum_count_matches_single_shard_oracle` | PASS — `test result: ok` |
| Raw-SQL pgwire reachability and topology failure handling | Same command — all matrix tests use `tokio_postgres::Client::simple_query`; `empty_scatter_topology_returns_actionable_error` proves `RS-2028` and actionable `next_steps` | PASS — `test result: ok` |
| Production CLI refreshes all configured owners at one frontier | `rtk cargo test -p rockstream-cli --test query_time_topology_provider_tests` | PASS — three configured shard roots return exact rows; a stale frontier returns user-visible `RS-2030` with `next_steps` |
| Invalid SQL returns an actionable RockStream error rather than an empty response | Same command — `query_time_invalid_sql_returns_actionable_rs_error` | PASS — raw SQL returns `RS-2026` with `next_steps` |
| Bounded batches preserve the full result and release all fill levels | Same command — `query_time_scatter_batches_never_exceed_row_or_byte_budget` | PASS — exact aggregate is `["200010000", "20000"]`; terminal fill level is exactly `{ rows: 0, bytes: 0, batches: 0 }`; peak levels remain within configured row, byte, and batch bounds |
| A relation above the retired 1,000,000-row cap completes | `rtk cargo test -p rockstream-gateway --test query_time_large_relation_tests large_relation_over_old_row_cap_aggregate_matches_oracle` | PASS — exact aggregate `["500001500001", "1000001"]` |
| A relation above the retired 64-MiB cap completes | `rtk cargo test -p rockstream-gateway --test query_time_large_relation_tests large_relation_over_old_byte_cap_aggregate_matches_oracle` | PASS — exact aggregate `["2147581953", "65537"]` |
| Pathological work fails before a partial response | `rtk cargo test -p rockstream-gateway --test query_time_large_relation_tests pathological_query_budget_returns_documented_error_without_truncation` | PASS — `RS-2029`, `3 rows or 1024 bytes`, and `next_steps` |
| Local-filesystem SlateDB correctness | `rtk cargo test -p rockstream-gateway --test query_time_multi_shard_scatter_tests` — `multi_shard_scatter_lfs_where_join_group_by_exact_oracle` | PASS — exact `WHERE`, `JOIN`, and grouped aggregate rows over three SlateDB-backed shards |
| Real S3-compatible backend correctness and lifecycle | `rtk cargo test -p rockstream-gateway --features testcontainers --test query_time_multi_shard_scatter_minio_tests` — `multi_shard_scatter_minio_where_join_group_by_exact_oracle` | PASS — test starts and tears down MinIO through TestContainers; exact `WHERE`, `JOIN`, and grouped aggregate rows over three S3-backed shards |
| Seeded adversarial scatter ordering preserves completeness | `rtk cargo test -p rockstream-sim --features simulation --test query_time_scatter_sim_tests` — `scatter_frontier_buggify_preserves_complete_exact_result` | PASS — seeds `0x51_13_0001`, `0x51_13_0002`, and `0x51_13_0003` inject reader-selection, batch-arrival, and frontier-validation faults; each returns exactly `[(1, west), (2, east), (3, north)]` at pinned frontier `17` |
| Paged reader honors exact page bounds | `rtk cargo test -p rockstream-storage reader_scan_prefix_pages_obeys_exact_row_and_byte_bounds` | PASS — exact pages `[(pfx_a, aa)]`, `[(pfx_b, bb)]`, and `[(pfx_c, cc)]` |
| v0.51.2 index-planning regression | `rtk cargo test -p rockstream-gateway --test create_index_query_planning_tests` | PASS — `test result: ok` |

## Dispatch-wiring audit

`rtk rg -n "analyze_select_query\\(&self\\.catalog|query_time_datafusion_response\\(|query_time_datafusion_select\\(|StreamingTable::try_new|datafusion_batches_to_query_response\\(|with_shard_db_and_query_time_shard_topology|scan_prefix_pages\\(" crates/rockstream-gateway/src/server.rs crates/rockstream-storage/src/reader.rs`

PASS — parser/relation analysis is reached at `server.rs:3618`; query-time
dispatch invokes `query_time_datafusion_response` at `:3703`; authorization is
checked before topology selection in `:4099`; the topology-aware executor is
called at `:4112`; all relations are registered as `StreamingTable`s at `:9834`;
each partition reads pages through `ShardReader::scan_prefix_pages` at `:9997`;
and results are encoded through `datafusion_batches_to_query_response` at
`:4115`. `EXPLAIN` uses the same topology-aware helper at `:2978`.

## Phase 4 status

PASS — production CLI startup now constructs a refreshable all-owning-shard
provider from `--query-time-shard-dir`, validates a common durable frontier on
every query, and fails closed with `RS-2030` on a mismatch.
