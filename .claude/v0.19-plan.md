# Version v0.19 Implementation Plan — Frontier Consumers & Exchange GC

## 1. Deferred-Item Audit
- Checked all prior sign-offs in `sign-offs/` (`v0.1.md` through `v0.18.md`).
- `Option-based buggify control API` (deferred in v0.2 to v0.5) is out of scope and does not overlap with the frontier progress tracking of `v0.19`.
- `Automatic background compact-on-idle` (deferred in v0.3 to v0.7) is out of scope and does not overlap with the frontier progress tracking of `v0.19`.
- `Direct lowering logic bypass for trivial plans` (deferred in v0.7 to v0.10) is out of scope.
- `HOP/SESSION time windows` (deferred in v0.12 to v0.19) is out of scope.
- Audit is complete: no prior deferred items overlap this version's scope.

---

## 2. Vertical Slices

### Slice 1: M2 Multi-Source Vector Frontier Model (FizzBee)
- **Goal**: Extend the M2 model in `formal/m2_frontier_agg.fizz` to support vector-antichain meet operations representing `FreshnessToken` (multi-source progress) instead of scalar values. Verify invariants (order independence, pessimistic staleness, single-publisher, stale-write rejection) and document results in `formal/findings.md`.
- **Changes**:
  - [MODIFY] [m2_frontier_agg.fizz](file:///Users/grove/projects/rockstream/formal/m2_frontier_agg.fizz): Generalize state to vectors of epochs, update meet calculations, and prove assertions.
  - [NEW] [findings.md](file:///Users/grove/projects/rockstream/formal/findings.md): Add section documenting M2 vector meet checks.
- **Test**: Run `make verify` and check that the spec is green.

### Slice 2: Vector Meet & `FreshnessToken` in Rust
- **Goal**: Implement `FreshnessToken` (representing `BTreeMap<SourceId, SourceProgress>`) and vector meet operations in `rockstream-types`.
- **Changes**:
  - [MODIFY] [frontier.rs](file:///Users/grove/projects/rockstream/crates/rockstream-types/src/frontier.rs): Define `FreshnessToken` struct and its lattice properties. Add tests verifying commutativity and associativity.
- **Test**: Run `rtk cargo test -p rockstream-types --lib frontier` verifying vector meet lattice properties.

### Slice 3: Operator Frontier Consumers
- **Goal**: Track input frontiers in operators. Stateful operators (Join and TumbleWindowOp) use input frontiers to safely close window buckets and emit deltas.
- **Changes**:
  - [MODIFY] [op.rs](file:///Users/grove/projects/rockstream/crates/rockstream-ops/src/op.rs): Add methods to push/track input frontiers.
  - [MODIFY] [task.rs](file:///Users/grove/projects/rockstream/crates/rockstream-ops/src/task.rs): Update task loop to propagate channel frontiers to operators.
  - [MODIFY] [time_window.rs](file:///Users/grove/projects/rockstream/crates/rockstream-ops/src/time_window.rs): TumbleWindowOp closes window buckets using the input frontier instead of watermark.
- **Test**: Run time-window LFS/MinIO tests to verify window closing and correct delta emission under multi-shard scenarios.

### Slice 4: Exchange Inbox & Outbox Garbage Collection
- **Goal**: Reclaim shuffle inbox/outbox storage (`ShardDb` key prefixes `0x04` and `0x05`) once their corresponding epoch is committed.
- **Changes**:
  - [MODIFY] [persistence.rs](file:///Users/grove/projects/rockstream/crates/rockstream-runtime/src/exchange/persistence.rs): Implement scan-and-delete of old inboxes/outboxes up to a given epoch.
  - [MODIFY] [service.rs](file:///Users/grove/projects/rockstream/crates/rockstream-runtime/src/exchange/service.rs): Trigger GC whenever the published cluster frontier advances.
- **Test**: Run tests checking that shuffle keys in ShardDb are deleted after the frontier advances.

### Slice 5: Control-Plane Config Derivation
- **Goal**: Derive freshness target parameters (`min_epoch_ms` / `max_epoch_ms` and initial parallelism) dynamically in the control plane.
- **Changes**:
  - [MODIFY] [lib.rs](file:///Users/grove/projects/rockstream/crates/rockstream-control/src/lib.rs): Derive parameters from targets.
- **Test**: Control plane unit tests verifying target-to-epoch limits mapping.

### Slice 6: Multi-Source Parity Soak (E2E)
- **Goal**: Integrate all components and verify a join over two sources running at different ingestion rates (one fast, one slow) converges correctly under `SimRuntime` with bounded shuffle storage.
- **Changes**:
  - [NEW] [progress_soak.rs](file:///Users/grove/projects/rockstream/crates/rockstream-sim/tests/progress_soak.rs): Add simulation integration test validating multi-source join convergence and bounded shuffle storage against MinIO.
- **Test**: Run E2E multi-source progress soak.

---

## 3. Test Commitments & Proof Mapping

### Oracle / Property / Simulation Tests
- **Vector Frontier Lattice Property Tests**: Property tests asserting lattice invariants (commutativity, associativity, absorption) for vector `FreshnessToken` in `rockstream-types`.
- **Vector Frontier Reordering convergence test**: Verifies that any permutation of vector frontier reports converges to the same vector meet under `SimRuntime`.
- **Multi-Source Join Ingestion Parity Test**: Simulation test asserting a join over asymmetric source ingestion rates (fast/slow) converges to correct output with bounded buffers.

### Durability / LFS / TestContainers Tests
- **Bounded Shuffle Storage Test**: Checks that inbox/outbox storage usage (number of keys) in `ShardDb` stays bounded under continuous execution against MinIO.
- **Tumble Window Compaction Filter Test**: Durability test verifying compaction filter handles vector frontiers correctly to prune old window state.

---

## 4. Proof Claims to Test Mapping

| Proof Claim | Target Test(s) |
|---|---|
| A join over two sources at different ingestion rates produces correct output with no premature emission and no infinite buffering | `test_multi_source_join_progress` (in `progress_soak.rs`) |
| Shuffle storage usage stays bounded under sustained throughput (measured against MinIO) | `test_shuffle_storage_gc_bounded` (in `progress_soak.rs`) |
| **FizzBee**: the M2 multi-source meet model is green and its `SimRuntime` mirror (arbitrary frontier-report reorderings converge to the same cluster vector frontier) passes | `make verify` and `test_frontier_reordering_convergence` (with vector frontiers) |
| The M2 §3.6 mapping rows are populated | Table in `FIZZBEE_TEST_PLAN.md` §3.6 |
