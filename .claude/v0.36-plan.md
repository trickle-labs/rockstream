# v0.36 Plan — Nexmark Retraction & Z-Set Correctness

## Deferred-Item Audit

Scanned all prior `sign-offs/v*.md` files for any deferred, postponed, or out-of-scope items.

**Result: no prior deferrals overlap this version's scope.**
- `HOP`/`SESSION` windows were deferred in `v0.12` and `q11` (session windows) was deferred in `v0.35` to `v0.37` where the `SESSION` operator will be implemented.
- `q10` (Parquet sink) was deferred to `v0.37` (cold-tier sinks).
- These do not overlap with the retraction correctness of `q0-q9` and `q12-q22`.

---

## Vertical Slices

### S1 — Retraction Test Harness Core & Q0–Q3 Verification (LFS)

**What:**
- Create `crates/rockstream-oracle/tests/nexmark_retraction.rs` if it does not exist.
- Add `rockstream-gateway` to `dev-dependencies` in `crates/rockstream-oracle/Cargo.toml`.
- Implement a test runner setup in `nexmark_retraction.rs` that:
  - Spawns `GatewayServer` using LFS-backed `ShardDb`.
  - Connects to pgwire via `tokio-postgres`.
  - Registers schemas for `person`, `auction`, and `bid`.
  - Registers views `q0–q3`.
  - Ingests initial randomized base events, verifies `incremental == batch` against DataFusion.
  - Applies a mixture of updates/deletes (mixed inserts, updates, and deletes) to the tables via pgwire and verifies `incremental == batch` against DataFusion.
- Implement integration test `test_nexmark_q0_q3_retraction_lfs`.

**Exit test (green):**
- Integration test `test_nexmark_q0_q3_retraction_lfs` — registers schemas, views, applies base events + updates/deletes, and asserts bit-identical correctness.

---

### S2 — Complete LFS Retraction Verification for Q4–Q9 and Q12–Q22

**What:**
- Extend the retraction test harness to register views `q4–q9` and `q12–q22` on LFS.
- Implement integration tests:
  - `test_nexmark_q4_q9_retraction_lfs`
  - `test_nexmark_q12_q22_retraction_lfs`
- Verify retraction correctness under mixed sequences.

**Exit test (green):**
- Integration tests `test_nexmark_q4_q9_retraction_lfs` and `test_nexmark_q12_q22_retraction_lfs` pass.

---

### S3 — MinIO (S3 via TestContainers) Integration for Retraction Tests

**What:**
- Wire `testcontainers-modules::minio::MinIO` to run retraction tests over a MinIO-backed ShardDb.
- Implement:
  - `test_nexmark_q0_q9_retraction_minio`
  - `test_nexmark_q12_q22_retraction_minio`
- Gracefully skip if Docker/TestContainers is not available.

**Exit test (green):**
- Integration tests `test_nexmark_q0_q9_retraction_minio` and `test_nexmark_q12_q22_retraction_minio` pass.

---

### S4 — 100 Seeds Verification Soak

**What:**
- Implement a soak test in `nexmark_retraction.rs` that runs retraction correctness tests under 100 randomized seeds to satisfy the proof obligation.
- Implement `test_nexmark_retraction_100_seeds_lfs`.

**Exit test (green):**
- Integration test `test_nexmark_retraction_100_seeds_lfs` passes.

---

### S5 — Criterion Micro-Benchmarks for Delta Propagation Cost & Delta Amplification

**What:**
- Add `nexmark` benchmark definition to `crates/rockstream-ops/Cargo.toml`.
- Create `crates/rockstream-ops/benches/nexmark.rs`.
- Benchmark delta propagation cost (µs/row and delta amplification factor) at 0.1%, 1%, and 10% change rates.
- Assert that delta amplification factor is $\le 10\times$ for all stateful queries.

**Exit test (green):**
- Criterion benchmark runs successfully via `rtk cargo bench --package rockstream-ops --bench nexmark`.

---

## Test Commitments (Oracle & Durability)

No new physical engine operators or durability paths are introduced in this phase. The scope is SQL retraction verification and benchmark orchestration.

| Slice | Oracle/property test | LFS test | MinIO/TC test |
|---|---|---|---|
| Slice | Oracle/property test | LFS test | MinIO/TC test |
| S1 | `test_nexmark_q0_q3_retraction_lfs` | `test_nexmark_q0_q3_retraction_lfs` | — |
| S2 | `test_nexmark_q4_q9_retraction_lfs`, `test_nexmark_q12_q22_retraction_lfs` | `test_nexmark_q4_q9_retraction_lfs`, `test_nexmark_q12_q22_retraction_lfs` | — |
| S3 | — | — | `test_nexmark_q0_q9_retraction_minio`, `test_nexmark_q12_q22_retraction_minio` |
| S4 | `test_nexmark_retraction_100_seeds_lfs` | `test_nexmark_retraction_100_seeds_lfs` | — |
| S5 | `bench_nexmark` (amplification check) | `bench_nexmark` | — |

No range deletion is used in the nexmark queries or ingestion paths.

---

## Proof Mapping

| Proof claim | Mapped test(s) |
|---|---|
| **P1** Every Nexmark view (q0–q9, q12–q22) maintains `incremental == batch` after mixed INSERT+UPDATE+DELETE sequences | `test_nexmark_q0_q3_retraction_lfs` (S1), `test_nexmark_q4_q9_retraction_lfs` (S2), `test_nexmark_q12_q22_retraction_lfs` (S2), `test_nexmark_q0_q9_retraction_minio` (S3), `test_nexmark_q12_q22_retraction_minio` (S3) |
| **P2** Correctness maintains under 100 randomised seeds | `test_nexmark_retraction_100_seeds_lfs` (S4) |
| **P3** Delta amplification factor ≤10× for all stateful queries | `bench_nexmark` (S5) |
| **P4** Criterion benchmarks record baseline numbers for delta propagation cost at 0.1%/1%/10% change rates | `bench_nexmark` (S5) |
| **P5** All tests pass on LFS and MinIO | `test_nexmark_q0_q3_retraction_lfs` (S1), `test_nexmark_q4_q9_retraction_lfs` (S2), `test_nexmark_q12_q22_retraction_lfs` (S2), `test_nexmark_q0_q9_retraction_minio` (S3), `test_nexmark_q12_q22_retraction_minio` (S3), `test_nexmark_retraction_100_seeds_lfs` (S4) |

Every proof claim has at least one test mapped to it.

---

## Bounded Buffers & Queues

No new in-memory queues or unbounded buffers are introduced in this phase, as events are generated on demand and views are refreshed during transaction boundaries.
