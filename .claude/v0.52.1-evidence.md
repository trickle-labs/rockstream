# v0.52.1 Phase 4 evidence — PASS

S3 snapshot construction now fetches and converts only one bounded object
chunk per call. The gateway resumes the next chunk from the durable M3 cursor;
no snapshot path materializes all fenced rows or batches.

## Executed tests

| Test | Command | Result | Output excerpt |
| --- | --- | --- | --- |
| Fence property | `rtk cargo test -p rockstream-connectors snapshot_delta_fence_property_exactly_once_under_restarts` | PASS | Exit 0; passing output suppressed by the required summary filter. |
| LFS cursor M3 atomicity | `rtk cargo test -p rockstream-connectors --test source_durability_lfs_tests backfill_cursor_m3_atomicity_lfs` | PASS | Exit 0; passing output suppressed by the required summary filter. |
| MinIO cursor M3 atomicity | `rtk cargo test -p rockstream-connectors --test source_durability_minio_tests backfill_cursor_m3_atomicity_minio` | PASS | Exit 0; passing output suppressed by the required summary filter. |
| Direct catalog status row | `rtk cargo test -p rockstream-gateway show_backfill_status_reports_exact_running_row` | PASS | Exit 0; passing output suppressed by the required summary filter. |
| Raw-pgwire status and publication gate | `rtk cargo test -p rockstream-gateway --test source_ddl_tests show_backfill_status`; `rtk cargo test -p rockstream-gateway --test source_ddl_tests backfill_publication_gate_blocks_partial_relation` | PASS | Raw pgwire proves status reachability, the exact seven-column phase/cursor/estimate row, exact `RS-4022` and `RS-2001` negatives, and a separate pre-publication SELECT rejection. |
| Bounded cursor cleanup | `rtk cargo test -p rockstream-connectors --test source_durability_lfs_tests backfill_` | PASS | LFS cleanup verifies exact removal of committed checkpoint and cursor via bounded scans and point deletes. |
| Gateway and connector units | `rtk cargo test -p rockstream-gateway --lib`; `rtk cargo test -p rockstream-connectors --lib` | PASS | Exit 0; passing output suppressed by the required summary filter. |
| Atomic M3 lifecycle (LFS) | `rtk cargo test -p rockstream-connectors --test source_durability_lfs_tests backfill_m3_commits_output_cursor_checkpoint_and_frontier_lfs` | PASS | One batch durably commits exact output row, frontier, committed checkpoint, fence cursor, and `RUNNING` lifecycle record. |
| Atomic M3 lifecycle (MinIO) | `rtk cargo test -p rockstream-connectors --test source_durability_minio_tests backfill_m3_commits_output_cursor_checkpoint_and_frontier_minio` | PASS | TestContainers MinIO proves the same exact M3 record set. |
| Fence restart (LFS) | `rtk cargo test -p rockstream-connectors --test source_durability_lfs_tests fence_restart_has_no_gap_or_overlap_lfs` | PASS | Reopens the same SlateDB filesystem store and asserts the exact three output records, checkpoint, and `RUNNING` lifecycle. |
| Fence restart (MinIO) | `rtk cargo test -p rockstream-connectors --test source_durability_minio_tests fence_restart_has_no_gap_or_overlap_minio` | PASS | Reopens the same TestContainers MinIO bucket and asserts the exact three output records, checkpoint, and `RUNNING` lifecycle. |
| Lifecycle/publication durability (LFS) | `rtk cargo test -p rockstream-gateway --test backfill_lifecycle_lfs_tests unpublished_view_never_reads_partial_lfs` | PASS | Reopens the same local SlateDB store with exact partial output and `CATCHING_UP` lifecycle metadata, then raw pgwire rejects the view with the complete `RS-4022` message. |
| Lifecycle/publication durability (MinIO) | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests unpublished_view_never_reads_partial_minio` | PASS | Reopens the same Testcontainers MinIO-backed store with the exact partial output and `CATCHING_UP` lifecycle metadata, then raw pgwire rejects the view with the complete `RS-4022` message. |
| All kill-point cursor recovery (LFS) | `rtk cargo test -p rockstream-connectors --test source_durability_lfs_tests resume_all_three_kill_points_from_committed_cursor_lfs` | PASS | Reopens after committed `SNAPSHOTTING`, `CATCHING_UP` snapshot, and `CATCHING_UP` live epochs; exact output is `row-1,row-2,row-3` with final `RUNNING` lifecycle and no replay. |
| All kill-point cursor recovery (MinIO) | `rtk cargo test -p rockstream-connectors --test source_durability_minio_tests resume_all_three_kill_points_from_committed_cursor_minio` | PASS | Reopens the same Testcontainers MinIO state after each boundary and proves the exact output and final lifecycle. |
| Live-delta cap (LFS) | `rtk cargo test -p rockstream-gateway --test backfill_lifecycle_lfs_tests full_live_delta_buffer_returns_rs4020_lfs` | PASS | Exact `RS-4020` rejection occurs before the LFS-backed shard has any durable write. |
| Live-delta cap (MinIO) | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests full_live_delta_buffer_returns_rs4020_minio` | PASS | Exact `RS-4020` rejection occurs before the MinIO-backed shard has any durable write. |
| Seeded backfill restart simulation | `rtk cargo test -p rockstream-sim --features simulation --test backfill_fence_sim_tests backfill_fence_simruntime_restarts_preserve_exactly_once` | PASS | Seeds `0x5201..0x5203` exercise snapshot capture, M3 prepare, delta drain, publish, and restart after epochs 1 and 2; exact output has no replay. |
| Adapter fence capture | `rtk cargo test -p rockstream-connectors --lib cdc_fence_captures_committed_snapshot_and_queued_live_lsn`; `rtk cargo test -p rockstream-connectors --lib test_s3_source_basic` | PASS | PostgreSQL CDC captures committed snapshot LSN and queued live LSN without consuming input; S3 captures the stable sorted file manifest. Kafka captures its current assigned/recovered offsets. |
| Separate backfill admission | `rtk cargo test -p rockstream-gateway --lib backfill_budget_rejects_without_degrading_running_view` | PASS | Reservation rejects with `RS-4021` without changing a running view; live-delta cap rejects with `RS-4020`. |
| Source/table binding | `rtk cargo test -p rockstream-gateway --test source_ddl_tests create_source_binds_same_named_existing_table_schema` | PASS | A source created after same-named `CREATE TABLE` records and returns the exact table schema; metadata-only source DDL remains unbound. |
| Raw pgwire S3 lifecycle, live ingestion, retraction, update, and restart | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests s3_source_backfill_reaches_pgwire_and_publishes_exact_rows_minio` | PASS | Real MinIO Testcontainers input flows through `CREATE SOURCE`, bounded snapshot/M3 publication, a later object while running, restart with reconstructed catalog metadata, then another later object. Schema-driven JSON preserves `TEXT` and `DECIMAL(12,2)` and the maintained aggregate follows exact transitions `30.75 → 36 → restart → 43 → delete → 36 → update → 40`; the update’s old/new weighted images share one M3 and durable cursor `6`. |
| Raw pgwire multi-source join lifecycle and restart | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests two_s3_sources_publish_a_join_only_after_both_snapshots_complete` | PASS | Two independently fenced S3 sources are driven before one shared publication. The exact inner join publishes only after both snapshots, accepts a left-side live record, survives gateway reconstruction with both cursors, then accepts a right-side live record; no rows are replayed. |
| Raw pgwire multi-source TEXT/FULL join | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests two_s3_text_sources_drive_full_join_retractions` | PASS | Two schema-driven TEXT-key S3 sources publish exact `FULL JOIN` unmatched rows, then a right-side live record retracts and replaces the left unmatched output in the same source lifecycle. |
| Raw pgwire source-backed tumbling window | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests s3_source_backfill_updates_tumbling_window_exactly` | PASS | A fenced S3 snapshot creates the exact two TUMBLE windows; a later S3 object updates only the affected window through the running M3 worker. |
| Raw pgwire source-backed ROW_NUMBER | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests s3_source_backfill_updates_row_number_exactly` | PASS | A fenced S3 snapshot publishes the exact partitions and ranks; a later source record retracts/replaces the affected partition so every final row has its exact rank. |
| Raw pgwire source-backed SESSION window | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests s3_source_backfill_merges_session_windows_exactly` | PASS | A fenced S3 snapshot creates exact session rows; a later record merges the affected session, retracting the old row and publishing the exact merged interval. The gateway is then rebuilt on the same MinIO state and returns the identical merged rows. |
| Raw pgwire source-backed HOP window | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests s3_source_backfill_updates_hop_windows_exactly` | PASS | A fenced S3 snapshot publishes the exact duplicate-preserving HOP buckets; a later event creates two new buckets and the complete result is asserted. |
| Raw pgwire Kafka replay, restart, and live ingestion | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests kafka_source_backfill_and_live_updates_reach_pgwire` | PASS | Real Kafka Testcontainers records preserve `TEXT` and `DECIMAL(12,2)` through the schema-driven JSON adapter, replay to `30.75`, advance to `36`, survive gateway reconstruction on the same ShardDb and consumer group without replay, then advance to exact output `43` and durable cursor `4`. |
| Raw pgwire PostgreSQL CDC pgoutput lifecycle and restart | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests postgres_cdc_pgoutput_backfill_live_update_and_restart_reach_pgwire` | PASS | A PostgreSQL Testcontainer with native logical decoding proves `CREATE SOURCE … TYPE postgres_cdc FORMAT pgoutput`, repeatable-read snapshot fence, insert/update preimages, M3-only slot acknowledgement, gateway reconstruction, and one later upstream insert without replay. |
| Bounded S3 snapshot and restart | `rtk cargo test -p rockstream-connectors bounded_snapshot_resumes_from_the_committed_chunk_cursor`; `rtk cargo test -p rockstream-gateway source_snapshot_restart_resumes_at_committed_cursor_without_replay` | PASS | A committed chunk cursor resumes only the remaining snapshot rows; during `SNAPSHOTTING`, exact status reports epoch 1, one row remaining, and estimate 2,001; gateway restart then produces the exact 2,001-row output and publishes at epoch 3. |
| Multi-million bounded snapshot, all restart points | `rtk cargo test -p rockstream-connectors --test backfill_resume_tests multi_million_rows_resume_mid_snapshot_mid_interleave_mid_catch_up_exact_oracle` | PASS | An object-store S3 source streams 2,000,001 fenced rows in 10,000-row chunks. It recreates the source after a committed mid-snapshot cursor, after the first interleaved live delta, and immediately before publication; the exact ordered result is all snapshot IDs plus the three post-fence IDs once each. |
| Multi-chunk gateway S3 lifecycle | `rtk cargo test -p rockstream-gateway --test source_ddl_postgres_webhook_tests backfill_preserves_running_view_freshness_slo_multi_worker` | PASS | The real MinIO pgwire path processes a 50,000-row source in multiple bounded chunks while preserving the separate running view's freshness SLO. |
| Stable S3 live cursor | `rtk cargo test -p rockstream-connectors manifest_live_cursor_delivers_a_later_object_that_sorts_before_the_fence` | PASS | The durable live cursor is a per-object manifest, so a post-fence object sorted before an existing key is delivered exactly once. |
| S3 live byte cap | `rtk cargo test -p rockstream-connectors manifest_live_cursor_rejects_an_oversize_record_without_advancing` | PASS | An oversized record returns the coded poll failure before advancing its manifest cursor; a later bounded poll receives the exact record once. |
| Schema-driven S3 rows | `rtk cargo test -p rockstream-connectors json_rows_` | PASS | The connector preserves bound `TEXT` and `DECIMAL(12,2)` values in its weighted Arrow batch and rejects a malformed boolean with exact `RS-4004`; it no longer coerces missing or non-integer cells to zero. |
| Weighted S3 source changes | `rtk cargo test -p rockstream-connectors json_change_rows_preserve_explicit_delete_weights` | PASS | S3 JSON now accepts either a legacy row array or `{values, weight}` change record; explicit negative weights reach the Arrow batch and the raw-pgwire MinIO lifecycle proof validates delete and old/new update images through M3. |
| Durable TEXT-key state | `rtk cargo test -p rockstream-ops utf8_packer_state_restores_from_the_caller_owned_batch`; `rtk cargo test -p rockstream-ops text_group_by_restores_its_surrogate_before_processing_live_rows` | PASS | UTF-8 surrogate mappings enter the caller-owned M3 batch and a recompiled TEXT aggregate restores them before post-restart input. |
| Operator matrix | `rtk cargo test -p rockstream-gateway --test backfill_operator_matrix_tests` | PASS | Every named aggregate, join, and window cell uses native PostgreSQL pgoutput snapshot/live insert-delete/restart. TEXT and DECIMAL rows are decoded from native text tuples into typed Arrow batches; exact final oracle rows and durable epoch `3` are asserted. |

## Proof-claim audit

| Claim | Required proof | Result | Evidence / gap |
| --- | --- | --- | --- |
| Restart at all three kill points resumes exactly and equals batch | Resume test, LFS/MinIO pairs, matrix | PASS | The named 2,000,001-row object-store proof recreates its source at each kill point and asserts the full ordered oracle; LFS/MinIO pairs, seeded simulation, and the source-backed matrix independently prove committed-cursor recovery. |
| Fence has no gap or overlap under restarts | Property, LFS/MinIO pairs, SimRuntime | PASS | Property, LFS/MinIO reopen pairs, seeded simulation, S3/Kafka gateway restart, PostgreSQL pgoutput gateway restart, and the full source-backed matrix all pass. |
| Unpublished views never expose partial rows | Gateway gate and LFS/MinIO lifecycle tests | PASS | Raw pgwire proves the exact `RS-4022` rejection for `CATCHING_UP`; LFS and Testcontainers MinIO pairs reopen the persisted partial output and lifecycle metadata before repeating that rejection. |
| Running view freshness survives concurrent backfill | `source_ddl_postgres_webhook_tests::backfill_preserves_running_view_freshness_slo_multi_worker` | PASS | While a 50,000-row bounded S3 backfill runs, a separate already-running source view receives a later object and reaches its exact aggregate within two seconds; the bulk view then publishes its exact 50,000-row aggregate. |
| Published view continues ingesting later source records | Long-lived source worker plus source-backed pgwire test | PASS | S3, Kafka, and PostgreSQL pgoutput all ingest later source records before and after gateway reconstruction through M3. |
| Status reaches real dispatcher over raw pgwire with exact fields | Raw-pgwire reachability and status tests | PASS | The status tests prove the route and exact fields; S3, Kafka, and PostgreSQL pgoutput lifecycle tests prove the durable cursor/estimate source. |
| Buffers and scans are bounded, observable, and reject/backpressure | Budget, LFS/MinIO, cursor-limit tests | PASS | `RS-4020`, `RS-4021`, and `RS-4019` guards are tested at their boundaries; LFS and MinIO prove the live-delta cap rejects before durable state changes. |
| Cleanup avoids SlateDB range deletion | Named cleanup test plus cursor LFS test | PASS | `source_durability_lfs_tests::backfill_cleanup_uses_bounded_scan_and_point_delete` proves exact cleanup with `scan_prefix_bounded` and `WriteBatch::delete`. |

## Dispatch-wiring audit

| Path | Result | Evidence / gap |
| --- | --- | --- |
| Create parser | PASS | Existing source DDL binds same-named tables and accepts S3, Kafka, and PostgreSQL CDC options. |
| Create dispatcher | PASS | Bound S3, Kafka, and native PostgreSQL pgoutput materialized views route through the durable coordinator across every dependency before publication. |
| Create executor | PASS | Gateway constructs S3, Kafka, and INT/BIGINT/TEXT/DECIMAL PostgreSQL pgoutput runtimes, captures/persists one fence/cursor per bound source, and starts weakly-owned live workers after shared publication and recovery. |
| Atomic persistence | PASS | Gateway M3 contains table input, output, operator state, frontier, checkpoint, fence cursor, and lifecycle; LFS/MinIO atomic tests and raw-pgwire S3/Kafka/PostgreSQL proofs cover creation and restart. |
| Catalog lifecycle | PASS | The persisted lifecycle rehydrates phase, per-source committed cursor, remaining/estimated rows, and published frontier before recovery selects publication or resumed backfill. |
| Admission | PASS | The source runtime reserves a separate live-delta lane, returns coded rejection before durable writes, and the multi-worker SLO proof confirms it does not impede a running view. |
| Status parser | PASS | Raw pgwire accepts the full statement and rejects missing names with `RS-2001`. |
| Status dispatcher | PASS | Raw-pgwire test reaches `dispatch_sync` and the catalog response encoder. |
| Status executor | PASS | S3, Kafka, and PostgreSQL pgoutput progress are persisted with each M3 and pgwire-proven through restart. Multi-source views expose a deterministic `source:epoch` cursor summary plus combined estimate. |
| Status response encoder | PASS | Raw-pgwire test asserts the complete seven-column row. |
| Publication gate | PASS | Shared `RS-4022` gates cover SELECT, query-time plans, COPY TO, and cursors; S3, Kafka, and PostgreSQL pgoutput lifecycles are durable. |

## Coverage matrix

`crates/rockstream-gateway/tests/backfill_operator_matrix_tests.rs` contains
all 28 named cells. Each uses a real PostgreSQL pgoutput fence, live
insert/delete M3, durable epoch/status assertion, and gateway restart before
checking the complete oracle rows.
