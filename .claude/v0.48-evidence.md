# v0.48 Evidence — Advanced DML & Scatter Pruning

Collected 2026-07-18 during Phase 4/5. Every proof claim from NEW_ROADMAP.md's v0.48 row and every additional proof obligation from `.claude/v0.48-plan.md` is backed by a runnable command below.

| Claim | Evidence | Status | Reproduce |
|---|---|---|---|
| `UPDATE`/`DELETE` parse `RETURNING` and malformed clauses return `RS-2022` | `rtk cargo test -p rockstream-gateway update_returning_star_parses` → `1 passed, 239 filtered out`; `rtk cargo test -p rockstream-gateway delete_returning_column_list_parses` → `1 passed, 239 filtered out`; `rtk cargo test -p rockstream-types advanced_dml_and_scatter_pruning_error_codes_registered` → `1 passed, 188 filtered out` (covers `RS-2013`, `RS-2017`, `RS-2022`) | PASS | See Notes A/B |
| `UPDATE ... RETURNING` uses true read-modify-write, preserves untouched columns, and dependent views stay `incremental == batch` | `rtk cargo test -p rockstream-gateway --test gateway_proof_tests update_read_modify_write_preserves_untouched_columns` → `1 passed`; `... update_returning_returns_new_row_after_commit` → `1 passed`; `... oracle_update_returning_incremental_equals_batch` → `1 passed, 100 filtered out`; durability: `rtk cargo test -p rockstream-gateway --test update_read_modify_write_durability_tests` → `2 passed` | PASS | See Notes C/D |
| `DELETE ... RETURNING` returns the pre-delete row, rollback discards buffered captures, and dependent views stay `incremental == batch` | `rtk cargo test -p rockstream-gateway --test gateway_proof_tests delete_returning_returns_row_that_existed_before_delete` → `1 passed`; `... delete_returning_discarded_on_rollback` → `1 passed`; `... oracle_delete_returning_incremental_equals_batch` → `1 passed, 100 filtered out`; durability: `rtk cargo test -p rockstream-gateway --test delete_returning_durability_tests` → `2 passed` | PASS | See Notes C/E |
| Frontier summaries persist `ShardColumnStats` (min/max + budget-capped Bloom) and survive restart | `rtk cargo test -p rockstream-types shard_column_stats_roundtrip_tests` → `1 passed`; `... bloom_filter_never_exceeds_budget_bytes_at_high_cardinality` → `1 passed`; `... config_scatter_pruning_defaults_roundtrip` → `1 passed`; `rtk cargo test -p rockstream-control checkpoint_persists_shard_column_stats_to_control_catalog` → `1 passed`; `rtk cargo test -p rockstream-control --test shard_column_stats_durability_tests` → `2 passed` | PASS | See Notes F/G |
| Multi-shard point reads safely bypass >90% of shards; pruning is safe under stale/racy checkpoint conditions | `rtk cargo test -p rockstream-gateway scatter_planner_prunes_shards_outside_min_max_bounds` → `1 passed`; `... scatter_planner_prunes_shards_via_bloom_negative` → `1 passed`; `... stale_shard_stats_falls_back_to_full_scatter_with_warning` → `1 passed`; `rtk cargo test -p rockstream-oracle scatter_pruning_result_set_equals_full_scatter_result_set` → `1 passed, 123 filtered out`; `rtk cargo test -p rockstream-sim --test shard_stats_checkpoint_sim_tests --features simulation` → `2 passed`; `rtk cargo test -p rockstream-gateway --test gateway_proof_tests proof_multi_shard_point_read_prunes_over_90_percent_of_shards` → `1 passed, 100 filtered out`; `... scatter_pruning_metrics_exported_correctly` → `1 passed, 100 filtered out` | PASS | See Notes H/I/J |
| Secondary indexes publish exact, non-probabilistic stats to the same pruning path | `rtk cargo test -p rockstream-gateway --test gateway_proof_tests create_index_publishes_precise_stats_immediately` → `1 passed, 100 filtered out`; `... indexed_column_stats_are_exact_not_probabilistic` → `1 passed, 100 filtered out` | PASS | See Note K |
| Ground rules: no range deletion on new persistence paths; new buffers stay bounded; no regressions | `rtk cargo clippy --workspace --all-targets -- -D warnings` → clean; `rtk bash scripts/check-error-codes.sh` → `OK`; `rtk bash scripts/check-invariant-pairs.sh` → `OK`; `rtk cargo test --workspace` → `1498 passed, 6 ignored (128 suites, 2246.89s)` | PASS | See Note L |

## Evidence Notes

### A. Parse / malformed RETURNING

```text
$ rtk cargo test -p rockstream-gateway update_returning_star_parses
cargo test: 1 passed, 239 filtered out (19 suites, 0.00s)

$ rtk cargo test -p rockstream-gateway delete_returning_column_list_parses
cargo test: 1 passed, 239 filtered out (19 suites, 0.00s)
```

### B. Error-code wiring (`RS-2013`, `RS-2017`, `RS-2022`)

```text
$ rtk cargo test -p rockstream-types advanced_dml_and_scatter_pruning_error_codes_registered
cargo test: 1 passed, 188 filtered out (3 suites, 0.00s)
```

`RS-2013` is now wired into the `UPDATE ... RETURNING` read-back miss path; `RS-2017` and `RS-2022` are registered with slugs, descriptions, severities, and next steps.

### C. `UPDATE` / `DELETE` correctness gates

```text
$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests update_read_modify_write_preserves_untouched_columns
cargo test: 1 passed, 100 filtered out (1 suite, 0.22s)

$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests update_returning_returns_new_row_after_commit
cargo test: 1 passed, 100 filtered out (1 suite, 0.21s)

$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests delete_returning_returns_row_that_existed_before_delete
cargo test: 1 passed, 100 filtered out (1 suite, 0.21s)

$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests delete_returning_discarded_on_rollback
cargo test: 1 passed, 100 filtered out (1 suite, 0.11s)
```

The v0.48 implementation also now resolves transaction-local updates/deletes against buffered writes, fixing multi-update-in-one-transaction correctness (proved by the green Nexmark retraction workspace tests in Note L).

### D. `UPDATE ... RETURNING` oracle + durability

```text
$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests oracle_update_returning_incremental_equals_batch
cargo test: 1 passed, 100 filtered out (1 suite, 5.14s)

$ rtk cargo test -p rockstream-gateway --test update_read_modify_write_durability_tests
cargo test: 2 passed (1 suite, 3.00s)
```

### E. `DELETE ... RETURNING` oracle + durability

```text
$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests oracle_delete_returning_incremental_equals_batch
cargo test: 1 passed, 100 filtered out (1 suite, 1.86s)

$ rtk cargo test -p rockstream-gateway --test delete_returning_durability_tests
cargo test: 2 passed (1 suite, 2.24s)
```

### F. `ShardColumnStats` type/config/budget proofs

```text
$ rtk cargo test -p rockstream-types shard_column_stats_roundtrip_tests
cargo test: 1 passed, 188 filtered out (3 suites, 0.00s)

$ rtk cargo test -p rockstream-types bloom_filter_never_exceeds_budget_bytes_at_high_cardinality
cargo test: 1 passed, 188 filtered out (3 suites, 0.01s)

$ rtk cargo test -p rockstream-types config_scatter_pruning_defaults_roundtrip
cargo test: 1 passed, 188 filtered out (3 suites, 0.00s)
```

### G. Checkpoint publish + durability

```text
$ rtk cargo test -p rockstream-control checkpoint_persists_shard_column_stats_to_control_catalog
cargo test: 1 passed, 166 filtered out (17 suites, 0.00s)

$ rtk cargo test -p rockstream-control --test shard_column_stats_durability_tests
cargo test: 2 passed (1 suite, 2.68s)
```

### H. Planner pruning + staleness fallback

```text
$ rtk cargo test -p rockstream-gateway scatter_planner_prunes_shards_outside_min_max_bounds
cargo test: 1 passed, 239 filtered out (19 suites, 0.01s)

$ rtk cargo test -p rockstream-gateway scatter_planner_prunes_shards_via_bloom_negative
cargo test: 1 passed, 239 filtered out (19 suites, 0.00s)

$ rtk cargo test -p rockstream-gateway stale_shard_stats_falls_back_to_full_scatter_with_warning
cargo test: 1 passed, 239 filtered out (19 suites, 0.00s)
```

### I. Safety oracle + seeded simulation

```text
$ rtk cargo test -p rockstream-oracle scatter_pruning_result_set_equals_full_scatter_result_set
cargo test: 1 passed, 123 filtered out (2 suites, 0.09s)

$ rtk cargo test -p rockstream-sim --test shard_stats_checkpoint_sim_tests --features simulation
cargo test: 2 passed (1 suite, 0.00s)
```

Permanent regression seeds: `4801` (`shard_stats.checkpoint_worker_crash`) and `4802` (`shard_stats.torn_read_race`).

### J. Headline >90% bypass + metrics

```text
$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests proof_multi_shard_point_read_prunes_over_90_percent_of_shards
cargo test: 1 passed, 100 filtered out (1 suite, 1.84s)

$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests scatter_pruning_metrics_exported_correctly
cargo test: 1 passed, 100 filtered out (1 suite, 0.00s)
```

`proof_multi_shard_point_read_prunes_over_90_percent_of_shards` asserts the planner keeps only shard `42` and that `scatter_shards_pruned_total / scatter_shards_total > 0.90`.

### K. Secondary-index exact stats

```text
$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests create_index_publishes_precise_stats_immediately
cargo test: 1 passed, 100 filtered out (1 suite, 0.16s)

$ rtk cargo test -p rockstream-gateway --test gateway_proof_tests indexed_column_stats_are_exact_not_probabilistic
cargo test: 1 passed, 100 filtered out (1 suite, 0.11s)
```

`MARK INDEX ... READY` now publishes exact membership filters for indexed columns into `shard_stats`, and the planner prunes an in-range non-member (`amy@example.com`) with zero false positives.

### L. Full regression / hygiene gates

```text
$ rtk cargo fmt --check
# clean, no output

$ rtk cargo clippy --workspace --all-targets -- -D warnings
cargo clippy: No issues found

$ rtk bash scripts/check-error-codes.sh
OK: all logged/returned errors carry an RS-XXXX code.

$ rtk bash scripts/check-invariant-pairs.sh
OK: all 41 FizzBee invariants have paired Rust coverage.

$ rtk cargo test --workspace
cargo test: 1498 passed, 6 ignored (128 suites, 2246.89s)
```

No item from v0.48 is deferred.
