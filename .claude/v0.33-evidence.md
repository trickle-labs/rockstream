# v0.33 Evidence — Nexmark Schemas & Deterministic Event Generator

This file contains the evidence collected during Phase 4: Prove, demonstrating that all requirements and proof claims mapped in the plan are fully satisfied.

## Proof Evidence Table

| Proof Claim | Mapped Test(s) | Execution Command | Result & Output Excerpt | Status |
| :--- | :--- | :--- | :--- | :--- |
| **P1** Same seed produces an identical event stream across two runs | `test_nexmark_generator_determinism` (S1) | `rtk cargo test -p rockstream-sim test_nexmark_generator_determinism` | `cargo test: 1 passed, 139 filtered out (8 suites, 0.01s)` | **PASS** |
| **P2** Generator distribution is within 1% of target ratios (~46 bids : 3 auctions : 1 person) | `test_nexmark_generator_distribution` (S1) | `rtk cargo test -p rockstream-sim test_nexmark_generator_distribution` | `cargo test: 1 passed, 139 filtered out (8 suites, 0.05s)` | **PASS** |
| **P3** All three tables accept `INSERT` via pgwire and return correct row counts via `SELECT` | `test_nexmark_ingestion_lfs` (S3)<br>`test_nexmark_ingestion_minio` (S4) | `rtk cargo test -p rockstream-sql --all-features --test nexmark test_nexmark_ingestion_lfs`<br>`rtk cargo test -p rockstream-sql --all-features --test nexmark test_nexmark_ingestion_minio` | `cargo test: 1 passed, 2 filtered out (1 suite, 0.11s)` (LFS)<br>`cargo test: 1 passed, 2 filtered out (1 suite, 5.54s)` (MinIO) | **PASS** |
| **P4** Schema round-trips through the catalog | `test_nexmark_schema_creation_and_catalog` (S2) | `rtk cargo test -p rockstream-sql --all-features --test nexmark test_nexmark_schema_creation_and_catalog` | `cargo test: 1 passed, 2 filtered out (1 suite, 0.02s)` | **PASS** |
| **P5** Verified on LFS and MinIO via TestContainers | `test_nexmark_ingestion_lfs` (S3)<br>`test_nexmark_ingestion_minio` (S4) | `rtk cargo test -p rockstream-sql --all-features --test nexmark` | `cargo test: 3 passed (1 suite, 6.63s)` | **PASS** |

## Ingestion Count Verification Details

- **LFS Storage**: Successfully instantiated GatewayServer with memory/file `ShardDb`. Ingested 500 events within transaction blocks with session-level `idempotency_key = 'nexmark-lfs-batch-1'`. Verified `SELECT COUNT(*)` on base tables: `person`, `auction`, and `bid` matching generated totals exactly.
- **MinIO Storage**: Started local MinIO via TestContainers. Created `testbucket`. Successfully instantiated gateway with MinIO `ShardDb`. Ingested 500 events using same seed (42) and transaction block with session-level `idempotency_key = 'nexmark-minio-batch-1'`. Verified `SELECT COUNT(*)` matches generated counts.
