# v0.56.1 Phase 4 proof evidence — PASS

Run date: 2026-08-16

## Exit checklist

- [x] Reachability tests (e2e pgwire): not applicable; this version has no SQL
  or wire-protocol feature.
- [x] Negative tests: not applicable to SQL/wire; CLI invalid-export coverage
  passes.
- [x] Dispatch-wiring audit: the CLI export and restore parser, dispatch,
  executor, and response paths are connected and covered by exact-output CLI
  tests.
- [x] Coverage matrix: all three LFS and all three real
  MinIO/TestContainers cells passed.
- [x] All other Proof obligations met.

## Proof commands and results

| Command | Result |
|---|---|
| `rtk docker info >/dev/null` | PASS — Docker daemon was ready before the MinIO run. |
| `rtk proxy cargo test -p rockstream-control --test disaster_recovery_tests` | PASS — 7 passed: all LFS and all MinIO/TestContainers bit-identity, sustained-write, and truncation cells, plus the no-range-delete test. MinIO cells each created their TestContainers-owned MinIO process. |
| `rtk proxy cargo test -p rockstream-control --test checkpoint_export_sim_tests --features simulation` | PASS — `m3_export_selection_is_single_epoch_under_buggify` passed with seed `0x56_01_0001`. |
| `rtk proxy cargo test -p rockstream-control checkpoint_export_` | PASS — 6 passed: named bounds, corrupt-resume rejection, path traversal rejection, sequential copy/marker-last, no range deletion, and bounded scan. |
| `rtk proxy cargo test -p rockstream-cli --test cli_disaster_recovery_tests --test disaster_recovery_runbook_tests --test cli_doc_coverage_tests` | PASS — 5 relevant tests passed: exact CLI/audit output, `RS-5035` missing-export rejection, CLI documentation coverage/conformance, and the documented measured-RPO/RTO drill. |

## Proof claims

| Claim | Evidence | Result |
|---|---|---|
| A full checkpoint in a separate bucket/region restores into a new cluster with every view bit-identical. | `lfs_export_restore_fresh_cluster_is_bit_identical` and real-TestContainers `minio_tc_export_restore_fresh_cluster_is_bit_identical` passed. | PASS |
| The drill follows only `docs/disaster-recovery.md`, and reports measured RPO and RTO. | `runbook_only_drill_records_measured_rpo_and_rto` passed. | PASS |
| Export during sustained writes restores one committed epoch with no partial epoch or torn view. | LFS and real-TestContainers MinIO `*_export_under_writes_restores_one_committed_epoch` tests passed; seeded SimRuntime M3 selection proof passed. | PASS |
| A deliberately truncated export fails closed with `RS-5035` and never produces partial cluster state. | LFS and real-TestContainers MinIO `*_truncated_export_fails_closed_rs5035` tests passed. | PASS |
| The export/restore route is a real documented CLI path with authorization and auditable result. | `checkpoint_export_restore_cli_outputs_and_audits_exactly`, `checkpoint_restore_rejects_missing_export_with_rs5035`, and documentation coverage/conformance tests passed. | PASS |
| No DR cleanup/copy path depends on SlateDB range deletion. | `export_restore_uses_no_slatedb_range_deletion` and `checkpoint_export_source_has_no_range_delete_path` passed. | PASS |
| DR resources remain bounded and a coordinated/in-flight export cannot mix epochs. | SimRuntime proof and six checkpoint-export bound/integrity tests passed. | PASS |

## Dispatch-wiring verification

`CheckpointCommand::Export` and `CheckpointCommand::Restore` are parsed and
dispatched in `crates/rockstream-cli/src/main.rs`; they invoke
`run_checkpoint_export` and `run_checkpoint_restore`, which reach the
transport executors and `CheckpointExportOutcome`/restore response formatting.
`checkpoint_export_restore_cli_outputs_and_audits_exactly` drives both routes
from argument parsing through text/JSON response encoding and audit output.
No path is MISSING.

## Remaining blockers

None.
