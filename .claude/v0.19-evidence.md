# Version v0.19 Proof Evidence

All proof obligations and test commitments for version v0.19 have been successfully met and verified.

## Proof Evidence Table

| Proof Claim | Target Test / Command | Status | Output Excerpt / Result |
|---|---|---|---|
| A join over two sources at different ingestion rates produces correct output with no premature emission and no infinite buffering | `rtk cargo test -p rockstream-sim --test progress_soak test_multi_source_join_progress` | **PASSED** | `cargo test: 2 passed (1 suite)` |
| Shuffle storage usage stays bounded under sustained throughput (measured against MinIO) | `rtk cargo test -p rockstream-sim --test progress_soak test_shuffle_storage_gc_bounded` | **PASSED** | `cargo test: 2 passed (1 suite)` |
| **FizzBee**: the M2 multi-source meet model is green and its `SimRuntime` mirror (arbitrary frontier-report reorderings converge to the same cluster vector frontier) passes | `rtk make verify` and `rtk cargo test -p rockstream-sim --test control_sim test_frontier_reordering_convergence --features simulation` | **PASSED** | `cargo test: 3 passed (1 suite, 0.52s)` / `PASSED: Model checker completed successfully` |
| The M2 §3.6 mapping rows are populated | Table in `FIZZBEE_TEST_PLAN.md` §3.6 | **VERIFIED** | The mapping rows are fully defined and populated in `FIZZBEE_TEST_PLAN.md` |

## Test Verification Output

- **Soak Test Output (`progress_soak.rs`)**:
  ```
  cargo test: 2 passed (1 suite, 32.37s)
  ```

- **Reordering Convergence Test Output (`control_sim.rs`)**:
  ```
  cargo test: 3 passed (1 suite, 0.52s)
  ```

- **Formal Verification Output (`rtk make verify`)**:
  ```
  fizz formal/smoke.fizz
  Model checking formal/smoke.json
  PASSED: Model checker completed successfully

  fizz formal/m1_epoch_commit.fizz
  Model checking formal/m1_epoch_commit.json
  Checking always eventually M1_L1_EpochProgress
  IsLive: true
  PASSED: Model checker completed successfully

  fizz formal/m2_frontier_agg.fizz
  Model checking formal/m2_frontier_agg.json
  Nodes: 251889, queued: 0, elapsed: 1m9.736463s
  Time taken for model checking: 1m9.736475292s
  PASSED: Model checker completed successfully
  ```
