# v0.59.8 Phase 5 Sign-off Evidence

## Results

| Proof claim | Evidence | Result |
|---|---|---|
| Correlated windows share exact physical slices, apply retractions, and stay bounded | `shared_window_tests` | PASS (4) |
| Hot keys split only for composable laws and normalize to a power of two | `hot_key_planning_tests` | PASS (2) |
| Power-of-two routing is deterministic and bounded; durable bucket state is preserved | `virtual_bucket_routing_tests`; `virtual_bucket_state_durability_tests` | PASS (8) |
| Snapshot-pinned, bounded chunk copy; committed-frontier cutover; delayed reclamation | `shard_migration_state_machine_tests` | PASS (13) |
| Epoch routing delivers each write to exactly the authoritative destination(s) | `dual_write_zero_loss_tests` | PASS (2) |
| Lease heat survives worker-death reassignment and decays before reassignment | `worker_death_causes_reassignment`; `heat_hint_decays_and_survives_reassignment` | PASS (2) |
| Shared window and migration type serialization/transition invariants | `rockstream-types` | PASS (278) |
| Formatting | `cargo fmt --check` | PASS |

## Commands

`cargo test -p rockstream-ops --test shared_window_tests --test virtual_bucket_state_durability_tests`

`cargo test -p rockstream-control --test hot_key_planning_tests --test shard_migration_state_machine_tests`

`cargo test -p rockstream-plan --test virtual_bucket_routing_tests`

`cargo test -p rockstream-runtime --test dual_write_zero_loss_tests`

`cargo test -p rockstream-control --lib worker_death_causes_reassignment`

`cargo test -p rockstream-control --lib heat_hint_decays_and_survives_reassignment`

`cargo test -p rockstream-types`

`cargo fmt --check`

R1 prerequisite: `evidence/r1-local/decision.json` records `GREEN` with 15
raw samples and three structural results on `MBP-M5Pro-48GB-v1`.
