# v0.45.7 Evidence — Error-Code Registry Enforcement Hardening

Reload context: `.claude/v0.45.7-plan.md` (deferred-item audit, live-codebase
audit, new-code table S1–S9 below cross-references that plan).

## S1 — `copy_state.rs::parse_copy_from_stmt` carries a registered code

All 8 previously-bare `Err(format!(...))` sites now return `RS-2021`
(`copy.malformed_stdin_statement`) via the registry, with actionable
`next_steps` text.
Test: `crates/rockstream-gateway/tests/copy_error_code_tests.rs::
copy_malformed_variants_return_rs2021`.
Run: `cargo test -p rockstream-gateway --test copy_error_code_tests` →
**2 passed** (`copy_malformed_variants_return_rs2021`,
`create_sink_malformed_ddl_variants_return_rs4007`).

## S2 — `server.rs` CREATE SINK option parser carries registered codes

`parse_create_sink_ddl`, `parse_create_sink_options`,
`parse_create_sink_option_value`, `parse_optional_u64_option`,
`parse_sql_single_quoted_string`, `split_top_level_comma_list` (~20 sites)
now embed `RS-4007` (existing code, previously only added by the wrapping
`create_sink_error_response`, now inline at construction so a bare textual
scan and direct unit tests of the parser both see it).
Test: `copy_error_code_tests.rs::create_sink_malformed_ddl_variants_return_rs4007`
— covers empty option value, unknown option, malformed quoting, and
non-numeric option-value variants. Passes (see S1 run above).

## S3 — `durable.rs`'s six failure modes are independently distinguishable

`RS_3010` split into: `RS-3011` (rate_limit_exhausted), `RS-3012`
(object_store_io_failure — both write and read paths), `RS-3013`
(buffer_capacity_exceeded), `RS-3014` (footer_serialization_failed),
`RS-3015` (footer_deserialization_failed), `RS-3016`
(footer_corrupt_or_undersized — covers both "truncated" and "footer length
larger than object" cases). `multiplexer.rs` reuses `RS-3017`
(exchange.ipc_shuffle_decode_error, new) for frame-coalescing failures
during compaction, replacing its two `RS_3010` reuse sites.
Test: `crates/rockstream-runtime/tests/durable_shuffle_error_code_tests.rs`,
9 tests: `rate_limit_exhausted_returns_rs3011`,
`generic_object_store_io_failure_returns_rs3012`,
`generic_object_store_io_failure_on_read_returns_rs3012`,
`buffer_capacity_exceeded_returns_rs3013`,
`footer_serialization_failure_code_is_registered_with_actionable_next_steps`,
`corrupt_footer_json_returns_rs3015`, `truncated_object_returns_rs3016`,
`footer_length_larger_than_object_returns_rs3016`,
`split_codes_are_pairwise_distinct` (asserts all six new codes are pairwise
unique — no residual overload).
Run: `cargo test -p rockstream-runtime --test durable_shuffle_error_code_tests`
→ **9 passed**.

## S4 — `serialization.rs::deserialize_zset` bare sites fixed

Both terminal `Err(...)` sites (no trailing `?`) now carry `RS-3017`
(`exchange.ipc_shuffle_decode_error`), the same code multiplexer.rs uses for
the related shuffle-decode failure family (kept in lock-step per the plan's
S3 coupling note). `loopback.rs` also updated to reference `RS-3018`
(`exchange.loopback_shard_not_active`, new) for its bare
`ok_or_else` sites.

## S5 — `sum_count.rs` / `weight_add.rs` accumulator decode sites fixed

Each file's one terminal bare-error site (accumulator wire-size mismatch)
now carries `RS-3501` (`merge.law_accumulator_decode_error`, new).

## S6 — Extended `check-error-codes.sh` (Rule 2) flags bare construction sites

`scripts/check-error-codes.sh` gained Pass 2: flags any
`Err(format!(...))`/`Err(String::from(...))`/`Err("...".to_string()/.into())`/
`.ok_or_else(|| format!(...))`/`.ok_or_else(|| "...")` site whose last
non-whitespace token before `;` isn't `?`, unless the text references
`RS-[0-9]{4}`, `error_code`, or `ErrorCode`. `#[cfg(test)]` modules and
`crates/*/src/bin/*.rs` are excluded per the plan's scope decisions.
Run: `./scripts/check-error-codes.sh` → `OK: all logged/returned errors
carry an RS-XXXX code.` (exit 0, real tree, zero violations).

## S7 — Self-test proves the check fails on a reintroduced bare error

`scripts/check-error-codes.test.sh`: mutates a copy of the tree by
reintroducing a bare `Err(format!("boom"))` in `crates/`, asserts the
extended script exits non-zero on the mutated copy, then asserts exit 0 on
the real tree.
Run: `bash scripts/check-error-codes.test.sh` → `OK: check-error-codes.sh
self-test passed.` (exit 0).
CI wiring: `.github/workflows/ci.yml` gained an `error-code checker
self-test` step (`bash scripts/check-error-codes.test.sh`) immediately
after the existing `check-error-codes.sh` step, so the self-test itself
runs on every push/PR, not just locally.

## S8 — Registry completeness for all new codes

`crates/rockstream-types/src/error_code.rs` registers `RS-2021`, `RS-3011`
through `RS-3018`, and `RS-3501`, each with a description and actionable
`next_steps` string. The registry's own exhaustiveness test (asserts every
registered code has non-empty description + next_steps, and all codes are
unique) passes.
Run: `cargo test -p rockstream-types error_code` → **6 passed, 174 filtered
out** (registry tests only; no regression in the other 174 rockstream-types
tests, which were simply not selected by the filter).

## S9 — Documentation (`DESIGN.md` §14.14 code table)

`DESIGN.md`'s error-code table updated with all 9 new codes (`RS-2021`,
`RS-3011`–`RS-3018`, `RS-3501`) in their correct owner ranges, none
colliding with the reserved `RS-2501`–`RS-2504` / `RS-3001`–`RS-3002`
ranges the plan's audit called out.
Verified: `git diff DESIGN.md` shows exactly the 9 new lines in the
error-code table, no other changes.

## S10 — No regression: full workspace test suite

Run: `cargo test --workspace` → **1381 passed, 6 ignored (98 suites,
1902.51s)**. Zero failures. The 6 ignored tests are pre-existing
soak/long-running tests gated behind `#[ignore]` from prior versions, not
new to this version.

## S11 — Style/lint gates

- `cargo fmt --check` → exit 0, no diff.
- `cargo clippy --workspace --all-targets -- -D warnings` → `No issues
  found`, exit 0.

## S12 — `make e2e`

Run: `make e2e` → `=== e2e PASSED ===`. LFS backend: 13/13 passed. MinIO
backend (via TestContainers/Docker): 6/6 passed (fencing, tiered store
routing, aged-SST cold-bucket migration, determinism gate, worker/control
e2e roles, storage API validation).

## S13 — `check-exit-criteria.sh`

Not yet runnable for v0.45.7 itself until the roadmap row is flipped to
`✅ Done` and `sign-offs/v0.45.7.md` exists (Phase 5 does this). Existing
prior-version sign-offs (`v0.45`–`v0.45.6`) remain green — confirmed no
regression: `./scripts/check-exit-criteria.sh` → `OK: v0.45`,
`OK: v0.45.1` … `OK: v0.45.6` (all prior Done versions have complete,
checked-off sign-offs).

## Deferred items

None. Every item in the roadmap's v0.45.7 Scope/Proof row is implemented,
tested, and documented above. `.claude/v0.45.7-plan.md` §1 confirms no
overlap with any prior version's deferred items either.
