# v0.68 Evidence

**Revision under test:** `72e1c1f7` plus the sign-off changes  
**Workspace version:** `0.68.0`  
**Date:** 2026-09-21  
**Backend:** local filesystem; E2E used the repository-managed local process setup and MinIO-backed migration test.

## V068-01

```text
rtk cargo test -p rockstream-control --test migration_record_durability_tests
rtk cargo test -p rockstream-control --test shard_migration_state_machine_tests
```

Result: 6 durability tests and 13 state-machine tests passed.

## V068-02

```text
rtk cargo test -p rockstream-cli --test r1_real_worker_data_plane_tests
```

Result: 2 tests passed, 1 explicitly ignored test requiring the direct gateway-to-worker source transport. The migration implementation is in `crates/rockstream-control/src/migration.rs`.

## V068-03–V068-05

Record durability, progress, lease fencing, and controller restart are covered by the control tests and real worker-process test above. The controller-restart check passed with 2 tests and 1 ignored transport-dependent test.

## V068-06

```text
rtk cargo test -p rockstream-sim --test sim_migration_crash_boundary_tests
rtk cargo test -p rockstream-sim --test shard_migration_sim_tests
```

Result: 1 and 3 tests passed respectively.

## V068-07–V068-10

The release-process worker lifecycle, pressure reporting, stable migration simulation, and bounded failure paths are covered by the real worker test, `crates/rockstream-control/src/skew.rs`, the state-machine suite, and the simulation suites. The worker process test passed on rerun after one host-pressure timing flake during the first workspace run.

## V068-11

```text
PATH="/Users/grove/projects/rockstream2/.cache/verus/0.2026.09.13.671956e:$PATH" rtk make verify
```

Result: 2047 and 68 Verus obligations verified; 30 manifest claims checked; all FizzBee specifications passed, including `formal/m6_shard_migration.fizz` (318 valid nodes, 0 errors).

## Common gates

```text
rtk cargo fmt --check
rtk cargo clippy -- -D warnings
rtk cargo test --workspace -- --test-threads=1
rtk make e2e
```

All passed. The E2E gate required escalated localhost process networking in this environment. The first parallel workspace run had one `AddrInUse` failure; the isolated test and the serial workspace rerun passed.
