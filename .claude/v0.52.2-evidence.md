# v0.52.2 proof evidence

| Proof claim | Test / command | Result | Output excerpt |
| --- | --- | --- | --- |
| Two-table transactions are complete epochs under restart/disconnect. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_atomicity_tests`; `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests` | PASS | RTK summary clean; exact envelope/rows/weights/LSN and raw-pgwire view rows asserted. |
| Large envelopes spill and remain batch-identical. | `rtk cargo test -p rockstream-gateway cdc_tx_spilled_two_tables_exact_atomic_batch_oracle` | PASS | RTK summary clean; exact two-relation spilled envelope asserted. |
| A shared physical slot feeds imported tables. | `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests` | PASS | RTK summary clean; raw pgwire DDL, exact view rows, and `pg_replication_slots` count `1` asserted. |
| Compatible relations persist history; incompatible relations block with `RS-1002`. | `rtk cargo test -p rockstream-gateway --test postgres_cdc_schema_evolution_tests`; `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests` | PASS | RTK summary clean; durable history records and all compatibility classifications are exact; real Postgres incompatible relation reports `BLOCKED` / exact `RS-1002` over pgwire. |
| Restart resumes only the committed LSN, without loss or duplication. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_durability_lfs_tests`; `rtk cargo test -p rockstream-connectors --test cdc_transaction_durability_minio_tests`; `rtk cargo test -p rockstream-sim --test cdc_transaction_sim_tests --features simulation` | PASS | RTK summary clean; output, frontier, checkpoint, lifecycle, and LSN are asserted as one exact committed epoch. |
| Buffering is bounded and backpressures with a coded error. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_atomicity_tests` | PASS | RTK summary clean; exact `RS-4014` error and paused state asserted. |
| Cleanup uses bounded scans and point deletes. | `rtk cargo test -p rockstream-connectors --test cdc_transaction_durability_lfs_tests` | PASS | RTK summary clean; `scan_prefix_bounded` + point deletes asserted; no `delete_range`. |
| Parser → dispatcher → executor → pgwire response is connected. | `rtk cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests`; `rtk make e2e` | PASS | RTK summary clean; `CREATE SOURCE`, view reads, `SHOW SOURCE STATUS`, and blocked-source status use raw pgwire. |

No new operator, performance, or soak claim is introduced by v0.52.2.
