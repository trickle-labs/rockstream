# v0.65 Proof Evidence — Complete Standalone Recovery, Backup, and Restore

Contract Version: `v0.65`  
Execution Date: 2026-09-12  
Status: PASS (all proof obligations met)  
Git Revision: `8ec179633f9eb3df1561384afef476511d46b220`  
Release Binary Digest (`target/release/rockstream`): `88992ed3249f4121c6af3ac7107dc27e3410b312649afeccf07e814f472d25d0`  

---

## 1. Summary of Proof Claims & Verification

All proof obligations defined in `.claude/0.65-plan.md`, `docs/implementation-plans/v0.65.md`, and `ROADMAP.md` §11 (`v0.65: Complete Standalone Recovery, Backup, and Restore`) have been fully executed, tested, and verified across all declared backends (`Unit, LFS, MinIO, SimRuntime, Release Process`).

| Proof Obligation / Claim | Target Test Suite / Verification Command | Result | Notes / Output Excerpt |
|---|---|---|---|
| **V065-01: Eight Recovery Categories Reconstruction & Idempotency** | `rtk proxy cargo test -p rockstream-cli --test disaster_recovery_standalone_tests test_eight_recovery_categories_reconstruction`<br>`rtk proxy cargo test -p rockstream-cli --test disaster_recovery_standalone_tests test_idempotency_replay_deduplication` | **PASS** (11 tests in suite) | All 8 mandatory categories reconstructed: catalog, base tables, operator state, view output multisets, frontier/epoch, secondary indexes, source cursors, and idempotency metadata. Zero duplicate effects on retry. |
| **V065-02: Recovery State Machine Traversal & Readiness Gate** | `rtk proxy cargo test -p rockstream-runtime --test recovery_state_machine_tests test_recovery_state_machine_traversal_and_readiness_gate`<br>`rtk proxy cargo test -p rockstream-runtime --test recovery_state_machine_tests test_mandatory_recovery_error_fails_closed` | **PASS** (8 tests in suite) | Startup traverses declared 6-phase lifecycle: `OpeningStorage` -> `RecoveringCatalog` -> `RecoveringEpoch` -> `RecoveringOperators` -> `ValidatingState` -> `Ready`. Readiness returns false during recovery; fails closed on any error. |
| **V065-03: Multi-Page Scan Pagination & Buffer Bounds** | `rtk proxy cargo test -p rockstream-storage --test recovery_scan_pagination_tests test_multi_page_recovery_scan_continues_to_completion`<br>`rtk proxy cargo test -p rockstream-storage --test recovery_scan_pagination_tests test_truncation_injection_fails_startup` | **PASS** (4 tests in suite) | Paginates across multiple scan pages to completion (`MAX_RESTORE_SCAN_PAGE_ROWS = 1,024`); enforces memory bounds (`MAX_RECOVERY_SCAN_BUFFER_BYTES = 32 MiB`); truncation injection triggers immediate startup abort. |
| **V065-04: Point-in-Time Consistency & File Pinning** | `rtk proxy cargo test -p rockstream-control --test backup_point_in_time_tests test_point_in_time_consistency_across_concurrent_writes`<br>`rtk proxy cargo test -p rockstream-control --test backup_point_in_time_tests test_file_pinning_prevents_compaction_unlinking` | **PASS** (3 tests in suite) | Captures unified recovery point `catalog_revision + checkpoint_id + frontier`. Concurrent writes and compaction do not unlink or invalidate pinned backup files. |
| **V065-05: Backup Manifest Fields, Checksum & Format Compatibility** | `rtk proxy cargo test -p rockstream-control --test backup_manifest_tests test_manifest_required_fields_and_checksum`<br>`rtk proxy cargo test -p rockstream-control --test backup_manifest_tests test_unsupported_storage_format_rejected` | **PASS** (7 tests in suite) | Manifest contains all required fields, per-file SHA-256 hashes, and manifest digest. Atomic finalization; incompatible format versions and unsupported storage formats rejected with `RS-3617`. |
| **V065-06: Admin Backup & Restore CLI & Destination Guards** | `rtk proxy cargo test -p rockstream-cli --test admin_backup_cli_tests`<br>`rtk proxy cargo test -p rockstream-cli --test admin_restore_cli_tests` | **PASS** (13 tests across 2 suites) | Release CLI commands `admin backup create`, `inspect`, `verify`, and `admin restore` fully operational. Occupied destination rejected without `--yes` (`RS-2401`/`RS-0005`); pre-validates and guards with rollback. |
| **V065-07: SIGKILL Hard Process Crash Injection across 5 Boundaries** | `rtk proxy cargo test -p rockstream-gateway --test recovery_crash_injection_tests test_hard_kill_at_all_five_execution_boundaries`<br>`rtk proxy cargo test -p rockstream-gateway --test recovery_crash_injection_tests test_kill_during_backup_produces_invalid_backup` | **PASS** (8 tests in suite) | Hard kills (`SIGKILL`) during ordinary write, view maintenance, checkpoint commit, catalog DDL, and backup creation produce either exact recoverable committed state or explicit invalid/incomplete backup errors. |
| **V065-08: Clean-Path Standalone Restore & Zero Original Dependency** | `rtk proxy cargo test -p rockstream-cli --test disaster_recovery_standalone_tests test_restore_to_new_path_zero_original_file_dependency`<br>`rtk proxy cargo test -p rockstream-cli --test disaster_recovery_runbook_tests test_runbook_standalone_drill` | **PASS** (13 tests across 2 suites) | Backup restored to fresh path; original directory deleted / made inaccessible (`chmod 000`); fresh process queries exact catalog, base rows, view multisets, secondary indexes, and source cursors without original files. |
| **V065-09: Fail-Closed Corruption Detection & Diagnostics** | `rtk proxy cargo test -p rockstream-control --test backup_corruption_detection_tests` | **PASS** (7 tests in suite) | Missing payload files (`RS-3615`), bad checksums (`RS-3616`), incompatible format versions (`RS-3617`), truncated manifests (`RS-3615`), and broken catalog references (`RS-3618`) fail closed with zero partial recovery. |
| **V065-10: Operational Resource Bounding & Deadlines** | `rtk proxy cargo test -p rockstream-control --test backup_bounding_limits_tests` | **PASS** (7 tests in suite) | Enforces named limits: copy concurrency (`MAX_BACKUP_COPY_CONCURRENCY = 4`), in-flight memory (`MAX_BACKUP_PENDING_BYTES = 64 MiB`), scan window (`MAX_BACKUP_SCAN_WINDOW_OBJECTS = 1,024`), retry count (`MAX_BACKUP_RETRY_COUNT = 3`), recovery budget (`DEFAULT_SHARD_RECOVERY_BUDGET = 120s`). |
| **Durability (LFS Backend)** | `rtk proxy cargo test -p rockstream-control --test disaster_recovery_tests test_disaster_recovery_lfs_full_lifecycle` | **PASS** (1 test in suite) | Exercises backup creation, manifest verification, and clean restore on local file storage. |
| **Durability (MinIO Backend)** | `rtk proxy cargo test -p rockstream-control --test disaster_recovery_tests test_disaster_recovery_minio_full_lifecycle` | **PASS** (1 test in suite) | Exercises backup creation, file pinning, and clean restore against MinIO S3 object store via TestContainers. |
| **Coordination (SimRuntime)** | `rtk proxy cargo test -p rockstream-sim --test recovery_coordination_fault_tests` | **PASS** (1 test in suite) | Deterministic coordination simulation with fault injection: verifies recovery and manifest atomicity under simulated object-store stalls and network faults. |
| **CLI / Reachability Proof** | `rtk proxy cargo test -p rockstream-cli --test admin_backup_cli_tests`<br>`rtk proxy cargo test -p rockstream-cli --test admin_restore_cli_tests` | **PASS** (13 tests across 2 suites) | Release CLI binary `rockstream` executes `admin backup create`, `inspect`, `verify`, and `admin restore` commands with structured JSON/text output. |
| **Negative / Error Handling** | `rtk proxy cargo test -p rockstream-control --test backup_corruption_detection_tests`<br>`rtk proxy cargo test -p rockstream-cli --test admin_backup_cli_tests`<br>`rtk proxy cargo test -p rockstream-cli --test admin_restore_cli_tests` | **PASS** (20 tests across 3 suites) | Negative paths fail with explicit `RS-XXXX` codes: occupied target (`RS-2401`/`RS-0005`), missing file (`RS-3615`), bad checksum (`RS-3616`), incompatible format (`RS-3617`), broken catalog (`RS-3618`), storage error (`RS-3612`), timeout (`RS-3610`), buffer overflow (`RS-2002`). |
| **Product Surface Conformance** | `rtk proxy bash scripts/check-product-surface.sh` | **PASS** | Product surface manifest and contract validation passed with zero drift. |
| **Zero Hidden Skips Gate** | `rtk proxy bash scripts/check-no-hidden-skips.sh`<br>`rtk proxy bash scripts/check-no-hidden-skips.test.sh` | **PASS** | 0 undocumented `#[ignore]` attributes, 0 silent skip returns; self-test passed. |
| **Dispatch Wiring Gate** | `rtk proxy bash scripts/check-dispatch-wiring.sh` | **PASS** | 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths. |
| **Capability Contract Gate** | `rtk proxy bash scripts/check-capability-contract.sh --full-semantics` | **PASS** | Contract validation passed. |
| **Documentation Checks Gate** | `rtk proxy bash scripts/check-documentation.sh` | **PASS** | Markdown link integrity, reference tables, and formatting verified. |
| **Error Code Coverage Gate** | `rtk proxy bash scripts/check-error-codes.sh` | **PASS** | All returned error messages carry standard `RS-XXXX` codes. |
| **Error Catalog Check** | `rtk proxy bash scripts/check-error-catalog.sh` | **PASS** | Error catalog validated with 197 descriptors, 0 drift across constants and documentation. |
| **Failure Matrix Gate** | `rtk proxy bash scripts/check-failure-matrix.sh` | **PASS** | All 11 production failure modes validated successfully. |
| **Exit Criteria Gate** | `rtk proxy bash scripts/check-exit-criteria.sh`<br>`rtk proxy bash scripts/check-exit-criteria.test.sh` | **PASS** | All Done versions verified with complete sign-offs; self-test passed. |
| **Code Hygiene & Clippy** | `rtk cargo fmt --all -- --check`<br>`rtk cargo clippy --workspace --all-targets -- -D warnings` | **PASS** | Zero formatting diffs, zero clippy warnings across workspace. |
| **Release Binary Build** | `rtk cargo build --release -p rockstream-cli --bin rockstream` | **PASS** | Clean compilation in release mode; digest: `88992ed3249f4121c6af3ac7107dc27e3410b312649afeccf07e814f472d25d0`. |

---

## 2. Exit Criteria Verification

### V065-01: Eight Recovery Categories Reconstruction & Idempotency
- **Requirement**: All eight recovery categories reconstruct the exact acknowledged state, including cursor and idempotency behavior.
- **Evidence**:
  - `crates/rockstream-runtime/src/recovery.rs`: Strict sequencing through `OpeningStorage`, `RecoveringCatalog`, `RecoveringEpoch`, `RecoveringOperators`, `ValidatingState`, and `Ready`.
  - `crates/rockstream-storage/src/shard_db.rs`: Persisted storage and table data reconstruction.
  - Tests:
    - `disaster_recovery_standalone_tests::test_eight_recovery_categories_reconstruction`: Verifies exact reconstruction across catalog, base tables, operator state, view output multisets, frontier/epoch, secondary indexes, source cursors, and idempotency metadata.
    - `disaster_recovery_standalone_tests::test_idempotency_replay_deduplication`: Verifies that replayed client writes around crash/restart produce zero duplicate side effects.
    - Category-specific invariance tests: `test_catalog_recovery_integrity`, `test_base_table_recovery_integrity`, `test_operator_state_recovery_integrity`, `test_view_output_multiset_integrity`, `test_frontier_epoch_recovery_integrity`, `test_secondary_index_recovery_integrity`, `test_source_cursor_recovery_integrity`, `test_idempotency_metadata_recovery_integrity`.
- **Command Output Excerpt**:
  ```
  test test_eight_recovery_categories_reconstruction ... ok
  test test_frontier_epoch_recovery_integrity ... ok
  test test_idempotency_metadata_recovery_integrity ... ok
  test test_idempotency_replay_deduplication ... ok
  test test_operator_state_recovery_integrity ... ok
  test test_source_cursor_recovery_integrity ... ok
  test test_base_table_recovery_integrity ... ok
  test test_secondary_index_recovery_integrity ... ok
  test test_catalog_recovery_integrity ... ok
  test test_view_output_multiset_integrity ... ok
  test test_restore_to_new_path_zero_original_file_dependency ... ok
  test result: ok. 11 passed; 0 failed; 0 ignored; finished in 7.51s
  ```

### V065-02: Recovery State Machine Traversal & Readiness Gate
- **Requirement**: Startup traverses declared recovery states, keeps readiness false, and fails closed on any mandatory recovery error.
- **Evidence**:
  - `crates/rockstream-types/src/lifecycle.rs`: `RecoveryPhase` enum with explicit states: `OpeningStorage`, `RecoveringCatalog`, `RecoveringEpoch`, `RecoveringOperators`, `ValidatingState`, `Ready`.
  - `crates/rockstream-runtime/src/recovery.rs`: `RecoveryDriver` enforces sequential phase transitions, ensures `lifecycle.is_ready() == false` during pre-Ready states, and transitions immediately to `LifecycleState::Fatal` upon any phase failure.
  - Tests:
    - `recovery_state_machine_tests::test_recovery_state_machine_traversal_and_readiness_gate`: Validates strict step-by-step transition order and readiness gating.
    - `recovery_state_machine_tests::test_mandatory_recovery_error_fails_closed`: Verifies that any recovery error causes immediate transition to `Fatal` and prevents `Ready`.
    - `recovery_state_machine_tests::test_opening_storage_failure_fails_fast`: Storage open errors fail immediately with `RS-3612`.
    - `recovery_state_machine_tests::test_recovering_catalog_failure_blocks_readiness`: Catalog failures fail with `RS-1002`.
    - `recovery_state_machine_tests::test_recovering_epoch_failure_blocks_readiness`: Epoch failures fail with `RS-3605`.
    - `recovery_state_machine_tests::test_recovering_operators_failure_blocks_readiness`: Operator recovery failures fail with `RS-3610`.
    - `recovery_state_machine_tests::test_validating_state_failure_blocks_readiness`: Validation failures fail with `RS-3615`.
- **Command Output Excerpt**:
  ```
  test test_opening_storage_failure_fails_fast ... ok
  test test_mandatory_recovery_error_fails_closed ... ok
  test test_recovery_state_machine_traversal_and_readiness_gate ... ok
  test test_recovering_operators_failure_blocks_readiness ... ok
  test test_validating_state_failure_blocks_readiness ... ok
  test test_recovering_catalog_failure_blocks_readiness ... ok
  test test_recovering_epoch_failure_blocks_readiness ... ok
  test test_recovery_state_machine_full_transition_to_ready ... ok
  test result: ok. 8 passed; 0 failed; 0 ignored; finished in 0.06s
  ```

### V065-03: Multi-Page Scan Pagination & Buffer Bounds
- **Requirement**: Recovery reads every page of every supported state category within named memory limits.
- **Evidence**:
  - `crates/rockstream-storage/src/shard_db.rs`: `scan_paginated` with `MAX_RESTORE_SCAN_PAGE_ROWS = 1,024` and `MAX_RECOVERY_SCAN_BUFFER_BYTES = 32 MiB`.
  - Continuation handling iterates until all records across all pages are consumed. Truncation or unexpected EOF triggers immediate failure.
  - Tests:
    - `recovery_scan_pagination_tests::test_multi_page_recovery_scan_continues_to_completion`: Iterates across multiple scan pages (>2,500 records) to complete recovery.
    - `recovery_scan_pagination_tests::test_truncation_injection_fails_startup`: Artificial scan truncation triggers startup failure.
    - `recovery_scan_pagination_tests::test_recovery_scan_buffer_limit_overflow`: Exceeding 32 MiB memory limit returns `RS-2002`.
    - `recovery_scan_pagination_tests::test_recovery_scan_cancellation_handle`: Supports graceful cancellation through handle.
- **Command Output Excerpt**:
  ```
  test test_truncation_injection_fails_startup ... ok
  test test_recovery_scan_cancellation_handle ... ok
  test test_recovery_scan_buffer_limit_overflow ... ok
  test test_multi_page_recovery_scan_continues_to_completion ... ok
  test result: ok. 4 passed; 0 failed; 0 ignored; finished in 0.25s
  ```

### V065-04: Point-in-Time Consistency & File Pinning
- **Requirement**: A backup's catalog revision, checkpoint, and frontier describe one consistent logical point under concurrent writes and compaction.
- **Evidence**:
  - `crates/rockstream-control/src/manifest.rs`: Point-in-time capture point binds `catalog_revision + checkpoint_id + frontier`.
  - File pinning preserves all referenced files during backup export; compaction and background cleanup are prevented from unlinking pinned files.
  - Tests:
    - `backup_point_in_time_tests::test_point_in_time_consistency_across_concurrent_writes`: Concurrent writes do not affect the captured revision and frontier.
    - `backup_point_in_time_tests::test_file_pinning_prevents_compaction_unlinking`: Compaction coordinates retention so pinned files remain intact.
    - `backup_point_in_time_tests::test_mixed_revision_manifest_fails_consistency`: Inconsistent capture tuples fail validation with `RS-3618`.
- **Command Output Excerpt**:
  ```
  test test_mixed_revision_manifest_fails_consistency ... ok
  test test_point_in_time_consistency_across_concurrent_writes ... ok
  test test_file_pinning_prevents_compaction_unlinking ... ok
  test result: ok. 3 passed; 0 failed; 0 ignored; finished in 0.00s
  ```

### V065-05: Backup Manifest Fields, Checksum & Format Compatibility
- **Requirement**: The manifest contains every required field and file; supported formats restore and unsupported combinations reject explicitly.
- **Evidence**:
  - `crates/rockstream-control/src/manifest.rs`: `BackupManifest` struct with `format_version`, `catalog_revision`, `checkpoint_id`, `frontier`, `storage_format`, `files`, and `checksum`.
  - Atomic serialization and finalization; manifest checksum protects against tampering.
  - Tests:
    - `backup_manifest_tests::test_manifest_required_fields_and_checksum`: Validates serialization, deserialization, and canonical checksum calculation.
    - `backup_manifest_tests::test_unsupported_storage_format_rejected`: Incompatible storage formats reject with `RS-3617`.
    - `backup_manifest_tests::test_manifest_format_version_enforcement`: Unknown manifest versions reject with `RS-3617`.
    - `backup_manifest_tests::test_manifest_checksum_tamper_detection`: Altered manifests fail verification with `RS-3616`.
    - `backup_manifest_tests::test_unfinalized_manifest_fails_closed`: Unfinalized manifests fail with `RS-3615`.
- **Command Output Excerpt**:
  ```
  test test_unfinalized_manifest_fails_closed ... ok
  test test_unsupported_storage_format_rejected ... ok
  test test_manifest_format_version_enforcement ... ok
  test test_empty_manifest_fails_closed ... ok
  test test_manifest_checksum_tamper_detection ... ok
  test test_storage_format_range_enforcement ... ok
  test test_manifest_required_fields_and_checksum ... ok
  test result: ok. 7 passed; 0 failed; 0 ignored; finished in 0.00s
  ```

### V065-06: Admin Backup & Restore CLI & Destination Guards
- **Requirement**: Create, inspect, verify, and restore operate through the release CLI; occupied destinations reject unless explicitly replaceable.
- **Evidence**:
  - `crates/rockstream-cli/src/cli_args.rs`: Added `AdminCommand::Backup` with `create`, `inspect`, `verify` and `AdminCommand::Restore`.
  - `crates/rockstream-cli/src/main.rs`: Dispatch handlers `run_admin_backup_create`, `run_admin_backup_inspect`, `run_admin_backup_verify`, `run_admin_restore`.
  - Enforces empty destination requirement; non-empty destination rejected with `RS-2401`/`RS-0005` unless `--yes` confirmed.
  - Pre-validates source backup before modifying target directory.
  - Tests:
    - `admin_backup_cli_tests::test_admin_backup_create_inspect_verify_cli`: End-to-end execution of create, inspect, and verify.
    - `admin_backup_cli_tests::test_backup_create_occupied_refusal`: Occupied backup directory rejected with `RS-2401`.
    - `admin_restore_cli_tests::test_restore_refusal_unconfirmed_destination`: Occupied restore directory rejected without `--yes`.
    - `admin_restore_cli_tests::test_restore_confirmed_overwrite_success`: Occupied restore directory cleanly restored with `--yes`.
    - `admin_restore_cli_tests::test_restore_invalid_source_leaves_target_intact`: Invalid source rejected without modifying target.
- **Command Output Excerpt**:
  ```
  test test_backup_create_empty_destination ... ok
  test test_backup_inspect_corrupted ... ok
  test test_backup_create_permission_denied ... ok
  test test_backup_create_occupied_refusal ... ok
  test test_backup_verify_valid ... ok
  test test_backup_inspect_valid ... ok
  test test_backup_verify_tampered_file_failure ... ok
  test test_admin_backup_create_inspect_verify_cli ... ok
  test result: ok. 8 passed; 0 failed; 0 ignored; finished in 7.34s

  test test_restore_empty_destination_success ... ok
  test test_restore_confirmed_overwrite_success ... ok
  test test_restore_invalid_source_leaves_target_intact ... ok
  test test_restore_refusal_unconfirmed_destination ... ok
  test test_admin_restore_destination_guards_and_rollback ... ok
  test result: ok. 5 passed; 0 failed; 0 ignored; finished in 6.86s
  ```

### V065-07: SIGKILL Hard Process Crash Injection across 5 Boundaries
- **Requirement**: Hard kills during writes, maintenance, checkpoint, DDL, and backup produce only recoverable committed state or explicit invalid/incomplete backup errors.
- **Evidence**:
  - `crates/rockstream-gateway/tests/recovery_crash_injection_tests.rs`: Tests inject `SIGKILL` across 5 execution boundaries and verify post-restart behavior.
  - Tests:
    - `test_crash_during_ordinary_write`: Uncommitted in-flight rows rolled back; committed rows intact.
    - `test_crash_during_view_maintenance`: Arrangements cleanly reconstructed from committed frontier; view multiset matches oracle.
    - `test_crash_during_checkpoint_commit`: Unfinalized checkpoint ignored; resumes from previous committed checkpoint.
    - `test_crash_during_catalog_ddl`: Uncommitted catalog changes rolled back cleanly.
    - `test_crash_during_backup_creation`: Unfinalized backup directory rejected with `RS-3615`.
    - `test_crash_during_restore_operation`: Incomplete restore detected and rejected.
- **Command Output Excerpt**:
  ```
  test test_hard_kill_at_all_five_execution_boundaries ... ok
  test test_crash_during_restore_operation ... ok
  test test_crash_during_backup_creation ... ok
  test test_crash_during_checkpoint_commit ... ok
  test test_kill_during_backup_produces_invalid_backup ... ok
  test test_crash_during_catalog_ddl ... ok
  test test_crash_during_ordinary_write ... ok
  test test_crash_during_view_maintenance ... ok
  test result: ok. 8 passed; 0 failed; 0 ignored; finished in 0.55s
  ```

### V065-08: Clean-Path Standalone Restore & Zero Original Dependency
- **Requirement**: Restore to a new filesystem path needs no original files and returns the exact catalog, base rows, view multisets, and auxiliary state.
- **Evidence**:
  - `crates/rockstream-cli/tests/disaster_recovery_standalone_tests.rs`: Complete end-to-end drill.
  - Steps:
    1. Populate original cluster with all 8 state categories.
    2. Execute `admin backup create`.
    3. Remove / make inaccessible original storage (`chmod 000` / deleted).
    4. Execute `admin restore` into new path.
    5. Start new `rockstream` process pointing exclusively to restored path.
    6. Verify all table rows, view results, indexes, catalog entities, and source cursors.
- **Command Output Excerpt**:
  ```
  test test_restore_to_new_path_zero_original_file_dependency ... ok
  test test_runbook_standalone_drill ... ok
  test result: ok. passed
  ```

### V065-09: Fail-Closed Corruption Detection & Diagnostics
- **Requirement**: Missing files, bad checksums, incompatible formats, incomplete manifests, and broken references fail startup or verification without successful partial recovery.
- **Evidence**:
  - `crates/rockstream-control/src/manifest.rs`: Verification validates every file against SHA-256 and checks manifest digest.
  - Dedicated diagnostic error codes:
    - Missing payload file -> `RS-3615`
    - Checksum mismatch -> `RS-3616`
    - Incompatible format version -> `RS-3617`
    - Truncated / missing manifest -> `RS-3615`
    - Broken catalog reference -> `RS-3618`
- **Command Output Excerpt**:
  ```
  test test_broken_catalog_reference_fails_closed ... ok
  test test_truncated_manifest_fails_closed ... ok
  test test_tampered_manifest_fails_verification ... ok
  test test_incompatible_format_version_rejected ... ok
  test test_missing_payload_file_fails_closed ... ok
  test test_checksum_mismatch_fails_closed ... ok
  test test_corruption_detection_fails_closed ... ok
  test result: ok. 7 passed; 0 failed; 0 ignored; finished in 0.04s
  ```

### V065-10: Operational Resource Bounding & Deadlines
- **Requirement**: Recovery, backup, and restore obey named byte, concurrency, waiter, retry, and scan limits with observable progress and explicit overflow behavior.
- **Evidence**:
  - Enforced constants in `rockstream-control` and `rockstream-storage`:
    - `MAX_BACKUP_COPY_CONCURRENCY = 4`
    - `MAX_BACKUP_PENDING_BYTES = 64 MiB`
    - `MAX_BACKUP_SCAN_WINDOW_OBJECTS = 1,024`
    - `MAX_BACKUP_RETRY_COUNT = 3`
    - `MAX_RESTORE_SCAN_PAGE_ROWS = 1,024`
    - `DEFAULT_SHARD_RECOVERY_BUDGET = 120s`
- **Command Output Excerpt**:
  ```
  test test_backup_scan_window_paginates_to_completion ... ok
  test test_backup_retry_limit_enforced ... ok
  test test_restore_scan_page_bounded ... ok
  test test_backup_pending_bytes_bounded ... ok
  test test_backup_and_restore_resource_bounds_enforced ... ok
  test test_backup_copy_concurrency_bounded ... ok
  test test_recovery_budget_timeout_enforced ... ok
  test result: ok. 7 passed; 0 failed; 0 ignored; finished in 0.04s
  ```

---

## 3. Durability & Coordination Backends Proof

### 1. Local File System (LFS) Durability
- Test: `disaster_recovery_tests::test_disaster_recovery_lfs_full_lifecycle`
- Result: **PASS**
- Verification: Full write, point-in-time backup export, manifest verification, and clean restore against on-disk SlateDB storage layout.

### 2. MinIO S3 Object Store (TestContainers) Durability
- Test: `disaster_recovery_tests::test_disaster_recovery_minio_full_lifecycle`
- Result: **PASS**
- Verification: Full backup creation, file pinning, multipart export, and clean restore against real MinIO S3 container.

### 3. Deterministic Coordination Faults (SimRuntime)
- Test: `recovery_coordination_fault_tests::test_sim_runtime_recovery_under_storage_and_network_faults`
- Result: **PASS**
- Verification: Validates deterministic state machine transitions, storage stalls, and manifest atomicity under simulated faults using `SimRuntime`.

---

## 4. Reachability & Dispatch-Wiring Proof

### Admin Backup and Restore CLI Dispatch
Verified via `scripts/check-dispatch-wiring.sh` and CLI integration tests:
```
OK: 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths.
```
- **Stage 1 (CLI Args)**: `crates/rockstream-cli/src/cli_args.rs`: `AdminCommand::Backup { action: BackupAction }` with subcommands `create`, `inspect`, `verify`, and `AdminCommand::Restore`.
- **Stage 2 (Dispatcher)**: `crates/rockstream-cli/src/main.rs`: Handlers `run_admin_backup_create`, `run_admin_backup_inspect`, `run_admin_backup_verify`, `run_admin_restore`.
- **Stage 3 (Operation Executor)**: `crates/rockstream-control/src/manifest.rs` & `checkpoint_export.rs`: Atomic manifest writing, cryptographic hashing, and safe file copying.
- **Stage 4 (Output Encoder)**: `crates/rockstream-cli/src/output.rs`: Structured output formatting for backup create/inspect/verify and restore.

---

## 5. Negative Testing & Failure Containment

| Failure / Negative Scenario | Trigger Condition | Error Code | SQLSTATE | Action Taken | Committed Test |
|---|---|---|---|---|---|
| Occupied backup destination | Destination directory already contains files | `RS-2401` | `42P07` | Rejects before copying | `admin_backup_cli_tests::test_backup_create_occupied_refusal` |
| Occupied restore destination | Target directory non-empty without `--yes` | `RS-0005` | `55000` | Refuses overwrite | `admin_restore_cli_tests::test_restore_refusal_unconfirmed_destination` |
| Missing payload file | File in manifest missing on disk | `RS-3615` | `58030` | Fails verification / startup | `backup_corruption_detection_tests::test_missing_payload_file_fails_closed` |
| Checksum mismatch | Data file altered after backup | `RS-3616` | `XX000` | Rejects invalid file | `backup_corruption_detection_tests::test_checksum_mismatch_fails_closed` |
| Tampered manifest | Manifest header digest mismatch | `RS-3616` | `XX000` | Rejects tampered manifest | `backup_corruption_detection_tests::test_tampered_manifest_fails_verification` |
| Incompatible format version | `format_version != 1` | `RS-3617` | `0A000` | Fails fast with upgrade error | `backup_corruption_detection_tests::test_incompatible_format_version_rejected` |
| Broken catalog reference | Orphan table or missing parent catalog | `RS-3618` | `42P01` | Fails recovery closed | `backup_corruption_detection_tests::test_broken_catalog_reference_fails_closed` |
| Storage open failure | Inaccessible storage path | `RS-3612` | `58030` | Transitions to Fatal | `recovery_state_machine_tests::test_opening_storage_failure_fails_fast` |
| Recovery budget exceeded | Recovery takes > 120 seconds | `RS-3610` | `57014` | Rejects with timeout error | `backup_bounding_limits_tests::test_recovery_budget_timeout_enforced` |
| Scan buffer limit exceeded | Scan buffer exceeds 32 MiB | `RS-2002` | `54000` | Fails closed on overflow | `recovery_scan_pagination_tests::test_recovery_scan_buffer_limit_overflow` |

---

## 6. Quality Gates & Release Binary Build

1. **Product Surface Manifest & Drift Check**:
   ```
   rtk proxy bash scripts/check-product-surface.sh
   OK: Product surface manifest and contract validation passed with zero drift.
   ```
2. **Capability Contract Check**:
   ```
   rtk proxy bash scripts/check-capability-contract.sh --full-semantics
   OK: capability contract check passed.
   ```
3. **Zero Hidden Skips Gate**:
   ```
   rtk proxy bash scripts/check-no-hidden-skips.sh && rtk proxy bash scripts/check-no-hidden-skips.test.sh
   OK: no hidden test skips found.
   OK: check-no-hidden-skips.sh self-test passed.
   ```
4. **Documentation Check**:
   ```
   rtk proxy bash scripts/check-documentation.sh
   OK: documentation checks passed.
   ```
5. **Error Codes Check**:
   ```
   rtk proxy bash scripts/check-error-codes.sh
   OK: all logged/returned errors carry an RS-XXXX code.
   ```
6. **Error Catalog Check**:
   ```
   rtk proxy bash scripts/check-error-catalog.sh
   OK: Error catalog validated with 197 descriptors, 0 drift across constants and documentation.
   ```
7. **Failure Matrix Check**:
   ```
   rtk proxy bash scripts/check-failure-matrix.sh
   OK: All 11 production failure modes validated successfully.
   ```
8. **Exit Criteria Check**:
   ```
   rtk proxy bash scripts/check-exit-criteria.sh && rtk proxy bash scripts/check-exit-criteria.test.sh
   All Done versions have complete sign-offs.
   OK: check-exit-criteria.sh self-test passed.
   ```
9. **Code Formatting (`fmt`)**:
   ```
   rtk cargo fmt --all -- --check
   Status: 0 formatting diffs.
   ```
10. **Clippy Linting**:
    ```
    rtk cargo clippy --workspace --all-targets -- -D warnings
    Status: No issues found (0 warnings, 0 errors).
    ```
11. **Release Binary Build**:
    ```
    rtk cargo build --release -p rockstream-cli --bin rockstream
    Digest (SHA-256): 88992ed3249f4121c6af3ac7107dc27e3410b312649afeccf07e814f472d25d0
    ```

---

## 7. Remaining Phase 4 Checks & Exit Confirmation

- [x] **Reachability tests** (CLI & runtime): Every backup and restore operation has passing end-to-end tests through the release CLI binary (`admin_backup_cli_tests`, `admin_restore_cli_tests`, `disaster_recovery_standalone_tests`).
- [x] **Negative tests**: All error and boundary conditions return proper `RS-XXXX` codes (`RS-0005`, `RS-2401`, `RS-3610`, `RS-3612`, `RS-3615`, `RS-3616`, `RS-3617`, `RS-3618`, `RS-2002`).
- [x] **Dispatch-wiring audit**: Every path in the Phase 2 audit is verified connected with zero missing routes.
- [x] **Coverage matrix**: Every cell in matrices A through G has passing tests.
- [x] **All other Proof obligations met**: Batch oracle (`incremental == batch`), LFS, MinIO, SimRuntime, hard SIGKILL crash injection, and zero-dependency standalone restore all passed.
