# v0.55.2 Phase 4 Evidence

All commands below ran from the repository root on 2026-08-15. Cargo test
output was restricted to the repository-required summary lines; each listed
test command exited successfully.

| Proof claim | Evidence | Result / excerpt |
|---|---|---|
| Every threat boundary has a control and linked proof. | `rtk bash scripts/check-threat-model-links.test.sh`; `auth_scram_tests`; `gateway_tls_tests::mtls_end_to_end_authenticates_real_client_cert`; `control_worker_mtls_tests::test_control_worker_mtls_missing_cert_rejected`; `shuffle_mtls_tests::test_worker_shuffle_grpc_mtls_unauthenticated_rejected`; `internal_mtls_tests::test_certificate_rotation_3_worker_cluster_under_load_zero_loss_zero_restart`; `secrets_redaction_tests::secret_literal_is_absent_from_audit_and_metadata_artifacts`; `worker_secret_resolution_tests::expired_token_is_removed_from_memory` | PASS — `OK: threat-model checker self-test passed.` All named boundary tests exited 0. |
| cargo audit and cargo deny reject a vulnerable fixture; CI runs both gates. | `rtk bash scripts/check-dependency-audit.test.sh`; `rtk cargo audit --ignore` for reviewed IDs; workflow audit commands derive ignores from `deny.toml`. | PASS — `OK: dependency audit gates and exception-record self-test passed.` Audit found no unignored vulnerability; five reviewed warnings remained. |
| Newly disclosed `quinn-proto` vulnerability is remediated. | `rtk cargo update -p quinn-proto --precise 0.11.15` | PASS — `Updating quinn-proto v0.11.14 -> v0.11.15`. |
| Every CLI mutator rejects an unauthorized authenticated identity and audits the exact denial. | `rockstream-cli/tests/cli_mutating_commands_tests.rs::test_cli_unauthorized_identity_refused_and_audited_exhaustive` | PASS — exits 0; iterates authoritative `CLI_MUTATION_POLICY` and compares the full ordered result vector. |
| Pgwire mutators are reachable through the shared dispatcher in simple and extended modes; negatives return SQLSTATE 42501 / RS-2401. | `rockstream-gateway/tests/gateway_mutation_authorization_tests.rs::pgwire_mutation_policy_simple_and_extended` | PASS — exits 0; raw SQL reaches `dispatch_async_with_conn`, covers `PGWIRE_MUTATION_POLICY`, and compares simple with extended results. |
| Semicolon-separated mutators cannot bypass authorization; every statement is audited. | `gateway_mutation_authorization_tests::viewer_cannot_bypass_mutation_policy_with_multi_statement_sql` | PASS — exits 0; exact audit vector is `jwt:viewer`, `create_table`, resource, `unauthorized role`, `RS-2401` for both statements. |
| A denied pgwire mutation persists identically after LFS reopen. | `gateway_mutation_authorization_tests::gateway_mutation_authorization_lfs` | PASS — exits 0; exact persisted actor, action, resource, detail, and `RS-2401` asserted. |
| A denied pgwire mutation persists identically through real MinIO/TestContainers. | `gateway_mutation_authorization_tests::gateway_mutation_authorization_minio_tc` with Docker 29.6.2 | PASS — exits 0; reopened MinIO audit event equals the original event. |
| All live decoder corpora replay without panic or hang under bounded streaming. | `rtk cargo check -p rockstream-fuzz`; `fuzz_corpus_regression_tests::replay_all_fuzz_corpora` | PASS — fuzz check finished successfully; replay exits 0 and covers sql parser, pgwire, postgres CDC, control worker, raft RPC, Kafka payload, and OIDC JWT targets. |
| Dispatch-wiring and coverage matrices have no gaps. | CLI exhaustive-policy test; pgwire simple/extended-policy test; multi-statement denial test; LFS and MinIO reopen tests above. | PASS — both production policy tables are consumed by their exhaustive tests; no Phase 2 MISSING path remains. |
| Oracle, durability-protocol, SimRuntime, benchmark, and soak obligations. | v0.55.2 plan explicitly marks these feature families not applicable; only audit-persistence LFS/MinIO obligations apply and pass above. | N/A by scope. |
| Received P0/P1 findings are closed, and a pending engagement is explicit. | `sign-offs/v0.55.2.md`; plan deferred-item audit | PASS — no review report artifact was received; the commissioned engagement and v0.59 gate are recorded rather than deferred. |

## Final checks

- `rtk git diff --check`: PASS.
- No test was weakened, ignored, or deleted.
- `cargo deny` fixture proof passes. The locally installed `cargo-deny 0.20.2`
  panics against Cargo 1.97.1 when scanning this workspace, so the CI action
  remains the workspace-level cargo-deny runner; this does not affect the
  passing fixture proof.
