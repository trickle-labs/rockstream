# v0.37 Evidence — Extended Query Protocol & Prepared Statements

All vertical slices and proof claims for Version 0.37 have been successfully verified. 

## Proof Evidence Table

| Proof Claim / Slice | Test / Command | Result / Output Excerpt |
|:---|:---|:---|
| **S1: SSL Negotiation & Parameter Status**<br>Verify SSLRequest does not cause connection failures and startup parameters are returned. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_ssl_negotiation_downgrade` | `test test_ssl_negotiation_downgrade ... ok` |
| **S2: Stateful Prepared Statement Cache**<br>Verify `PREPARE` and parameter inference/casting, and that `DEALLOCATE` works. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_prepared_statement_caching_and_deallocate` | `test test_prepared_statement_caching_and_deallocate ... ok` |
| **S3: Describe Statement & Portal**<br>Verify describes return correct parameter types and row schemas. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_prepared_statement_caching_and_deallocate` (also covers Describe) | `test test_prepared_statement_caching_and_deallocate ... ok` |
| **S4: Extended Query Pipeline & Limits**<br>Verify Parse -> Bind -> Execute pipeline, statements limit (1000, RS-2600), and portals limit (1000, RS-2601). | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_extended_query_pipeline`<br>`rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_prepared_statements_limit`<br>`rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_portals_limit` | `test test_extended_query_pipeline ... ok`<br>`test test_prepared_statements_limit ... ok`<br>`test test_portals_limit ... ok` |
| **S5: Portal Suspension with `max_rows`**<br>Verify suspended portals resume and return correct rows sequentially. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_portal_suspension_max_rows` | `test test_portal_suspension_max_rows ... ok` |
| **S6: Multi-Statement & Empty Queries**<br>Verify semicolon-separated simple queries and empty queries. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests test_multi_statement_and_empty_queries` | `test test_multi_statement_and_empty_queries ... ok` |
| **S7: E2E Driver Verification**<br>Verify tokio-postgres prepared queries with parameters work E2E. | `rtk cargo test -p rockstream-gateway --test gateway_extended_query_tests` | `cargo test: 7 passed` |
| **Workspace Regression Suite**<br>Verify all existing gateway tests pass. | `rtk cargo test -p rockstream-gateway --all-targets` | `cargo test: 104 passed, 1 ignored (6 suites, 221.69s)` |

## Bounded Buffers & Queues Verification

- `PreparedStatementCache`: verified up to `MAX_PREPARED_STATEMENTS_PER_CONNECTION = 1000`, returns PostgreSQL error `53200` with code `RS-2600` and next steps.
- `PortalStoreCache`: verified up to `MAX_PORTALS_PER_CONNECTION = 1000`, returns PostgreSQL error `53200` with code `RS-2601` and next steps.
