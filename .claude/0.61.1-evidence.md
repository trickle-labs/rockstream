# v0.61.1 Proof Evidence — External Performance Baseline

Contract Version: `v0.61.1`  
Execution Date: 2026-09-10  
Status: PASS (all proof obligations met)  
Git Revision: `f1038f43cecc349e4b15e2773336b1598d9bbdbf`  
Profile Version: `MBP-M5Pro-48GB-v2` (`benchmarks/r1-local/profile-v0611.toml`)  
Thresholds Version: `contract_version = 2` (`benchmarks/r1-local/thresholds-v0611.toml`)  

---

## 1. Summary of Proof Claims & Verification

All proof obligations defined in `.claude/v0.61.1-plan.md`, `docs/implementation-plans/v0.61.1.md`, and `ROADMAP.md` §5.2 (`v0.61.1: External performance baseline`) have been fully implemented, executed, tested, and verified.

| Proof Obligation / Claim | Target Test Suite / Verification Command | Result | Notes / Output Excerpt |
|---|---|---|---|
| **V0611-01: Versioned Profile & Frozen Target Contract** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test profile_contract_tests`<br>`rtk proxy python3 scripts/check-r1-local-evidence.py .` | **PASS** (3 tests) | Verifies `profile-v0611.toml` (`MBP-M5Pro-48GB-v2`) and `thresholds-v0611.toml` freeze targets; asserts historical `MBP-M5Pro-48GB-v1` evidence remains byte-exact; verifies write-amplification ratio (<= 1.10) frozen prior to measurement. |
| **V0611-02: Decoupled Offered Load & Latency Metrics** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test metric_decoupling_tests` | **PASS** (3 tests) | Validates timer-based offered-load scheduler independent of completion; proves separate tracking of read p99 (<= 10.0 ms), commit p99 (<= 25.0 ms), and freshness p99 (<= 100.0 ms); bounds generator queue (10,000 items) and tracks drops and generator delays. |
| **V0611-03: One-Key Update Matrix Accounting (1K, 100K, 10M)** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_one_key` | **PASS** (3 tests) | Measures runnable 1K and 100K standalone cells against constant-write rule; reports lookup cost separately; accounts for 10M beyond-RAM cell with concrete blocker assigned to `v0.67.1`. |
| **V0611-04: Compatible Views Matrix Accounting (1 & 20 views)** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_views` | **PASS** (3 tests) | Measures runnable 1-view and 20-view standalone workloads; records shared-execution comparison as blocked to `v0.67` without claiming unmeasured reductions. |
| **V0611-05: Worker Scaling Matrix Accounting (1, 2, 4, 8 workers)** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_worker` | **PASS** (5 tests) | Measures standalone 1-worker cell with verified OS worker PID telemetry; assigns 2, 4, 8 distributed workers to `v0.67` with host resource limits identified; rejects inactive workers. |
| **V0611-06: Concurrent Ingestion and Query Matrix Accounting** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_concurrent` | **PASS** (1 test) | Measures mixed standalone load; reports separate read p99 and freshness results against frozen targets without hiding load by throttling generator. |
| **V0611-07: State Beyond RAM, Compaction & Restart Accounting** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_compaction`<br>`rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test baseline_durability_tests` | **PASS** (4 tests) | Measures standalone compaction and node restart with bounded storage backlog and flush tracking; assigns state larger than RAM to `v0.67.1` with concrete blocker. |
| **V0611-08: Overload, Worker Loss & Migration Accounting** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test harness_matrix_tests test_standalone_overload` | **PASS** (3 tests) | Measures standalone overload under bounded backpressure, exact results, and recovery timing; assigns worker loss to `v0.67` and migration to `v0.68`. |
| **V0611-09: Negative Qualification & Harness Integrity Rejection** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test negative_qualification_tests`<br>`rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test oracle_validation_tests` | **PASS** (11 tests) | Harness rejects missing required cells, inactive workers, synthetic sample runs, corrupt binary digests, unowned blocked cells, wrong column values, duplicate rows, missing rows, and stale epochs. |
| **V0611-10: Telemetry Metrics Collection & Cost Model Reporting** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test report_metrics_tests` | **PASS** (11 tests) | Verifies `RawSample` records state writes, intermediate rows, network bytes, object store requests per change, RSS, queue age, control load, physical flushes; verifies cost per million changes formula; enforces CV <= 0.15 across 5 pairs. |
| **Durability (LFS & MinIO)** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test baseline_durability_tests` | **PASS** (3 tests) | Asserts baseline persists across restart on LFS and MinIO; asserts all harness and node cleanup uses scan-and-delete without SlateDB range deletion. |
| **Coordination (SimRuntime)** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test baseline_coordination_tests` | **PASS** (1 test) | Seeded `SimRuntime` proves generator backpressure and timeout handling under delayed gateway responses. |
| **Documentation Integrity** | `rtk cargo test --manifest-path tools/r1-local-harness/Cargo.toml --test documentation_checks`<br>`rtk bash scripts/check-documentation.sh` | **PASS** (2 checks) | Verifies `benchmarks/r1-local/README.md` documents baseline targets, decoupled metrics, matrix accounting, ownership, and local scope. |
| **Dispatch Wiring Gate** | `rtk bash scripts/check-dispatch-wiring.sh` | **PASS** | 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths. |
| **Capability Contract Gate** | `rtk bash scripts/check-capability-contract.sh --full-semantics` | **PASS** | Contract validation passed with zero violations. |
| **Zero Hidden Skips Gate** | `rtk bash scripts/check-no-hidden-skips.sh` | **PASS** | 0 undocumented `#[ignore]` attributes, 0 silent skip returns. |
| **Product Surface Conformance** | `rtk bash scripts/check-product-surface.sh` | **PASS** | Product surface manifest and contract validation passed with zero drift. |
| **Exit Criteria Gate** | `rtk bash scripts/check-exit-criteria.sh` | **PASS** | All Done versions verified with complete sign-offs. |
| **Code Hygiene & Clippy** | `rtk cargo fmt --all -- --check`<br>`rtk cargo clippy --manifest-path tools/r1-local-harness/Cargo.toml --all-targets -- -D warnings` | **PASS** | Zero formatting diffs, zero clippy warnings. |

---

## 2. Exit Criteria Proof Verification Details

### V0611-01: Versioned Workload Profile & Frozen Target Contract
- **Requirement**: A new frozen profile identifies all targets and comparison rules before measurement; previous evidence remains unchanged.
- **Evidence**:
  - `profile-v0611.toml`: Declares `contract_version = 2`, `profile_id = "MBP-M5Pro-48GB-v2"`, `state = "frozen"`, 128-byte payload, `Durable` mode, 60s measurement, 10s warmup, 5 repetitions, max CV 0.15, write amplification max ratio 1.10.
  - `thresholds-v0611.toml`: Declares `read_p99_ms = 10.0`, `commit_p99_ms = 25.0`, `freshness_p99_ms = 100.0`, `max_generator_delay_ms = 5.0`, `max_queue_depth = 10000`.
  - `profile_contract_tests::test_v0611_profile_and_thresholds_freeze`: Verifies contract version 2, frozen state, and target values.
  - `profile_contract_tests::test_historical_v1_contract_remains_intact`: Asserts historical `profile.toml` (`MBP-M5Pro-48GB-v1`) and `evidence/r1-local/candidates.json` are byte-exact and unmodified.
  - `profile_contract_tests::test_write_amplification_rule_frozen_before_measurement`: Confirms write amplification threshold (1.10) is frozen before measurement.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test profile_contract_tests: 3 passed (1 suite, 0.00s)
  python3 scripts/check-r1-local-evidence.py .: R1 local contract verified
  ```

### V0611-02: Standalone Release Baseline with Decoupled Metrics
- **Requirement**: The standalone release baseline contains measured read, commit, and correctness-based freshness p99, including generator delay, overload, errors, and timeouts.
- **Evidence**:
  - `metric_decoupling_tests::test_independent_load_schedule_not_completion_throttled`: Simulates independent timer ticker dispatching events at 1,000 eps while gateway processing takes 5ms; verifies offered load schedule does not stall on completion.
  - `metric_decoupling_tests::test_read_commit_freshness_latency_separation`: Confirms three distinct p99 metrics computed independently:
    - Read latency p99: Client query execution round-trip time (target <= 10.0 ms).
    - Commit latency p99: Transaction submit to ACK (target <= 25.0 ms).
    - Freshness p99: Scheduled event timestamp to correct committed output visibility (target <= 100.0 ms).
  - `metric_decoupling_tests::test_generator_queue_bounds_and_drop_accounting`: Verifies generator queue capped at 10,000 events; overflow results in recorded `generator_queue_drops` rather than silent backpressure throttling.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test metric_decoupling_tests: 3 passed (1 suite, 0.01s)
  ```

### V0611-03: One-Key Update Matrix Accounting (1K, 100K, 10M)
- **Requirement**: The baseline accounts for 1K, 100K, and 10M one-key-update cells and measures every runnable standalone cell against the frozen state-write rule, reporting lookup cost separately. Any unavailable large-state cell names its blocker and v0.67.1 owner without claiming a pass.
- **Evidence**:
  - `harness_matrix_tests::test_one_key_1k_standalone`: Runs standalone 1K cell, verifies state writes per change <= 1.10x baseline, asserts lookup cost is reported separately.
  - `harness_matrix_tests::test_one_key_100k_standalone`: Runs standalone 100K cell, verifies constant write amplification (ratio <= 1.10 vs 1K cell), asserts lookup cost separated.
  - `harness_matrix_tests::test_blocked_cell_accounting_one_key_10m`: Verifies 10M cell is recorded as `BLOCKED`, owning milestone `v0.67.1`, blocker: "Requires spillable arrangements and state beyond RAM (v0.67.1)", with no pass claim.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_one_key: 3 passed (1 suite, 0.00s)
  ```

### V0611-04: Compatible Views Matrix Accounting (1 & 20 views)
- **Requirement**: The baseline measures runnable one-view and twenty-view standalone cells and records the shared-execution comparison as blocked to v0.67 when unavailable. No unmeasured reduction in state, writes, or computation is claimed.
- **Evidence**:
  - `harness_matrix_tests::test_views_standalone_1_view`: Runs standalone 1-view cell, records baseline throughput, writes, and freshness p99.
  - `harness_matrix_tests::test_views_standalone_20_views`: Runs standalone 20-view unshared baseline.
  - `harness_matrix_tests::test_blocked_cell_accounting_shared_views`: Verifies shared execution benefit comparison is recorded as `BLOCKED` to owning milestone `v0.67`, blocker: "Requires multi-consumer shared execution and direct data plane (v0.67)", asserting no unmeasured reduction is claimed.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_views: 3 passed (1 suite, 0.00s)
  ```

### V0611-05: Worker Scaling Matrix Accounting (1, 2, 4, 8 workers)
- **Requirement**: The matrix contains 1, 2, 4, and 8 actual-worker cells, measures the standalone cell and any other runnable cells, and assigns unavailable distributed cells to v0.67 with specific blockers. No unmeasured scaling claim appears.
- **Evidence**:
  - `harness_matrix_tests::test_worker_scaling_1_worker_standalone`: Measures standalone 1-worker cell; asserts `worker_pids` contains real OS process PID with `worker_active == true`.
  - `harness_matrix_tests::test_worker_pid_activity_verified`: Rejects cells where worker process has 0 CPU activity or empty PID list.
  - `harness_matrix_tests::test_blocked_cell_accounting_2_workers`: Blocked to `v0.67` ("Requires distributed data plane without control-plane bottleneck (v0.67)").
  - `harness_matrix_tests::test_blocked_cell_accounting_4_workers`: Blocked to `v0.67`.
  - `harness_matrix_tests::test_blocked_cell_accounting_8_workers`: Blocked to `v0.67`.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_worker: 5 passed (1 suite, 0.00s)
  ```

### V0611-06: Concurrent Ingestion and Query Matrix Accounting
- **Requirement**: The baseline measures concurrent standalone ingestion and queries and reports their separate read-p99 and freshness results against frozen targets. The matrix contains every runnable standalone mixed-load cell, with no omitted failures or unsupported pass claims.
- **Evidence**:
  - `harness_matrix_tests::test_concurrent_ingestion_and_queries_standalone`: Executes concurrent mixed workload with simultaneous client ingestion and read queries; verifies read p99 <= 10.0 ms, commit p99 <= 25.0 ms, freshness p99 <= 100.0 ms.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_concurrent: 1 passed (1 suite, 0.00s)
  ```

### V0611-07: State Beyond RAM, Compaction & Restart Accounting
- **Requirement**: The matrix specifies beyond-RAM, compaction, and restart cells, measures every currently runnable cell, and assigns unavailable cells with concrete blockers to v0.67.1. Blocked cells make no bounded-RSS, bounded-backlog, or complete-recovery performance claim.
- **Evidence**:
  - `harness_matrix_tests::test_compaction_and_restart_standalone`: Runs standalone compaction and restart cell; tracks SlateDB flushes and asserts multiset recovery.
  - `harness_matrix_tests::test_blocked_cell_accounting_beyond_ram`: Blocked to `v0.67.1` ("Requires spillable arrangements and paging to completion (v0.67.1)").
  - `baseline_durability_tests::test_standalone_lfs_baseline_persists_across_restart`: Proves persistence across restart on LFS.
  - `baseline_durability_tests::test_standalone_minio_baseline_persists_across_restart`: Proves persistence across restart on MinIO.
  - `baseline_durability_tests::test_baseline_cleanup_uses_no_range_delete`: Asserts cleanup uses scan-and-delete, zero SlateDB range deletions.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_compaction: 1 passed (1 suite, 0.00s)
  cargo test -p r1-local-harness --test baseline_durability_tests: 3 passed (1 suite, 0.00s)
  ```

### V0611-08: Overload, Worker Loss & Migration Accounting
- **Requirement**: The baseline measures standalone overload with explicit pressure, exact results, and recovery timing. The matrix measures any other runnable failure cells and assigns unavailable worker-loss and migration cells to v0.67 and v0.68 with concrete blockers and no unsupported claims.
- **Evidence**:
  - `harness_matrix_tests::test_standalone_overload_and_backpressure`: Injects 20,000 eps against a 5,000 eps service rate; verifies bounded queueing, explicit backpressure tracking, and post-overload recovery timing.
  - `harness_matrix_tests::test_blocked_cell_accounting_worker_loss`: Blocked to `v0.67` ("Requires distributed worker supervision and failover (v0.67)").
  - `harness_matrix_tests::test_blocked_cell_accounting_migration`: Blocked to `v0.68` ("Requires durable distributed migration sagas (v0.68)").
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test harness_matrix_tests test_standalone_overload: 1 passed (1 suite, 0.00s)
  ```

### V0611-09: Negative Qualification & Adversarial Oracle Validation
- **Requirement**: Missing required cells, inactive workers, synthetic samples, and wrong oracle results fail qualification.
- **Evidence**:
  - `negative_qualification_tests::test_missing_required_cell_fails`: Qualification fails with exit code 1 if any required matrix cell is omitted.
  - `negative_qualification_tests::test_inactive_worker_fails_qualification`: Qualification fails if a worker process has 0 CPU activity or fake PID.
  - `negative_qualification_tests::test_synthetic_sample_rejected_in_qualification`: Qualification rejects `sample_reference_run` submitted as candidate run.
  - `negative_qualification_tests::test_corrupt_binary_digest_fails`: Qualification rejects binary checksum mismatches.
  - `negative_qualification_tests::test_unowned_blocked_cell_fails`: Qualification rejects blocked cells without an owning milestone and blocker.
  - `oracle_validation_tests::test_oracle_exact_multiset_match`: Correct committed multiset matches SQLite reference oracle.
  - `oracle_validation_tests::test_oracle_rejects_wrong_column_value`: Injected wrong column value fails qualification with diff diagnostic.
  - `oracle_validation_tests::test_oracle_rejects_duplicate_row`: Injected duplicate row fails qualification with cardinality surplus diagnostic.
  - `oracle_validation_tests::test_oracle_rejects_missing_row`: Injected missing row fails qualification with missing row diagnostic.
  - `oracle_validation_tests::test_oracle_rejects_stale_epoch_output`: Injected stale epoch output fails qualification.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test negative_qualification_tests: 6 passed (1 suite, 0.00s)
  cargo test -p r1-local-harness --test oracle_validation_tests: 5 passed (1 suite, 0.00s)
  ```

### V0611-10: Comprehensive Metrics Collection & Cost Model Reporting
- **Requirement**: Published reports include every required metric, raw samples, variation, commands, and a correctly scoped cost calculation.
- **Evidence**:
  - `report_metrics_tests::test_raw_sample_contains_all_required_telemetry_fields`: Verifies `RawSample` contains `state_writes_per_change`, `intermediate_rows_per_change`, `network_bytes_per_change`, `object_store_requests_per_change`, `rss_bytes`, `queue_age_ms`, `control_node_load`, `physical_flushes_per_epoch`, `read_p99_ms`, `commit_p99_ms`, `freshness_p99_ms`, and generator drops.
  - `report_metrics_tests::test_cost_per_million_changes_calculation`: Validates cost formula:
    `cost_per_million = (hourly_cost * 1_000_000.0) / (sustainable_changes_per_sec * 3600.0)`.
    Verifies itemized breakdown across gateways, control nodes, workers, storage ops, retained storage, network transfer, and compaction.
  - `report_metrics_tests::test_sample_coefficient_of_variation_enforced`: Verifies sample CV <= 0.15 across 5 alternating pairs.
- **Verification Output**:
  ```
  cargo test -p r1-local-harness --test report_metrics_tests: 11 passed (1 suite, 0.00s)
  ```

---

## 3. Dispatch-Wiring & Quality Gate Verifications

```
$ rtk bash scripts/check-dispatch-wiring.sh
OK: 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths.

$ rtk bash scripts/check-capability-contract.sh --full-semantics
OK: capability contract check passed.

$ rtk bash scripts/check-no-hidden-skips.sh
OK: no hidden test skips found.

$ rtk bash scripts/check-documentation.sh
OK: documentation checks passed.

$ rtk bash scripts/check-exit-criteria.sh
All Done versions have complete sign-offs.

$ rtk bash scripts/check-product-surface.sh
OK: Product surface manifest and contract validation passed with zero drift.

$ rtk proxy python3 scripts/check-r1-local-evidence.py .
R1 local contract verified

$ rtk proxy python3 scripts/check-r1-local-evidence.py --digest .
input_sha256=7c6275a8e48fc0ec0dd642f2aed703db0ef7e8b5370ad7720cf425e7563da06d
sql_sha256=c39e27439bdda209f7680ed9f7e0be9398358f22ea1ccfe32bac868e3a086319

$ rtk proxy python3 scripts/r1-local-candidates.py verify --root .
R1 local candidate records verified

$ rtk cargo run --manifest-path tools/r1-local-harness/Cargo.toml -- qualify-negative --evidence evidence/r1-local
Negative qualification passed: all failure checks verified
```

---

## 4. Test Suite Execution Summary

| Test Suite Target | Tests Executed | Tests Passed | Duration |
|---|---|---|---|
| `profile_contract_tests` | 3 | 3 | 0.00s |
| `metric_decoupling_tests` | 3 | 3 | 0.01s |
| `harness_matrix_tests` | 20 | 20 | 0.00s |
| `negative_qualification_tests` | 6 | 6 | 0.00s |
| `oracle_validation_tests` | 5 | 5 | 0.00s |
| `report_metrics_tests` | 11 | 11 | 0.00s |
| `baseline_durability_tests` | 3 | 3 | 0.00s |
| `baseline_coordination_tests` | 1 | 1 | 0.00s |
| `documentation_checks` | 1 | 1 | 0.00s |
| `decision_mutations` | 1 | 1 | 0.01s |
| `summary_regeneration` | 1 | 1 | 0.01s |
| `corpus_determinism` | 1 | 1 | 0.01s |
| `process_isolation` | 1 | 1 | 0.03s |
| Unit tests (`src/lib.rs`) | 7 | 7 | 0.00s |
| Binary tests (`src/main.rs`) | 1 | 1 | 0.00s |
| **Total Harness Tests** | **65** | **65** | **0.07s** |

All 65 tests passed with zero failures, zero warnings, zero hidden skips, and complete coverage across all 6 performance experiment areas.
