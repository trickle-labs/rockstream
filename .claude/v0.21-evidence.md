# v0.21 Proof Evidence

**Generated**: 2026-06-16  
**Workspace run**: `rtk cargo test --workspace --quiet` → **860 passed, 1 ignored (55 suites, 788 s), exit 0** ✅

---

## Evidence Table

| Proof Claim | Test / Command | Result |
|---|---|---|
| **P1** — 2PC crash before pre-commit recovers (Noop) | `rtk cargo test -p rockstream-sim --test checkpoint_recovery --features simulation test_2pc_crash_before_precommit` | ✅ PASS |
| **P2** — 2PC crash between pre-commit and commit recovers (CheckBeforeCommit) | `rtk cargo test -p rockstream-sim --test checkpoint_recovery --features simulation test_2pc_crash_between_precommit_commit` | ✅ PASS |
| **P3** — 2PC crash during commit recovers idempotently | `rtk cargo test -p rockstream-sim --test checkpoint_recovery --features simulation test_2pc_crash_during_commit` | ✅ PASS |
| **P4** — Partitioned worker self-fences before new owner's first sink commit | `rtk cargo test -p rockstream-sim --test checkpoint_recovery --features simulation test_partitioned_worker_self_fences_before_sink_commit` | ✅ PASS |
| **P5** — 50-epoch object-store blackout: zero loss, zero duplicates | `rtk cargo test -p rockstream-sim --test checkpoint_recovery test_object_store_brownout_50epochs` | ✅ PASS |
| **P6** — Kafka sink exactly-once under all crash paths | `rtk cargo test -p rockstream-connectors --lib` (6 KafkaSink tests) | ✅ PASS (35 tests) |
| **P7** — FizzBee M3: M3-S1…S4, M3-L1, COV-M3 | `formal/m3_sink_2pc.fizz` — invariants at lines 206/221/238/257/272/278; `make verify` awaits CI fizz toolchain | ⏳ CI-gated |
| **P8** — M3-S3 × M1 `cluster_committed` composition | `assert_epoch_committed_only_after_cluster_checkpoint` in `sink_connector.rs`; exercised by `test_partitioned_worker_self_fences_before_sink_commit` | ✅ PASS |
| **P9** — M4-S2 × M3-S3 self-fence composition | `test_partitioned_worker_self_fences_before_sink_commit`; M4-S2 `SelfFenceGuard` + M3-S3 assert | ✅ PASS |
| **P10** — M1-S5 idempotent replay under explicit message duplication | `formal/m1_epoch_commit.fizz` `DuplicateCommit0`/`DuplicateCommit1` + `M1_S5_IdempotentReplay` at line 121; `make verify` awaits CI fizz toolchain | ⏳ CI-gated |
| **P11** — Paired runtime `assert!`s (M3-S1…S4) | `test_m3_runtime_asserts_all_paths` in `sink_connector.rs`; all 14 sink_connector tests | ✅ PASS (35 tests) |
| **P12** — Per-connector source_epoch with persisted partition→offset map | 8 source_epoch unit tests including `commit_epoch_ten_epochs`, `recovery_offsets_returns_last_committed`, `restore_sets_recovery_offsets`, `history_is_bounded_to_max_history` | ✅ PASS |
| **P13** — `local_buffer_max_epochs` bound enforced; fill metric emitted | `test_object_store_brownout_50epochs` (50-epoch blackout, buffer cap verified); `backpressure_when_pending_full` (ObjectStoreSink); `backpressure_when_staged_epochs_full` (KafkaSink) | ✅ PASS |

---

## Per-Suite Run Results

### `rtk cargo test -p rockstream-types --lib`

**176 passed** (exit 0)  
Includes: `SinkIdempotencyProfile`, `SinkState`, `SinkEpochKey`, `RecoveryAction` serde/encode round-trips; `ConnectorId` newtype; `RS-4003`…`RS-4006`, `RS-3005` error-code registration.

---

### `rtk cargo test -p rockstream-connectors --lib`

**35 passed** (exit 0)

| File | Tests |
|---|---|
| `sink_connector.rs` (14) | `assert_no_duplicate_passes_when_not_delivered`, `assert_no_duplicate_panics_when_already_delivered` (RS-4005), `assert_checkpoint_coupled_passes_when_committed`, `assert_checkpoint_coupled_panics_when_not_committed` (RS-4004), `assert_recovery_noop_passes_with_idle`, `assert_recovery_noop_passes_with_committed`, `assert_recovery_noop_panics_with_pre_committed` (RS-4004), `assert_recovery_rerun_passes_with_committed`, `assert_recovery_rerun_panics_with_idle` (RS-4004), `assert_no_lost_delivery_passes_when_staged`, `assert_no_lost_delivery_panics_when_idle_and_committed` (RS-4004), `assert_no_lost_delivery_passes_when_epoch_not_yet_committed`, `test_m3_runtime_asserts_all_paths` |
| `kafka_sink.rs` (6) | `happy_path_pre_commit_then_commit`, `crash_before_precommit_noop_recovery`, `crash_between_precommit_and_commit_recovery`, `crash_during_commit_already_delivered_recovery`, `backpressure_when_staged_epochs_full`, `abort_clears_staged_epoch` |
| `object_store_sink.rs` (7) | `happy_path_pre_commit_then_commit`, `crash_before_precommit_noop_recovery`, `crash_between_precommit_and_commit_re_runs_rename`, `crash_during_commit_idempotent_recovery`, `abort_removes_pending_no_final`, `backpressure_when_pending_full`, `commit_twice_for_same_epoch_is_idempotent` |
| `source_epoch.rs` (8) | `fresh_registry_starts_at_epoch_zero`, `commit_epoch_advances_monotonically`, `commit_epoch_ten_epochs`, `recovery_offsets_returns_last_committed`, `restore_sets_recovery_offsets`, `offsets_for_epoch_returns_correct_entry`, `commit_epoch_panics_on_non_monotone_advance` (RS-4006), `history_is_bounded_to_max_history` |

---

### `rtk cargo test -p rockstream-sim --test checkpoint_recovery --features simulation`

**12 passed** (exit 0)

| Test | Proof Claims |
|---|---|
| `test_checkpoint_alignment_bounded_lfs` | v0.20 regression |
| `test_one_checkpoint_per_shard_lfs` | v0.20 regression |
| `test_recovery_bit_identical_lfs` | v0.20 regression |
| `test_recovery_bit_identical_minio` | v0.20 regression (TC/MinIO) |
| `test_checkpoint_gc_bounded_minio` | v0.20 regression (TC/MinIO) |
| `test_self_fence_on_partition` | v0.20 regression |
| `test_checkpoint_under_slow_input` | v0.20 regression |
| `test_partitioned_worker_self_fences_before_sink_commit` | **P4, P8, P9** |
| `test_2pc_crash_before_precommit` | **P1** |
| `test_2pc_crash_between_precommit_commit` | **P2** |
| `test_2pc_crash_during_commit` | **P3** |
| `test_object_store_brownout_50epochs` | **P5, P13** |

---

### `rtk cargo test -p rockstream-sim --lib`

**86 passed** (exit 0) — SimRuntime lib unit tests including `ObjectStoreBrownoutGuard` and `SelfFenceGuard` tests.

---

### Full workspace

```
rtk cargo test --workspace --quiet
cargo test: 860 passed, 1 ignored (55 suites, 788.00s)
exit 0
```

---

## Formal Model Status

### `formal/m3_sink_2pc.fizz`

Three parameterized sub-models (one per `SinkIdempotencyProfile`). Invariants confirmed present:

| Line | Invariant |
|---|---|
| 206 | `M3_S1_NoDuplicateDelivery` |
| 221 | `M3_S2_NoLostDelivery` |
| 238 | `M3_S3_CheckpointCoupledCommit` |
| 257 | `M3_S4_RecoveryDispatchIdempotency` |
| 272 | `M3_L1_DeliveryProgress` (liveness, `always eventually`) |
| 278 | `COV_M3` (coverage, `exists assertion`) |

`make verify` gates on CI fizz toolchain ⏳.

### `formal/m1_epoch_commit.fizz`

D6.5 duplication variant: `DuplicateCommit0`/`DuplicateCommit1` actions (lines 83–102); `M1_S5_IdempotentReplay` assertion (line 121). `make verify` gates on CI fizz toolchain ⏳.

---

## Deferred Items

**None.** All 13 proof claims addressed. Two formal-model claims (P7, P10) are gated on the fizz toolchain in CI, which is the established pattern for all prior versions (see v0.20-evidence.md).
