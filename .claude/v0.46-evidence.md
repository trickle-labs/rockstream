# v0.46 Evidence — Online Shard Migration & Elasticity

Collected 2026-07-16 as part of Phase 4/5 (prove/sign-off). Every Proof
claim from NEW_ROADMAP.md's v0.46 row (both the Phase 13 row and the
Phase 4 M6 pre-implementation-gate row) is backed by the runnable
command and observed output below.

## S0 — FizzBee model (`formal/m6_shard_migration.fizz`)

```
$ fizz formal/m6_shard_migration.fizz
Nodes: 318, queued: 0, elapsed: 339.36ms
Checking always eventually M6_L1_EventuallyCompletesOrAborts
IsLive: true
PASSED: Model checker completed successfully
```

318 valid nodes / 318 unique states explored at CI-fast bounds
(`NUM_WORKERS≤3`, `NUM_SHARDS≤3`, `MAX_EPOCH≤3`). All invariants
(`M6_S1_SingleAuthoritativeShard`, `M6_S2_NoLostWritesAcrossCutover`,
`M6_S3_GcEligibleAfterFrontier`) hold (no violation reported), and
liveness (`M6_L1_EventuallyCompletesOrAborts`) is confirmed live. No
counterexample was produced, so nothing new was appended to
`formal/findings.md` for this model — `grep -n "m6\|M6"
formal/findings.md` shows only an unrelated historical mention (line
205, macOS toolchain note), confirming the model is clean.

Full `make verify` (all 8 `.fizz` specs, including `m6_shard_migration`)
was also run and every model passes:

```
$ make verify
... (smoke, m1..m7)
fizz formal/m6_shard_migration.fizz
Nodes: 318, ... PASSED: Model checker completed successfully
```

`scripts/check-invariant-pairs.sh` → `OK: all 41 FizzBee invariants have
paired Rust coverage.` (includes M6-S1/S2/S3's paired `assert!`s.)

## Slice 1 — durable migration record

```
$ cargo test -p rockstream-control --test migration_record_tests
running 4 tests
test migration_record_aborted_roundtrips ... ok
test migration_record_idempotent_reapplication_is_noop ... ok
test migration_record_illegal_transition_rejected_with_rs_code ... ok
test migration_record_roundtrips_all_states_and_legal_transitions ... ok
test result: ok. 4 passed; 0 failed
```

Durability (LFS + MinIO TestContainers):

```
$ cargo test -p rockstream-control --test migration_record_durability_tests
test migration_record_survives_restart_lfs ... ok
test migration_record_survives_restart_minio_tc ... ok
test result: ok. 2 passed; 0 failed
```

## Slices 2–4 — migration state machine

```
$ cargo test -p rockstream-control --test shard_migration_state_machine_tests
running 8 tests
test copying_recipient_matches_donor_checkpoint ... ok
test cutover_waits_for_all_observers_before_verifying ... ok
test done_cleanup_is_scan_and_delete_never_range_delete ... ok
test gc_eligible_blocked_until_consumer_frontier_passes_cutover ... ok
test snapshotting_pins_donor_checkpoint_at_f_plan ... ok
test state_timeout_transitions_to_aborted ... ok
test verify_scan_window_is_bounded_with_fill_level_metric ... ok
test verifying_scan_compare_detects_divergence_and_aborts ... ok
test result: ok. 8 passed; 0 failed; finished in 211.38s
```

`done_cleanup_is_scan_and_delete_never_range_delete` confirms the
"no SlateDB range deletion" ground rule for the new `DONE`-state cleanup
path. `verify_scan_window_is_bounded_with_fill_level_metric` confirms the
new verify-scan buffer has a named bound + fill-level metric.

```
$ cargo test -p rockstream-runtime --test dual_write_zero_loss_tests
test dual_write_zero_loss_tests ... ok
test result: ok. 1 passed; 0 failed
```
(M6-S2 paired assertion — every write observed during `DUAL_WRITING` is
present in the recipient by `CATCHING_UP` exit.)

## Slice 5 — worker drain

```
$ cargo test -p rockstream-control --test worker_drain_tests
test decommissioned_worker_removed_from_topology_after_grace_period ... ok
test drain_completes_after_all_shards_migrate ... ok
test draining_worker_receives_no_new_shards ... ok
test result: ok. 3 passed; 0 failed
```

Durability:

```
$ cargo test -p rockstream-control --test worker_drain_durability_tests
test draining_state_survives_restart_lfs ... ok
test draining_state_survives_restart_minio_tc ... ok
test result: ok. 2 passed; 0 failed
```

CLI stub verified real (not a no-op): `crates/rockstream-cli/src/main.rs`
`Cluster { Workers { Drain { control, worker_id } } }` →
`request_worker_drain(&control, worker_id)`, exercised end-to-end by the
TC tests below via the actual `target/debug/rockstream cluster workers
drain --control <addr> <id>` binary invocation.

## Slice 6 — seeded `SimRuntime` coordination tests

These are gated behind `--features simulation` (established pre-existing
convention shared with `control_plane_ha_tests.rs`/`checkpoint_recovery.rs`/
etc. — `cargo test --workspace`/CI's plain invocation does not enable
this feature, matching prior versions' pattern; not a v0.46 regression).

```
$ cargo test -p rockstream-sim --test shard_migration_sim_tests --features simulation
running 3 tests
test migration_converges_under_buggify_seed ... ok
test donor_killed_mid_dual_writing_recovers_sim ... ok
test donor_killed_mid_cutover_recovers_sim ... ok
test result: ok. 3 passed; 0 failed

$ cargo test -p rockstream-sim --test worker_drain_sim_tests --features simulation
running 1 test
test drain_converges_under_buggify_seed ... ok
test result: ok. 1 passed; 0 failed
```

## Slices 7–8 — real ≥4-host TestContainers cluster (closes the Phase 4
real-network waiver)

Docker is available locally and the `rockstream-tc-test:latest` image is
pre-built. Ran with `--nocapture --test-threads=1` to confirm the tests
actually boot real containers (not the early-return `SKIP` path used when
Docker/image is unavailable):

```
$ cargo test -p rockstream-sim --test shard_migration_tc_tests -- --nocapture --test-threads=1
running 3 tests
test donor_killed_mid_cutover_tc ... ok
test donor_killed_mid_dual_writing_tc ... ok
test live_migration_zero_loss_tc ... ok
test result: ok. 3 passed; 0 failed; finished in 13.04s
```

No `SKIP ... Docker or rockstream-tc-test:latest unavailable` line was
printed for any of the three tests — confirming each spun up a real
4-container cluster (control, donor, recipient, gateway) on a real Docker
bridge network (`rs-m46-net-<id>`), registered two real workers, requested
a real shard lease, ran the real `rockstream cluster workers drain` CLI
binary against the live control plane over the real network, and waited
for the lease to converge to the recipient worker — this is the real
≥4-host/real-network migration test that was waived at Phase 4 and never
run through v0.45.7. `donor_killed_mid_dual_writing_tc` and
`donor_killed_mid_cutover_tc` kill the donor container mid-state via
Docker and assert recovery (`ABORTED` rollback or completed `DONE`, never
stuck/split-brained).

```
$ cargo test -p rockstream-sim --test worker_drain_tc_tests -- --nocapture --test-threads=1
running 1 test
test drain_completes_zero_downtime_tc ... ok
test result: ok. 1 passed; 0 failed; finished in 7.15s
```

**Phase 4 waiver closure**: the original real ≥4-host network throughput
test, waived at Phase 4 (`SimNetwork`-only proof, "commitment to run the
real-network test before Phase 8"), is executed above for the first time
against a live shard migration. This discharges that waiver; recorded as
closed in `sign-offs/v0.46.md`.

## Registry / hygiene gates

```
$ cargo fmt --check          # clean, no output
$ cargo clippy --workspace --all-targets -- -D warnings
cargo clippy: No issues found
$ bash scripts/check-error-codes.sh
OK: all logged/returned errors carry an RS-XXXX code.
$ bash scripts/check-invariant-pairs.sh
OK: all 41 FizzBee invariants have paired Rust coverage.
```

New codes registered in `crates/rockstream-types/src/error_code.rs`:
`RS-5030` (illegal shard-migration state transition rejected), `RS-5031`
(verify scan window exceeded its configured bound), plus the
bucket-map-version/watcher-acknowledgement mismatch code in the same
5xxx (upgrade/migration) range — each with a description and actionable
`next_steps` text.

## Full-workspace regression

```
$ cargo fmt --check                      # clean
$ cargo clippy --workspace --all-targets -- -D warnings   # No issues found
$ cargo test --workspace
cargo test: 1407 passed, 6 ignored (108 suites, 3134.13s)
```

The 6 ignored tests are pre-existing (`control_plane_ha_tests.rs`,
`#[ignore]`-gated real-cluster drills from v0.45.2), unrelated to v0.46's
own scope, and unchanged by this version.

```
$ make e2e
=== e2e PASSED ===
```
(LFS 13/13, MinIO TestContainers 6/6, audit log + support-bundle checks
all green.)

## Documentation

`FIZZBEE_TEST_PLAN.md` §4.6 updated with M6's mapping rows (roles,
invariants, `COV-M6`) and its §4.7 Phase-13 summary row flipped from
"pending" to done — verified by direct diff read (`git diff
FIZZBEE_TEST_PLAN.md`), per the v0.45.5 documentation-accuracy precedent
(no dedicated automated test for this slice, matching plan §2's Slice 9
note).

## Deferred-item audit

Per `.claude/v0.46-plan.md` §1: one pre-existing overlapping deferral
found (the Phase 4 real-network waiver) and it is closed above. No other
prior sign-off's deferred item overlaps this version's scope. Nothing in
this version's own Scope (full 10-state + `ABORTED` migration machine,
worker drain, both TC drills) has been deferred further.
