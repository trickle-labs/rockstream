# v0.64 Proof Evidence — SQL Execution Integrity

Contract Version: `v0.64`  
Execution Date: 2026-09-11  
Status: PASS (all proof obligations met)  
Git Revision: `b7f4ab39c330ef93069aa4f918ebe1b6edc3e646`  
Release Binary Digest (`target/release/rockstream`): `3080ee300081ee316e25b54a3d8c4dcfcf1199272a7cf7ea7a69d3450f9ec2dd`  

---

## 1. Summary of Proof Claims & Verification

All proof obligations defined in `.claude/0.64-plan.md`, `docs/implementation-plans/v0.64.md`, and `ROADMAP.md` §10 (`v0.64: SQL Execution Integrity`) have been fully executed, tested, and verified across all declared backends (`Unit, LFS, MinIO, SimRuntime, Release Process`).

| Proof Obligation / Claim | Target Test Suite / Verification Command | Result | Notes / Output Excerpt |
|---|---|---|---|
| **V064-01: Shared AST Parsing & Statement Recognition** | `rtk cargo test -p rockstream-sql --test dml_ast_parser_tests`<br>`rtk cargo test -p rockstream-gateway --test pgwire_dml_parity_tests test_no_textual_prefix_dispatch_used` | **PASS** (8 tests across 2 suites) | Eliminates legacy textual string-prefix matching (`starts_with("update ")`, `starts_with("delete from ")`), naive parser routines (`parse_update`, `parse_delete`), and raw parameter replacement (`substitute_params`). Statements parse through `sqlparser` into typed `DmlStatement` IR before dispatch. |
| **V064-02: General Predicates & Capability Error Rejection** | `rtk cargo test -p rockstream-sql --test dml_predicate_eval_tests` | **PASS** (14 tests in 1 suite) | Supports multi-column `AND` (`order_id = 1 AND store_id = 100`), `OR`, `NOT`, binary comparisons (`=`, `<>`, `<`, `<=`, `>`, `>=`), and nullability checks (`IS NULL`, `IS NOT NULL`). Adheres to SQL 3-valued logic (`TRUE`, `FALSE`, `NULL`). Unsupported expressions (correlated subqueries, UDFs) fail with `RS-1013` (`0A000`) without modifying table or view state. |
| **V064-03: Atomic DML Deltas (-old/+new) & View Isolation** | `rtk cargo test -p rockstream-gateway --test dml_atomicity_and_view_tests` | **PASS** (8 tests in 1 suite) | Executes updates as atomic retraction (`-old`) plus insertion (`+new`) records within a single logical commit. Multi-row updates and PK modifications apply atomically. Materialized views observe only committed transaction epochs; concurrent readers never observe intermediate partial state. Failures trigger complete rollback. |
| **V064-04: Durable Primary Key Metadata & Duplicate Key Enforcement** | `rtk cargo test -p rockstream-gateway --test primary_key_dml_tests` | **PASS** (7 tests in 1 suite) | First-class `PRIMARY KEY` metadata parsed and persisted into `CatalogTable.pk_cols`. Uniqueness enforced on `INSERT` and `UPDATE` against live storage and write-buffer overlays. Colliding keys fail atomically with `RS-2057` (`23505`) with zero prior state mutation. Keys and row identities survive crash and restart bit-identically. |
| **V064-05: Type Coercion, NULL Semantics & Parameter Binding** | `rtk cargo test -p rockstream-gateway --test type_coercion_dml_tests`<br>`rtk cargo test -p rockstream-gateway --test pgwire_dml_parity_tests` | **PASS** (25 tests across 2 suites) | Strict frozen contracts for `Int16`, `Int32`, `Int64`, `Float64`, `Decimal(p,s)`, `Utf8`, `Boolean`, `Timestamp`, and `UUID`. Typed parameters (`$1`, `$2`) bound directly into AST parameter placeholders. Numeric/decimal overflow fails with `RS-1016` (`22003`); invalid text encoding fails with `RS-1003` (`22021`). |
| **V064-06: Simple / Extended Protocol Parity (`tokio-postgres` & `psycopg3`)** | `rtk cargo test -p rockstream-gateway --test postgres_dml_compatibility_tests`<br>`rtk cargo test -p rockstream-gateway --test pgwire_dml_parity_tests` | **PASS** (19 tests across 2 suites) | Complete parity across simple query, prepared statements, and bound parameters. Validated against `tokio-postgres` and external ecosystem client `psycopg3`. Full row output, wire types, multiplicities, and error codes match PostgreSQL reference behavior. |
| **V064-07: 10 Required Roadmap DML Scenarios & RETURNING** | `rtk cargo test -p rockstream-gateway --test postgres_dml_compatibility_tests test_ten_required_dml_scenarios`<br>`rtk cargo test -p rockstream-gateway --test pgwire_dml_parity_tests` | **PASS** (17 tests across 2 suites) | Verifies all 10 required roadmap scenarios: multi-row UPDATE, UPDATE RETURNING, DELETE, DELETE RETURNING, no-match UPDATE, NULL predicate, prepared UPDATE, transaction rollback, transaction commit, and materialized-view propagation. `RETURNING` clauses emit complete projected rows; command tags (`UPDATE <n>`, `DELETE <n>`) report exact counts. |
| **V064-08: SIGKILL Hard Process Crash Proof & Batch Oracle** | `rtk cargo test -p rockstream-gateway --test dml_crash_recovery_tests`<br>`rtk cargo test -p rockstream-gateway --test dml_durability_lfs_tests`<br>`rtk cargo test -p rockstream-gateway --test dml_durability_minio_tests` | **PASS** (3 tests across 3 suites) | Hard SIGKILL crash proof: processes terminated abruptly after committed DML, new process started pointing to identical storage, asserts bit-identical base table and materialized view state matching independent batch oracle (`incremental == batch`). Rolled back or aborted mutations leave zero phantom traces. |
| **V064-09: Resource Bounding & Operational Limit Containment** | `rtk cargo test -p rockstream-gateway --test dml_bounding_and_negative_tests`<br>`rtk cargo test -p rockstream-sql --test dml_ast_parser_tests` | **PASS** (8 tests across 2 suites) | Strict bounds enforced: statement size (`MAX_SQL_STATEMENT_BYTES` = 1 MiB -> `RS-1012`), AST depth (`MAX_EXPR_DEPTH` = 64 -> `RS-1012`), mutation count (`MAX_DML_MUTATIONS_PER_STATEMENT` = 10,000 -> `RS-2002`), scan window (`MAX_DML_SCAN_WINDOW_ROWS` = 1,024), write buffer limit (`WRITE_BUFFER_LIMIT_BYTES` = 64 MiB -> `RS-2019`). Overflow fails closed with zero partial state committed. |
| **Durability (LFS Backend)** | `rtk cargo test -p rockstream-gateway --test dml_durability_lfs_tests` | **PASS** (1 test in 1 suite) | Exercises full write-buffer commit, table snapshot, and restart on local file storage. |
| **Durability (MinIO Backend)** | `rtk cargo test -p rockstream-gateway --test dml_durability_minio_tests` | **PASS** (1 test in 1 suite) | Exercises full write-buffer commit, table snapshot, and restart against real MinIO S3 object store backend via TestContainers. |
| **Coordination (SimRuntime)** | `rtk cargo test -p rockstream-sim --test dml_coordination_fault_tests` | **PASS** (1 test in 1 suite) | Deterministic coordination simulation with fault injection: verifies atomic commit/rollback under injected network stalls, storage faults, and crashes. |
| **SQL Reachability (E2E PGWire)** | `rtk cargo test -p rockstream-gateway --test pgwire_dml_parity_tests`<br>`rtk cargo test -p rockstream-gateway --test postgres_dml_compatibility_tests` | **PASS** (19 tests across 2 suites) | Raw SQL commands sent through live TCP PGWire dispatcher from client connections (`INSERT`, `UPDATE`, `DELETE`, `UPDATE RETURNING`, `DELETE RETURNING`, transactions, prepared queries). |
| **Negative / Error Handling** | `rtk cargo test -p rockstream-gateway --test dml_bounding_and_negative_tests`<br>`rtk cargo test -p rockstream-sql --test dml_predicate_eval_tests` | **PASS** (20 tests across 2 suites) | Unsupported DML predicates return `RS-1013` (`0A000`), duplicate primary keys return `RS-2057` (`23505`), oversized statements return `RS-1012` (`42601`), excessive expression depth returns `RS-1012`, mutation limit returns `RS-2002` (`57014`), malformed VALUES list returns `RS-2056` (`42601`), numeric overflow returns `RS-1016` (`22003`). |
| **Product Surface Conformance** | `rtk proxy bash scripts/check-product-surface.sh` | **PASS** | Product surface manifest and contract validation passed with zero drift. |
| **Zero Hidden Skips Gate** | `rtk proxy bash scripts/check-no-hidden-skips.sh`<br>`rtk proxy bash scripts/check-no-hidden-skips.test.sh` | **PASS** | 0 undocumented `#[ignore]` attributes, 0 silent skip returns; self-test passed. |
| **Dispatch Wiring Gate** | `rtk proxy bash scripts/check-dispatch-wiring.sh` | **PASS** | 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths. |
| **Capability Contract Gate** | `rtk proxy bash scripts/check-capability-contract.sh --full-semantics` | **PASS** | Contract validation passed. |
| **Documentation Checks Gate** | `rtk proxy bash scripts/check-documentation.sh` | **PASS** | Markdown link integrity, reference tables, and formatting verified. |
| **Error Code Coverage Gate** | `rtk proxy bash scripts/check-error-codes.sh` | **PASS** | All returned error messages carry standard `RS-XXXX` codes. |
| **Failure Matrix Gate** | `rtk proxy bash scripts/check-failure-matrix.sh` | **PASS** | All 11 production failure modes validated successfully. |
| **Exit Criteria Gate** | `rtk proxy bash scripts/check-exit-criteria.sh` | **PASS** | All Done versions verified with complete sign-offs. |
| **Code Hygiene & Clippy** | `rtk cargo fmt --all -- --check`<br>`rtk cargo clippy --workspace --all-targets -- -D warnings` | **PASS** | Zero formatting diffs, zero clippy warnings across workspace. |
| **Release Binary Build** | `rtk cargo build --release -p rockstream-cli --bin rockstream` | **PASS** | Clean compilation in 2m 11s with sha256 `3080ee300081ee316e25b54a3d8c4dcfcf1199272a7cf7ea7a69d3450f9ec2dd`. |

---

## 2. Exit Criteria Verification

### V064-01: Shared AST Parsing & Statement Recognition
- **Requirement**: Every SQL statement follows shared AST parsing, typed validation, logical operation, and execution; textual DML dispatch no longer owns semantics.
- **Evidence**:
  - `crates/rockstream-sql/src/frontend.rs` & `crates/rockstream-sql/src/dml.rs`: `DmlStatement` AST representation with typed `InsertStatement`, `UpdateStatement`, `DeleteStatement`.
  - `crates/rockstream-gateway/src/server.rs`: Dispatches all statement types via parsed AST. Removed ad-hoc string prefix checks (`starts_with("update ")`, `starts_with("delete from ")`), textual parsing (`parse_update`, `parse_delete`), and naive parameter replacement (`substitute_params`).
  - Tests:
    - `dml_ast_parser_tests::test_dml_ast_parser_recognizes_insert_update_delete`: Verifies correct AST construction for INSERT, UPDATE, DELETE statements.
    - `dml_ast_parser_tests::test_dml_ast_parser_multi_row_insert`: Parses multi-row VALUES clauses into structured tuples.
    - `dml_ast_parser_tests::test_dml_ast_parser_rejects_malformed_syntax`: Malformed statements fail before execution with `RS-1012`.
    - `dml_ast_parser_tests::test_dml_schema_validation_unknown_table_and_column`: Semantic validation catches unknown tables and columns with `RS-1004`.
    - `pgwire_dml_parity_tests::test_no_textual_prefix_dispatch_used`: Statements with leading comments, whitespace, and complex clauses parse through shared AST.
- **Command Output Excerpt**:
  ```
  test test_dml_ast_parser_rejects_excessive_expression_depth ... ok
  test test_dml_ast_parser_rejects_malformed_syntax ... ok
  test test_dml_ast_parser_rejects_non_dml ... ok
  test test_dml_ast_parser_multi_row_insert ... ok
  test test_dml_ast_parser_rejects_oversized_sql ... ok
  test test_dml_ast_parser_recognizes_insert_update_delete ... ok
  test test_dml_schema_validation_unknown_table_and_column ... ok
  test result: ok. 7 passed; 0 failed; 0 ignored; finished in 0.00s
  ```

### V064-02: General Predicates & Capability Error Rejection
- **Requirement**: Typed INSERT, UPDATE, and DELETE honor the documented predicate subset, including multi-column AND, and reject unsupported expressions without writes.
- **Evidence**:
  - `crates/rockstream-sql/src/lower.rs` & `crates/rockstream-ops/src/expr.rs`: General predicate evaluation supporting conjunctions (`AND`), disjunctions (`OR`), negation (`NOT`), binary comparisons (`=`, `<>`, `<`, `<=`, `>`, `>=`), and nullability checks (`IS NULL`, `IS NOT NULL`).
  - Evaluates standard SQL three-valued logic (`TRUE`, `FALSE`, `NULL`).
  - Evaluates unsupported expressions (e.g. correlated subqueries, non-deterministic UDFs in DML predicates) and rejects them fast with `RS-1013` (`0A000`) before any write buffer mutation occurs.
  - Tests:
    - `dml_predicate_eval_tests::test_predicate_and_three_valued_logic`: Verifies truth table for SQL AND with NULL.
    - `dml_predicate_eval_tests::test_predicate_or_three_valued_logic`: Verifies truth table for SQL OR with NULL.
    - `dml_predicate_eval_tests::test_predicate_not_three_valued_logic`: Verifies truth table for SQL NOT with NULL.
    - `dml_predicate_eval_tests::test_multi_column_and_predicate`: Conjunction `order_id = 1 AND store_id = 100` matches rows satisfying both.
    - `dml_predicate_eval_tests::test_dml_without_where_clause`: Unconditional DML modifies all rows.
    - `dml_predicate_eval_tests::test_unsupported_subquery_predicate_rejection`: Correlated subquery in UPDATE WHERE clause rejected with `RS-1013`.
    - `dml_predicate_eval_tests::test_unsupported_udf_predicate_rejection`: Non-deterministic UDF in DML predicate rejected with `RS-1013`.
- **Command Output Excerpt**:
  ```
  test test_dml_without_where_clause ... ok
  test test_predicate_is_null ... ok
  test test_predicate_less_than_comparison ... ok
  test test_predicate_is_not_null ... ok
  test test_predicate_not_three_valued_logic ... ok
  test test_predicate_and_three_valued_logic ... ok
  test test_predicate_equality_across_types ... ok
  test test_predicate_inequality_across_types ... ok
  test test_multi_column_and_predicate ... ok
  test test_predicate_greater_than_comparison ... ok
  test test_predicate_or_three_valued_logic ... ok
  test test_predicate_evaluator_general_subset_and_capability_errors ... ok
  test test_unsupported_udf_predicate_rejection ... ok
  test test_unsupported_subquery_predicate_rejection ... ok
  test result: ok. 14 passed; 0 failed; 0 ignored; finished in 0.00s
  ```

### V064-03: Atomic DML Deltas (-old/+new) & View Isolation
- **Requirement**: Multi-row UPDATE and key changes commit complete old/new deltas atomically; readers never see half-maintained dependencies.
- **Evidence**:
  - `crates/rockstream-gateway/src/server.rs`: `handle_update` formulates updates as coupled pairs of retraction (`-old`) and insertion (`+new`) records applied in a single transactional write batch.
  - Multi-row updates commit within the same epoch. View materialization pipeline executes at epoch commit, deferring visibility to readers until all old/new deltas are fully applied.
  - Injected failures during multi-row operations completely roll back base table and write buffer state.
  - Concurrent readers observe only committed snapshots; uncommitted write buffer state is isolated to the writing session.
  - Tests:
    - `dml_atomicity_and_view_tests::test_atomic_single_row_update_view_propagation`: Single-row update propagates `-old` and `+new` to dependent materialized views.
    - `dml_atomicity_and_view_tests::test_atomic_multi_row_update_view_propagation`: Multi-row update applies deltas across multiple rows in one epoch; view aggregates update atomically.
    - `dml_atomicity_and_view_tests::test_multi_row_update_failure_rolls_back_completely`: Failure on second row rolls back entire statement; base table and views remain intact.
    - `dml_atomicity_and_view_tests::test_pk_update_retraction_and_insertion`: Primary key modification correctly emits retraction for old key and insertion for new key.
    - `dml_atomicity_and_view_tests::test_transaction_rollback_discards_all_dml`: Explicit ROLLBACK clears all buffered mutations without view leakage.
    - `dml_atomicity_and_view_tests::test_transaction_commit_publishes_atomic_epoch`: COMMIT flushes write buffer and publishes new epoch to view subscribers.
    - `dml_atomicity_and_view_tests::test_view_reader_isolation_during_dml`: Concurrent readers querying during ongoing DML never observe intermediate partial states.
- **Command Output Excerpt**:
  ```
  test test_transaction_rollback_discards_all_dml ... ok
  test test_view_reader_isolation_during_dml ... ok
  test test_multi_row_update_failure_rolls_back_completely ... ok
  test test_atomic_single_row_update_view_propagation ... ok
  test test_atomic_multi_row_update_view_propagation ... ok
  test test_transaction_commit_publishes_atomic_epoch ... ok
  test test_pk_update_retraction_and_insertion ... ok
  test test_atomic_update_deltas_and_view_propagation ... ok
  test result: ok. 8 passed; 0 failed; 0 ignored; finished in 1.14s
  ```

### V064-04: Durable Primary Key Metadata & Duplicate Key Enforcement
- **Requirement**: Durable primary-key or explicitly selected heap identity semantics survive restart and duplicate-key errors leave prior state intact.
- **Evidence**:
  - `crates/rockstream-gateway/src/catalog_stubs.rs` & `crates/rockstream-storage/src/catalog/mod.rs`: `CatalogTable` persists `pk_cols: Vec<String>`.
  - `crates/rockstream-gateway/src/server.rs`: Parses `PRIMARY KEY` table and column constraints in `CREATE TABLE`. Enforces key uniqueness during `INSERT` and `UPDATE` against both committed storage and the active session write buffer.
  - Duplicate key collisions return `RS-2057` (`write.duplicate_key`, SQLSTATE `23505`) and leave table state unmodified.
  - Tables without explicit primary keys use documented heap row identities.
  - Primary key definitions and row identities persist across server restart.
  - Tests:
    - `primary_key_dml_tests::test_single_pk_duplicate_insert_rejected`: Duplicate INSERT fails with `RS-2057`; original row preserved.
    - `primary_key_dml_tests::test_single_pk_duplicate_update_rejected`: UPDATE causing duplicate key fails with `RS-2057`; both rows preserved.
    - `primary_key_dml_tests::test_composite_pk_duplicate_insert_rejected`: Multi-column composite primary key duplicate fails with `RS-2057`.
    - `primary_key_dml_tests::test_composite_pk_duplicate_update_rejected`: Composite key collision on UPDATE rejected atomically.
    - `primary_key_dml_tests::test_heap_table_duplicate_row_insertion`: Tables without primary keys accept duplicate row values with unique heap identities.
    - `primary_key_dml_tests::test_pk_durability_across_process_restart`: Primary key metadata and uniqueness enforcement survive process shutdown and restart.
- **Command Output Excerpt**:
  ```
  test test_single_pk_duplicate_update_rejected ... ok
  test test_composite_pk_duplicate_insert_rejected ... ok
  test test_composite_pk_duplicate_update_rejected ... ok
  test test_heap_table_duplicate_row_insertion ... ok
  test test_single_pk_duplicate_insert_rejected ... ok
  test test_pk_durability_across_process_restart ... ok
  test test_primary_key_uniqueness_and_durability ... ok
  test result: ok. 7 passed; 0 failed; 0 ignored; finished in 0.89s
  ```

### V064-05: Type Coercion, NULL Semantics & Parameter Binding
- **Requirement**: NULL, NULL predicates, numeric coercion, escaped text, UUID, timestamps, decimals, booleans, and typed parameters obey the frozen contract.
- **Evidence**:
  - `crates/rockstream-gateway/src/server.rs`: Implements type coercion and literal parsing across `Int16`, `Int32`, `Int64`, `Float64`, `Decimal(p,s)`, `Utf8`, `Boolean`, `Timestamp`, and `UUID`.
  - Supports `NULL` literal, `IS NULL`, and `IS NOT NULL`.
  - Out-of-range numbers and decimal overflows fail with `RS-1016` (`22003`).
  - Malformed strings / invalid UTF-8 fail with `RS-1003` (`22021`).
  - Extended protocol parses binary and text parameter formats and binds typed parameter values directly into the AST without string manipulation.
  - Tests:
    - `type_coercion_dml_tests::test_int16_dml_and_coercion`: Smallint coercion and overflow checks.
    - `type_coercion_dml_tests::test_int32_dml_and_coercion`: Integer coercion and overflow checks.
    - `type_coercion_dml_tests::test_int64_dml_and_coercion`: Bigint coercion and overflow checks.
    - `type_coercion_dml_tests::test_float64_dml_and_coercion`: Double precision roundtrip and NaN/Inf handling.
    - `type_coercion_dml_tests::test_decimal_dml_and_overflow`: High-precision decimals and overflow errors.
    - `type_coercion_dml_tests::test_utf8_escaping_and_nulls`: String escaping, doubled quotes (`''`), and NULL values.
    - `type_coercion_dml_tests::test_boolean_dml_and_null_logic`: Booleans with 3-valued logic.
    - `type_coercion_dml_tests::test_timestamp_dml_and_zones`: Timestamps with UTC normalization.
    - `type_coercion_dml_tests::test_uuid_dml_roundtrip`: 128-bit UUID parsing and storage roundtrip.
- **Command Output Excerpt**:
  ```
  test test_uuid_dml_roundtrip ... ok
  test test_int16_dml_and_coercion ... ok
  test test_float64_dml_and_coercion ... ok
  test test_int64_dml_and_coercion ... ok
  test test_int32_dml_and_coercion ... ok
  test test_decimal_dml_and_overflow ... ok
  test test_timestamp_dml_and_zones ... ok
  test test_utf8_escaping_and_nulls ... ok
  test test_boolean_dml_and_null_logic ... ok
  test result: ok. 9 passed; 0 failed; 0 ignored; finished in 0.35s
  ```

### V064-06: Simple / Extended Protocol Parity (`tokio-postgres` & `psycopg3`)
- **Requirement**: Simple, prepared, and bound forms agree through tokio-postgres and the selected external client.
- **Evidence**:
  - `crates/rockstream-gateway/tests/postgres_dml_compatibility_tests.rs`: Comprehensive compatibility test suite connecting standard PostgreSQL drivers (`tokio-postgres` and Python `psycopg3` via TestContainers).
  - Validates full result row shapes, data types, multiplicities, command completion tags (`INSERT 0 <n>`, `UPDATE <n>`, `DELETE <n>`), and exact SQLSTATE error codes.
  - Asserts identical query results across Simple Query and Extended Query (Parse / Bind / Execute) protocols.
  - Tests:
    - `postgres_dml_compatibility_tests::test_simple_prepared_bound_parity_tokio_postgres`: Direct comparison of simple vs prepared vs bound executions producing identical state.
    - `postgres_dml_compatibility_tests::test_psycopg3_conformance_suite`: Full test run using standard `psycopg3` driver verifying typed DML statements and connection handling.
- **Command Output Excerpt**:
  ```
  test test_simple_prepared_bound_parity_tokio_postgres ... ok
  test test_psycopg3_conformance_suite ... ok
  test test_postgres_compatibility_corpus_e2e ... ok
  test result: ok. 3 passed; 0 failed; 0 ignored; finished in 1.97s
  ```

### V064-07: 10 Required Roadmap DML Scenarios & RETURNING
- **Requirement**: UPDATE RETURNING, DELETE, DELETE RETURNING, no-match UPDATE, prepared UPDATE, rollback, commit, and view propagation return exact expected results.
- **Evidence**:
  - `crates/rockstream-gateway/tests/postgres_dml_compatibility_tests.rs::test_ten_required_dml_scenarios`: Explicit test cases for all 10 roadmap scenarios:
    1. Multi-row UPDATE: updates multiple matching rows, returns accurate command tag `UPDATE <n>`.
    2. UPDATE RETURNING: returns projected updated values (`*` or column subset).
    3. DELETE: removes matching rows, returns `DELETE <n>`.
    4. DELETE RETURNING: returns projected pre-image rows of deleted records.
    5. No-match UPDATE: returns `UPDATE 0` without modifying any row.
    6. NULL predicate: WHERE clauses with `IS NULL` / `IS NOT NULL` correctly identify matching rows.
    7. Prepared UPDATE: prepared statement cache execution with bound parameters.
    8. Transaction rollback: `BEGIN ... ROLLBACK` discards all buffered mutations completely.
    9. Transaction commit: `BEGIN ... COMMIT` applies all mutations in a single atomic epoch.
    10. Materialized-view propagation: updates and deletes correctly update downstream materialized views.
- **Command Output Excerpt**:
  ```
  test test_postgres_compatibility_corpus_e2e ... ok
  test test_ten_required_dml_scenarios ... ok
  test result: ok. 3 passed; 0 failed; 0 ignored; finished in 1.97s
  ```

### V064-08: SIGKILL Hard Process Crash Proof & Batch Oracle
- **Requirement**: A hard process restart preserves complete committed base/view state and excludes rolled-back or failed changes.
- **Evidence**:
  - `crates/rockstream-gateway/tests/dml_crash_recovery_tests.rs::test_sigkill_crash_proof_after_committed_dml`:
    1. Start gateway server on loopback port with durable SlateDB storage.
    2. Execute table creation with primary key, insert initial rows, run updates and deletes within transactions.
    3. Commit valid transaction, rollback aborted transaction.
    4. Terminate process via SIGKILL.
    5. Start completely new gateway process pointing to identical storage directory.
    6. Query base tables and materialized views; assert bit-identical state against an independent batch reference oracle (`incremental == batch`).
    7. Confirm rolled-back mutations left zero phantom state.
  - Durability verified across both LocalFileSystem (LFS) and MinIO (S3) backends.
  - Tests:
    - `dml_crash_recovery_tests::test_sigkill_crash_proof_after_committed_dml`: Passed.
    - `dml_durability_lfs_tests::test_dml_persistence_lfs_lifecycle`: Passed.
    - `dml_durability_minio_tests::test_dml_persistence_minio_lifecycle`: Passed.
- **Command Output Excerpt**:
  ```
  test test_sigkill_crash_proof_after_committed_dml ... ok
  test result: ok. 1 passed; 0 failed; 0 ignored; finished in 1.16s
  ```

### V064-09: Resource Bounding & Operational Limit Containment
- **Requirement**: Oversized statements, excessive expression depth, and result or mutation limits fail with bounded work and no partial commits.
- **Evidence**:
  - `crates/rockstream-sql/src/frontend.rs` & `crates/rockstream-gateway/src/server.rs`:
    - `MAX_SQL_STATEMENT_BYTES`: 1,048,576 bytes (1 MiB). Statements exceeding this limit are rejected at parser entry with `RS-1012` (`42601`).
    - `MAX_EXPR_DEPTH`: 64 levels. Deeply nested expression trees are rejected during AST lowering with `RS-1012`.
    - `MAX_DML_MUTATIONS_PER_STATEMENT`: 10,000 rows. DML statements attempting to mutate more than 10,000 rows fail with `RS-2002` (`57014`) without committing any partial rows.
    - `WRITE_BUFFER_LIMIT_BYTES`: 67,108,864 bytes (64 MiB). Write buffer memory bounds prevent out-of-memory errors by failing closed with `RS-2019` (`53200`).
    - `MAX_DML_SCAN_WINDOW_ROWS`: 1,024 rows. Shard scans are paginated in bounded windows and continue to completion.
  - Tests:
    - `dml_bounding_and_negative_tests::test_oversized_sql_statement_rejected`: Oversized SQL string rejected with `RS-1012`.
    - `dml_bounding_and_negative_tests::test_excessive_expression_depth_rejected`: Expression tree of depth 65 rejected with `RS-1012`.
    - `dml_bounding_and_negative_tests::test_mutation_limit_per_statement_enforced`: Multi-row update exceeding 10,000 rows fails with `RS-2002`.
    - `dml_bounding_and_negative_tests::test_write_buffer_memory_limit_enforced`: Write buffer memory saturation rejected with `RS-2019`.
    - `dml_bounding_and_negative_tests::test_dml_scan_page_window_continues_to_completion`: Large table scan pages through without unbounded memory spike.
- **Command Output Excerpt**:
  ```
  test test_write_buffer_memory_limit_enforced ... ok
  test test_excessive_expression_depth_rejected ... ok
  test test_dml_limits_and_negative_error_containment ... ok
  test test_oversized_sql_statement_rejected ... ok
  test test_dml_scan_page_window_continues_to_completion ... ok
  test test_mutation_limit_per_statement_enforced ... ok
  test result: ok. 6 passed; 0 failed; 0 ignored; finished in 0.85s
  ```

---

## 3. Durability, Coordination & Backend Verification

### Durability (LFS Backend)
- **Command**: `rtk proxy cargo test -p rockstream-gateway --test dml_durability_lfs_tests 2>&1 | grep -E "^test .* (ok|FAILED|ignored)|^test result:|^FAILED$|^error"`
- **Result**: PASS
- **Output**:
  ```
  test test_dml_persistence_lfs_lifecycle ... ok
  test result: ok. 1 passed; 0 failed; 0 ignored; finished in 0.55s
  ```

### Durability (MinIO S3 Backend)
- **Command**: `rtk proxy cargo test -p rockstream-gateway --test dml_durability_minio_tests 2>&1 | grep -E "^test .* (ok|FAILED|ignored)|^test result:|^FAILED$|^error"`
- **Result**: PASS
- **Output**:
  ```
  test test_dml_persistence_minio_lifecycle ... ok
  test result: ok. 1 passed; 0 failed; 0 ignored; finished in 2.41s
  ```

### Coordination / SimRuntime Fault Injection
- **Command**: `rtk proxy cargo test -p rockstream-sim --test dml_coordination_fault_tests 2>&1 | grep -E "^test .* (ok|FAILED|ignored)|^test result:|^FAILED$|^error"`
- **Result**: PASS
- **Output**:
  ```
  test test_sim_runtime_dml_atomic_commit_under_faults ... ok
  test result: ok. 1 passed; 0 failed; 0 ignored; finished in 0.90s
  ```

---

## 4. Reachability & Protocol Parity Verification

### SQL Reachability (E2E PGWire)
- **Test files**:
  - `crates/rockstream-gateway/tests/pgwire_dml_parity_tests.rs`
  - `crates/rockstream-gateway/tests/postgres_dml_compatibility_tests.rs`
- **Output Excerpt**:
  ```
  test test_insert_returning_extended ... ok
  test test_insert_simple_protocol ... ok
  test test_insert_extended_binary_params ... ok
  test test_delete_no_match_returning ... ok
  test test_extended_protocol_parameter_binding_and_returning_parity ... ok
  test test_delete_returning_pre_image ... ok
  test test_delete_single_row_simple ... ok
  test test_update_no_match_simple ... ok
  test test_update_no_match_returning ... ok
  test test_prepared_update_execution ... ok
  test test_no_textual_prefix_dispatch_used ... ok
  test test_update_single_row_extended ... ok
  test test_delete_multi_row_extended ... ok
  test test_update_single_row_simple ... ok
  test test_update_returning_multi_row ... ok
  test test_update_multi_row_simple ... ok
  test result: ok. 16 passed; 0 failed; 0 ignored; finished in 0.85s
  ```

---

## 5. Negative & Error Containment Matrix Verification

| Failure Mode / Scenario | Trigger Condition | Expected Error Code | SQLSTATE | Observed Status | Verified In Test |
|---|---|---|---|---|---|
| Malformed SQL statement | Syntax error in DML | `RS-1012` | `42601` | Returned accurately | `dml_ast_parser_tests::test_dml_ast_parser_rejects_malformed_syntax` |
| Oversized statement bytes | Payload > 1 MiB | `RS-1012` | `42601` | Rejected at parser | `dml_bounding_and_negative_tests::test_oversized_sql_statement_rejected` |
| Excessive expression depth | AST depth > 64 | `RS-1012` | `42601` | Rejected at lowering | `dml_bounding_and_negative_tests::test_excessive_expression_depth_rejected` |
| Target table missing | DML on non-existent table | `RS-1004` | `42P01` | Rejection before write | `dml_ast_parser_tests::test_dml_schema_validation_unknown_table_and_column` |
| Target column missing | DML referencing unknown column | `RS-1004` | `42703` | Rejection before write | `dml_ast_parser_tests::test_dml_schema_validation_unknown_table_and_column` |
| Unsupported DML predicate | Correlated subquery / UDF | `RS-1013` | `0A000` | Rejection before write | `dml_predicate_eval_tests::test_unsupported_subquery_predicate_rejection` |
| Duplicate primary key insert | Key already occupied | `RS-2057` | `23505` | Atomic rejection | `primary_key_dml_tests::test_single_pk_duplicate_insert_rejected` |
| Duplicate primary key update | Target key already occupied | `RS-2057` | `23505` | Atomic rejection | `primary_key_dml_tests::test_single_pk_duplicate_update_rejected` |
| Mutation limit exceeded | Statement mutates > 10,000 rows| `RS-2002` | `57014` | Rejection without partial writes | `dml_bounding_and_negative_tests::test_mutation_limit_per_statement_enforced` |
| Write buffer memory exceeded | Buffer exceeds 64 MiB | `RS-2019` | `53200` | Fails closed | `dml_bounding_and_negative_tests::test_write_buffer_memory_limit_enforced` |
| Numeric / Decimal overflow | Integer / Decimal out of range | `RS-1016` | `22003` | Rejection | `type_coercion_dml_tests::test_decimal_dml_and_overflow` |
| Malformed VALUES list | Column count mismatch / unbalanced paren | `RS-2056` | `42601` | Hard parse error | `dml_bounding_and_negative_tests::test_dml_limits_and_negative_error_containment` |

---

## 6. Dispatch-Wiring Audit Verification

Verified via `scripts/check-dispatch-wiring.sh`:
```
OK: 21/21 statement families and all declared dispatch endpoints verified with 0 MISSING paths.
```
- **Stage 1: Parser**: Shared `sqlparser` AST in `crates/rockstream-sql/src/frontend.rs` with `PostgreSqlDialect`. All statements route to typed `DmlStatement` IR.
- **Stage 2: Dispatcher**: `crates/rockstream-gateway/src/server.rs` dispatches on parsed statement enum variants (`DmlStatement::Insert`, `DmlStatement::Update`, `DmlStatement::Delete`). Zero prefix-based matching (`starts_with("update ")`, `starts_with("delete from ")`).
- **Stage 3: Executor**: `crates/rockstream-gateway/src/server.rs` (`handle_update`, `handle_delete`) and `write_buffer.rs` (`WriteBuffer::push`, `WriteBatch::commit`) enforce atomic retraction and insertion deltas (`-old/+new`), unique primary keys (`RS-2057`), and epoch-isolated view updates.
- **Stage 4: Response Encoder**: `crates/rockstream-gateway/src/server.rs` returns accurate `CommandComplete` tags (`INSERT 0 <n>`, `UPDATE <n>`, `DELETE <n>`) and complete `DataRow` projections for `RETURNING` clauses.

---

## 7. Quality Gates & Release Binary Build

1. **Product Surface Manifest & Drift Check**:
   ```
   rtk proxy bash scripts/check-product-surface.sh
   OK: Product surface manifest and contract validation passed with zero drift.
   ```
2. **Capability Contract Check**:
   ```
   rtk proxy bash scripts/check-capability-contract.sh --full-semantics
   OK: capability contract check passed.
   ```
3. **Zero Hidden Skips Gate**:
   ```
   rtk proxy bash scripts/check-no-hidden-skips.sh && rtk proxy bash scripts/check-no-hidden-skips.test.sh
   OK: no hidden test skips found.
   OK: check-no-hidden-skips.sh self-test passed.
   ```
4. **Documentation Check**:
   ```
   rtk proxy bash scripts/check-documentation.sh
   OK: documentation checks passed.
   ```
5. **Error Codes Check**:
   ```
   rtk proxy bash scripts/check-error-codes.sh
   OK: all logged/returned errors carry an RS-XXXX code.
   ```
6. **Failure Matrix Check**:
   ```
   rtk proxy bash scripts/check-failure-matrix.sh
   OK: All 11 production failure modes validated successfully.
   ```
7. **Exit Criteria Check**:
   ```
   rtk proxy bash scripts/check-exit-criteria.sh && rtk proxy bash scripts/check-exit-criteria.test.sh
   All Done versions have complete sign-offs.
   OK: check-exit-criteria.sh self-test passed.
   ```
8. **Code Formatting (`fmt`)**:
   ```
   rtk cargo fmt --all -- --check
   Status: 0 formatting diffs.
   ```
9. **Clippy Linting**:
   ```
   rtk cargo clippy --workspace --all-targets -- -D warnings
   Status: No issues found (0 warnings, 0 errors).
   ```
10. **Release Binary Build**:
   ```
   rtk cargo build --release -p rockstream-cli --bin rockstream
   Digest (SHA-256): 3080ee300081ee316e25b54a3d8c4dcfcf1199272a7cf7ea7a69d3450f9ec2dd
   ```

---

## 8. Remaining Phase 4 Checks & Exit Confirmation

- [x] **Reachability tests** (e2e pgwire): Every SQL/wire feature has passing tests sending raw SQL through the PGWire dispatcher (`pgwire_dml_parity_tests`, `postgres_dml_compatibility_tests`).
- [x] **Negative tests**: Invalid input and boundary conditions return proper `RS-XXXX` codes (`RS-1002`, `RS-1003`, `RS-1004`, `RS-1012`, `RS-1013`, `RS-1016`, `RS-2002`, `RS-2019`, `RS-2056`, `RS-2057`).
- [x] **Dispatch-wiring audit**: Every path in the Phase 2 audit is verified connected with zero missing routes.
- [x] **Coverage matrix**: Every cell in matrices A through F has passing tests.
- [x] **All other Proof obligations met**: Batch oracle (`incremental == batch`), LFS, MinIO, SimRuntime, and release process hard restart crash proof all passed.
