# v0.51.5 Proof Evidence — Gateway-Facing TLS Termination & Binary Wire Format

Roadmap Proof column: TLS handshake (real, not stubbed) + mTLS CN extraction
from a validated client certificate + correct binary wire encoding for every
currently-text-supported OID. No new IVM operators, no new durability path
(see plan §4/§5) — the oracle/LFS/MinIO/simulation/chaos/soak rungs are N/A
for this version's scope, as pre-committed in the plan.

## 1. TLS foundation (Slices 1–3)

| Claim | Test | Command | Result |
|---|---|---|---|
| `GatewayConfig` TLS fields round-trip through `to_string()`/`load_from_str()` | `config::tests::test_config_roundtrip` | `cargo test -p rockstream-types config_roundtrip` | `test config::tests::test_config_roundtrip ... ok` — 1 passed |
| `docs/configuration.md` table stays in sync with `RockstreamConfig` defaults (incl. new `tls_cert_path`/`tls_key_path`/`tls_ca_cert_path`) | `test_configuration_doc_matches_rockstream_config_defaults` | `cargo test -p rockstream-types --test docs_conformance_tests test_configuration_doc_matches_rockstream_config_defaults` | `test test_configuration_doc_matches_rockstream_config_defaults ... ok` — 1 passed |
| `GatewayServer::with_tls(...)` builds a real `rustls::ServerConfig`/`TlsAcceptor` and binds | `gateway_with_tls_starts_and_binds` | `cargo test -p rockstream-gateway --test gateway_tls_tests` | `ok` |
| mTLS without `tls_ca_cert_path` fails fast at startup (not a silent fallback) | `mtls_auth_without_ca_cert_path_fails_to_start` | (same run) | `ok` |
| Pre-v0.51.5 plaintext SSL-downgrade path (`sslmode` not set / TLS not configured) is unchanged | `test_ssl_negotiation_downgrade` (existing, untouched) | `cargo test -p rockstream-gateway --test gateway_extended_query_tests test_ssl_negotiation_downgrade` | `test test_ssl_negotiation_downgrade ... ok` — 1 passed, 6 filtered out |
| Real TLS handshake end-to-end; `tokio-postgres` + `sslmode=require` client runs `SELECT 1` successfully | `tls_handshake_completes_and_query_succeeds_sslmode_require` | (same gateway_tls_tests run) | `ok` |
| Raw socket bytes after `SSLRequest`/`'S'` are TLS record-framed (`0x16`), not plaintext pgwire messages | `raw_socket_bytes_after_sslrequest_are_encrypted_not_plaintext` | (same run) | `ok` |

## 2. mTLS (Slice 4)

| Claim | Test | Result |
|---|---|---|
| Client presenting a cert signed by the configured CA (CN `test-client-1`) is assigned `Principal::CertCn { cn: "test-client-1" }`, not a spoofable/system identity | `mtls_end_to_end_authenticates_real_client_cert` | `ok` |
| Connection with no client cert is rejected at the TLS layer (handshake fails before any pgwire startup message) | `mtls_rejects_connection_with_no_client_cert` | `ok` |
| Connection with an untrusted (non-CA-signed) client cert is rejected at the TLS layer | `mtls_rejects_connection_with_untrusted_client_cert` | `ok` |

**Full `gateway_tls_tests` suite (Slices 1–4), single run:**
```
$ cargo test -p rockstream-gateway --test gateway_tls_tests
test gateway_with_tls_starts_and_binds ... ok
test mtls_auth_without_ca_cert_path_fails_to_start ... ok
test raw_socket_bytes_after_sslrequest_are_encrypted_not_plaintext ... ok
test mtls_rejects_connection_with_no_client_cert ... ok
test mtls_rejects_connection_with_untrusted_client_cert ... ok
test tls_handshake_completes_and_query_succeeds_sslmode_require ... ok
test mtls_end_to_end_authenticates_real_client_cert ... ok
test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.04s
```

## 3. Binary wire format (Slices 5–11)

Every currently-text-supported OID's binary encoding is proven by an
explicit incremental-vs-text-vs-binary round trip (two connections — one
text-format, one binary-format — same `SELECT`, decoded values compared),
covering `NULL`, boundary/epoch values, and (for containers) empty/`NULL`-
element/multi-element cases per the plan's Green-test descriptions.

```
$ cargo test -p rockstream-gateway --test binary_format_round_trip_tests
test binary_json_roundtrip ... ok                  # Slice 9 (JSON)
test binary_interval_roundtrip ... ok               # Slice 10 (INTERVAL, hand-rolled ToSql)
test binary_float8_array_roundtrip ... ok           # Slice 11 (arrays)
test binary_numeric_roundtrip ... ok                # Slice 8 (NUMERIC/rust_decimal)
test binary_bool_array_roundtrip ... ok             # Slice 11
test binary_int4_roundtrip ... ok                   # Slice 5 (baseline regression)
test binary_timestamp_roundtrip ... ok              # Slice 6
test binary_date_roundtrip ... ok                   # Slice 6
test binary_float8_roundtrip ... ok                 # Slice 5
test binary_bool_roundtrip ... ok                   # Slice 5
test binary_int8_array_roundtrip ... ok             # Slice 11
test binary_jsonb_roundtrip ... ok                  # Slice 9 (leading 0x01 version byte)
test binary_text_array_roundtrip ... ok             # Slice 11
test binary_float4_roundtrip ... ok                 # Slice 5
test binary_time_roundtrip ... ok                   # Slice 6
test binary_int4_array_roundtrip ... ok             # Slice 11
test binary_int8_roundtrip ... ok                   # Slice 5
test binary_int2_roundtrip ... ok                   # Slice 5
test binary_uuid_roundtrip ... ok                   # Slice 7
test binary_timestamptz_roundtrip ... ok            # Slice 6
test binary_uuid_array_roundtrip ... ok             # Slice 11
test result: ok. 21 passed; 0 failed; 0 measured; 0 filtered out; finished in 0.23s
```

21/21 pass: 6 (Slice 5 baseline: int2/int4/int8/float4/float8/bool) + 4
(Slice 6: timestamp/timestamptz/date/time) + 1 (Slice 7: uuid) + 1 (Slice 8:
numeric) + 2 (Slice 9: json/jsonb) + 1 (Slice 10: interval) + 6 (Slice 11:
int4/int8/text/float8/bool/uuid arrays) = 21, matching every OID family
named in the plan.

## 4. No-regression check

```
$ cargo build --workspace --tests
cargo build: 0 errors, 2 warnings (29 crates)   # pre-existing linker-note + future-incompat warnings, unrelated to this change

$ cargo clippy -p rockstream-gateway -p rockstream-types --tests
2 warnings (clippy::question_mark style suggestions on pre-existing, unmodified
dispatch-site error-handling pattern) — 0 errors
```

No `#[ignore]` attributes were added to any new or existing test
(`gateway_tls_tests.rs`, `binary_format_round_trip_tests.rs`) — verified by
grep. No test was weakened, skipped, or deleted to make the build pass.

## 5. Ground rules not applicable to this version's scope (pre-committed in plan §4/§5)

- **Oracle / property (`incremental == batch`)**: N/A — no new IVM operator.
- **LFS/MinIO durability pair**: N/A — no new durability path; TLS is a
  pre-commit transport concern, binary encoding is a post-commit
  presentation-layer transform.
- **Deterministic simulation / crash-chaos**: N/A — no new commit/replay/
  crash-recovery path introduced.
- **Benchmark / soak**: No performance or long-running-endurance claim is
  made by this version's roadmap row.

## Summary

| Slice | Proof status |
|---|---|
| 1 — GatewayConfig TLS fields + CLI + docs | ✅ proven |
| 2 — `with_tls` + rustls ServerConfig builder | ✅ proven |
| 3 — Real TLS handshake wired through serve()/serve_background() | ✅ proven |
| 4 — Real mTLS CN extraction | ✅ proven |
| 5 — Binary baseline (int2/int4/int8/float4/float8/bool) | ✅ proven |
| 6 — Binary timestamp/timestamptz/date/time | ✅ proven |
| 7 — Binary uuid | ✅ proven |
| 8 — Binary numeric | ✅ proven |
| 9 — Binary json/jsonb | ✅ proven |
| 10 — Binary interval | ✅ proven |
| 11 — Binary arrays (6 types) | ✅ proven |

All 11 slices' Proof claims are satisfied with passing, unmodified,
non-ignored tests. Nothing has been deferred to a later version.
