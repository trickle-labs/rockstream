# Version v0.18 Implementation Plan — Frontier Protocol & Formal Verification

## 1. Deferred-Item Audit
- Checked all prior sign-offs in `sign-offs/` (`v0.1.md` through `v0.17.md`).
- `Option-based buggify control API` (deferred in v0.2 to v0.5) is out of scope and does not overlap with the frontier progress tracking of `v0.18`.
- `Automatic background compact-on-idle` (deferred in v0.3 to v0.7) is out of scope and does not overlap with the frontier progress tracking of `v0.18`.
- `Direct lowering logic bypass for trivial plans` (deferred in v0.7 to v0.10) is out of scope.
- `HOP/SESSION time windows` (deferred in v0.12 to v0.19) is out of scope.
- Audit is complete: no prior deferred items overlap this version's scope.

---

## 2. Vertical Slices

### Slice 1: Frontier & Product-Order Timestamps
- **Goal**: Implement `Frontier<T>` (antichain over `PartialOrd`) and `ProductTimestamp` supporting product-order comparison and least upper bounds.
- **Changes**:
  - [MODIFY] [frontier.rs](file:///Users/grove/projects/rockstream/crates/rockstream-types/src/frontier.rs): Generalize `Antichain<T>` to `PartialOrd`, define `Frontier<T: PartialOrd + Clone>` and its `meet`/`join`/`advance` operations. Implement `ProductTimestamp` representing multi-dimensional logical timestamps.
- **Test**: Run `rtk cargo test -p rockstream-types --lib frontier` to verify lattice properties (associativity, commutativity, absorption, distributivity) for scalar and vector frontiers.

### Slice 2: FizzBee Toolchain Bootstrap
- **Goal**: Configure the `formal/` directory, conventions, pinned FizzBee runner, and integration with `make verify` and CI.
- **Changes**:
  - [NEW] [conventions.md](file:///Users/grove/projects/rockstream/formal/conventions.md): Modeling conventions.
  - [NEW] [README.md](file:///Users/grove/projects/rockstream/formal/README.md): Index of specifications.
  - [NEW] [fizz.yaml](file:///Users/grove/projects/rockstream/fizz.yaml): Project-level model checker options.
  - [MODIFY] [Makefile](file:///Users/grove/projects/rockstream/Makefile): Add `verify` target checking all specifications.
- **Test**: Run `make verify` on a smoke test spec.

### Slice 3: M1 Retro-model (Epoch Commit)
- **Goal**: Model the epoch-commit protocol in FizzBee and backfill runtime assertions.
- **Changes**:
  - [NEW] [m1_epoch_commit.fizz](file:///Users/grove/projects/rockstream/formal/m1_epoch_commit.fizz): Epoch commit spec.
  - [MODIFY] [shard_db.rs](file:///Users/grove/projects/rockstream/crates/rockstream-storage/src/shard_db.rs): Add paired runtime assertions for M1 safety and replay.
- **Test**: Ensure M1 model safety and liveness check passes in `make verify`.

### Slice 4: M2 Model (Frontier Aggregation)
- **Goal**: Model frontier aggregation and lease-based publishing.
- **Changes**:
  - [NEW] [m2_frontier_agg.fizz](file:///Users/grove/projects/rockstream/formal/m2_frontier_agg.fizz): Frontier aggregation spec with scalar and multi-source vector meet.
- **Test**: Ensure M2 model safety and liveness check passes in `make verify`.

### Slice 5: Per-Shard Frontier Reporter
- **Goal**: Bundle a per-shard frontier reporter into the epoch commit path.
- **Changes**:
  - [MODIFY] [shard_db.rs](file:///Users/grove/projects/rockstream/crates/rockstream-storage/src/shard_db.rs): Emit `ShardFrontierReport` on successful commit.
- **Test**: Local filesystem backend integration tests pass.

### Slice 6: Control-Plane Aggregator & CLI Role
- **Goal**: Implement the `FrontierAggregator` in the control plane and add the `--role=frontier` CLI command.
- **Changes**:
  - [MODIFY] [control_plane](file:///Users/grove/projects/rockstream/crates/rockstream-control/src/lib.rs): Implement hierarchical aggregator meeting shard reports.
  - [MODIFY] [cli](file:///Users/grove/projects/rockstream/crates/rockstream-cli/src/lib.rs): Accept `--role=frontier` option.
  - [MODIFY] [service.rs](file:///Users/grove/projects/rockstream/crates/rockstream-control/src/service.rs): Implement paired runtime assertions for M2 safety.
- **Test**: Run unit and integration tests verifying correct aggregation and CLI parsing.

### Slice 7: SimRuntime Progress & stress verification
- **Goal**: Verify reordering convergence and aggregate convergence under simulated faults.
- **Changes**:
  - [MODIFY] [control_sim.rs](file:///Users/grove/projects/rockstream/crates/rockstream-sim/tests/control_sim.rs): Add reordering parity and high-concurrency aggregation stress tests.
- **Test**: Run simulation tests verifying that arbitrary reordering converges deterministically.

---

## 3. Test Commitments & Proof Mapping

### Oracle / Property / Simulation Tests
- **Frontier Lattice Property Tests** (`test_frontier_lattice_properties`): Property tests asserting lattice invariants (distributivity, absorption, associativity) on scalar and vector frontiers.
- **Frontier Reordering Parity Test** (`test_frontier_reordering_convergence`): Assert that arbitrary delay/reordering of shard reports converges to the same vector frontier.
- **Frontier Aggregation Stress Test** (`test_frontier_aggregation_stress`): Simulates aggregation across thousands of shards × hundreds of operators under fault injection.

### Durability / LFS / TestContainers Tests
- **Frontier Commit Integration Test**: Verifies per-shard reporter publishes to object store and control plane can recover from stored checkpoints.

---

## 4. Proof Claims to Test Mapping

| Proof Claim | Target Test(s) |
|---|---|
| `SimRuntime`: arbitrary frontier-report reorderings converge to the same cluster vector frontier as serial delivery | `test_frontier_reordering_convergence` |
| `SimRuntime`: a frontier-aggregation stress test over thousands of shards × hundreds of operators converges without the control plane subscribing to each shard feed | `test_frontier_aggregation_stress` |
| **FizzBee**: `make verify` is green in CI | `make verify` output in CI |
| **FizzBee**: M1 safety M1-S1…S5, liveness M1-L1, and coverage COV-M1 pass | M1 model check run via `make verify` |
| **FizzBee**: M2 safety M2-S1…S4, liveness M2-L1…L2, and coverage COV-M2 pass | M2 model check run via `make verify` |
| **Assertions**: each invariant maps to a paired runtime `assert!` in `rockstream-control`, `rockstream-runtime`, and `rockstream-storage` | Paired `assert!` checks in `ShardDb` and `FrontierAggregator` |
