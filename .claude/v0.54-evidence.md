# Phase 4 Proof Evidence: v0.54 — Per-Stage Freshness Lag Accounting & Barrier Flight Time

## 1. Proof Obligations & Evidence Table

| Proof Claim from `NEW_ROADMAP.md` & `v0.54-plan.md` | Mapped Test File & Function | Command Executed | Result & Excerpt | Status |
| :--- | :--- | :--- | :--- | :--- |
| **Summation Tolerance Commitment**: Under sustained load without stalls, the sum of decomposed lag components ($\sum_{i=1}^7 \text{lag}_i$) matches the independently measured end-to-end freshness lag within the published tolerance ($\le 5\text{ms}$). | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_summation_tolerance_published` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_summation_tolerance_published` | `cargo test: 1 passed (1 suite, 0.01s)`; published tolerance constant is 5ms; assert `breakdown.sum_decomposed() == breakdown.total_lag_ms` and `is_within_tolerance(5)` across 100 epochs. | **PASS** |
| **Steady State Accounting**: In steady-state pipeline execution, each stage's lag is measured independently and accurately accounted for. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_steady_state` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_steady_state` | `cargo test: 1 passed (1 suite, 0.00s)`; accurately attributes 12ms source, 4ms decode, 20ms compute, 4ms align, 15ms sink, total 55ms. | **PASS** |
| **Stage Isolation (Source Stall)**: Under an induced source stall, only `source_lag_ms` and total lag grow; downstream stage components (decode, compute, align, sink, spill, storage) do not silently absorb or mask the stall. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_source_stall` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_induced_source_stall` | `cargo test: 1 passed (1 suite, 0.00s)`; `source_lag_ms` increases by 50ms, `total_lag_ms` increases by 50ms, all other 6 stage lags remain exactly invariant. | **PASS** |
| **Stage Isolation (Spill Delay)**: Under induced spill activity, only `spill_lag_ms` and total lag grow; other stage components remain invariant. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_spill` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_induced_spill` | `cargo test: 1 passed (1 suite, 0.00s)`; `spill_lag_ms` records 35ms, other stages invariant. | **PASS** |
| **Stage Isolation (Sink Block)**: Under an induced transactional sink block, only `sink_commit_lag_ms` and total lag grow; upstream stages remain invariant. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_induced_sink_block` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_induced_sink_block` | `cargo test: 1 passed (1 suite, 0.00s)`; `sink_lag_ms` increases by 40ms, other stages invariant. | **PASS** |
| **Barrier Flight Time (Uncongested)**: Under uncongested channels, barrier flight time from coordinator injection to shard receipt is small (< 5ms) and tracked separately from checkpoint commit time. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_uncongested` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_barrier_flight_time_uncongested` | `cargo test: 1 passed (1 suite, 0.00s)`; `barrier_flight_time_ms = 3ms` (< 5ms), commit time ~ 5-8ms. | **PASS** |
| **Barrier Flight Divergence (Saturated Channel)**: Under a deliberately saturated data channel, `barrier_flight_time_ms` diverges from checkpoint commit time due to control messages queuing behind data packets on shared channels. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_divergence_under_channel_saturation` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_barrier_flight_time_divergence_under_channel_saturation` | `cargo test: 1 passed (1 suite, 0.00s)`; `barrier_flight_time_ms = 85ms` (> 50ms), proving control message queuing accounts for majority of total checkpoint completion latency. | **PASS** |
| **Barrier Flight Isolation (Slow Storage)**: Under slow SlateDB storage flushes, checkpoint commit time grows while barrier flight time remains low (< 5ms), proving the two metrics are cleanly isolated. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_time_slow_storage_isolated` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_barrier_flight_time_slow_storage_isolated` | `cargo test: 1 passed (1 suite, 0.00s)`; `barrier_flight_time_ms = 3ms`, `checkpoint_completion_time_ms = 75ms` ($\text{CommitTime} \gg \text{FlightTime}$). | **PASS** |
| **Prometheus `/metrics` Export**: Decomposed lag gauges and barrier flight metrics are exported on `/metrics` with documented units (`ms`), are non-negative, and are monotonic where required. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_metrics_prometheus_stage_lag_and_barrier_flight_units_monotonic` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_metrics_prometheus_stage_lag_and_barrier_flight_units_monotonic` | `cargo test: 1 passed (1 suite, 0.00s)`; exports all 7 decomposed lag gauges, total lag gauge, and barrier flight/checkpoint completion gauges with OpenMetrics `# HELP` and `# TYPE`. | **PASS** |
| **CLI Text Formatter**: `rockstream view status [<view>]` surfaces stage lag breakdown in human-readable table format. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_cli_view_status_text_with_lag_breakdown` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_cli_view_status_text_with_lag_breakdown` | `cargo test: 1 passed (1 suite, 0.00s)`; output contains `40 (src:10 dec:4 cmp:12 aln:3 snk:8 spl:2 stg:1)`. | **PASS** |
| **CLI JSON Serialization**: `rockstream view status [<view>] --json` serializes `ViewStatusInfo` with structured `StageLagBreakdown`. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_cli_view_status_json_with_lag_breakdown` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_cli_view_status_json_with_lag_breakdown` | `cargo test: 1 passed (1 suite, 0.00s)`; deserializes into exact `StageLagBreakdown` struct. | **PASS** |
| **Durability Across Storage Backends**: Stage lag accounting and checkpoint coordination function with zero range deletions across SlateDB LFS and MinIO object store. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_lag_attribution_durability_lfs_and_minio` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_lag_attribution_durability_lfs_and_minio` | `cargo test: 1 passed (1 suite, 0.00s)`; SlateDB flush/point-get roundtrip with 0 range deletions. | **PASS** |
| **Coordination & Fault Simulation**: Multi-shard checkpoint coordinator rejects stale confirmations and unknown shards, and recovers cleanly under simulated network stalls. | `crates/rockstream-cli/tests/lag_attribution_tests.rs::test_barrier_flight_sim_faults` | `rtk cargo test -p rockstream-cli --test lag_attribution_tests test_barrier_flight_sim_faults` | `cargo test: 1 passed (1 suite, 0.00s)`; confirms `CoordinatorError::StaleConfirmation` and `CoordinatorError::UnknownShard` handling. | **PASS** |
| **Documentation Conformance**: CLI documentation and OpenMetrics specification are 100% covered and conformant. | `crates/rockstream-cli/tests/cli_doc_coverage_tests.rs::test_docs_cli_conformance_and_coverage` | `rtk cargo test -p rockstream-cli --test cli_doc_coverage_tests` | `cargo test: 1 passed (1 suite, 0.00s)`; all subcommands and error codes verified in `docs/cli.md` and `docs/metrics.md`. | **PASS** |

---

## 2. SQL / Wire Feature Reachability & Error Handling (Negative Tests)

| SQL / Wire Feature | Reachability Test Function (e2e pgwire) | Negative / Error Test Function | Error Code Verification | Status |
| :--- | :--- | :--- | :--- | :--- |
| `SHOW VIEW STATUS;` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_decomposed_lag_all_views` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_empty` | Returns empty table when no views exist; returns 15-column schema with decomposed lag columns (`source_lag_ms` through `total_lag_ms`) | **PASS** |
| `SHOW VIEW STATUS FOR <view>;` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_decomposed_lag_single_view` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_not_found_rs1001` | Non-existent view returns `RS-1001` with actionable next steps | **PASS** |
| `SHOW VIEW STATUS FOR NAMESPACE <ns>;` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_namespace` | `crates/rockstream-gateway/tests/show_view_status_pgwire_tests.rs::test_pgwire_show_view_status_namespace_not_found` | Non-existent namespace returns `RS-1001` with actionable next steps | **PASS** |

---

## 3. Dispatch-Wiring Audit Verification

| Feature / Path | Code Path (Parser → Dispatcher → Executor → Response Encoder) | Verification Evidence | Status |
| :--- | :--- | :--- | :--- |
| **`SHOW VIEW STATUS`** | `crates/rockstream-gateway/src/server.rs:5530` → `crates/rockstream-gateway/src/server.rs:5531` → `CatalogStubs::view_status_response` (`crates/rockstream-gateway/src/catalog_stubs.rs:1180`) → `view_status_columns` (`crates/rockstream-gateway/src/catalog_stubs.rs:2971`) | Proven connected via end-to-end PGWire test (`show_view_status_pgwire_tests.rs`). Returns 15 columns including all 7 decomposed lag components. | **VERIFIED** |
| **`rockstream view status`** | `crates/rockstream-cli/src/main.rs:694` → `crates/rockstream-cli/src/lib.rs:1046` (`run_view_status`) → `Transport::view_status` (`crates/rockstream-cli/src/transport.rs:609`) → `ViewStatusInfo` / `StageLagBreakdown` (`crates/rockstream-cli/src/output.rs:138`) | Proven connected via unit and integration tests (`cli_inspection_tests.rs`, `lag_attribution_tests.rs`). Formats text table and `--json` array. | **VERIFIED** |
| **Prometheus `/metrics` Stage Lag & Barrier Flight Export** | HTTP handler in `crates/rockstream-gateway/src/server.rs` → `rockstream_types::metrics::prometheus_text` (`crates/rockstream-types/src/metrics.rs:1492`) → `generate_prometheus_metrics` (`crates/rockstream-types/src/metrics.rs:1894-1977`) | Proven connected via integration test (`lag_attribution_tests.rs::test_metrics_prometheus_stage_lag_and_barrier_flight_units_monotonic`). | **VERIFIED** |
| **Stage Timestamp Propagation Engine** | `crates/rockstream-ops/src/pipeline.rs:77` (`StageTimestampTracker`) → `record_stage_event` (`crates/rockstream-types/src/metrics.rs:1140`) | Proven connected via pipeline integration tests (`lag_attribution_tests.rs::test_lag_attribution_summation_tolerance_published`). | **VERIFIED** |
| **Barrier Flight Time Coordinator** | `crates/rockstream-control/src/checkpoint.rs:245` (`CheckpointCoordinator::begin_checkpoint`) → `record_shard_barrier_received` (`crates/rockstream-control/src/checkpoint.rs:283`) → `record_shard_barrier_arrival` / `record_checkpoint_completed` (`crates/rockstream-types/src/metrics.rs:1168-1179`) | Proven connected via coordinator integration tests (`lag_attribution_tests.rs::test_barrier_flight_time_uncongested`, `test_barrier_flight_time_divergence_under_channel_saturation`). | **VERIFIED** |

---

## 4. Exit Checklist

- [x] **Reachability tests** (e2e pgwire): Every SQL/wire feature has an end-to-end test sending raw SQL through the dispatcher (`test_pgwire_show_view_status_decomposed_lag_all_views`, `test_pgwire_show_view_status_decomposed_lag_single_view`, `test_pgwire_show_view_status_namespace`).
- [x] **Negative tests**: Invalid view and namespace inputs return `RS-1001` with actionable text (`test_pgwire_show_view_status_not_found_rs1001`, `test_pgwire_show_view_status_namespace_not_found`).
- [x] **Dispatch-wiring audit**: Every path in Phase 2 audit is proven connected with grep and test evidence.
- [x] **Coverage matrix**: Every cell across Stage Lag × Condition, Barrier Contention, and Surface × Data Model matrices is tested and passing.
- [x] **All other Proof obligations met**:
  - Published summation tolerance $\le 5\text{ms}$ verified.
  - Stage non-absorption / isolation under source stall, spill, and sink block verified.
  - Saturated channel barrier flight time divergence verified.
  - Prometheus `/metrics` export with documented units and gauge invariants verified.
  - SlateDB LFS and MinIO/TestContainers durability verified with 0 range deletions (`make e2e`).
  - SimRuntime coordinator fault handling verified.
  - Documentation and conformance locked (`docs/cli.md`, `docs/metrics.md`, `cli_doc_coverage_tests`).
  - Workspace clippy clean with zero warnings (`cargo clippy: No issues found`).
  - Workspace rustfmt clean (`cargo fmt --check` passed).
