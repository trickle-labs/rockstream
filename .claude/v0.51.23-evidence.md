# v0.51.23 Evidence — Real Operator State-Size Accounting & Wiring the Admission/Skew Control Loop

## Overview
- **Version:** `v0.51.23`
- **Focus:** Incremental O(1) `state_bytes()` state accounting across all 11 stateful operators, metrics reporting via `set_pipeline_state_bytes`, prospective batch ingestion quota enforcement (`WorkerQuotaManager::try_allocate_batch`), control loop skew footprint integration, and CI invariant gate (`check-operator-state-bytes.sh`).
- **Status:** ✅ All Proof obligations met & passing cleanly.

---

## Proof Evidence Table

| Proof Claim / Requirement | Target Test / Tool | Command Executed | Result | Excerpt / Verification |
| --- | --- | --- | --- | --- |
| **Claim 1: O(1) Operator State Accounting (5 shapes + 6 additional)** | `operator_state_accounting_tests.rs` | `rtk cargo test -p rockstream-ops --test operator_state_accounting_tests` | **PASS** | `cargo test: 13 passed (1 suite, 0.23s)` — verifies `state_bytes()` on `Join`, `OuterJoin`, `Aggregate`, `Distinct`, `TopK`, `MinMax`, `Window`, `TimeWindow`, `IndexArrange`, `Lateral`, `Recursion`. |
| **Claim 2: Hostile Tenant Prospective Quota Enforcement** | `hostile_tenant_real_ingest_quota_tests.rs` | `rtk cargo test -p rockstream-sim --test hostile_tenant_real_ingest_quota_tests` | **PASS** | `cargo test: 3 passed (1 suite, 0.00s)` — asserts `ViewState::OverBudgetRejected` and audit event `RS-5003`/`RS-9001` on prospective quota overflow, with zero starvation for well-behaved tenants. |
| **Claim 3: Skew Control Loop Integration with Measured State** | `skew_control_loop_sim_tests.rs` | `rtk cargo test -p rockstream-sim --test skew_control_loop_sim_tests --features simulation` | **PASS** | `cargo test: 1 passed (1 suite, 5.23s)` — `adaptive_skew_split_triggers_and_completes_sim` verifies split decision and completion using measured state bytes under `buggify!` injection. |
| **Claim 4: CI Operator State Accounting Invariant Gate** | `scripts/check-operator-state-bytes.sh` & `accounting_gate_tests.rs` | `rtk bash scripts/check-operator-state-bytes.sh` && `rtk cargo test -p rockstream-sim --test accounting_gate_tests` | **PASS** | `OK: all stateful operators implement state_bytes accounting for arrangement fields.` and `cargo test: 2 passed (1 suite, 0.55s)`. |
| **LFS Durability** | `test_operator_state_accounting_durability_lfs` | `rtk cargo test -p rockstream-ops --test operator_state_accounting_tests test_operator_state_accounting_durability_lfs` | **PASS** | SlateDB checkpoint & recovery restores `state_bytes()` accurately. |

---

## Detailed Coverage Matrix Results

| Stateful Operator | Key Type | Value / Payload Type | Test Function | Result |
| --- | --- | --- | --- | --- |
| **Join** | `int64` | `(int64, int64)` | `test_join_state_bytes` | **PASS** |
| **Outer Join** | `string` | `(string, int64)` | `test_outer_join_state_bytes` | **PASS** |
| **Aggregate** | `int64` | `int64` | `test_aggregate_state_bytes` | **PASS** |
| **Distinct** | `int64` | N/A | `test_distinct_state_bytes` | **PASS** |
| **TopK** | `int64` | `int64` | `test_topk_state_bytes` | **PASS** |
| **MinMax** | `int64` | `int64` | `test_minmax_state_bytes` | **PASS** |
| **Window** | `int64` | `int64` | `test_window_state_bytes` | **PASS** |
| **Time Window** | `timestamp` | `int64` | `test_time_window_state_bytes` | **PASS** |
| **Index Arrange** | `int64` | `Vec<u8>` | `test_index_arrange_state_bytes` | **PASS** |
| **Lateral** | `int64` | `int64` | `test_lateral_state_bytes` | **PASS** |
| **Recursion** | `int64` | `int64` | `test_recursion_state_bytes` | **PASS** |

---

## Dispatch-Wiring Verification

1. **Operator State Accounting**:
   - Trait definition: `crates/rockstream-ops/src/op.rs` (`Operator::state_bytes(&self) -> u64`)
   - 11 Operator implementations: `join.rs`, `outer_join.rs`, `aggregate.rs`, `minmax.rs`, `distinct.rs`, `topk.rs`, `window.rs`, `time_window.rs`, `index_arrange.rs`, `lateral.rs`, `recursion.rs`
   - Pipeline aggregation: `StatefulPipeline::state_bytes` in `crates/rockstream-ops/src/live_exec.rs` & `pipeline.rs`
   - Metrics target: `set_pipeline_state_bytes` in `crates/rockstream-types/src/metrics.rs`

2. **Prospective Ingest Quota**:
   - Allocator lookup: `WorkerQuotaManager::try_allocate_batch` in `crates/rockstream-runtime/src/quota.rs`
   - Audit / Rejection path: `handle_prospective_rejection` returning `ViewState::OverBudgetRejected` with `RS-5003`/`RS-9001` ledger rejections.

3. **Control Loop Skew Footprint**:
   - `ShardFootprintReport` in `crates/rockstream-control/src/skew.rs`
   - Real state metrics feeding split trigger in `AdaptiveSkewSplitter`.

4. **CI Invariant Gate**:
   - Validation script: `scripts/check-operator-state-bytes.sh`
   - Script self-test: `scripts/check-operator-state-bytes.test.sh`
   - CI Workflow: `.github/workflows/ci.yml` job step added and verified.

---

## Phase 4 Exit Checklist

- [x] **Reachability tests**: All state metric pipeline paths connected via `StatefulPipeline` and `set_pipeline_state_bytes`.
- [x] **Negative tests**: Quota over-budget prospective rejection returning `ViewState::OverBudgetRejected` and audit event log entries.
- [x] **Dispatch-wiring audit**: Every path in Phase 2 audit verified connected.
- [x] **Coverage matrix**: All 11 stateful operator cells tested and passing.
- [x] **Proof Obligations**: All claims (1-4, LFS durability, SimRuntime buggify, CI accounting gate) satisfied with concrete evidence.
