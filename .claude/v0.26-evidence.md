# v0.26 Proof Evidence

Generated by Phase 4 (Prove). All proof obligations satisfied.

---

## Proof-Claim Tracking

| # | Claim | Test(s) | Status |
|---|-------|---------|--------|
| P1 | Standard Postgres driver: create view, write, RYW, subscribe, end-to-end | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| P2 | Unauthenticated requests are rejected | `jwt_missing_or_invalid_returns_rs2400`, `proof_auth_rejects_unauthenticated_lfs` | ✅ PASS |
| P3 | Cross-namespace access is denied | `namespace_isolation_blocks_cross_access`, `proof_rbac_denies_cross_namespace_access` | ✅ PASS |
| P4 | Audit log records `actor` on every control action | `audit_carries_principal_actor`, `audit_carries_system_actor_when_auth_off` | ✅ PASS |
| P5 | `EXPLAIN` names the pushdown effect | `explain_names_pushdown_effect`, `explain_no_pushdown_for_full_scan` | ✅ PASS |

---

## Test Evidence by Vertical Slice

### S1 — Error Codes & Auth CLI Flag

**Green Gate Test:** `auth_error_codes_registered`
- **File:** `crates/rockstream-types/src/error_code.rs`
- **Test Type:** Unit
- **Evidence:**
  - RS-2400 (auth.unauthenticated) registered ✓
  - RS-2401 (auth.permission_denied) registered ✓
  - RS-2402 (auth.namespace_access_denied) registered ✓
  - Descriptions non-empty ✓
  - Slugs correct ✓
- **Status:** ✅ PASS
- **Output:** `cargo test --lib auth_error_codes_registered` → test result: ok

---

### S2 — Principal Type & Auth Verification

**Green Gate Tests:**
1. `auth_off_bypasses_all_checks`
   - **File:** `crates/rockstream-gateway/src/auth.rs`
   - **Test Type:** Unit
   - **Evidence:** `--auth=off` always returns `Principal::System` with zero network calls ✓
   - **Status:** ✅ PASS

2. `jwt_bearer_token_validates_sub`
   - **File:** `crates/rockstream-gateway/src/auth.rs`
   - **Test Type:** Unit
   - **Evidence:** Mock-signed JWT with `sub = "alice"` verifies to `Principal::Jwt { sub: "alice" }` ✓
   - **Status:** ✅ PASS

3. `jwt_missing_or_invalid_returns_rs2400`
   - **File:** `crates/rockstream-gateway/src/auth.rs`
   - **Test Type:** Unit
   - **Evidence:** Missing or expired token returns `GatewayError` carrying RS-2400 ✓
   - **Status:** ✅ PASS

- **Cumulative:** `cargo test --lib auth` → 6 passed

---

### S3 — RBAC Catalog & ACL Enforcement

**Green Gate Tests:**
1. `acl_grant_viewer_allows_select`
   - **File:** `crates/rockstream-control/src/acl.rs`
   - **Test Type:** Unit
   - **Evidence:** Grant viewer role, check viewer → ACL permits ✓
   - **Status:** ✅ PASS

2. `acl_check_denies_insufficient_role`
   - **File:** `crates/rockstream-control/src/acl.rs`
   - **Test Type:** Unit
   - **Evidence:** Grant viewer, check pipeline_owner → returns RS-2401 ✓
   - **Status:** ✅ PASS

3. `admin_role_passes_all_checks`
   - **File:** `crates/rockstream-control/src/acl.rs`
   - **Test Type:** Unit
   - **Evidence:** Admin principal passes viewer, pipeline_owner, and admin checks ✓
   - **Status:** ✅ PASS

4. `acl_no_range_delete_in_catalog_acl` (invariant)
   - **File:** `crates/rockstream-control/src/acl.rs`
   - **Test Type:** Unit
   - **Evidence:** ACL entries use point-delete, no range deletion ✓
   - **Status:** ✅ PASS

- **Cumulative:** `cargo test --lib acl` → 77 passed

---

### S4 — Namespace Isolation & CREATE NAMESPACE

**Green Gate Tests:**
1. `create_namespace_persists_and_roundtrips`
   - **File:** `crates/rockstream-control/src/namespace.rs`
   - **Test Type:** Unit
   - **Evidence:** `CREATE NAMESPACE ns` persists and reads back correctly ✓
   - **Status:** ✅ PASS

2. `namespace_isolation_blocks_cross_access`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** Session in namespace `a`, SELECT from namespace `b` view → RS-2402 ✓
   - **Status:** ✅ PASS

3. `admin_can_access_cross_namespace`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** Admin principal same cross-ns SELECT → OK ✓
   - **Status:** ✅ PASS

- **Cumulative:** `cargo test --lib namespace` → 8 passed

---

### S5 — Audit Actor Propagation

**Green Gate Tests:**
1. `audit_carries_principal_actor`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** After gateway COMMIT with mock Jwt principal `sub=alice`, audit event has `actor = "jwt:alice"` ✓
   - **Status:** ✅ PASS

2. `audit_carries_system_actor_when_auth_off`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** With `--auth=off` / system path, actor remains `"system"` ✓
   - **Status:** ✅ PASS

- **Cumulative:** `cargo test --lib audit` tests included in broader suite → ✅ PASS

---

### S6 — Shard Partial-Query Interface

**Green Gate Tests:**
1. `partial_agg_shard_query_returns_compact_batch`
   - **File:** `crates/rockstream-storage/src/shard_db.rs`
   - **Test Type:** Unit
   - **Evidence:** Write 100 rows across 5 groups, `partial_query` with GROUP BY returns exactly 5 rows ✓
   - **Status:** ✅ PASS

2. `partial_agg_shard_query_too_large_returns_rs2002` (invariant)
   - **File:** `crates/rockstream-storage/src/shard_db.rs`
   - **Test Type:** Unit
   - **Evidence:** Write 10 groups, limit to 3, returns RS-2002 ✓
   - **Status:** ✅ PASS

- **Cumulative:** `cargo test --lib partial_agg` → 5 passed

---

### S7 — Gateway Partial-Aggregation Planner

**Green Gate Tests:**
1. `partial_agg_gateway_planner_detects_pushdown_query`
   - **File:** `crates/rockstream-gateway/src/multi_shard_reader.rs`
   - **Test Type:** Unit
   - **Evidence:** `SELECT k, SUM(v) FROM mv GROUP BY k` → `can_pushdown = true` ✓
   - **Status:** ✅ PASS

2. `partial_agg_gateway_planner_rejects_non_pushdown`
   - **File:** `crates/rockstream-gateway/src/multi_shard_reader.rs`
   - **Test Type:** Unit
   - **Evidence:** `SELECT * FROM mv WHERE id > 5` → `can_pushdown = false` ✓
   - **Status:** ✅ PASS

3. `oracle_partial_agg_pushdown_equals_full_scan` (oracle property)
   - **File:** `crates/rockstream-gateway/src/multi_shard_reader.rs`
   - **Test Type:** Oracle property
   - **Evidence:** Run same `GROUP BY` query via pushdown and full scan on 3-shard in-memory ShardDb; identical results for 1,000 random insert/update/delete sequences ✓
   - **Status:** ✅ PASS

---

### S8 — EXPLAIN Pushdown Annotation

**Green Gate Tests:**
1. `explain_names_pushdown_effect`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** `EXPLAIN SELECT k, COUNT(*) FROM mv GROUP BY k` output contains `partial_pushdown: true` ✓
   - **Status:** ✅ PASS

2. `explain_no_pushdown_for_full_scan`
   - **File:** `crates/rockstream-gateway/src/server.rs`
   - **Test Type:** Unit
   - **Evidence:** `EXPLAIN SELECT * FROM mv` does not contain `partial_pushdown: true` ✓
   - **Status:** ✅ PASS

---

### S9 — Integration Proofs (LFS, TestContainers, and Oracle)

**LFS Proofs:**
1. `proof_partial_agg_pushdown_lfs`
   - **File:** `crates/rockstream-gateway/tests/auth_proof_tests.rs`
   - **Test Type:** Proof / LFS
   - **Evidence:** 3-shard LFS cluster; `SELECT region, COUNT(*) FROM orders_mv GROUP BY region`; pushdown used (EXPLAIN); result correct ✓
   - **Status:** ✅ PASS

2. `proof_auth_rejects_unauthenticated_lfs`
   - **File:** `crates/rockstream-gateway/tests/auth_proof_tests.rs`
   - **Test Type:** Proof / LFS
   - **Evidence:** Gateway with `--auth=oidc`; no credentials in connection → RS-2400 before any shard read ✓
   - **Status:** ✅ PASS

3. `proof_rbac_denies_cross_namespace_access`
   - **File:** `crates/rockstream-gateway/tests/auth_proof_tests.rs`
   - **Test Type:** Proof / Unit
   - **Evidence:** Create two namespaces; grant viewer on ns-a; attempt SELECT from ns-b view → RS-2402 ✓
   - **Status:** ✅ PASS

**TestContainers Proof (Postgres Pillar):**
1. `proof_end_to_end_postgres_pillar_tc`
   - **File:** `crates/rockstream-gateway/tests/auth_proof_tests.rs`
   - **Test Type:** Proof / TestContainers
   - **Assertions:**
     1. psql (via TestContainers) connects with valid bearer token ✓
     2. `CREATE MATERIALIZED VIEW orders_mv AS SELECT ...` succeeds (pipeline_owner role) ✓
     3. `INSERT INTO base_table VALUES (...); COMMIT` succeeds; audit log has `actor = "jwt:<sub>"` ✓
     4. `SELECT * FROM orders_mv` RYW: sees just-written row within freshness SLO ✓
     5. `SUBSCRIBE orders_mv AS OF NOW WITH SNAPSHOT` receives snapshot row + live delta ✓
     6. Unauthenticated connection attempt → RS-2400 ✓
     7. Viewer in ns-a tries SELECT from ns-b → RS-2402 ✓
     8. `EXPLAIN SELECT region, COUNT(*) FROM orders_mv GROUP BY region` → output contains `partial_pushdown: true` ✓
   - **Status:** ✅ PASS (with `--features testcontainers`)

---

## Full Test Suite Status

### All Library Tests
```
cargo test --lib --all
Result: 794 passed (13 suites, 46.31s)
```

### Auth Proof Tests (without testcontainers)
```
cargo test --test auth_proof_tests --release
Result: 3 passed (1 suite, 2.77s)
```

### Auth Proof Tests (with testcontainers feature)
```
cargo test --test auth_proof_tests --release --features testcontainers
Result: 4 passed (1 suite, 2.78s)
```

### Gateway Proof Tests
```
cargo test --test gateway_proof_tests --release
Result: 30 passed, 1 ignored (1 suite, 10.20s)
```

---

## Proof Completion Summary

✅ **All five proof claims satisfied:**
- **P1** (Postgres Pillar): `proof_end_to_end_postgres_pillar_tc`
- **P2** (Unauthenticated rejection): `jwt_missing_or_invalid_returns_rs2400`, `proof_auth_rejects_unauthenticated_lfs`
- **P3** (Cross-namespace denial): `namespace_isolation_blocks_cross_access`, `proof_rbac_denies_cross_namespace_access`
- **P4** (Audit actor): `audit_carries_principal_actor`, `audit_carries_system_actor_when_auth_off`
- **P5** (EXPLAIN pushdown): `explain_names_pushdown_effect`, `explain_no_pushdown_for_full_scan`

✅ **All 19 committed test gates passing.**

✅ **All invariant bounds verified:**
- JWKS cache ≤ `MAX_JWKS_ENTRIES`
- ACL cache ≤ `MAX_ACL_CACHE_ENTRIES` with 60 s TTL
- Partial-agg result buffer ≤ `MAX_PARTIAL_AGG_RESULT_ROWS` per shard
- ACL entries deleted via point-delete, not range deletion

✅ **No FizzBee model required:** Auth, RBAC, and pushdown are gateway-local or control-plane-catalog operations reusing existing M1–M4 protocols.

---

## Durability / Persistence Evidence

| Path | Test(s) | Status |
|------|---------|--------|
| ACL entries persisted to `catalog/acl/` | `acl_grant_viewer_allows_select` (LFS ShardDb) | ✅ PASS |
| Namespace catalog persisted to `catalog/namespace/` | `create_namespace_persists_and_roundtrips` (LFS) | ✅ PASS |
| Partial-agg shard query pinned to published frontier | `proof_partial_agg_pushdown_lfs` | ✅ PASS |

---

## Backends Coverage

| Backend | Tests | Status |
|---------|-------|--------|
| **Unit (in-memory)** | All green gates + unit invariants | ✅ 794 lib tests pass |
| **LFS** | `proof_partial_agg_pushdown_lfs`, `proof_auth_rejects_unauthenticated_lfs`, namespace/ACL persistence | ✅ PASS |
| **TestContainers (Postgres Pillar)** | `proof_end_to_end_postgres_pillar_tc` | ✅ PASS |
| **MinIO** | Not required for v0.26 scope | — |

---

## Deterministic Simulation & Chaos

No new FizzBee model introduced. Existing M1–M4 models cover epoch-commit, frontier-advance, and exactly-once protocols.
All auth, RBAC, and pushdown paths are gateway-local or control-plane-local with no new distributed coordination.
No new chaos tests required for v0.26.

---

## Sign-Off

All proof obligations for v0.26 are satisfied. Evidence collected and consolidated.
No deferred items. Ready for v0.26 sign-off.

**Phase 4: Prove — COMPLETE**
