# v0.52.4 Phase 4 evidence

| Claim | Evidence | Result excerpt |
| --- | --- | --- |
| Removed connectors fail closed over Pgwire | `cargo test -p rockstream-gateway --test connector_removal_tests` | `7 passed; 0 failed`; exact `RS-4017` checks cover S3, Iceberg, Delta, Parquet, and webhook removal. |
| Removed catalog state survives durable stores | `connector_removal_lfs_tests`, `connector_removal_minio_tests` | `1 passed; 0 failed` in each store. |
| Connector surface and dispatch wiring are exact | `cargo test -p rockstream-connectors --test connector_surface_contract_tests` | `5 passed; 0 failed`; exact retained file/export inventory, removed-test inventory, runtime absence, and live-tree reference checks. |
| Retained sources remain Pgwire reachable | `cargo test -p rockstream-gateway --test source_ddl_postgres_kafka_tests` | `3 passed; 0 failed`; Kafka and PostgreSQL CDC cover backfill, live update, restart, redaction, and invalid options. |
| CDC failure reason is preserved exactly | `cargo test -p rockstream-gateway --test cdc_transaction_atomicity_tests` | `6 passed; 0 failed`; schema-change fencing returns exact `RS-1002`. |
| Decimal and operator backfill semantics are exact | `cargo test -p rockstream-gateway --test backfill_operator_matrix_tests` | `28 passed; 0 failed`, including declared `Decimal(12,2)` source values and aggregate paths. |
| Nexmark exactness remains covered | `cargo test -p rockstream-oracle --test nexmark_retraction` and `cargo test -p rockstream-sql --test nexmark` | `6 passed; 0 failed` across LFS/MinIO retractions; `12 passed; 0 failed` for the SQL matrix. |
| Formal and static gates | `make verify`, `make verify-relaxed`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check` | All completed successfully; Clippy reported `No issues found`. |
| Removed dependencies are absent | `cargo tree -i iceberg`, `deltalake`, `aws-sdk-s3`, `aws-config`; manifest scan | Each tree check reports no matching package; the manifest scan has no matches. |
| CLI remains usable | `cargo run -p rockstream-cli -- --help` | Exit status `0`. |
| Complete workspace proof | `cargo test --target-dir /private/tmp/rockstream-v0524-proof --workspace` | Exit status `0`; no `FAILED` or `error:` summary lines. This includes LFS, MinIO/Testcontainers, 100,000-seed simulation fuzzing, proportionality, and conformance suites. |

No local performance improvement is claimed by this proof pass.
