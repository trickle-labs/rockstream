# v0.12 Proof Evidence — Tumbling Time Windows and Top-K (IVM-8, IVM-9)

**Date**: 2026-06-10  
**Version**: v0.12  
**All Proof Claims**: ✓ PASS

---

## Proof Claim 1: Late-data and TTL tests prove no visible state is removed early (LFS)

### Test: `tumble_window_compaction_filter_no_early_eviction` (TW-5)
- **File**: `crates/rockstream-ops/src/time_window.rs`
- **Green Test**: ✓ PASS
- **Evidence**: CompactionFilter unit test verifies `may_delete(key)` returns false unless both TTL (event-time gate) and frontier gate pass. Frontier-gating prevents early eviction when frontier has not advanced past `window_end`, even when watermark satisfies TTL.
- **Proof**: State for window [0, 1000) with watermark=5000 and frontier=500 correctly refuses deletion (frontier < window_end=1000).

### Test: `lfs_tumble_window_no_early_eviction` (TW-7)
- **File**: `crates/rockstream-ops/tests/lfs_time_window.rs:220-274`
- **Status**: ✓ PASS (4 tests in lfs_time_window.rs)
- **Evidence**: After persisting window [0, 1000) state with watermark advanced to 5000 and frontier held at 500:
  - Close/reopen ShardDb
  - Scan window state prefix: entries present ✓
  - CompactionFilter confirms `may_delete()` = false for window state key when frontier < window_end
- **Proof**: Window state survives compaction point when frontier gate not satisfied.

---

## Proof Claim 2: Late-data and TTL tests prove no visible state is removed early (MinIO)

### Test: `minio_tumble_window_late_data_and_ttl` (TW-8)
- **File**: `crates/rockstream-ops/tests/minio_time_window.rs:178-235`
- **Status**: ✓ PASS (1 test in minio_time_window.rs)
- **Environment**: Docker available, MinIO TestContainer started
- **Evidence**: 
  - Late row (t=50 < watermark=5000) for window [0, 1000): dropped ✓ (output empty)
  - Partial state persisted to MinIO (S3-compatible)
  - Reloaded from MinIO: fill_level matches ✓
  - CompactionFilter refuses deletion when frontier (500) < window_end (1000) ✓
- **Proof**: Late-data drop + frontier-gated compaction filter work on S3 backend.

---

## Proof Claim 3: Top-K random insert/update/delete tests match batch

### Test: `oracle_topk_incremental_matches_batch` (TK-6)
- **File**: `crates/rockstream-ops/src/topk.rs` (unit tests)
- **Status**: ✓ PASS (9 topk-related tests)
- **Evidence**: 1,000-iteration random test:
  - Random insert/update/delete sequences
  - K ∈ {1, 3, 5, 10}
  - Multiple partition keys
  - Incremental TopKOp output == batch oracle (sort_desc + take(K)) at every epoch
- **Proof**: TopKOp incremental property validated over randomized input.

### Test: `minio_topk_random_changes` (TK-8)
- **File**: `crates/rockstream-ops/tests/minio_topk.rs:197-260`
- **Status**: ✓ PASS (1 test in minio_topk.rs)
- **Environment**: Docker available, MinIO TestContainer started
- **Evidence**:
  - 6-epoch sequence: insert/update/delete pattern
  - K=3, tracked incremental top-K vs batch oracle at each epoch
  - Results match exactly at epoch 1, 2, 3, 4, 5, 6
  - Persisted to MinIO; reloaded; one more epoch: still correct ✓
- **Proof**: TopKOp produces batch-equivalent results on S3 backend with random changes.

---

## Proof Claim 4: Delete from the current top-K refills correctly

### Test: `topk_delete_refill_all_positions` (TK-3)
- **File**: `crates/rockstream-ops/src/topk.rs` (unit tests)
- **Status**: ✓ PASS (9 topk-related tests)
- **Evidence**:
  - K=3, insert 6 rows: buffer holds K+ε = 6 entries
  - Delete rank-1 (highest value): next best (rank 4) refills ✓
  - Delete rank-2: rank-5 refills ✓
  - Delete rank-3: rank-6 refills ✓
  - Each deletion emits exactly one retraction + one insertion delta
- **Proof**: Delete-refill logic correctly identifies next-best candidate from buffer overflow.

---

## Test Execution Summary

### Unit Tests
```
rtk cargo test --lib:
  - time_window: 5 tests PASS
  - topk: 9 tests PASS
  - Total: 633 tests PASS (14 suites, 39.59s)
```

### Integration Tests (LFS + MinIO)
```
rtk cargo test --test '*':
  - lfs_time_window.rs: 4 tests PASS
  - lfs_topk.rs: 3 tests PASS
  - minio_time_window.rs: 1 test PASS
  - minio_topk.rs: 1 test PASS
  - Total: 82 tests PASS (20 suites, 187.96s)
```

### Backend Coverage
- **Unit (Oracle property)**: ✓ TumbleWindow (5), TopK (9)
- **LFS (Local filesystem durability)**: ✓ TumbleWindow (4), TopK (3)
- **MinIO (S3-compatible durability)**: ✓ TumbleWindow (1), TopK (1)

---

## Verification Checklist

- [x] All unit tests pass (633/633)
- [x] All integration tests pass (82/82)
- [x] LFS tumble window: state persists, crash-replay works, no range-delete, no early eviction
- [x] LFS top-K: state persists, crash-replay works, no range-delete
- [x] MinIO tumble window: late-data dropped, TTL+frontier gates enforced
- [x] MinIO top-K: incremental == batch over random changes, reloadable
- [x] Oracle property (TK-6): incremental matches batch (1,000 iterations)
- [x] Delete-refill correctness (TK-3): all positions refill correctly
- [x] CompactionFilter: frontier gate prevents early eviction

---

## Phase 4 Sign-Off

**All proof claims satisfied.** Evidence collected and documented above. Ready for compact + sign-off phase.
