CREATE TABLE r1_source (id BIGINT PRIMARY KEY, group_id BIGINT NOT NULL, dimension_id BIGINT NOT NULL, value BIGINT NOT NULL, active BOOLEAN NOT NULL);
CREATE MATERIALIZED VIEW r1_shared_01 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_02 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_03 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_04 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_05 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_06 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_07 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_08 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_09 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_10 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_11 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_12 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_13 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_14 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_15 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_16 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_17 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_18 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_19 AS SELECT group_id, SUM(value) AS total FROM r1_source GROUP BY group_id;
CREATE MATERIALIZED VIEW r1_shared_20 AS SELECT group_id, COUNT(*) AS rows FROM r1_source GROUP BY group_id;
