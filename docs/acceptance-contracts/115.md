# Acceptance contract: #115 Preserve tumble-window multiplicity and correction order independence

Contract revision: v1  
Source: GitHub issue #115, all ten acceptance criteria; RockStream test-suite audit C3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Compiled tumble windows preserve signed multiplicity, aggregates, durable recovery, and the declared correction-order contract.

Storage handoff: Canonical destination `docs/acceptance-contracts/115.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #115 AC1 | For size 10 and row `(timestamp=1,value=7)`, weights `+2,-1,-1` emit exactly the three declared weighted deltas. | Collapsing to row presence fails. | Compiled tumble-window pipeline. | Exact signed deltas. | Three-epoch delta test. | planned |
| R2 | #115 AC2 | Complete accumulated bags contain weights 2, then 1, then no row. | Incorrect multiplicity fails. | Window state output. | Exact bags. | Full bag assertions. | planned |
| R3 | #115 AC3 | Downstream grouped COUNT/SUM returns exactly `(2,14)`, then `(1,7)`, then no group. | Aggregate/count-only mismatch fails. | Production downstream aggregate. | Independent expected aggregates. | Exact grouped results. | planned |
| R4 | #115 AC4 | Persist/reopen between transitions matches uninterrupted schemas, nullable values, signed deltas, and aggregates. | In-memory-only or schema-loss recovery fails. | Durable window state. | Uninterrupted execution. | Interleaved reopen comparison. | planned |
| R5 | #115 AC5 | A unit-weight-only mutation fails the multiplicity regression. | Presence-only assertion fails. | Same window test. | Exact weighted oracle. | Targeted mutation check. | planned |
| R6 | #115 AC6 | Drop policy with `(1,10)` and `(100,99)` yields exactly the two declared rows and finalizes window 0. | Wrong finalization or row values fail. | Compiled Drop-policy window. | Exact weighted output/frontier. | Drop-policy case. | planned |
| R7 | #115 AC7 | Applying correction `[(1,10):-1,(2,20):+1]` in either order from the same finalized state produces the same result. | Incidental input order dependence fails. | Correction input path. | Order-independent expected result. | Two deterministic permutations. | planned |
| R8 | #115 AC8 | Both correction orders emit exactly `{(0,1,10):-1,(0,2,20):+1}` and leave the declared final bag. | Missing or reordered delta fails. | Window correction output. | Exact signed bag. | Full output assertions. | planned |
| R9 | #115 AC9 | Restoring the finalized starting state from durable storage produces the identical correction results. | Fresh-only coverage fails. | Durable restore and correction. | Uninterrupted finalized state. | Restore-before-permutation test. | planned |
| R10 | #115 AC10 | If canonical correction order is part of the product contract, the producer boundary enforces and tests it; otherwise permutations are not silently accepted as equivalent. | Unstated order behavior fails. | Producer boundary and window contract. | Authorized product decision. | Boundary test or explicit exclusion. | gap |

## Unresolved gaps

- The issue does not establish whether correction order is canonical product behavior or must be commutative. The producer-boundary contract must be resolved before readiness.

## Open questions

- Is correction order guaranteed by the product contract, or must the window state accept both permutations?

## Out of scope

- New window policies beyond the stated tumble size and Drop policy.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
