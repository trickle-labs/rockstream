# Acceptance contract: #127 Reject invalid qualification and derive reports from executed results

Contract revision: v1  
Source: GitHub issue #127, all six acceptance criteria; RockStream test-suite audit A3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Qualification executes selected mandatory checks, rejects invalid execution, and derives exact reports from real results.

Storage handoff: Canonical destination `docs/acceptance-contracts/127.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #127 AC1 | An unknown suite fails before build/run with the exact defined error and never produces a passing report. | Fixed-success report or post-build failure fails. | Release qualification entry point. | Defined unknown-suite error. | Unknown-suite self-test. | planned |
| R2 | #127 AC2 | Reports list exact executed scenario IDs, statuses, test-process exits, and mandatory omissions, with aggregates derived from those results. | Fixed counts or absent identities fail. | Qualification result manifest. | Actual subprocess results. | Full normalized report assertion. | planned |
| R3 | #127 AC3 | Zero mandatory execution, missing observations/services, unexpected ignored cases, and failed test process each reject qualification. | Any invalid execution reported success fails. | Qualification execution gate. | Defined failure for each condition. | Five negative controls. | planned |
| R4 | #127 AC4 | A comment-only fixture cannot satisfy mutation or recovery proof; named checks execute and intended negative controls fail. | Textual function-name search fails. | Mutation checker and named checks. | Executed behavior and negative control. | Comment-only and mutated fixtures. | planned |
| R5 | #127 AC5 | Self-tests assert the entire normalized report or error for empty selection, comment-only input, malformed/incomplete records, unexpected skip, failed check, and valid run. | Count-only assertions fail. | Checker self-test interface. | Exact normalized result contract. | Six full-output self-tests. | planned |
| R6 | #127 AC6 | Fast helper checks remain separate from release qualification, and successful evidence preserves candidate identity and selected settings. | Helper-only result or missing identity fails. | Qualification report and job selection. | Execution settings and candidate record. | Successful manifest comparison. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- A new generalized test framework.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
