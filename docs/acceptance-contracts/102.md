# Acceptance contract: #102 Preserve workload IDs across log and snapshot recovery

Contract revision: v1  
Source: GitHub issue #102, all five acceptance criteria; Rockstream test-suite audit B2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Workload identifiers and definitions remain stable across allocation gaps, deletion, log replay, snapshot recovery, and subsequent operations.

Storage handoff: Canonical destination `docs/acceptance-contracts/102.md`, revision v1; saved in the repository and linked from issue #102.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #102 AC1 | After consuming ID 1 and creating a workload, the live mapping is ID 1 absent and ID 2 containing the full workload definition. | Reconstructed allocation order or missing definition fails. | Workload allocation and lookup APIs. | Exact ID-to-definition mapping. | Live mapping regression before recovery. | planned |
| R2 | #102 AC2 | Log-only recovery preserves that exact mapping. | Workload moving from ID 2 to ID 1 fails. | Catalog log replay and lookup. | Pre-recovery full mapping. | Log-only recovery test with all fields. | planned |
| R3 | #102 AC3 | Snapshot, compaction, and snapshot recovery preserve the identical mapping and full definition. | Enumeration-derived IDs fail. | Snapshot recovery and lookup API. | Same mapping as live state. | Snapshot recovery regression. | planned |
| R4 | #102 AC4 | Multiple workloads with allocation gaps and one deletion recover with every original lookup and definition unchanged, and the deleted ID absent. | Name-only lookup or collapsed IDs fails. | Workload catalog lookup. | Complete original mapping. | Gap/deletion recovery case. | planned |
| R5 | #102 AC5 | Deleting by an original workload ID after recovery removes only that workload and a second recovery preserves the complete surviving mapping. | Wrong workload deletion or later remapping fails. | Delete, recover, and lookup APIs. | Exact surviving mapping. | Delete-after-recovery regression. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Changing public workload allocation policy beyond preserving established IDs.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
