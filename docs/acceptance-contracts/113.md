# Acceptance contract: #113 Preserve TopK spilled multiplicity and isolate shared-storage operators

Contract revision: v1  
Source: GitHub issue #113, all five acceptance criteria; RockStream test-suite audit C2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Production TopK preserves weighted multiplicity and isolates operators sharing spill storage after the real spill threshold is crossed.

Storage handoff: Canonical destination `docs/acceptance-contracts/113.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #113 AC1 | Descending K=2 with ranks 1..100000 and rank 100001 inserted twice yields final bag `{100001:2}` and the second insertion emits `{100001:+1,100000:-1}`. | Unit-only or presence-only output fails. | Production TopK input/delta API. | Exact weighted bag and delta. | Threshold-crossing TopK regression. | planned |
| R2 | #113 AC2 | Deleting one copy emits `{100001:-1,100000:+1}` and leaves `{100001:1,100000:1}`. | Wrong multiplicity or retraction fails. | Production TopK deletion path. | Exact signed output and state. | Duplicate deletion case. | planned |
| R3 | #113 AC3 | Deleting the remaining copy emits `{100001:-1,99999:+1}` and leaves `{100000:1,99999:1}`. | Incorrect replacement or residual duplicate fails. | Production TopK deletion path. | Exact signed output and state. | Second deletion case. | planned |
| R4 | #113 AC4 | A K=1 operator sharing the DB and partition returns exactly `{-1:1}` and neither operator reads or changes the other's spill entries. | Cross-operator contamination fails. | Shared spill storage and operator IDs. | Isolated expected relation. | Two-operator shared-storage regression. | planned |
| R5 | #113 AC5 | At least one test crosses the real threshold and proves spill occurred, comparing complete weighted output. | In-memory fixture or unverified threshold fails. | Spill backend observation. | Actual spill marker plus full output. | Threshold/spill assertion and mutation-sensitive output. | planned |

## Unresolved gaps

- The exact production threshold and named integration job are not identified in the issue; resolve them before implementation handoff.

## Open questions

- What exact threshold/observation proves spill for this version, and which integration job owns it?

## Out of scope

- General spill-performance tuning or new storage partitioning.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
