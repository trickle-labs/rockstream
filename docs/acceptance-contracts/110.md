# Acceptance contract: #110 Account for every test target and mandatory case in CI

Contract revision: v1  
Source: GitHub issue #110, all five acceptance criteria; RockStream test-suite audit A1 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: CI discovers and accounts for every relevant test target and mandatory case, while expensive jobs remain explicit and failures remain visible.

Storage handoff: Canonical destination `docs/acceptance-contracts/110.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #110 AC1 | Every simulation target and all other workspace targets are assigned to a concrete PR, nightly, or release invocation with required features and services. | Newly unassigned target fails the assignment check. | Cargo metadata and workflow assignment checker. | Complete discovered target set. | Assignment self-test and workflow manifest. | planned |
| R2 | #110 AC2 | Reports record exact executed test IDs, outcomes, feature selection, and explicit skip reasons. | Binary discovery, ignored mandatory tests, or counts alone fail. | Test runner result manifest. | Actual execution transcript. | Exact manifest assertions. | planned |
| R3 | #110 AC3 | Self-tests reject an unassigned target, zero selected mandatory tests, missing feature/service, unexpected skip, and ignored mandatory case. | Any invalid case reported successful fails. | Assignment/qualification checker. | Defined error for each invalid input. | One negative control per case. | planned |
| R4 | #110 AC4 | Expensive seed and Docker suites run in appropriate jobs without blanket retries or hidden failures. | Hidden, silently skipped, or blanket-retried failures fail. | Workflow job definitions. | Declared job and failure behavior. | Workflow inspection plus checker self-tests. | planned |
| R5 | #110 AC5 | Documentation names the status checks that collectively form mandatory safety gates and exposes separately scheduled HA, formal, and verification failures. | Aggregate Check alone masking a required job fails. | CI workflow and documentation. | Required-check inventory. | Exact workflow/documentation check. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Redesigning the test framework or moving every expensive test into PR CI.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
