# Acceptance contract: #122 Deliver Kafka snapshot and change payloads that match the view

Contract revision: v1  
Source: GitHub issue #122, all six acceptance criteria; RockStream test-suite audit D4 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: The production Kafka sink delivers an exact materialized-view snapshot followed by exact incremental changes and recovers without gaps or duplicates.

Storage handoff: Canonical destination `docs/acceptance-contracts/122.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #122 AC1 | A view containing `(1,'a')` and `(2,'b')` emits its snapshot, then update key 1 to `c`, delete key 2, and recovers after interruption. | Marker-only or precomputed output fails. | Production view-to-Kafka sink and consumer. | Exact operation history. | End-to-end snapshot/change scenario. | planned |
| R2 | #122 AC2 | Snapshot and change payloads preserve row identity, values, operation kind or signed weight, and transaction boundaries, reconstructing exactly `(1,'c')`. | Equal counts with wrong contents fail. | Actual consumer payload stream. | Independent expected records and relation. | Full typed payload comparison. | gap |
| R3 | #122 AC3 | Snapshot completion precedes corresponding live changes without gaps or duplicates through the production view-to-sink path. | Out-of-order, missing, or duplicate record fails. | Consumer sequence and completion boundary. | Expected ordering/invariant. | Completion and stream audit. | planned |
| R4 | #122 AC4 | Required payload and progress metadata persist and restore, and uninterrupted and recovered outputs compare identically. | Progress-only or unrecovered output fails. | Sink persistence/recovery APIs. | Uninterrupted expected records. | Interrupted/recovered comparison. | planned |
| R5 | #122 AC5 | Changing a value while preserving row count, omitting a deletion, or duplicating a change fails qualification. | Count-only marker checks fail. | Sink qualification assertions. | Exact stream mismatch oracle. | Three negative controls. | planned |
| R6 | #122 AC6 | If row delivery is unsupported, qualification remains failing or explicitly unsupported and removes the affirmative Core payload claim. | Unsupported path reported as proven fails. | Capability/qualification manifest. | Declared support state. | Unsupported-path qualification check. | planned |

## Unresolved gaps

- The “contractual snapshot and change payload stream” is not linked to a canonical payload specification; define the exact field names, encoding, and transaction-boundary representation before readiness.

## Open questions

- Which canonical payload specification governs row identity, operation kind/weight, and transaction boundaries?

## Out of scope

- Additional sink formats or connector-wide exactly-once claims.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
