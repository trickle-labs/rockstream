# Acceptance contract: #104 Reject Raft startup when persisted term or vote is unreadable

Contract revision: v1  
Source: GitHub issue #104, all five acceptance criteria; RockStream test-suite audit B4 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Startup distinguishes genuinely absent Raft state from unreadable or corrupt state and never forgets persisted authority.

Storage handoff: Canonical destination `docs/acceptance-contracts/104.md`, revision v1; saved in the repository and linked from issue #104.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #104 AC1 | Persisted term 7 and vote A are restored exactly on ordinary restart. | Any defaulted or changed term/vote fails. | `RaftPersistentStore::load` and startup. | Exact persisted authority state. | Round-trip restart test. | planned |
| R2 | #104 AC2 | Malformed state, permission failure, transient GET failure, and body-read failure each return the defined storage/corruption error instead of empty state. | Treating unreadable data as absent fails. | Injected persistent-store load boundary. | Defined error per failure mode. | Four independent startup regressions. | planned |
| R3 | #104 AC3 | Every failed startup produces the exact empty vote-grant and leadership-event transcript. | Any authority event after failed load fails. | Startup/Raft event interface. | Expected empty transcript. | Full transcript assertions. | planned |
| R4 | #104 AC4 | Stored bytes are unchanged before and after every failed startup. | Reset or overwrite of evidence fails. | Persistent-store object inspection. | Byte-identical stored state. | Before/after byte comparison. | planned |
| R5 | #104 AC5 | Truly absent state permits initial bootstrap with the exact defined initial authority state. | Confusing absence with failure fails. | Missing-object startup path. | Defined bootstrap state. | Missing-state control case. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Distributed leader-election liveness beyond startup state loading.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
