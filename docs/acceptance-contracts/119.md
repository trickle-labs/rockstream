# Acceptance contract: #119 Reject malformed Bind values with controlled protocol errors

Contract revision: v1  
Source: GitHub issue #119, all five acceptance criteria; RockStream test-suite audit D2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Malformed extended-query binary parameters are rejected explicitly, leave state unchanged, and recover the connection when the gateway contract permits.

Storage handoff: Canonical destination `docs/acceptance-contracts/119.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #119 AC1 | Truncated array headers, short element payloads, invalid numeric widths, impossible dimensions, and inconsistent declared lengths are rejected through the real Bind path. | Defaults or unchecked later reads fail. | Production Parse/Bind/Execute/Sync. | Gateway protocol contract. | One malformed case per decoder boundary. | planned |
| R2 | #119 AC2 | Each malformed case produces the complete expected protocol transcript, SQLSTATE, and transaction status. | Accepting any error or disconnect fails. | Protocol transcript boundary. | Exact contractual transcript. | Full transcript assertions. | planned |
| R3 | #119 AC3 | Every server task result is observed so an internal panic fails the test even if the client sees a closed connection. | Client-only observation fails. | Server task join/diagnostic path. | No panic and exact task result. | Capture and assert all task outcomes. | planned |
| R4 | #119 AC4 | Rejected mutating statements leave the complete table state unchanged and never install a default or partial value. | Partial mutation/default coercion fails. | Gateway DML and storage state. | Pre-request full table state. | Exact before/after rows and metadata. | planned |
| R5 | #119 AC5 | After Sync, the next query returns the exact permitted result; connection-fatal errors terminate as defined while an independent valid connection remains usable. | Wrong recovery or service-wide impact fails. | Sync recovery and independent connection. | Defined protocol recovery contract. | Recovery and fatal-boundary cases. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Unsupported binary types not named by the issue.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
