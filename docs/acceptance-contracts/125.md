# Acceptance contract: #125 Make deterministic chaos verify a real incremental workload

Contract revision: v1  
Source: GitHub issue #125, all six acceptance criteria; RockStream test-suite audit A2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: `docs/acceptance-contracts/111.md` v1 and its typed independent-oracle outcome.

Intended outcome: Deterministic chaos exercises a real incremental production path with identical workload histories and observes exact recovery behavior.

Storage handoff: Canonical destination `docs/acceptance-contracts/125.md`, revision v1; saved in the repository and linked from issue #125.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #125 AC1 | Reference and faulted executions receive the identical complete input history while workload and fault seeds remain independent. | Shared random stream or divergent input fails. | Deterministic workload/fault runner. | Pre-generated input history and both seeds. | Full history/seed manifest. | planned |
| R2 | #125 AC2 | A production ingestion/operator/recovery path produces complete typed weighted results and source progress at every required frontier, including fault and restart. | Epoch counts or simulation-only path fails. | Production execution and query/progress API. | Independent typed oracle from #111. | Frontier-by-frontier comparison. | planned |
| R3 | #125 AC3 | Actual recovery transitions and elapsed times are observed, rather than assigned from passing ranges or fixed zero counters. | Manufactured timing/data-loss values fail. | Fault injection and event observation. | Actual event timestamps/state. | Event transcript and duration assertions. | planned |
| R4 | #125 AC4 | A value change, duplicate row, suppressed acknowledgement, and over-deadline recovery each produce the expected failed result. | Count-preserving corruption that passes fails. | Qualification negative-control interface. | Defined failure reason per mutation. | Four independent negative controls. | planned |
| R5 | #125 AC5 | Production transitions, both seeds, and minimized failing history are recorded. | Pass/Fail-only loop fails. | Run manifest and failure artifact. | Actual execution identity. | Exact manifest and minimized history. | planned |
| R6 | #125 AC6 | The verified scenario runs in a bounded scheduled seed job without claiming unexecuted operator or failure coverage. | Unsupported coverage claim fails. | Scheduled workflow and capability report. | Executed target set. | Scheduled job and honest report check. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Every chaos category or an unbounded seed campaign.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
