# Acceptance contract: #107 Handle recovered migrations explicitly during control startup

Contract revision: v1  
Source: GitHub issue #107, all five acceptance criteria; RockStream test-suite audit B7 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Control startup resumes supported persisted migrations explicitly and exposes unsupported recovery as a durable blocked state without silently discarding records.

Storage handoff: Canonical destination `docs/acceptance-contracts/107.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #107 AC1 | Restarting from Copying, FencingOld, Verifying, and GcEligible produces the exact externally visible resumed or blocked outcome for each phase. | Silent discard or incorrect phase transition fails. | Production control startup and migration record API. | Defined phase outcome. | One restart case per phase. | planned |
| R2 | #107 AC2 | Cleanup interrupted after partial donor deletion, Done persistence, and history persistence before active-record deletion resumes through production startup. | Startup ignoring the active record fails. | Migration cleanup and startup. | Persisted record and data state. | Three interruption schedules. | planned |
| R3 | #107 AC3 | Completed recovery yields complete donor/recipient maps, authoritative leases, terminal history, and no active record. | Partial map or retained active record fails. | Migration state machine and catalog. | Exact expected maps, leases, history. | Full-record recovery assertions. | planned |
| R4 | #107 AC4 | Blocked recovery reports explicit status, retains the full record, and leaves remaining data unchanged. | Treating blocked work as handled fails. | Startup status and storage API. | Defined blocked record and unchanged data. | Exact status/record/data assertions. | planned |
| R5 | #107 AC5 | Unrelated buckets remain byte-identical and a second restart produces the same final records, leases, and data. | Collateral mutation or non-idempotent restart fails. | Storage inspection and restart path. | Byte-identical unrelated state and repeat result. | Before/after and second-restart comparison. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Adding new migration phases or changing migration ownership policy.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
