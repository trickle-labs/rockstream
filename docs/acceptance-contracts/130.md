# Acceptance contract: #130 Run bounded fuzz-engine jobs and retain reproducible failures

Contract revision: v1  
Source: GitHub issue #130, all six acceptance criteria; RockStream test-suite audit A5 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Scheduled jobs execute bounded coverage-guided fuzzing for every registered decoder target, retain reproducible failures/corpus improvements, and report coverage claims honestly.

Storage handoff: Canonical destination `docs/acceptance-contracts/130.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #130 AC1 | Every registered target receives a scheduled bounded engine run with explicit duration, timeout, and memory limits, while fixed regression replay remains in PR CI. | Compile-only or zero-execution target fails. | Fuzz target registry and scheduled workflow. | Registered target set and job budget. | Workflow manifest and deliberate zero-execution fixture. | gap |
| R2 | #130 AC2 | Minimized failures and useful corpus additions persist across runs with target, toolchain, seed/reproducible input, command, budget, and candidate revision. | Unreplayable or unidentified artifact fails. | Fuzz artifact store and replay command. | Exact artifact metadata. | Deliberate failing target fixture and artifact comparison. | planned |
| R3 | #130 AC3 | Engine crash, target failure, out-of-contract timeout, or missing mandatory target fails visibly rather than reporting compile-only success. | Hidden failure or successful compile-only soak fails. | Fuzz job exit/report gate. | Defined failure classification. | Failure fixtures and exact job result. | planned |
| R4 | #130 AC4 | At least one known-valid seed reaches meaningful parsing branches for each decoder, and malformed-input regressions remain retained. | Invalid-only or corpus-loss run fails. | Target seed corpus and engine invocation. | Known-valid seeds and retained regression corpus. | Seed execution manifest. | planned |
| R5 | #130 AC5 | A deliberately failing target proves failure artifacts are captured and deterministically replayed, and a zero-execution fixture fails the job check. | Missing artifact or nondeterministic replay fails. | Fuzz runner self-test. | Exact failing input/output. | Failing target and zero-execution fixtures. | planned |
| R6 | #130 AC6 | Reports claim only byte-decoder coverage and do not claim stateful protocol or semantic-equivalence coverage without an executing target. | Unsupported coverage claim fails. | Coverage/report manifest. | Executed target capability set. | Report scope assertion. | planned |

## Unresolved gaps

- The issue does not enumerate the seven registered targets, name the fuzz engine, or identify the exact scheduled job and resource budgets.

## Open questions

- What are the authoritative seven target names, engine invocation, schedule, timeout, and memory limits?

## Out of scope

- Stateful protocol or semantic-equivalence fuzzing until an executing target exists.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
