# Acceptance contract: #103 Fail catalog recovery when stored history cannot be read completely

Contract revision: v1  
Source: GitHub issue #103, all five acceptance criteria; Rockstream test-suite audit B3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Catalog recovery fails closed on incomplete or corrupt durable history and never publishes a partial catalog.

Storage handoff: Canonical destination `docs/acceptance-contracts/103.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #103 AC1 | A complete table, view, index, and dependency catalog can be persisted, snapshotted, compacted, and recovered with complete metadata. | Missing or partial records fail. | Catalog persistence and recovery API. | Original complete catalog. | Full metadata recovery test. | planned |
| R2 | #103 AC2 | Snapshot-list failure, snapshot GET failure, body-read failure, and malformed snapshot data each return the defined recovery error and publish no catalog when history cannot reconstruct state. | Swallowed error or partial catalog fails. | Injected object-store recovery boundary. | Defined error and no published state. | One negative case per failure mode. | planned |
| R3 | #103 AC3 | A log-list failure after one successful item returns the defined error rather than a catalog assembled from a partial listing. | Partial listing accepted as success fails. | Log listing/replay boundary. | Expected fail-closed outcome. | Deterministic injected listing failure. | planned |
| R4 | #103 AC4 | Failed recovery preserves the complete stored-object inventory and bytes. | Recovery rewriting or deleting evidence fails. | Object-store inspection API. | Byte-identical before/after snapshot. | Inventory and byte comparison. | planned |
| R5 | #103 AC5 | When complete earlier history remains, safe fallback recovers the full original revision and metadata. | Fallback from incomplete history or count-only comparison fails. | Recovery fallback path. | Original full revision and records. | Explicit recoverable-history case. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- New storage backends or a generic recovery framework.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
