# Acceptance contract: #124 Compare PostgreSQL conformance against a pinned PostgreSQL server

Contract revision: v1  
Source: GitHub issue #124, all six acceptance criteria; RockStream test-suite audit A4 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: A bounded PostgreSQL compatibility corpus compares RockStream with a pinned PostgreSQL server using identical client-visible behavior and exact typed results/errors.

Storage handoff: Canonical destination `docs/acceptance-contracts/124.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #124 AC1 | Both actual servers start at pinned versions and receive identical schema, data, statements, and client behavior. | RockStream-only or floating reference version fails. | Same client against both servers. | Reference server execution. | Integration job startup and transcript. | gap |
| R2 | #124 AC2 | Comparisons include complete column metadata, nullable values, command tags, SQLSTATEs, and transaction state, with intended differences explicitly bounded. | Non-exact or undocumented difference fails. | Client result/error boundary. | Pinned PostgreSQL observations and compatibility spec. | Full differential record. | planned |
| R3 | #124 AC3 | Numeric precision case returns exactly IDs/values `(1,1234.5678)` and `(2,9999.0000)`, rejects overflow with defined error, and leaves table contents unchanged. | Nonempty-only or partial state check fails. | PostgreSQL-compatible SQL/DML path. | Exact pinned PostgreSQL result/error. | Precision and overflow regression. | planned |
| R4 | #124 AC4 | Existing exact array-membership cases compare complete row results, not only command completion. | Message-only success fails. | Array query result path. | Pinned PostgreSQL rows. | Exact array corpus comparison. | planned |
| R5 | #124 AC5 | Altering one row value, type, or error code makes the differential case fail with a useful statement/result difference. | Mutation-insensitive comparator fails. | Differential comparison harness. | Exact expected mismatch. | Three negative controls. | planned |
| R6 | #124 AC6 | The runnable corpus runs in an integration CI job with required services, and unavailable reference infrastructure fails mandatory execution explicitly. | Silent skip or unavailable-as-success fails. | CI job and service preflight. | Mandatory execution contract. | Workflow and preflight assertions. | planned |

## Unresolved gaps

- The exact narrow corpus and pinned PostgreSQL version are not named in the issue.

## Open questions

- Which PostgreSQL version and exact numeric/array case list are the supported corpus?

## Out of scope

- An unrestricted PostgreSQL compatibility project.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
