# Acceptance contract: #121 Keep Kafka recovery pending while delivery outcome is unknown

Contract revision: v1  
Source: GitHub issue #121, all five acceptance criteria; RockStream test-suite audit D4 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Kafka recovery distinguishes unknown delivery from absence or presence and never republishes a record whose broker outcome is unresolved.

Storage handoff: Canonical destination `docs/acceptance-contracts/121.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #121 AC1 | After epoch 1 is committed with its original payload, acknowledgement is lost, delivery lookup is unavailable, and producer access remains possible. | Awaited successful commit or unavailable producer does not establish unknown outcome. | Real broker producer, lookup, and recovery path. | Controlled broker fault schedule. | Exact fault-injection scenario. | planned |
| R2 | #121 AC2 | Recovery returns the defined retryable unknown-outcome error and leaves the exact broker record sequence unchanged while lookup is unavailable. | Republishing or generic error fails. | Recovery lookup/publish boundary. | Original broker record bytes and sequence. | Full error and sequence comparison. | gap |
| R3 | #121 AC3 | After lookup restoration and two recovery attempts, the topic contains exactly one original epoch-1 record, the exact resolved sink state, and no staged epoch. | Duplicate publication or residual staging fails. | Broker topic and sink recovery state. | Exact expected record/state. | Two-repeat recovery regression. | planned |
| R4 | #121 AC4 | Interruption before broker commit eventually delivers exactly one record with the same original bytes. | Loss, duplication, or changed bytes fail. | Pre-commit interruption path. | Original payload bytes. | Separate pre-commit recovery case. | planned |
| R5 | #121 AC5 | Consumer setup failure, poll failure, timeout, confirmed absence, and confirmed presence are distinct outcomes; uncertainty is not simulated by dropping a sink after successful commit. | Collapsed error classes or fake uncertainty fails. | Consumer/lookup result classification. | Defined outcome taxonomy. | One case per outcome with exact result. | planned |

## Unresolved gaps

- The issue refers to a “defined retryable unknown-outcome error” but does not identify its exact error code or public API representation.

## Open questions

- What exact error code/message is the authoritative retryable unknown-outcome contract?

## Out of scope

- General Kafka exactly-once behavior outside the two interruption schedules.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
