# Acceptance contract: #112 Preserve NULL keys and payload validity in production outer joins

Contract revision: v1  
Source: GitHub issue #112, all five acceptance criteria; RockStream test-suite audit C1 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Compiled production outer joins preserve NULL validity, matching semantics, signed weights, and durable recovery behavior.

Storage handoff: Canonical destination `docs/acceptance-contracts/112.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #112 AC1 | Identical `(NULL,7)` inputs produce exactly the declared LEFT, FULL, SEMI, and ANTI weighted outputs. | NULL matching or wrong join type fails. | Compiled production join operators. | Exact typed rows, validity, and weights. | Four operator cases. | planned |
| R2 | #112 AC2 | Retracting the right row emits empty LEFT/SEMI/ANTI deltas and exactly the declared FULL retraction. | Missing or extra signed delta fails. | Production retraction path. | Independent expected deltas. | Retraction assertions. | planned |
| R3 | #112 AC3 | Joining `(1,NULL)` with `(1,0)` yields exactly `(1,NULL,1,0)` and preserves NULL versus zero. | Coercion or NULL-as-zero fails. | Compiled join input/output. | Typed expected row. | NULL/zero distinction case. | planned |
| R4 | #112 AC4 | Schemas, values, validity, and signed weights remain exact through compiled execution and persist/reopen. | Count-only or in-memory-only checks fail. | Operator persistence/recovery seam. | Full typed weighted relation. | Persist/reopen comparison. | planned |
| R5 | #112 AC5 | Replacing NULL with zero or matching NULL keys makes the targeted regression fail. | Mutation-insensitive test fails. | Same production join test. | Exact expected mismatch. | Two targeted wrong-behavior mutations. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- New join types or optimizer rewrites not required by the listed cases.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
