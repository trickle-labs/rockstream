# Acceptance contract: trickle-labs/rockstream#137 — Apply workload changes by effective revision

Contract revision: v1
Source: [issue #137](https://github.com/trickle-labs/rockstream/issues/137), acceptance criteria 1–3
Parent contract: `docs/acceptance-contracts/132.md`, revision v2
Parent snapshot: commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, [immutable contract](https://github.com/trickle-labs/rockstream/blob/7a73050dbb28fdf61584d052b9521b2e7dce9835/docs/acceptance-contracts/132.md), SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734`
Contribution: [canonical decomposition](https://github.com/trickle-labs/rockstream/issues/132#issuecomment-5832445481), S5; `trickle-labs/rockstream#132 v2:R30` and `trickle-labs/rockstream#132 v2:R31` (workload-control contribution of UIE-M7-04)
Prerequisites: #133 S1 must provide verified live usage and limits; #134 S2 must apply accepted workload revisions in scheduling and epoch owners. Before dependent qualification, confirm the earlier milestone release-process outcomes named in the canonical decomposition.
Inherited constraints: Use existing workload permissions and the frozen owner-acknowledgement deadline of 5,000 ms from `benchmarks/r1-local/profile-v072.toml` revision 1 (commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, SHA-256 `d5cf97b3d0a181cddd78dbf811e22224c4767bd1948143b45beb38390464719a`). Do not claim instant effectiveness or expand the later reviewed-change console workflow. Parent v2 and applicable earlier milestone guarantees remain binding.

Intended outcome: Operators can make revision-conditioned changes to memory limit, parallelism, priority, and freshness, and observe whether active owners have applied those changes and how enforcement behaves.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/137.md`, revision v1; retrieval confirmed by the invoking workflow.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | `trickle-labs/rockstream#132 v2:R30`; issue #137 criterion 1 | Apply memory, parallelism, priority, and freshness edits only when the supplied expected workload revision matches the current revision; a stale concurrent edit returns a conflict and leaves the accepted workload state unchanged. | Exercise each setting alone and together. A last-writer-wins update, partial update after conflict, or changed effective setting on conflict fails. | Public `ALTER WORKLOAD` operation and subsequent `SHOW WORKLOAD STATUS FOR <name>` response. | The submitted expected revision, serialized workload state, and exact conflict response establish the expected compare-and-update result independently of handler logic. | `v072_workload_conflict`: issue two edits from the same revision, assert exact success/conflict responses and exact before/after setting and revision rows; also assert exact rows after individual and combined setting updates. | planned |
| R2 | `trickle-labs/rockstream#132 v2:R30`; issue #137 criterion 1 | Reject a memory-limit reduction below verified non-reclaimable live use without changing the accepted or effective limit. | Test at the verified-use boundary and below it. An unverified estimate, changed limit, or partial settings update on rejection fails. | Public workload change and status operations, with S1's verified live-use/limit reporting. | The verified live-use observation from S1 and requested limit determine whether the reduction is admissible; status readback establishes that rejection preserved the old value. | `v072_workload_conflict`: exact boundary and below-boundary edits, with the verified usage record and exact response plus complete requested/effective status rows before and after. | planned |
| R3 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 2 | Propagate each accepted revision to its active owners and record acknowledgements against the frozen 5,000 ms owner-acknowledgement deadline. | Do not report an accepted request as instantly effective. Missing or late acknowledgements remain pending or failed. | Public workload change/status operations and active resource, scheduler, and freshness owners. | The accepted revision and frozen `profile-v072.toml` revision 1 deadline are compared with each owner's independently recorded acknowledgement revision and timestamp. | `v072_workload_propagation`: exact accepted revision and per-owner acknowledgement transcript, asserting each owner revision and timestamp against the 5,000 ms deadline, including a delayed acknowledgement. | planned |
| R4 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 2 | Report requested and effective revisions separately, with each active owner's pending or failed state visible until that owner acknowledges the accepted revision. | Catalog readback alone, a single ambiguous revision, or marking a pending/failed owner effective fails. | Public `SHOW WORKLOAD STATUS` / `SHOW WORKLOAD STATUS FOR <name>` response and owner status. | The exact accepted revision and independently observed per-owner acknowledgements define the expected requested/effective state. | `v072_workload_propagation`: assert exact complete status rows before acknowledgement, after each acknowledgement, and on a failed owner; verify requested and effective revisions and owner states separately. | planned |
| R5 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 3 | Under active load, an accepted memory-limit change reaches enforcement and produces the corresponding observable pressure behavior. | A catalog-only value change, idle owner, or behavior inconsistent with the accepted revision fails. Preserve the existing resource-owner rules. | Public workload changes/status plus process-level workload execution and resource-owner diagnostics. | A fixed offered-work transcript, exact accepted limit, and independent expected memory/pressure observations. | `v072_workload_propagation`: run active work before and after a memory-limit change; retain exact status, resource, and result transcript and assert the resulting pressure behavior. | planned |
| R6 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 3 | Under active load, accepted parallelism, priority, and freshness changes reach scheduling and freshness enforcement. | A catalog-only value change, idle owner, or behavior inconsistent with the accepted revision fails. Preserve the existing scheduler and freshness rules. | Public workload changes/status plus process-level competing workloads and scheduler diagnostics. | A fixed offered-work transcript and exact accepted settings determine expected active concurrency, priority order, and freshness observations. | `v072_workload_propagation`: run competing workloads before and after each setting change; retain exact status, scheduling, freshness, and result transcript and assert the changed enforcement behavior. | planned |
| R7 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 3 | If an owner is unavailable, report its accepted revision as pending or failed and keep it ineffective; after recovery, apply the requested revision and report it effective within the frozen acknowledgement deadline. | Never infer effectiveness from catalog persistence or claim a missing owner's acknowledgement. Recovery must not lose or duplicate accepted changes. | Public workload change/status operations, owner outage/recovery controls, and process-level behavior. | Injected owner availability and its exact acknowledgement revision/time, compared with requested/effective status and behavior after recovery. | `v072_workload_propagation`: exact outage, pending/failed, recovery, acknowledgement, and behavior transcript; assert no effect while unavailable and effectiveness by 5,000 ms after recovery. | planned |
| R8 | `trickle-labs/rockstream#132 v2:R31`; issue #137 criterion 3 | After process restart, retain the accepted workload revision and show the corresponding changed pressure and scheduling behavior once active owners acknowledge it. | A catalog-only restart readback, loss of the accepted revision, or claiming effective behavior before owner acknowledgement fails. | Public workload status and process-level workload execution across restart. | The pre-restart accepted revision and fixed workload transcript, plus post-restart owner acknowledgements and observed behavior. | `v072_workload_propagation`: restart the process with pending and accepted owner states; assert exact recovered requested/effective status, acknowledgement records, and before/after workload behavior and results. | planned |

## Unresolved gaps

- None. Current gateway coverage checks requested setting fields and catalog status, but not revision conditions, effective owner state, outage/restart propagation, or enforcement behavior; the named process and public-interface cases above are the planned evidence path for those missing checks.

## Open questions

- None. The parent v2 contract and issue #137 settle the observable change/status outcome and public seam.

## Out of scope

- Reworking scheduler, epoch, or resource-owner algorithms allocated to other slices; S5 verifies accepted revisions reach their active enforcement owners.
- The later reviewed-change console workflow.
- Unrelated capacity benchmarks, UIE-M5 query/export behavior, subscriptions, and v0.72 sign-off/report publication.

## Change notes

- Initial contract. Child rows R1–R8 refine parent obligations `trickle-labs/rockstream#132 v2:R30` and `trickle-labs/rockstream#132 v2:R31`; child IDs are local to issue #137.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation. Implement the smallest complete solution inside the spec envelope. Preserve requirement IDs and promised outcomes. Capture the resulting candidate for separate /review-implementation and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate. Record actual evidence and verdicts in a separate proof report.
