# Acceptance contract: trickle-labs/rockstream#134 — Schedule fairly with durable adaptive epochs

Contract revision: v1
Source: [issue #134](https://github.com/trickle-labs/rockstream/issues/134), including outcome, criteria, inherited constraints, evidence approach, and prerequisite; canonical decomposition [issue #132 comment 5832445481](https://github.com/trickle-labs/rockstream/issues/132#issuecomment-5832445481), S2 allocation.
Parent contract: `docs/acceptance-contracts/132.md`, revision v2.
Parent snapshot: Immutable parent commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`; captured checked-in text SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734` (verified against file).
Contribution: S2 “Schedule fairly with durable adaptive epochs”; `trickle-labs/rockstream#132 v2:R7`, `#132 v2:R8`, `#132 v2:R9`, `#132 v2:R10`, plus the commit-byte and acknowledgement-waiter portions of `#132 v2:R6`. Parent completion checks: `v072_fair_schedule`, `v072_ordered_failover`, `v072_epoch_adaptation`, `v072_durable_epochs`, and `v072_bounded_resources`.
Prerequisites: #133 must make the resource ledger and pressure state available for fair admission and epoch pressure input. Confirm linked v0.71, v0.61.1, v0.61.2, v0.62.1, v0.65.1, v0.67.1, v0.68, v0.69, and v0.70 sign-off outcomes/evidence before dependent qualification.

Intended outcome: Competing workloads retain bounded access while adaptive epochs preserve ordered, durable results through failure and restart.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/134.md`, revision v1; retrieval confirmed by the invoking workflow.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | Issue #134 criterion 4; `trickle-labs/rockstream#132 v2:R6` commit-byte portion | Bound pending physical commit bytes, expose fill, and apply backpressure or an error when staging would exceed the limit. | Empty, exact-limit, over-limit, and single-epoch-over-limit inputs; no unbounded bytes or silent loss. | `PhysicalCommitGroup` staging/metrics in `crates/rockstream-ops/src/group_commit.rs`. | Published physical-commit byte limit and exact submitted epoch sizes determine expected admission and retained epochs; production currently names MAX_GROUP_COMMIT_PENDING_BYTES. | `v072_bounded_resources/commit_bytes`: release-process boundary/overflow transcript; assert exact fill, error, and subsequent committed results. | planned |
| R2 | Issue #134 criterion 4; `trickle-labs/rockstream#132 v2:R6` acknowledgement-waiter portion | Bound and expose active durability waiters; reject/backpressure excess waiters without losing or duplicating accepted acknowledgements. | Exact limit and one over; waiters released after success and failed commit/retry. | `PhysicalCommitGroup` waiter registration/completion and metrics in `crates/rockstream-ops/src/group_commit.rs`. | Named code limit `MAX_GROUP_COMMIT_WAITERS = 1024` in `crates/rockstream-ops/src/group_commit.rs`; profile `max_waiters = 1024` independently agrees. Use exact request identities and durable epoch outcomes as expected values. | `v072_bounded_resources/ack_waiters`: exact request/completion transcript at limit, overflow, and retry; assert final waiter fill. | planned |
| R3 | Issue #134 criterion 1; `trickle-labs/rockstream#132 v2:R7` | Select runnable work using priority, memory pressure, freshness target, and available parallelism. | Vary each input independently; missing inputs or exceeding parallelism fail. | Public multi-workload scheduling and diagnostics; scheduler seams `crates/rockstream-ops/src/scheduler.rs` and `crates/rockstream-control/src/scheduler.rs`. | Frozen profile revision 1 and independently specified runnable-work/input transcript. | `v072_fair_schedule/input_decisions`: release-process competing workloads; assert exact selection sequence and observed values for all four inputs. | planned |
| R4 | Issue #134 criterion 1; `trickle-labs/rockstream#132 v2:R7` | Meet frozen fairness, runnable-wait, and queue-age bounds while reserving capacity for reads, maintenance, and control work. | Starvation, profile bound breach, or reserved-capacity interference fails even if throughput is high. | Public multi-workload behavior, read/maintenance/control responses, queue-age and scheduling diagnostics. | Frozen profile priorities, `max_runnable_wait_ms`, `max_queue_age_ms`, `foreground_reserved_bytes`, and freshness SLO; independent offered-work schedule and complete result oracle. | `v072_fair_schedule/fairness_reservations`: exact schedule/age transcript and read, maintenance, control outputs under load; compare each against frozen limits. | planned |
| R5 | Issue #134 criterion 2; `trickle-labs/rockstream#132 v2:R8` | Preserve one ordered owner per shard and dependent-view order. | Inject owner failure/reassignment; duplicate owners, early dependent visibility, or missing/duplicated result rows fail. | Public ingestion, ownership diagnostics, and committed queries; `crates/rockstream-control/src/scheduler.rs`. | Independent shard/dependency graph, input epochs, and complete expected result multiset per committed epoch. | `v072_ordered_failover/ownership_and_order`: failure/reassignment transcript with exact owner history and per-epoch rows. | planned |
| R6 | Issue #134 criterion 2; `trickle-labs/rockstream#132 v2:R8` | Release capacity held by failed work so another runnable workload progresses. | Retained reservation or stalled unrelated workload after failure fails. | Public workload progress and resource-ledger/pressure diagnostics. | Independent admitted-work schedule and expected resource ownership after termination. | `v072_ordered_failover/failure_capacity_release`: exact before/after ledger, failure response, and other-workload committed results. | planned |
| R7 | Issue #134 criterion 3; `trickle-labs/rockstream#132 v2:R9` | Adapt epoch size using ingest rate, commit latency, state pressure, and freshness target; keep every choice inside configured lower and upper bounds. | Idle, burst, slow commit, and overload; vary each signal. Missing signal or out-of-bound update fails. | Runtime epoch/commit configuration and public diagnostics; `crates/rockstream-control/src/freshness.rs`, `crates/rockstream-ops/src/group_commit.rs`, and issue-identified `crates/rockstream-runtime/src/epoch_compaction.rs`. | Frozen `profile-v072.toml` epoch bounds and independent signal/expected-size transcript. | `v072_epoch_adaptation/signal_transcript`: release-process cases for idle/burst/slow-commit/pressure; assert exact settings, four inputs, and bounds. | planned |
| R8 | Issue #134 criterion 3; `trickle-labs/rockstream#132 v2:R9` | Reject inconsistent epoch lower/upper bounds at startup. | Lower greater than upper must error; silent clamping or running invalid settings fails. | Public process configuration/startup diagnostics. | Exact invalid configuration and settled profile contract. | `v072_epoch_adaptation/invalid_bounds_startup`: launch with inverted bounds; assert exact diagnostic and nonzero result, plus valid-boundary startup. | planned |
| R9 | Issue #134 criterion 4; `trickle-labs/rockstream#132 v2:R10` | Preserve complete logical-epoch atomicity and dependent-result order through resizing, group commit, failed flush, retry, and restart; acknowledge only durable commits. | No partial visibility, failed/unresolved acknowledgement, reordered/duplicated epochs, or loss of acknowledged data after restart. | Public commit acknowledgements and committed queries across restart; `PhysicalCommitGroup` and runtime epoch path. | Independent dependency graph and full expected rows per epoch; durable frontier follows successful storage flush. | `v072_durable_epochs`: inject write/flush failures across resized/grouped epochs, retry and restart; assert exact acknowledgements, frontier, and complete results before/after recovery. | planned |

## Unresolved gaps

- No outcome, seam, or oracle gap identified. Named v0.72 release-process cases and exact output evidence remain to be produced; current adjacent tests alone do not establish them.

## Open questions

- None. The issue and parent v2 settle the outcome and seams.

## Out of scope

- S1's general ledger, admission, and pressure work except as prerequisite/input; S2 owns only the commit-byte and acknowledgement-waiter portions of R6.
- Interactive queries, subscriptions, export, workload revision propagation, capacity matrix, cost report, and sign-off assembly allocated to other slices.
- Changes to frozen priorities, fairness/epoch bounds, or parent v2 agreement.

## Change notes

- Initial child contract v1. Child IDs are local to #134; Source maps each row to qualified parent obligations.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-implementation and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
