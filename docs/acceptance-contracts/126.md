# Acceptance contract: #126 Measure worker-failure recovery from actual view and sink results

Contract revision: v1  
Source: GitHub issue #126, all six acceptance criteria; RockStream test-suite audit A2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: `docs/acceptance-contracts/122.md` v1 and its exact Kafka payload outcome.

Intended outcome: One real worker-failure scenario observes the production source-to-view-to-sink path, recovers correctly, and emits reusable exact observations.

Storage handoff: Canonical destination `docs/acceptance-contracts/126.md`, revision v1; saved in the repository and linked from issue #126.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #126 AC1 | Production source, view, worker, storage, and sink process a fixed insert/update/delete history; a worker is interrupted while active and work resumes. | Copied input or helper-only path fails. | Real process/container scenario. | Fixed independent history. | End-to-end interruption/resume case. | planned |
| R2 | #126 AC2 | Actual view and consumed sink records match complete typed weighted output, reconstructed relation, source offsets, and ownership against independent expected results. | Broker-only offsets or counts fail. | Running view/sink consumers and query API. | Independent expected relation/oracle. | Full typed result comparison. | planned |
| R3 | #126 AC3 | Fault-to-detection, new lease ownership, and first correct query at the required frontier are measured from actual events; Docker command duration is separate. | Command duration used as recovery metric fails. | Event timestamps and ownership/query observations. | Scenario frontier and event timeline. | Exact duration fields. | planned |
| R4 | #126 AC4 | Buffer and staging state are observed from the running application where claimed; input copies, fixed literals, and broker-only checks do not satisfy application correctness. | Manufactured state fails. | Application state/metrics interface. | Actual application observations. | State observation assertions. | planned |
| R5 | #126 AC5 | Corrupted value, duplicate record, suppressed progress, and missed recovery deadline each fail for its intended reason. | Wrong reason or tolerated corruption fails. | Qualification negative-control path. | Defined failure reason. | Four separate negative controls. | planned |
| R6 | #126 AC6 | Qualification reuses actual observations and exact scenario identity, reports sample counts honestly, and does not present one recovery as a meaningful p99. | Invented observations or p99 claim fails. | Result artifact/manifest. | Actual event/result record. | Reusable qualification record comparison. | gap |

## Unresolved gaps

- The exact reusable scenario artifact identity and storage location are not defined; #128 and #129 depend on this outcome.

## Open questions

- Where is the canonical scenario manifest/result artifact stored, and what exact identity do consumers reference?

## Out of scope

- Every Docker chaos category or statistical p99 claims from a single observation.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
