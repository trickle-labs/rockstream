# Acceptance contract: #109 Verify recovery immediately after durable commit acknowledgement

Contract revision: v1  
Source: GitHub issue #109, all five acceptance criteria; RockStream test-suite audit B9 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: A process killed immediately after a successful durable commit acknowledgement recovers exactly the acknowledged state without requiring catalog recreation.

Storage handoff: Canonical destination `docs/acceptance-contracts/109.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #109 AC1 | A defined insert/update/delete history reaches the child only after the successful durable commit acknowledgement. | Acknowledgement before durable commit fails. | Parent/child process test and commit acknowledgement. | Exact acknowledged history. | Controlled process test with acknowledgement barrier. | planned |
| R2 | #109 AC2 | The child is killed immediately, without delay or clean shutdown, and storage is reopened in a fresh process. | Grace interval or clean shutdown fails. | Process kill and storage reopen. | Immediate-crash scenario identity. | Exact kill schedule and restart transcript. | planned |
| R3 | #109 AC3 | Fresh recovery queries full base and aggregate rows, catalog definitions, committed frontier, and idempotency metadata without recreating tables or views. | Recreated definitions or partial checks fail. | Fresh query/recovery API. | Expected committed state. | Full state comparison. | planned |
| R4 | #109 AC4 | Injected flush failure returns the exact failed acknowledgement outcome rather than success. | Successful acknowledgement on failed flush fails. | Storage flush and commit result. | Defined failure outcome. | Failure-injection assertion. | planned |
| R5 | #109 AC5 | Unacknowledged writes follow the permitted unknown-outcome contract rather than being incorrectly required absent. | Treating unknown as confirmed absence fails. | Recovery result and acknowledgement boundary. | Documented allowed outcome. | Explicit unacknowledged-write case. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- New durability semantics beyond the existing commit acknowledgement contract.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
