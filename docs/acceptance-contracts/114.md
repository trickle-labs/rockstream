# Acceptance contract: #114 Make TopK spill recovery and storage errors fail safely

Contract revision: v1  
Source: GitHub issue #114, all five acceptance criteria; RockStream test-suite audit C2 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Spilled TopK state recovers exactly and storage failures return explicit errors without installing partial or successful state.

Storage handoff: Canonical destination `docs/acceptance-contracts/114.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #114 AC1 | Production TopK crosses the real threshold, persists and closes storage, reopens it, and restores the backend it claims to have spilled. | In-memory or wrong-backend recovery fails. | Production TopK and storage reopen. | Spill observation and restored state. | Threshold-crossing reopen test. | planned |
| R2 | #114 AC2 | After recovery, deleting 100001 at descending K=2 emits exactly `{100001:-1,99999:+1}` and leaves `{100000:1,99999:1}`. | Wrong delta or state fails. | Recovered TopK deletion API. | Exact weighted relation. | Full delta/state assertion. | planned |
| R3 | #114 AC3 | Recovered and uninterrupted execution produce identical complete schemas and weighted deltas through duplicate insertion and deletion on both LFS and MinIO jobs. | Backend-specific divergence or count-only output fails. | Production storage backends and TopK. | Uninterrupted reference execution. | LFS and MinIO integration comparisons. | planned |
| R4 | #114 AC4 | Spill scan failure returns the defined storage error, emits no successful result, and leaves previously accepted operator state unchanged. | Partial state publication fails. | Injected spill scan boundary. | Defined error and prior full state. | Failure injection and state comparison. | planned |
| R5 | #114 AC5 | A truncated persisted record returns the defined corruption error and installs no partial restored state. | Partial restore or success fails. | Persisted-record decoder/recovery. | Defined corruption error and no state. | Truncated-record negative control. | planned |

## Unresolved gaps

- The exact production threshold and named LFS/MinIO job assignment are not identified in the issue; resolve them before implementation handoff.

## Open questions

- What exact threshold/observation and CI job names are authoritative for the spill claim?

## Out of scope

- New spill formats or backend implementations.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
