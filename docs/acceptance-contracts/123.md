# Acceptance contract: #123 Run named operator qualification cases through production operators

Contract revision: v1  
Source: GitHub issue #123, all six acceptance criteria; RockStream test-suite audit A4 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: `docs/acceptance-contracts/111.md` v1 and its typed independent-oracle outcome.

Intended outcome: Named operator qualification cases execute the declared production operators and compare their complete typed weighted output, including retractions.

Storage handoff: Canonical destination `docs/acceptance-contracts/123.md`, revision v1; saved in the repository and linked from issue #123.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #123 AC1 | Each affected case instantiates the named production operator with declared Arrow types and verifies complete schema, weighted output, and nullable values. | Handwritten maps or row counts fail. | Production operator constructors and output API. | Typed independent oracle from #111. | Exact operator/schema/value assertions. | planned |
| R2 | #123 AC2 | A left join with an unmatched left row emits the complete matched and NULL-extended bag, and switching to an inner join fails the case. | Wrong operator or missing NULL row fails. | Production left-join operator. | Exact expected weighted bag. | Wrong-operator mutation. | planned |
| R3 | #123 AC3 | Descending TopK size two over ranks 10,20,30 yields exactly 30 and 20; deleting 30 emits its retraction and insertion of 10. | Distinct or wrong ranking behavior fails. | Production TopK input/retraction path. | Exact expected weighted output. | Insertion/deletion case. | planned |
| R4 | #123 AC4 | Window, distinct, arrangement, and runtime qualification cases execute the corresponding production behavior and verify insertion and retraction outputs. | Input construction without execution fails. | Named production operators. | Independent typed expected results. | One targeted case per behavior. | planned |
| R5 | #123 AC5 | Qualification records actual operator identity or otherwise directly establishes the execution path. | Handwritten expected maps alone fail. | Operator identity/trace seam. | Executed path identity. | Manifest or direct operator assertion. | planned |
| R6 | #123 AC6 | Misleading capability names/links are corrected and a wrong-operator mutation fails the exact assertion. | Documentation-only proof fails. | Qualification manifest and test assertions. | Exact expected capability and mismatch. | Targeted mutation and name check. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Operators not named in the issue and broader SQL compatibility.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
