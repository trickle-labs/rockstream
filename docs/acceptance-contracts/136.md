# Acceptance contract: trickle-labs/rockstream#136

Contract revision: v1
Source: GitHub issue trickle-labs/rockstream#136, “What this delivers,” all five acceptance criteria, inherited constraints/non-goals, prerequisites, and evidence approach.
Parent contract: `docs/acceptance-contracts/132.md`, revision v2.
Parent snapshot: exact parent v2 text SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734`; immutable file at commit [`7a73050dbb28fdf61584d052b9521b2e7dce9835`](https://github.com/trickle-labs/rockstream/blob/7a73050dbb28fdf61584d052b9521b2e7dce9835/docs/acceptance-contracts/132.md).
Contribution: canonical decomposition `trickle-labs/rockstream#132` comment 5832445481, S4. This child owns the stream portion of `trickle-labs/rockstream#132 v2:R27`, all of `trickle-labs/rockstream#132 v2:R28`, and the subscription-resource portion of `trickle-labs/rockstream#132 v2:R6`. S3 (#135) supplies the read token; S2 (#134) supplies committed ordered epochs.
Prerequisites: trickle-labs/rockstream#134 must establish committed ordered epoch behavior sufficient to frame complete epochs; trickle-labs/rockstream#135 must supply the actual materialized-view read token for snapshot framing. Confirm those outcomes and linked inherited milestone evidence before dependent qualification; issue closure alone is insufficient.

Intended outcome: A subscriber receives complete committed snapshots and ordered epochs within explicit bounds, or an explicit reset/resnapshot outcome.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/136.md`, revision v1; retrieval confirmed by the invoking workflow.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #136 acceptance criterion 1; `trickle-labs/rockstream#132 v2:R27` | A snapshot is framed with explicit start and end markers and sequence information that identifies its complete boundary. | Missing, repeated, or ambiguous boundary/sequence information fails; an empty snapshot still has a complete boundary. | Public `SUBSCRIBE ... WITH SNAPSHOT` stream through the gateway. | Hand-authored expected frame transcript for a fixed snapshot, including the empty case. | `v072_subscription_epochs`: capture the exact public frame transcript for empty and multi-row snapshots; compare every frame and sequence field to the expected transcript. | planned |
| R2 | #136 acceptance criterion 1; `trickle-labs/rockstream#132 v2:R27` | Each published epoch is framed as one complete committed unit in strict sequence order. | Missing, duplicated, reordered, partial, or uncommitted epoch frames fail. | Public subscription stream after committed writes. | Independent commit transcript and expected epoch ordering. | `v072_subscription_epochs`: submit known commits, capture every frame, and assert exact epoch sequence and complete frame boundaries. | planned |
| R3 | #136 acceptance criterion 1; `trickle-labs/rockstream#132 v2:R27` | Epoch frames preserve every weighted change, including duplicate rows, and expose each committed atomic change set together. | Deduplication, changed multiplicity/diff, split atomic change sets, or half of an uncommitted update fails. | Public subscription stream plus the commit operation. | Hand-authored expected row multiset and atomic commit transcript. | `v072_subscription_epochs`: compare exact row/diff multisets and frame grouping for duplicates and multi-row atomic commits, before and after commit. | planned |
| R4 | #136 acceptance criterion 2; `trickle-labs/rockstream#132 v2:R27` | The snapshot is paired with the actual materialized-view read token supplied by S3, and emitted freshness reflects that token. | A fabricated, stale, missing, or mismatched token, or visibility of an uncommitted half-update, fails. | Public subscription snapshot stream with S3’s materialized-view read result. | The exact token returned by the independent S3 read operation and the commit transcript. | `v072_subscription_epochs`: assert the complete public snapshot/token transcript equals the S3 token and matching committed snapshot; include an uncommitted update and assert it is absent. | planned |
| R5 | #136 acceptance criterion 3; `trickle-labs/rockstream#132 v2:R28`; frozen `profile-v072.toml` `[subscriptions].max_snapshot_rows = 10000` | A snapshot of at most 10,000 rows is accepted; a larger snapshot is rejected as a complete unit, and snapshot row fill is observable. | Returning a partial snapshot, silently dropping rows, or failing to report fill at the boundary fails. | Public subscription stream and subscription fill metrics. | Frozen profile value and independently enumerated complete snapshot rows. | `v072_subscription_bounds`: run exactly 10,000 and 10,001 snapshot rows; assert the exact complete result or explicit rejection and exact reported row fill. | planned |
| R6 | #136 acceptance criterion 3; `trickle-labs/rockstream#132 v2:R28`; frozen `profile-v072.toml` `[subscriptions].max_epoch_bytes = 8388608` | A complete epoch of at most 8,388,608 encoded bytes is accepted; a larger complete epoch is rejected atomically, and epoch byte fill is observable. | Splitting an oversized epoch, dropping rows, or exposing a partial epoch fails. | Public subscription stream and subscription fill metrics. | Frozen profile value and independently encoded expected epoch. | `v072_subscription_bounds`: submit exact-limit and over-limit epochs; assert exact complete frame or explicit rejection and exact byte fill, including unchanged visible state after rejection. | planned |
| R7 | #136 acceptance criterion 3; `trickle-labs/rockstream#132 v2:R28`; frozen `profile-v072.toml` `[subscriptions].max_buffer_bytes = 16777216` | Each stream’s buffered data is bounded to 16,777,216 bytes and its current buffer fill is observable. | Buffer growth beyond the limit, row loss, or missing fill observation fails. | Public subscription stream and per-stream buffer metrics. | Frozen profile value and independently computed bytes queued by a controlled consumer. | `v072_subscription_bounds`: drive a paused consumer through exact-limit and over-limit queued data; assert exact buffer fill and the documented backpressure/error or reset response without lost complete units. | planned |
| R8 | #136 acceptance criterion 3; `trickle-labs/rockstream#132 v2:R28`; frozen `profile-v072.toml` `[subscriptions].max_streams = 16` | At most 16 concurrent subscription streams are admitted, and stream-count fill is observable. | Admitting stream 17 or failing to expose occupancy/release fails. | Public subscription admission and stream-count metrics. | Frozen profile value and exact set of opened streams. | `v072_subscription_bounds`: open 16 then 17 streams; assert exact admission/error outcomes and occupancy, then close one and assert the freed slot is reusable. | planned |
| R9 | #136 acceptance criterion 3; `trickle-labs/rockstream#132 v2:R28`; frozen `profile-v072.toml` `[subscriptions].max_slow_client_ms = 30000` | A slow client is allowed no more than 30,000 ms at the bounded backpressure state; its fill and terminal outcome are observable. | Unbounded retention, silently dropped complete units, or reported completion with a live over-limit stream fails. | Public subscription stream and slow-client/buffer metrics. | Frozen profile value and controlled client-read timing. | `v072_subscription_bounds`: hold a client unread through the 30,000 ms boundary; assert exact terminal frame/error, elapsed time, and final buffer/stream fill. | planned |
| R10 | #136 acceptance criteria 3 and inherited constraint; `trickle-labs/rockstream#132 v2:R28` | If a complete snapshot or epoch exceeds its configured unit bound, reject the whole unit and require a reset/resnapshot outcome; do not silently drop rows. | Partial delivery or advancing the subscriber sequence past a rejected unit fails. | Public subscription stream and subsequent reconnect/resnapshot. | Complete independently enumerated oversized unit and expected sequence state. | `v072_subscription_bounds`: follow each oversized-unit rejection with a fresh snapshot; assert exact rejection/reset transcript, no partial rows, and no skipped sequence in the new snapshot. | planned |
| R11 | #136 acceptance criterion 4 and inherited constraint; `trickle-labs/rockstream#132 v2:R27`; `trickle-labs/rockstream#132 v2:R28` | On reconnect, lost replay, expiry, or process restart, the subscriber must resnapshot unless durable replay has independent qualification. | Replaying unavailable history as complete, resuming with a gap, or claiming unqualified durable replay fails. | Public subscription reconnect/restart lifecycle. | Retained-history boundary, process lifecycle transcript, and independent complete current snapshot. | `v072_subscription_epochs` and `v072_subscription_bounds`: capture exact outcomes for reconnect, evicted replay, expiry, and restart; assert resnapshot is required and its complete result matches the independent snapshot oracle. | planned |

## Unresolved gaps

- None in the planned evidence path. The current `subscribe_handler.rs` and gateway tests expose row polling and backlog bounds but do not yet demonstrate the required public frame protocol, token pairing, or all frozen subscription limits; those are acceptance work, not evidence of completion.

## Open questions

- None. The source assigns the public subscription stream seam and says no open product decision is allocated.

## Out of scope

- S2 committed ordered epoch implementation (`trickle-labs/rockstream#134`) and S3 materialized-view read token (`trickle-labs/rockstream#135`), which are prerequisites.
- Durable replay qualification; resnapshot is required wherever durable replay is unavailable or unqualified.
- Unrelated parent obligations, including query/export behavior, workload resource classes outside subscriptions, and other sibling slices. S4 completion does not establish parent acceptance.

## Change notes

- Initial contract. Child requirements R1–R12 map to the qualified parent obligations in each Source cell; child IDs are local to #136.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation. Implement the smallest complete solution inside the spec envelope. Preserve requirement IDs and promised outcomes. Capture the resulting candidate for separate /review-implementation and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate. Record actual evidence and verdicts in a separate proof report.

## Storage handoff

Canonical destination: `docs/acceptance-contracts/136.md`, revision v1. Storage remains pending; the invoking workflow saves and rereads the approved contract.
