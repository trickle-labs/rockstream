# Acceptance contract: #138 Measure eight canonical workloads with real process evidence

Contract revision: v1
Source: trickle-labs/rockstream#138, title, “What this delivers,” acceptance criteria, inherited constraints, evidence approach, blocker, and handoff.
Parent contract: `docs/acceptance-contracts/132.md`, revision v2.
Parent snapshot: commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, file `docs/acceptance-contracts/132.md`, SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734`.
Contribution: canonical decomposition, issue #132 comment 5832445481, S6: trickle-labs/rockstream#132 v2:R1, trickle-labs/rockstream#132 v2:R11, validator contribution of trickle-labs/rockstream#132 v2:R12, and raw-sample contributions of trickle-labs/rockstream#132 v2:R19 and trickle-labs/rockstream#132 v2:R20. S6 implements the validator and captures eight workload records; S7/S8 supply their own comparison/fault cells and S9 checks whole-matrix completeness and publishes the report.
Prerequisites: Runner development has no child work blocker. Final capacity measurements require integrated S1 resource-envelope and S2 scheduling/epoch outcomes, plus confirmed linked earlier sign-off outcomes named in the decomposition (v0.71, v0.61.1, v0.61.2, v0.62.1, v0.65.1, v0.67.1, v0.68, v0.69, and v0.70). Confirm required artifacts/behavior; closed issues or checked boxes alone do not establish outcomes.

Intended outcome: The R1 runner produces reproducible release-process measurements for all eight canonical workloads using the frozen local v0.72 profile, independently checks complete committed results, and rejects synthetic, inactive, incomplete, or unjustifiably inapplicable evidence.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/138.md`, revision v1; retrieval confirmed by the invoking workflow.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | trickle-labs/rockstream#132 v2:R1; trickle-labs/rockstream#138 acceptance criterion 1 | Bind profile revision 1, its SHA-256, and each applicable workload threshold assignment to every run manifest and raw record before comparison. | The frozen profile is `benchmarks/r1-local/profile-v072.toml`, revision 1, digest `d5cf97b3d0a181cddd78dbf811e22224c4767bd1948143b45beb38390464719a`. Changing it after observing candidate measurements invalidates that comparison; a different host/backend requires a new profile revision. Values are planned limits, not capacity claims. | `tools/r1-local-harness/src/main.rs` runner preparation/run path; profile-bound manifest and raw sample schema. | The pinned profile bytes and revision, independently hashed; each run's assigned workload threshold is looked up from the profile's workload/regression tables. | `profile_v072_manifest_binding`: assert exact profile revision/digest and per-workload threshold assignment in each run record; mutate digest, revision, or assignment and assert exact rejection. | planned |
| R2 | trickle-labs/rockstream#132 v2:R11; trickle-labs/rockstream#138 acceptance criterion 2 and “What this delivers” | Execute filter/projection, group aggregate, high-cardinality aggregate, two-way join, window, PostgreSQL CDC, Kafka ingestion, and distributed exchange through release processes and public ingestion/query interfaces; compare the complete committed result multiset for each workload against an independent oracle. | A fixture, visibility probe, synthetic process, incomplete/truncated result, or uncommitted result cannot qualify. Preserve duplicate rows and require the result at the committed epoch. Final capacity runs wait for integrated S1/S2 and confirmed earlier outcomes; runner implementation itself does not. | R1 workload runner via public ingestion and query interfaces in `tools/r1-local-harness/src/main.rs`; cluster process seam in `cluster.rs`; oracle adapter in `oracle.rs`. | Independently computed complete expected multiset from the generated input/change transcript using SQLite, compared including duplicate multiplicity to the full committed query result. | `v072_eight_workloads`: run all eight named workloads in release processes; retain exact input/change transcript, committed epoch, actual and expected canonical rows, and exact multiset assertion/output for each. | planned |
| R3 | trickle-labs/rockstream#132 v2:R19; trickle-labs/rockstream#132 v2:R20; trickle-labs/rockstream#138 acceptance criterion 3 | Retain per-run raw samples and manifests containing release binary digest, active process identities, run command, profile/threshold identity, host/backend/topology, payload, state size, durability, scheduled offered load, generator delay, errors, timeouts, and independent complete-result identity for each S6 workload. | No hard-coded/synthetic measurements; inactive or identity-changing workers, dropped offered work, errors/timeouts, missing evidence fields, or a result digest without recoverable full oracle output fails qualification. This row covers S6 raw capture; S7/S8 own their additional samples and S9 owns final report assembly/validation. | `RawSample` serialization/validation in `tools/r1-local-harness/src/evidence.rs`; process observations in `cluster.rs` and `metrics.rs`; load schedule in `load.rs`; evidence qualification in `negative.rs`. | Observed process snapshots/PIDs, recorded environment and invocation, and independently scheduled offer transcript; SQLite result oracle and raw input/result artifacts. | `v072_raw_sample_completeness`: assert exact serialized records, environment/invocation metadata, scheduled load, and actual process IDs against the run transcript; negative cases remove required metadata, change binary/profile identity, mark workers inactive, inject delay/drop/error/timeout, or omit full-result artifact and assert exact failure output. | planned |
| R4 | trickle-labs/rockstream#132 v2:R12; trickle-labs/rockstream#138 acceptance criterion 4 and evidence approach | Provide a fail-closed full-matrix validator that rejects every omitted applicable cell, including sibling comparison/fault cells, as well as inactive workers, synthetic evidence, and non-applicable cells lacking a scope reason and owner. S6 completion requires its own eight workload records and negative validator fixtures; it does not require S7/S8 measurements to exist yet. S7/S8 supply their cells, and S9 checks complete parent matrix applicability. | The validator rejects a missing applicable sibling cell when validating a purported full matrix, while S6 completion is evaluated against only S6 deliverables. Historical blocked shortcuts and `sample_reference_run` never count as capacity evidence. A non-applicable cell must have a supported scope reason and accountable owner; unexplained omission is failure. | Matrix schema and validation in `tools/r1-local-harness/src/matrix.rs` and evidence gate in `negative.rs`. | Canonical v0.72 full-matrix definitions and declared applicability; S6 record set plus negative inputs differing by one invalid condition. | `v072_matrix_completeness`: assert exact acceptance of the eight complete S6 records and exact rejection of full-matrix fixtures omitting an applicable S6/S7/S8 cell, an inactive worker, synthetic evidence, or non-applicability without reason/owner; no sibling measurement run is required for these fixtures. | planned |

## Unresolved gaps

- None. The repository has the runner, matrix, raw-evidence, process, load, and independent-oracle seams plus existing integration/unit test entry points. New exact-output cases can extend those paths.

## Open questions

- None. The parent contract and S6 decomposition settle the outcomes and seams.

## Out of scope

- Executing S7 comparison cells or S8 fault/recovery cells; S6 provides validator support for them only.
- S9 whole-matrix completeness, assembled capacity report, regression/cost analysis, documentation, and sign-off.
- Changing or recalibrating profile revision 1 after candidate observations; changing host/backend or targets requires a separately versioned profile.
- Cloud capacity/cost claims, new workloads, the v0.73.1 console/export behavior, and broader product changes.

## Change notes

- Initial contract. S6's child criteria are mapped to qualified parent obligations; parent IDs remain distinct from these child-local IDs.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-implementation and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
