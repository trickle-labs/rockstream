# Acceptance contract: #129 Verify rolling upgrades between distinct versions under writes

Contract revision: v1  
Source: GitHub issue #129, all six acceptance criteria; RockStream test-suite audit A3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: `docs/acceptance-contracts/126.md` v1 with its actual recovery scenario outcome.

Intended outcome: A real three-worker rolling upgrade between distinct pinned versions preserves acknowledged writes, ownership, compatibility, and post-upgrade service behavior.

Storage handoff: Canonical destination `docs/acceptance-contracts/129.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #129 AC1 | Equal old/new image digests and unavailable required images are rejected; both identities are recorded and actual serving processes launch. | Version-printing or same-image success fails. | Release upgrade runner and process health. | Distinct pinned image identities. | Preflight and process manifest. | planned |
| R2 | #129 AC2 | One worker at a time is replaced while insert/update/delete traffic continues, recording exact acknowledged operations, ownership, and client-visible behavior. | Simultaneous replacement or no real traffic fails. | Real multi-worker cluster and client. | Acknowledged operation history. | Step-by-step upgrade transcript. | planned |
| R3 | #129 AC3 | After each replacement and at completion, complete relation, source/sink progress, schema compatibility, and absence of duplicate/lost acknowledged effects match expectations. | Constant epoch vectors or counts fail. | Running service/view/sink queries. | Independent expected relation and history. | Full checkpoint comparisons. | planned |
| R4 | #129 AC4 | New writes after final replacement and a fresh reopen of upgraded state succeed with expected results. | No post-upgrade or fresh-reopen write fails. | Client write and reopen path. | Expected post-upgrade relation. | New-write/reopen regression. | planned |
| R5 | #129 AC5 | Failed replacement produces the documented safe outcome; rollback/downgrade is tested only if explicitly supported, otherwise its defined rejection is asserted. | Invented rollback support fails. | Replacement failure boundary. | Product-supported outcome. | Failure-injection case. | planned |
| R6 | #129 AC6 | Scheduled release qualification records exact observation artifacts and cannot pass with constant epochs or successful container startup alone. | Startup-only qualification fails. | Release qualification job. | Actual upgrade artifact and scenario identity. | Complete success/corruption artifact comparison. | gap |

## Unresolved gaps

- The canonical reusable recovery/upgrade scenario artifact identity from #126 is not defined.

## Open questions

- What exact #126 scenario artifact and release job own the upgrade evidence?

## Out of scope

- Rollback/downgrade behavior unless the product explicitly supports it.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
