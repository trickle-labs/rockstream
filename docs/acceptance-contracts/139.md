# Acceptance contract: trickle-labs/rockstream#139 — Qualify scale and mixed-read capacity comparisons

Contract revision: v1
Source: [issue #139](https://github.com/trickle-labs/rockstream/issues/139), its four acceptance criteria and inherited constraints; canonical slice allocation in [issue #132 comment 5832445481](https://github.com/trickle-labs/rockstream/issues/132#issuecomment-5832445481).
Parent contract: `docs/acceptance-contracts/132.md`, revision v2.
Parent snapshot: commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, `docs/acceptance-contracts/132.md`, SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734`.
Contribution: S7 in issue #132 comment 5832445481; `trickle-labs/rockstream#132 v2:R12` comparison cells, `trickle-labs/rockstream#132 v2:R13`–`R16`, and the raw-sample portions of `trickle-labs/rockstream#132 v2:R19`–`R20`.
Prerequisites: S6 / issue #138 must provide its real-process runner, manifests, and complete-result oracle. Confirm linked release-process evidence for the parent’s v0.71 and earlier prerequisite chain (v0.61.1, v0.61.2, v0.62.1, v0.65.1, v0.67.1, v0.68, v0.69, and v0.70) before dependent qualification; issue closure or checked boxes alone do not establish these outcomes.

Intended outcome: Operators can compare one-key state-write cost, compatible-view sharing, active-worker scaling, and mixed ingestion/query latency with repeated release-process runs, complete independent result checks, and reproducible raw measurements under the frozen local v0.72 profile.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/139.md`, revision v1; retrieval confirmed by the invoking workflow.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | Issue #139 criterion 1; `trickle-labs/rockstream#132 v2:R13`; `trickle-labs/rockstream#132 v2:R12` comparison cells | Measure the state writes caused by the same one-key update at 1K, 100K, and 10M live groups, and show the write ratio meets the frozen maximum of 1.10. Report lookup cost separately. | All three group counts are required. Lookup work cannot be omitted or folded into the write measure; changing update shape or durability invalidates the comparison. | `tools/r1-local-harness` release-run path, workload inputs, worker state-write metrics, and comparison-cell manifest. | Frozen `benchmarks/r1-local/profile-v072.toml` revision 1 and identical offered updates; complete result multiset from the independent harness oracle. | `v072_one_key_scale`: five repeated samples per group count with raw state-write and lookup values, offered-update identity, profile/run manifest, and exact complete-result comparison. | planned |
| R2 | Issue #139 criterion 2; `trickle-labs/rockstream#132 v2:R14`; `trickle-labs/rockstream#132 v2:R12` comparison cells | Compare one compatible view with twenty compatible views against twenty private executions using unchanged payload, offered load, and durability; shared execution must meet the frozen 20-shared/20-private limits for state bytes (≤0.20), writes (≤0.20), and CPU (≤0.85). | The one-view baseline and both twenty-view cases are required. A changed workload parameter or incomplete results invalidate the comparison. | Release-run workload definitions, worker/operator metrics, matrix manifests, and the comparison report. | Frozen profile thresholds and identical view definitions and input; independent complete result oracle for each view. | `v072_shared_views`: repeated raw samples for one view, twenty private views, and twenty shared views; exact per-view output comparison and calculated state/write/CPU ratios. | planned |
| R3 | Issue #139 criterion 3; `trickle-labs/rockstream#132 v2:R15`; `trickle-labs/rockstream#132 v2:R12` comparison cells | Measure sustainable committed throughput with 1, 2, 4, and 8 active workers under the same offered workload and 100 ms freshness target; meet the frozen minimum throughput ratios of 1.25 at 2:1, 2.00 at 4:1, and 2.00 at 8:1, and identify host ceilings. | Every declared worker must show actual process and work activity. Idle/fabricated workers, relaxed freshness, or changed load invalidate the comparison. | Release-run worker manifests, worker PIDs/activity metrics, committed throughput and freshness metrics, and comparison-cell matrix. | Frozen profile ratios and freshness target; OS-observed worker processes plus exact committed input/output transcript and independent result oracle. | `v072_worker_scale`: five repeated four-point runs with PIDs, per-worker activity, committed throughput, freshness, host resource limits, complete results, and exact ratio assertions. | planned |
| R4 | Issue #139 criterion 4; `trickle-labs/rockstream#132 v2:R16`; `trickle-labs/rockstream#132 v2:R19` metric capture; `trickle-labs/rockstream#132 v2:R12` comparison cells | During concurrent ingestion and queries, independently measure and meet the frozen p99 limits for read latency (10 ms), durable commit latency (25 ms), and correct committed-result freshness (100 ms). | The three distributions remain separate; a visibility probe, commit latency substituted for read latency, or generator delay substituted for freshness fails. Offered load, 128-byte payload, durable mode, and freshness target remain fixed per profile. | `tools/r1-local-harness/src/load.rs` decoupled load path, metrics collection, release-run query/commit interfaces, and mixed-read matrix cell. | Frozen profile targets; scheduled event timestamps, durable acknowledgements, and the independent complete multiset oracle at the committed epoch. | `v072_mixed_reads`: repeated raw latency samples and p99 assertions for each metric, generator-delay observations, run manifest, and exact complete-result comparisons during mixed load. | planned |
| R5 | `trickle-labs/rockstream#132 v2:R12` comparison cells; raw-sample contribution to `trickle-labs/rockstream#132 v2:R19`–`R20` | Retain enough S7 run identity and raw measurement data to reproduce each comparison and establish that it used the frozen profile, release binaries, machine/backend, workload parameters, and repetition/noise rules. | Synthetic runs, missing raw samples, wrong profile revision, omitted comparison cells, or a profile changed after candidate results cannot qualify. S7 owns its comparison cells and raw samples only; full parent matrix assembly and report publication remain S9. | S6 manifest and raw-sample format as consumed by the R1-local harness and comparison outputs. | Frozen profile digest and revision, candidate/binary identities, process records, and recomputation from raw samples. | `v072_scale_evidence`: replay all S7 comparisons from retained manifests and raw samples; verify five repetitions, CV ≤0.15, profile digest, exact cell coverage, and result-oracle references. | planned |

## Unresolved gaps

- Prerequisite artifacts are not established by this contract draft: the invoking workflow must confirm S6’s qualifying runner/manifests/oracle and the applicable prior milestone evidence before implementation or qualification. The current repository harness has v0.61.1 profile/matrix behavior; its prior blocked 10M, shared-execution, and distributed-worker cells do not themselves establish the v0.72 S7 outcomes.

## Open questions

- None. The v0.72 profile fixes the local host and comparison values; changing machine or backend requires a new profile revision under the parent contract.

## Out of scope

- S6’s eight canonical workload qualification and general runner implementation, except consuming its prerequisite artifacts.
- S8 large-state recovery and overload fault cells.
- S9 full matrix assembly, final report publication, cost reporting, regression gating, documentation, and sign-off.
- Changes to profile thresholds after candidate results, cloud capacity/cost claims, and additional workloads.

## Change notes

- Initial contract v1. Child requirement IDs are local. Rows map to the qualified parent obligations shown in `Source`; no parent requirement is replaced or redefined.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation after prerequisite outcomes are confirmed. Implement the smallest complete solution inside this spec envelope. Preserve requirement IDs and promised outcomes. Capture the resulting candidate for separate /review-implementation and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate. Record actual evidence and verdicts in a separate proof report.
