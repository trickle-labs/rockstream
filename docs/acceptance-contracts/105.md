# Acceptance contract: #105 Prevent concurrent Raft persistence from regressing term and vote

Contract revision: v1  
Source: GitHub issue #105, all five acceptance criteria; RockStream test-suite audit B5 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Concurrent Raft persistence is ordered so an older write cannot overwrite newer durable authority, and durable state precedes dependent acknowledgements.

Storage handoff: Canonical destination `docs/acceptance-contracts/105.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #105 AC1 | Holding term-7 persistence, completing a term-8 vote for B, and releasing term 7 leaves recovered state exactly term 8 with vote B. | Term 7 overwriting term 8 fails. | Real Raft RPC handlers and persistence store. | Expected `{term:8,vote:B}`. | Deterministic persistence barrier test and restart. | planned |
| R2 | #105 AC2 | A term-8 vote request from another candidate is answered with term 8 and a denied vote after the race. | Regressed term or granted stale vote fails. | Vote RPC handler. | Raft voting rules and recovered state. | Exact response assertion. | planned |
| R3 | #105 AC3 | Overlapping vote, heartbeat, and election persistence paths preserve the exact expected final persisted and recovered state for each schedule. | Timing-only or sequential coverage fails. | All production persistence writers. | Schedule-specific expected states. | Explicit synchronization for each schedule. | planned |
| R4 | #105 AC4 | Responses requiring durable term or vote state are not acknowledged before the corresponding persistence succeeds. | Early acknowledgement before persistence fails. | RPC response boundary plus persistence barrier. | Acknowledgement ordering invariant. | Barrier assertion over response and durable write. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- Raft protocol changes beyond persistence ordering and acknowledgement sequencing.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
