# Acceptance contract: #117 Prevent lost exchange-credit notifications before waiter registration

Contract revision: v1  
Source: GitHub issue #117, all five acceptance criteria; RockStream test-suite audit C5 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: A credit release racing with waiter registration cannot strand a production sender, and credit accounting remains exact.

Storage handoff: Canonical destination `docs/acceptance-contracts/117.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #117 AC1 | With row budget 1 consumed, a second acquire paused after capacity check but before waiter registration can be released deterministically. | Timing sleep or missed race fails. | Production credit acquire/wait path. | Controlled barrier schedule. | Deterministic synchronization regression. | planned |
| R2 | #117 AC2 | Releasing the first reservation in that gap lets the second acquire finish within the bounded deadline without another release. | Lost notification or extra release fails. | Same production waiter path. | One-release completion oracle. | Deadline and release-count assertions. | planned |
| R3 | #117 AC3 | After acquisition, outstanding rows are one and available credit zero; after release, outstanding rows zero and available credit one. | Accounting mismatch fails. | Credit accounting snapshot. | Exact budget invariant. | Full state assertions. | planned |
| R4 | #117 AC4 | With two waiters, one released unit permits exactly one acquisition and the next release permits the remaining waiter. | Broadcast or lost wakeup fails. | Production waiter queue. | Exact completion transcript. | Two-waiter barrier test. | planned |
| R5 | #117 AC5 | The test uses production send barriers and covers a separately reachable bounded-permit wait path. | Standalone unrelated API coverage fails. | Production send/permit seams. | Executed path identity. | Named integration cases. | planned |

## Unresolved gaps

- The issue does not name the authoritative production waiter API or whether the bounded-permit path remains separately reachable.

## Open questions

- Which production path owns the equivalent bounded-permit wait, if it is still reachable?

## Out of scope

- Replacing the exchange-credit design or adding a generic notification library.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
