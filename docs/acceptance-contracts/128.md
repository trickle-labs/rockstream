# Acceptance contract: #128 Qualify recovery using the real observed scenario

Contract revision: v1  
Source: GitHub issue #128, all six acceptance criteria; RockStream test-suite audit A3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: `docs/acceptance-contracts/126.md` v1 with its reusable worker-recovery observations; `docs/acceptance-contracts/127.md` v1 with its fail-closed qualification runner outcome.

Intended outcome: Release qualification runs the real recovery scenario, records actual observations, and rejects substituted or unsupported proof.

Storage handoff: Canonical destination `docs/acceptance-contracts/128.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #128 AC1 | Qualification runs the actual scenario and records complete observed view/sink result, offsets, authority transition, recovery durations, and candidate identity. | Copied expected state or missing observation fails. | Qualification runner plus real scenario artifact. | #126 result and exact candidate. | Complete qualification record. | gap |
| R2 | #128 AC2 | First post-recovery query is fetched from the running service and independently compared, never populated from the expected result. | Oracle copy or stale query fails. | Running service query interface. | Independent expected relation. | Query capture and comparison. | planned |
| R3 | #128 AC3 | Missing events, constant timings, stale results, changed values, duplicate output, and helper-only node status each fail qualification. | Any substituted observation passes fails. | Qualification negative controls. | Intended failure reason per mutation. | Six corrupted-record cases. | planned |
| R4 | #128 AC4 | Result manifest distinguishes executed application proof from helper tests and rejects or marks unsupported mandatory claims without real evidence. | Overclaiming unsupported Kafka/CDC/distributed behavior fails. | Manifest capability fields. | Contract support boundary. | Manifest full-output comparison. | planned |
| R5 | #128 AC5 | Useful helper tests remain verifiable while names, documentation, and capability references describe what actually ran. | Misleading names/claims fail. | Test inventory and documentation. | Actual executed target set. | Targeted name/capability checks. | planned |
| R6 | #128 AC6 | Integrated scenario runs in the appropriate release/integration job and both complete and deliberately corrupted qualification records compare exactly. | Job omission or corrupted-record acceptance fails. | CI qualification job and report. | Expected successful/failed records. | Integrated job artifacts. | planned |

## Unresolved gaps

- The canonical reusable scenario identity and artifact location from #126 are not yet defined.

## Open questions

- What exact #126 manifest/result reference does this contract consume?

## Out of scope

- Proving unsupported Kafka, CDC, or distributed claims without an executed scenario.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
