# Acceptance contract: #116 Enforce exchange limits and release reservations when sends are cancelled

Contract revision: v1  
Source: GitHub issue #116, all ten acceptance criteria; RockStream test-suite audit C4/C5 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: The production sender enforces every exchange limit, accounts accepted work exactly, and releases reservations safely on cancellation.

Storage handoff: Canonical destination `docs/acceptance-contracts/116.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #116 AC1 | Independently saturating each non-row limit with row credits available allows only the first frame before acknowledgement. | Limit bypass or extra frame fails. | Production sender and receiver. | Exact first-frame transcript. | One saturation case per limit. | planned |
| R2 | #116 AC2 | The second send remains pending until capacity release, then completes without an unrelated extra acknowledgement. | Early completion or extra ack fails. | Production sender admission path. | Controlled acknowledgement/release schedule. | Barrier-controlled send test. | planned |
| R3 | #116 AC3 | Batches `{(1,10):1}` and `{(2,20):1}` are received precisely once with original schemas and weights. | Changed, duplicated, or missing batch fails. | Production send/delivery path. | Exact typed weighted batches. | Receiver full-output comparison. | planned |
| R4 | #116 AC4 | At each barrier accounting equals accepted outstanding work, and after both acknowledgements rows, batches, bytes, and pending requests are zero. | Counter drift or leak fails. | Sender accounting snapshot. | Accepted-work invariant. | Exact snapshot assertions. | planned |
| R5 | #116 AC5 | An oversized frame returns the contractual error, delivers no rows, and leaves the full accounting snapshot unchanged. | Partial delivery or reservation leak fails. | Production sender size admission. | Defined error and pre-send snapshot. | Oversized-frame negative control. | planned |
| R6 | #116 AC6 | Aborting a send after reservation but before delivery restores all reserved resources. | Leaked reservation or partial delivery fails. | Production sender with delivery barrier. | Zero delivery and baseline accounting. | Deterministic abort schedule. | planned |
| R7 | #116 AC7 | After cancellation, receiver is empty, outstanding rows/requests are zero, and available credits equal the original budget. | Any residual state fails. | Sender/receiver accounting API. | Original budget snapshot. | Full cancellation transcript. | planned |
| R8 | #116 AC8 | A subsequent full-budget frame completes and delivers its complete weighted batch exactly once. | Cancellation poisoning or duplicate delivery fails. | Same production sender. | Exact subsequent batch. | Post-cancel recovery send. | planned |
| R9 | #116 AC9 | Cancelling after confirmed delivery but before acknowledgement eventually leaves exactly one original batch and baseline accounting after acknowledgement or documented recovery. | Duplicate or lost delivered batch fails. | Delivery/acknowledgement boundary. | Exactly-once delivery oracle. | Controlled post-delivery cancellation. | planned |
| R10 | #116 AC10 | The tests exercise production sending and not an unrelated permit API, and any exposed cancellation defect is fixed at the shared accounting boundary. | Wrong seam or special-case fix fails. | Production sender call path. | Executed path identity and behavior. | Targeted integration test and mutation. | planned |

## Unresolved gaps

- The authoritative production sender seam and exact cancellation boundary need to be named in the implementation plan; current issue text refers to “the actual production sender” without an API identifier.

## Open questions

- Which sender method is the canonical public seam for both reservation and delivery cancellation?

## Out of scope

- A new flow-control abstraction or unrelated permit implementations.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
