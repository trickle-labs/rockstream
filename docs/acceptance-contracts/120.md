# Acceptance contract: #120 Make UPDATE and DELETE comparisons follow declared SQL types

Contract revision: v1  
Source: GitHub issue #120, all five acceptance criteria; RockStream test-suite audit D3 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Gateway DML comparisons use declared SQL types exactly through selection, mutation, rollback, durable reopen, and numeric boundaries.

Storage handoff: Canonical destination `docs/acceptance-contracts/120.md`, revision v1; saving pending commit and issue link.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #120 AC1 | Deleting text equal to `1` from rows `(1,'01'),(2,'1'),(3,'true'),(4,'t')` returns exactly row 2 and leaves the other three complete rows unchanged. | String guessing or broad coercion fails. | Production gateway DML entry point. | Exact typed rows and predicate semantics. | Exact DELETE/RETURNING regression. | planned |
| R2 | #120 AC2 | UPDATE, boolean-looking text, NULL, rollback, and durable reopen preserve exact returning rows, command tags, schemas, and all stored rows. | Count-only or reopened-state loss fails. | Gateway DML and transaction/reopen APIs. | Declared column types and exact state. | Full scenario with rollback and reopen. | planned |
| R3 | #120 AC3 | DOUBLE values zero and `1e-17` compare to zero so only zero is affected and the other value is preserved exactly for the declared type. | Epsilon equality fails. | Production predicate evaluator. | Exact DOUBLE semantics. | Two-row equality regression. | planned |
| R4 | #120 AC4 | DECIMAL one-unit updates preserve exact result and declared scale; invalid precision/overflow returns the defined error with no partial mutation. | Float conversion or partial write fails. | Decimal DML evaluator/storage. | Exact decimal arithmetic and error contract. | Precision/overflow cases with full state. | planned |
| R5 | #120 AC5 | Tests exercise both the production DML entry point and shared evaluator, and coercing text or losing precision makes the regression fail. | Gateway-only or mutation-insensitive proof fails. | Gateway plus evaluator seam. | Independent exact expected results. | Targeted wrong-coercion and precision mutations. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- SQL type behavior outside the listed text, boolean-looking text, NULL, DOUBLE, and DECIMAL cases.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
