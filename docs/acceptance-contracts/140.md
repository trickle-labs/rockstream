# Acceptance contract: #140 Qualify large-state recovery and sustained overload

Contract revision: v1
Source: [issue #140](https://github.com/trickle-labs/rockstream/issues/140), all four acceptance criteria and inherited constraints; [canonical S8 decomposition](https://github.com/trickle-labs/rockstream/issues/132#issuecomment-5832445481).
Parent contract: `docs/acceptance-contracts/132.md`, revision v2.
Parent snapshot: `trickle-labs/rockstream` commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, `docs/acceptance-contracts/132.md`, SHA-256 `cc91b392f193e771e38336c7da3c62480ef7337083ce1a5c301f576096335734`.
Contribution: S8 allocates `trickle-labs/rockstream#132 v2:R17`, `trickle-labs/rockstream#132 v2:R18`, large-state and fault cells of `trickle-labs/rockstream#132 v2:R12`, and retained raw samples for `trickle-labs/rockstream#132 v2:R19` and `trickle-labs/rockstream#132 v2:R20`. Canonical decomposition: issue #132 comment 5832445481.
Prerequisites: Issues #133 (S1 resource envelope), #134 (S2 fair scheduling and durable adaptive epochs), #135 (S3 bounded producer queries), and #138 (S6 release-process manifest and independent oracle), with their required outcomes confirmed before dependent qualification. Confirm the parent v2 prerequisite chain and linked outcomes: v0.71, v0.61.1, v0.61.2, v0.62.1, v0.65.1, v0.67.1, v0.68, v0.69, and v0.70; sign-off checkboxes alone do not establish availability.

Intended outcome: Large state and sustained multi-fault overload remain within the frozen local resource and recovery bounds and recover the exact committed results after process restart.

Storage handoff: Saved in this checkout at `docs/acceptance-contracts/140.md`, revision v1; retrieval confirmed by the invoking workflow.

Acceptance profile: `benchmarks/r1-local/profile-v072.toml` revision 1, commit `7a73050dbb28fdf61584d052b9521b2e7dce9835`, SHA-256 `d5cf97b3d0a181cddd78dbf811e22224c4767bd1948143b45beb38390464719a`. It fixes a 48 GiB Apple M5 Pro, 2 GiB worker hard budget, maximum RSS 15% above worker budget, 4 GiB storage backlog, 32 GiB disk per worker, 16 KiB spill/compaction I/O per change, 1,800 second overload duration, and 900 second recovery duration. These are planning limits and local claims only.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | `trickle-labs/rockstream#132 v2:R17`; issue #140 criterion 1 | Exercise supported state larger than RAM through eviction, compaction, shard migration, and complete process destruction/restart; recovery pagination reaches the actual end and the recovered committed output equals the complete expected result multiset within frozen RSS, disk, I/O, storage-backlog, and recovery limits. | Must use the frozen profile and real release processes. Truncated pages, incomplete state, limits exceeded, synthetic faults, or a probe/count standing in for complete results fail. | R1 local release-process harness process lifecycle (`tools/r1-local-harness/src/cluster.rs`) with the public workload, migration, and recovery query paths; run manifest and process/resource observations. | Independent complete input oracle and expected full result multiset, compared at the recovered committed epoch; frozen profile values determine resource and time bounds. | `v072_large_state`: record profile and binary digests, process identities, state size, fault/migration/compaction sequence, full paged output, exact multiset/epoch comparison, resource time series, and recovery duration; assert each configured limit. | planned |
| R2 | `trickle-labs/rockstream#132 v2:R17`; issue #140 criterion 1 and inherited constraint | Reject each unsupported large-state operator explicitly before it is reported as successfully run or silently substituted. | Silent operator substitution, partial success, or interpreting missing output as an empty valid result fails. | Public workload admission/compile result through the release-process harness. | Parent v2 requires explicit unsupported-operator rejection; the requested operator and returned diagnostic are compared directly. | `v072_large_state`: submit an unsupported large-state operator and assert its explicit rejection and absence of successful result/recovery evidence. | planned |
| R3 | `trickle-labs/rockstream#132 v2:R18`; issue #140 criteria 2–3 | Sustain the frozen 1,800-second overload run while applying shard churn, slow storage, worker loss, migration, compaction, backfill, connector pressure, and concurrent queries; remain in a bounded degraded state without OOM, unbounded/growing queues, runaway retries, or starved control work, and remain within frozen RSS, disk, I/O, and storage-backlog limits. | Every listed fault and query load must be exercised against real processes; omit none, use no synthetic fault claim, and do not infer boundedness from a final sample alone. Frozen profile revision changes require a new profile and comparison. | R1 multi-process cluster operations, public diagnostics, OS process resource sampling, and workload/query interfaces (`cluster.rs`, `load.rs`). | Frozen profile thresholds plus complete offered-input/fault schedule and explicit control-work progress expectation; observations are checked independently against those declared bounds. | `v072_overload_soak`: retain timestamped resource/queue/retry/control-work series and exact fault/process timeline for the full soak; assert all configured ceilings and no OOM, queue growth, runaway retries, or control starvation. | planned |
| R4 | `trickle-labs/rockstream#132 v2:R18`; issue #140 criterion 4 | After the sustained multi-fault run, restart the same persisted state and recover the complete exact committed result multiset within the frozen recovery bound. | Reuse the same state and input history; a partial page, stale/uncommitted result, duplicate/lost row, clean fixture, or separate non-faulted run fails. | Release-process cluster restart and complete recovery query through the R1 harness. | Independent full input oracle, epoch-aware multiset comparison (`tools/r1-local-harness/src/oracle.rs`), and the frozen 900-second recovery limit. | `v072_overload_soak`: capture restart identity, final committed epoch, every recovery page through completion, exact oracle comparison, and elapsed recovery time. | planned |
| R5 | `trickle-labs/rockstream#132 v2:R12`; S8 fault-cell contribution in issue #132 comment 5832445481 | Account for every applicable beyond-RAM/recovery and overload/fault matrix cell with its real-process result; any non-applicable cell has an explicit scope reason and owner, and no applicable fault cell remains omitted or blocked. | A cell marked complete without its required run evidence, an unjustified non-applicable cell, or a synthetic/omitted fault cell fails. This row covers only S8 fault cells, not S7 comparison cells or S9 full applicability assembly. | R1 matrix definitions, validator, and run manifest (`tools/r1-local-harness/src/matrix.rs`, `main.rs`). | Canonical matrix cell set and its applicability declarations from the frozen qualification scope. | `v072_matrix_completeness`: validate the S8 fault-cell subset against its manifests; assert exact cell IDs/statuses and reject missing evidence or unsupported non-applicable declarations. | planned |
| R6 | `trickle-labs/rockstream#132 v2:R19`; S8 raw-sample contribution in issue #132 comment 5832445481 | Retain raw measurements for the large-state and overload runs needed to reproduce their resource and recovery claims, including timestamped process/resource data and the measurements required to calculate frozen limits. | Aggregate summaries without underlying samples, missing units/timestamps, or absent denominators cannot establish the claim. Publication of the assembled parent report remains S9 scope. | R1 raw sample/evidence artifacts (`tools/r1-local-harness/src/evidence.rs`) tied to run manifests. | Frozen profile definitions and directly sampled process/resource counters. | `v072_large_state` and `v072_overload_soak`: persist raw timestamped samples with process IDs, units, profile/binary/run identity, input/fault timeline, and exact command; independently recompute reported maxima and recovery duration. | planned |
| R7 | `trickle-labs/rockstream#132 v2:R20`; S8 reproducibility/raw-sample contribution in issue #132 comment 5832445481 | Each S8 run artifact identifies the exact profile, release binary, corpus/input stream, workload/fault schedule, host/backend/topology, run identity, and raw samples so another run can be attributed and reproduced. | Synthetic, incomplete, mismatched, or wrongly attributed evidence fails; this child retains its S8 evidence and does not own S9's full evidence validator/report. | R1 run manifest and validated `RawSample` evidence artifact. | Byte digests and process identity recorded by the harness, checked against the actual profile, binary, corpus, and observed run. | `v072_evidence_validation`: for both S8 cases, validate manifest/sample identity against artifacts and rerun the recorded command; assert matching digests and complete reproducibility fields. | planned |

## Unresolved gaps

- The required outcomes for prerequisite issues #133, #134, #135, and #138 are not confirmed here; all four issues are currently open. Confirm the actual resource/scheduling/query outcomes and S6 manifest/oracle artifacts before dependent implementation and qualification. Issue status alone does not establish availability.

## Open questions

- None. The parent and child issue settle the required outcomes and frozen limits.

## Out of scope

- S7 scale and mixed-read comparisons; S9 publication of the combined capacity report, costs, regression decisions, full matrix applicability, and v0.72 sign-off.
- Implementation of generic fault-injection infrastructure, new workloads, synthetic capacity claims, cloud cost claims, or changes to the frozen profile based on observed candidate results.

## Change notes

- Initial contract.
- Current parent contract and issue-linked amendments were compared with the required v2 snapshot. Issue comments record v1 approval followed by v2 resolving its R1/R23/R29 planning gaps; no later source-linked amendment changes S8 scope.

## Implementation handoff

Hand off to an explicitly authorized `/implement-contract` invocation after the listed prerequisite outcomes are confirmed. Implement the smallest complete solution inside this contract. Preserve requirement IDs and promised outcomes. Capture the resulting candidate for separate `/review-implementation` and `/prove` phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate. Record actual evidence and verdicts in a separate proof report.
