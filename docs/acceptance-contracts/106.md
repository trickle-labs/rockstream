# Acceptance contract: #106 Commit concurrent changelog confirmations with complete contributions

Contract revision: v1  
Source: GitHub issue #106, all five acceptance criteria; RockStream test-suite audit B6 at source revision `5d9c3b919682140ec0b365d7dba848cd5cf5ecbb`.  
Parent contract: None  
Prerequisites: None

Intended outcome: Concurrent shard confirmations commit one complete changelog checkpoint with the same manifest and contribution map.

Storage handoff: Canonical destination `docs/acceptance-contracts/106.md`, revision v1; saved in the repository, issue link pending publication.

## Acceptance matrix

| ID | Source | Requirement | Boundaries / counterexamples | Seam | Oracle | Planned evidence | Plan state |
|---|---|---|---|---|---|---|---|
| R1 | #106 AC1 | When A is paused after recording its contribution and capturing its map, B records and confirms, and A resumes as final confirmation, the schedule completes without panic. | Stale-map panic or incomplete commit fails. | Changelog checkpoint coordinator. | Deterministic two-shard schedule. | Barrier-controlled concurrency regression. | planned |
| R2 | #106 AC2 | The schedule emits exactly one successful commit callback. | Duplicate, missing, or failed callback fails. | Commit callback interface. | One expected callback. | Exact callback count and payload. | planned |
| R3 | #106 AC3 | The callback contains the complete expected manifest and contributions from A and B. | Missing or stale contribution fails. | Callback payload. | Independent expected checkpoint IDs and contributions. | Full manifest/map equality. | planned |
| R4 | #106 AC4 | Latest committed equals that manifest and the coordinator holds zero credits afterward. | Stale latest commit or leaked credits fails. | Coordinator state inspection. | Expected manifest and zero-credit invariant. | Exact state assertions. | planned |
| R5 | #106 AC5 | Reverse confirmation order and the existing sequential case produce identical complete expected results. | Order-dependent output fails. | Same coordinator API. | Order-independent expected result. | Three deterministic schedules. | planned |

## Unresolved gaps

- None.

## Open questions

- None.

## Out of scope

- New checkpoint scheduling policies.

## Change notes

- Initial contract.

## Implementation handoff

Hand off to an explicitly authorized /implement-contract invocation.
Implement the smallest complete solution inside the spec envelope.
Preserve requirement IDs and promised outcomes.
Capture the resulting candidate for separate /review-contract and /prove phases.

## Proof handoff

Evaluate every requirement against this contract revision and one fixed candidate.
Record actual evidence and verdicts in a separate proof report.
