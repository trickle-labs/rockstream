[package]
name = "rockstream-ops"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Operator trait and per-operator implementations for RockStream"

# Intentionally dependency-free scaffold at v0.1. Dependencies (and benches) are
# added as operators are implemented test-first from v0.4 onward.
[dependencies]
