[package]
name = "rockstream-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Worker process, scheduler, epoch-commit coordinator, and exchange for RockStream"

# Intentionally dependency-free scaffold at v0.1. The scheduler, epoch-commit
# coordinator, and exchange paths are implemented test-first from v0.4 onward.
[dependencies]
