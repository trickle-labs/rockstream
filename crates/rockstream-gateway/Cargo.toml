[package]
name = "rockstream-gateway"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "PostgreSQL wire gateway service for RockStream"

# Intentionally dependency-free scaffold at v0.1. The pgwire gateway is the
# second pillar, built in the Postgres phase (v0.40+).
[dependencies]
