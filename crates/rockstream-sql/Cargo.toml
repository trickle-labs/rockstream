[package]
name = "rockstream-sql"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "SQL frontend built on DataFusion for RockStream"

# Intentionally dependency-free scaffold at v0.1. The DataFusion parse/bind/
# optimize frontend, extension nodes, and `LogicalPlan -> PlanNode` lowering are
# implemented test-first in v0.7.
[dependencies]
