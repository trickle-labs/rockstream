[package]
name = "rockstream-oracle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Batch reference engine and property-test harness for RockStream correctness"

# Intentionally dependency-free scaffold at v0.1. The DataFusion batch reference
# and proptest harness are implemented in v0.2.
[dependencies]
